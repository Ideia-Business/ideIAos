// SOURCE: IdeiaOS v14.1 | kind: spa-component-stub | targets: apps/cockpit
// =============================================================================
// CommandPalette.tsx — STUB de seam ⌘K (plano 14.1-02).
//
// Ponto-de-montagem ESTÁVEL do Command Palette ⌘K. App.tsx monta <CommandPalette/>
// uma vez (fora do switch de tela) — este é o seam: o plano 14.1-04 (Wave 3)
// PREENCHE este corpo (listener ⌘K + cmdk + allowlist default-deny de verbos
// locais reversíveis B1–B6 que POSTam a um endpoint loopback do read.js), SEM
// editar App.tsx. Como stub, não registra listener nem renderiza nada visível.
// =============================================================================
export function CommandPalette() {
  // 14.1-04 substitui este corpo pelo listener ⌘K + CommandDialog (cmdk).
  return null;
}

export default CommandPalette;
