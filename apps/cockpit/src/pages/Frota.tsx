// SOURCE: IdeiaOS v14.1 | kind: spa-page-stub | targets: apps/cockpit
// =============================================================================
// Frota.tsx — STUB de seam (plano 14.1-02).
//
// O plano 14.1-03 (Wave 3) PREENCHE este corpo: card por máquina + tabela densa
// de heartbeat + version-drift por STRING-EQUALITY (nunca semver). Lê GET /fleet
// do read.js (last_seen_epoch para frescor honesto). 14.1-03 NÃO edita App.tsx —
// só substitui este corpo. App.tsx já registra a rota 'frota' aqui (o seam).
// =============================================================================
export default function Frota() {
  return (
    <div className="rounded-lg border border-dashed border-border p-8 text-center text-sm text-muted-foreground">
      Frota — em construção (14.1-03)
    </div>
  );
}
