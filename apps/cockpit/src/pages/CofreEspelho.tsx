// SOURCE: IdeiaOS v14.1 | kind: spa-page-stub | targets: apps/cockpit
// =============================================================================
// CofreEspelho.tsx — STUB de seam (plano 14.1-02).
//
// O plano 14.1-03 (Wave 3) PREENCHE este corpo: matriz var × project METADATA-ONLY
// (GET /vault — sem coluna `value`; nenhum controle revela/copia/escreve um valor),
// banner de doutrina, estado-vazio celebrado. 14.1-03 NÃO edita App.tsx — só
// substitui este corpo. App.tsx já registra a rota 'cofre' aqui (o seam).
// =============================================================================
export default function CofreEspelho() {
  return (
    <div className="rounded-lg border border-dashed border-border p-8 text-center text-sm text-muted-foreground">
      Cofre-Espelho — em construção (14.1-03)
    </div>
  );
}
