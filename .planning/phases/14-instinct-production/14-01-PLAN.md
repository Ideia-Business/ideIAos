---
phase: 14
plan: 01
name: instinct-production
type: fix
autonomous: true
wave: 1
requirements: [R4-01, R4-02, R4-03, R4-04, R4-05]
---

# Fase 14 — Instinct Loop: Endurecimento para Produção

## Objetivo

Estancar o runaway loop (1331 spawns, incidente 2026-06-12), curar o estoque contaminado (~616+ instincts meta-junk), e colocar o loop de instincts em operação segura com limites duros.

## Context

- Incidente: loop runaway onde sessões haiku de análise geravam observações próprias e re-spawnavamam via session_end → 1331 spawns
- Estancado com sentinela futura + pkill
- ~1074 arquivos de instinct, maioria meta-junk (confidence 0.9 indevida, derivados de sessões de análise)
- Spec original: confidence inicial 0.3-0.6

## Tasks

### Task 1 — R4-01: Anti-runaway guard (IDEIAOS_INSTINCT_SPAWN)

**Tipo:** fix  
**Critério:** observe-tool-use e observe-session-end fazem `exit 0` imediato se `IDEIAOS_INSTINCT_SPAWN=1`; spawn exporta a env var.

Modificar:
- `source/hooks/observe-tool-use.sh`: adicionar guard no topo
- `source/hooks/observe-session-end.sh`: adicionar guard no topo + exportar env no spawn

### Task 2 — R4-02: Sentinela no spawn + cooldown 30min

**Tipo:** fix  
**Critério:** sentinela escrita ANTES do spawn; gate extra: se sentinela < 30min → exit 0.

Modificar:
- `source/hooks/observe-session-end.sh`: escrever sentinela antes de lançar claude; adicionar gate de cooldown

### Task 3 — R4-03: Curadoria do estoque contaminado

**Tipo:** fix (one-off script inline)  
**Critério:** backup tar.gz em ~/.ideiaos/backups/; estoque final ≤80 legítimos; confidence cap re-aplicado.

Script python3 inline: remover meta-junk, dedup por trigger slug, cap confidence >0.6 sem evidence_count ≥3.

### Task 4 — R4-04: Limites duros no SKILL.md instinct-analyze

**Tipo:** fix  
**Critério:** SKILL.md com regras INVIOLÁVEIS no topo: confidence inicial máx 0.6, máx 15 instincts por run, ignorar obs de sessões de análise, exit se IDEIAOS_INSTINCT_SPAWN=1.

Modificar:
- `source/skills/instinct-analyze/SKILL.md`
- Propagar para `~/.claude/skills/instinct-analyze/SKILL.md` (cp) + build-plugins

### Task 5 — Teste vivo controlado (1 spawn manual)

**Tipo:** validation  
**Critério:** após instalar os fixes nos hooks de ~/.claude/hooks/, 1 spawn manual haiku; por 3 min ≤1 log novo (sem cadeia); instincts novos ≤15, confidence ≤0.6.

### Task 6 — R4-05: /evolve ao vivo

**Tipo:** validation  
**Critério:** run real `timeout 150 claude -p "/evolve" --max-turns 20`; relatório com promovidos/decay; vault recebe ≥0 notas (ok 0 se nada maduro).

## Success Criteria

- R4-01: grep "IDEIAOS_INSTINCT_SPAWN" nos 2 hooks; cadeia parada em 1 spawn
- R4-02: 2 invocações consecutivas → 1 spawn
- R4-03: wc -l do estoque final ≤80; nenhum com confidence >0.6 sem evidence_count ≥3  
- R4-04: SKILL.md tem regras no topo; run controlado respeita limites
- R4-05: relatório de evolve gerado; nenhum crash
