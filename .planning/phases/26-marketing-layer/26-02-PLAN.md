---
phase: 26-marketing-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - source/agents/mkt-estrategista.md
  - source/agents/mkt-copywriter.md
  - source/agents/mkt-designer.md
  - source/agents/mkt-revisor.md
  - source/skills/marketing-research/SKILL.md
  - source/skills/marketing-research/references/profile-investigation.md
requirements: [R6-08, R6-09]
autonomous: true
must_haves:
  truths:
    - "Existem >=4 agents de marketing em source/agents/ com model routing no frontmatter (model + tools)"
    - "O agent designer reusa a suite de design IdeiaOS (banner-design/ui-ux-pro-max/slides), nao duplica as skills canva/image do OpenSquad"
    - "A skill marketing-research investiga perfis reais via Chrome DevTools MCP (que ja temos), sem nova dependencia de browser"
  artifacts:
    - path: "source/agents/mkt-estrategista.md"
      provides: "Agent estrategista de conteudo (opus/sonnet) — angulos, big idea, calendario editorial"
      contains: "model:"
    - path: "source/agents/mkt-copywriter.md"
      provides: "Agent copywriter (sonnet) — hooks, body, CTA por formato"
      contains: "model:"
    - path: "source/agents/mkt-designer.md"
      provides: "Agent designer (sonnet) — chama banner-design/ui-ux-pro-max/slides da suite IdeiaOS"
      contains: "model:"
    - path: "source/agents/mkt-revisor.md"
      provides: "Agent revisor (sonnet/haiku) — scoring + veto contra quality-criteria"
      contains: "model:"
    - path: "source/skills/marketing-research/SKILL.md"
      provides: "Skill Sherlock-adaptada: investiga perfis reais via Chrome DevTools MCP"
      contains: "name:"
  key_links:
    - from: "source/agents/mkt-designer.md"
      to: "suite de design IdeiaOS"
      via: "instrucao explicita de chamar /banner-design, /ui-ux-pro-max, /slides"
      pattern: "banner-design|ui-ux-pro-max|slides"
    - from: "source/skills/marketing-research/SKILL.md"
      to: "Chrome DevTools MCP"
      via: "uso do MCP existente (nao Playwright novo)"
      pattern: "Chrome DevTools|chrome-devtools|frontend-visual-loop"
---

<objective>
Criar os agents de conteudo (R6-09) e a skill de research de marketing (R6-08, Sherlock adaptado) que compoem o squad da Camada de Marketing. Os agents sao recrutados e orquestrados pelo `/marketing` (Plano 03); cada um tem responsabilidade unica, model routing no frontmatter e consome as best-practices do Plano 01 por formato.

Decisao de arquitetura central deste plano: o agent **designer NAO duplica** as skills canva/image-creator/image-ai-generator do OpenSquad. Em vez disso, **reusa a Suite de Design IdeiaOS** (`/banner-design`, `/ui-ux-pro-max`, `/slides`, `/design-system`) — que ja cobre rendering HTML->imagem (via Chrome DevTools MCP + image-creator-equivalent) e identidade de marca. Isso evita dependencia de Canva/imgBB e mantem o trabalho dentro do ecossistema. Sinergia documentada no agent designer e no SUMMARY.

A skill `marketing-research` adapta o Sherlock do OpenSquad: em vez de Playwright novo, usa o **Chrome DevTools MCP que ja temos** (o mesmo de /frontend-visual-loop e /web-quality), investigando perfis publicos para calibrar a geracao com metricas e padroes reais.

Output: 4 agents (mkt-estrategista, mkt-copywriter, mkt-designer, mkt-revisor) + skill marketing-research.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Formato de agent IdeiaOS (frontmatter: name, description, tools, model) + header # SOURCE
@source/agents/security-reviewer.md
@source/agents/code-explorer.md

# Suite de Design IdeiaOS a REUSAR (nao duplicar canva/image do OpenSquad)
@source/skills/banner-design/SKILL.md
# Tambem disponiveis na suite: /ui-ux-pro-max, /slides, /design-system, /brand, /design

# Sherlock do OpenSquad a ADAPTAR (Playwright -> Chrome DevTools MCP)
# Fonte: /tmp/_cmp_opensquad/_opensquad/core/prompts/sherlock-shared.md
# Skill IdeiaOS que ja usa Chrome DevTools MCP (modelo de uso do MCP existente):
@source/skills/frontend-visual-loop/SKILL.md

# Best-practices que os agents consomem por formato (criadas no Plano 01)
# source/rules/marketing/{copywriting,researching,review,image-design,strategist,...}.md
# Catalogo de referencia: /tmp/_cmp_opensquad/_opensquad/core/best-practices/_catalog.yaml

# Persona/composicao de squad do OpenSquad (referencia de design dos agents)
# /tmp/_cmp_opensquad/_opensquad/core/prompts/design.prompt.md (Phase E: Agent Design)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar os 4 content agents com model routing (estrategista, copywriter, revisor)</name>
  <files>source/agents/mkt-estrategista.md, source/agents/mkt-copywriter.md, source/agents/mkt-revisor.md</files>
  <action>
Criar 3 dos 4 agents (o designer fica na Task 2 por ter integracao com a suite). Cada agent segue o formato IdeiaOS: frontmatter com `name`, `description` (PT-BR, com gatilhos de quando usar), `tools` e `model`, seguido de `# SOURCE: OpenSquad MIT renatoasse/opensquad | adapted: IdeiaOS v6` na primeira linha do corpo (markdown `#`, nunca `<!--`), depois a persona em PT-BR.

Model routing (R6-09) — espelha o cost-tracking do IdeiaOS e o model_tier do OpenSquad:
- **mkt-estrategista** — `model: opus`. Trabalho de mais alto nivel cognitivo: gera angulos (a "perspectiva emocional sobre UMA pauta" — ver definicao de ANGULO em design.prompt.md), big idea, posicionamento, calendario editorial. tools: Read, Grep, WebSearch, WebFetch. Consome `source/rules/marketing/strategist.md` e `copywriting.md`. Persona alinhada ao "Estrategista" do README OpenSquad (gera ideias e define abordagem).
- **mkt-copywriter** — `model: sonnet`. Produz o conteudo final por formato: 3 hooks -> body -> CTA, seguindo o framework de copywriting.md. tools: Read, Grep, Write. Consome `source/rules/marketing/copywriting.md` + a rule de plataforma do formato-alvo (ex.: instagram-feed.md). Instruir: "antes do body, sempre apresentar 3 hooks com angulos psicologicos distintos e aguardar selecao" (regra hook-first do copywriting.md).
- **mkt-revisor** — `model: sonnet` (default) com nota de que tarefas simples de checagem podem rodar haiku. Faz scoring + veto contra `source/rules/marketing/review.md` e quality-criteria do formato. tools: Read, Grep. Output: veredito APROVADO/REJEITADO com feedback acionavel (espelha o loop de veto/on_reject do runner.pipeline.md, secao Veto Condition Enforcement). Instruir o loop: rejeicao volta ao copywriter com feedback especifico, max 2 ciclos.

Cada agent: persona em PT-BR (user-facing PT-BR e constraint), responsabilidade unica (YAGNI — design.prompt.md Phase E), e secao "Quando NAO usar" como nos agents IdeiaOS existentes. NAO embutir conhecimento de plataforma no agent (o /marketing injeta a best-practice por formato em runtime — Plano 03).
  </action>
  <verify>
    <automated>OK=0; for a in mkt-estrategista mkt-copywriter mkt-revisor; do f="source/agents/$a.md"; test -f "$f" && grep -q "^model:" "$f" && grep -q "^# SOURCE: OpenSquad MIT" "$f" && ! grep -q '<!--' "$f" && OK=$((OK+1)); done; echo "agents_ok=$OK/3"; test "$OK" -eq 3 && echo AGENTS3_OK</automated>
  </verify>
  <done>3 agents em source/agents/ (mkt-estrategista=opus, mkt-copywriter=sonnet, mkt-revisor=sonnet); cada um com `model:` e `tools:` no frontmatter, header `# SOURCE` no corpo, zero `<!--`, persona PT-BR com responsabilidade unica.</done>
</task>

<task type="auto">
  <name>Task 2: Criar o agent designer reusando a Suite de Design IdeiaOS</name>
  <files>source/agents/mkt-designer.md</files>
  <action>
Criar `source/agents/mkt-designer.md` no formato IdeiaOS (frontmatter name/description/tools/model + header `# SOURCE` no corpo).

`model: sonnet`. tools: Read, Write, Skill (para invocar skills da suite), Bash (para rodar render se necessario).

DECISAO DE ARQUITETURA (documentar explicitamente no corpo do agent): este designer NAO usa as skills canva/image-creator/image-ai-generator/image-fetcher do OpenSquad (todas MCP/API-key dependentes — Canva MCP, imgBB key). Em vez disso, REUSA a Suite de Design IdeiaOS que ja cobre o caso de uso:
- `/banner-design` — banners/social/ads (cobre image-design do OpenSquad).
- `/slides` — carrossel/apresentacao (cobre slide rendering).
- `/ui-ux-pro-max` + `/design-system` + `/brand` — identidade visual, tokens OKLCH, paletas, tipografia (cobre template-designer do OpenSquad).
- Render HTML->imagem: via Chrome DevTools MCP (o mesmo de /frontend-visual-loop), nao Playwright novo (cobre image-creator do OpenSquad).

O agent consome `source/rules/marketing/image-design.md` como conhecimento de design de conteudo, e delega a producao visual concreta as skills da suite. Persona PT-BR: "designer de conteudo que traduz copy aprovada em pecas visuais usando a suite de design do IdeiaOS". Secao "Integration" listando: le copy aprovada (do copywriter), chama as skills da suite, escreve a peca final. Secao "Quando NAO usar": video edicao (fora de escopo), design de produto/UI de app (isso e /ui-ux-pro-max direto).

Incluir nota: publicacao (Instagram/Resend/blotato) e MCP-dependente e fica como OPCIONAL/MANUAL — nao e responsabilidade deste agent nem deste plano (ver Plano 03 secao de publishing deferido).
  </action>
  <verify>
    <automated>f="source/agents/mkt-designer.md"; test -f "$f" && grep -q "^model:" "$f" && grep -q "^# SOURCE: OpenSquad MIT" "$f" && ! grep -q '<!--' "$f" && grep -Eq "banner-design|ui-ux-pro-max|slides" "$f" && echo DESIGNER_OK</automated>
  </verify>
  <done>source/agents/mkt-designer.md existe com model:sonnet, header # SOURCE, zero `<!--`, e referencia explicita a >=1 skill da suite (banner-design/ui-ux-pro-max/slides). Documenta a decisao de reuso vs duplicacao.</done>
</task>

<task type="auto">
  <name>Task 3: Criar a skill marketing-research (Sherlock via Chrome DevTools MCP)</name>
  <files>source/skills/marketing-research/SKILL.md, source/skills/marketing-research/references/profile-investigation.md</files>
  <action>
Criar a skill `marketing-research` (frontmatter-first: `name`, `description` em PT-BR com gatilhos), adaptando o Sherlock do OpenSquad (`/tmp/_cmp_opensquad/_opensquad/core/prompts/sherlock-shared.md`). Header `# SOURCE: OpenSquad MIT renatoasse/opensquad | adapted: IdeiaOS v6` na primeira linha do corpo.

ADAPTACAO CRITICA (R6-08): substituir Playwright (`npx playwright open --load-storage=...`) pelo **Chrome DevTools MCP que ja temos** (mesmo MCP usado por /frontend-visual-loop e /web-quality — confirmar o padrao de invocacao lendo frontend-visual-loop/SKILL.md). NAO introduzir nova dependencia de browser. A skill:
- Recebe 1-5 URLs de perfis publicos (Instagram/LinkedIn/YouTube/X) que o usuario admira.
- Usa Chrome DevTools MCP para navegar e extrair conteudo real (captions, slides, texto) — equivalente ao raw-content.md do Sherlock.
- Produz uma analise de padroes (hooks que funcionam, estrutura, cadencia, vocabulario, CTAs) — equivalente ao pattern-analysis.md / consolidated-analysis.md do Sherlock.
- A saida alimenta o mkt-estrategista e o mkt-copywriter para calibrar geracao com dados de primeira mao (priority rule do Sherlock: dados de investigacao > web research generico).

Constraints herdadas do Sherlock (preservar): "Never fabricate data. Never declare success over empty results"; se extracao falhar, salvar nota de erro; timeout/limite por perfil; salvar screenshots com path completo (nunca poluir raiz). Login: se houver paywall/login wall, pausar e pedir login manual ao usuario (sessao do proprio navegador do Chrome DevTools MCP).

`references/profile-investigation.md`: o detalhamento por plataforma (deteccao de URL, modos de investigacao single_post/profile_1/profile_3, formato da analise de padroes) — condensado do sherlock-shared.md, com header `# SOURCE` e zero `<!--`.

Esta skill e roteavel pela Deia (Plano 03 adiciona o sinal "analisa o perfil @X / inspira-se no estilo de Y") e invocavel pelo /marketing na fase discovery (passo de investigacao opcional).
  </action>
  <verify>
    <automated>f="source/skills/marketing-research/SKILL.md"; test -f "$f" && grep -q "^name:" "$f" && grep -q "^# SOURCE: OpenSquad MIT" "$f" && ! grep -q '<!--' "$f" && grep -Eqi "chrome devtools|chrome-devtools|frontend-visual-loop|devtools mcp" "$f" && test -f source/skills/marketing-research/references/profile-investigation.md && echo RESEARCH_OK</automated>
  </verify>
  <done>source/skills/marketing-research/SKILL.md existe (frontmatter-first, header # SOURCE, zero `<!--`), referencia explicita ao Chrome DevTools MCP (nao Playwright novo), + references/profile-investigation.md. Roteavel e invocavel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sherlock prompt (OpenSquad) -> skill marketing-research | Texto de terceiros adaptado. Mitigado: adaptacao manual (nao copia cega), header # SOURCE, sem `<!--`. |
| Perfis publicos externos -> agents | Conteudo investigado pode conter prompt injection embutido em captions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering | Conteudo investigado de perfis externos | mitigate | marketing-research trata conteudo extraido como DADO, nunca como instrucao; "Never fabricate / never declare success over empty" herdado do Sherlock. |
| T-26-06 | Information Disclosure | Sessao/cookies de browser do usuario | mitigate | Usa Chrome DevTools MCP existente (sessao do proprio usuario); login manual; nunca persiste credenciais em arquivo do repo. |
| T-26-07 | Spoofing | Agent com model errado (custo/qualidade) | accept | model routing explicito no frontmatter (opus para estrategia, sonnet para producao); revisavel via cost-tracking. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Este plano nao instala pacotes (apenas cria .md). Reusa Chrome DevTools MCP ja configurado — sem nova dependencia. |
</threat_model>

<verification>
| Criterio | Como verificar | Req |
|----------|----------------|-----|
| >=4 content agents | `ls source/agents/mkt-*.md \| wc -l` >= 4 | R6-09 |
| Model routing presente | `grep -L "^model:" source/agents/mkt-*.md` vazio | R6-09 |
| tools no frontmatter | `grep -L "^tools:" source/agents/mkt-*.md` vazio | R6-09 |
| Designer reusa suite | `grep -Eq "banner-design\|ui-ux-pro-max\|slides" source/agents/mkt-designer.md` | R6-09 |
| Skill research via Chrome DevTools MCP | `grep -iq "chrome devtools\|frontend-visual-loop" source/skills/marketing-research/SKILL.md` | R6-08 |
| Zero `<!--` | `grep -l '<!--' source/agents/mkt-*.md source/skills/marketing-research/*.md` vazio | R6-08, R6-09 |
</verification>

<success_criteria>
- 4 agents de marketing em source/agents/ com model routing (mkt-estrategista=opus, mkt-copywriter/designer/revisor=sonnet) e tools no frontmatter — cumpre R6-09.
- mkt-designer reusa a Suite de Design IdeiaOS (banner-design/ui-ux-pro-max/slides) em vez de duplicar canva/image do OpenSquad — sinergia documentada.
- Skill marketing-research investiga perfis reais via Chrome DevTools MCP existente (nao Playwright novo) — cumpre R6-08.
- Todos os arquivos com header `# SOURCE` e zero `<!--`.
</success_criteria>

<output>
Create `.planning/phases/26-marketing-layer/26-02-SUMMARY.md` when done.
</output>
