---
phase: "01"
plan: "01-03"
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/typecheck-on-edit.sh
autonomous: true
requirements: [typecheck]
must_haves:
  truths:
    - "Editar um .ts/.tsx com erro de tipo aciona tsc --noEmit e o erro chega ao Claude via exit 2 + additionalContext em segundos"
    - "Editar um .ts/.tsx valido nao gera ruido (exit 0 silencioso)"
    - "Em projeto sem tsc (sem node_modules/.bin/tsc e sem tsc global), o hook sai 0 silencioso"
  artifacts:
    - path: "hooks/typecheck-on-edit.sh"
      provides: "PostToolUse async que roda tsc --noEmit incremental e acorda Claude com erros"
      min_lines: 35
  key_links:
    - from: "hooks/typecheck-on-edit.sh"
      to: "$CWD/node_modules/.bin/tsc"
      via: "deteccao de tsc local com fallback npx --no-install"
      pattern: "node_modules/.bin/tsc"
---

<objective>
Entregar o hook typecheck-on-edit (PostToolUse, async + asyncRewake): apos editar um .ts/.tsx, roda tsc --noEmit incremental em background e, se houver erros, acorda o Claude com a mensagem via exit 2 + additionalContext. Hook mais delicado da fase (lentidao do tsc + deteccao de binario), por isso isolado em plano proprio.

Purpose: Erros de tipo aparecem em segundos durante a edicao, nao no commit.
Output: 1 script bash em hooks/. A integracao ao harness de smoke test e feita no plano 01-04 (Wave 2), que ja possui a versao final do test-hooks.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-quality-memory-hooks/01-RESEARCH.md
@hooks/extract-learnings-reminder.sh

<interfaces>
PostToolUse stdin: { tool_name, tool_input: { file_path }, cwd, session_id }.

Deteccao de tsc (RESEARCH Pitfall 1 + Code Example):

    TSC=""
    if [ -f "$CWD/node_modules/.bin/tsc" ]; then TSC="$CWD/node_modules/.bin/tsc"
    elif command -v tsc >/dev/null 2>&1; then TSC="tsc"; fi
    [ -z "$TSC" ] && exit 0   # tsc indisponivel -> silencioso

async+asyncRewake (registrado no plano 04 settings.json): script roda em background; exit 2 acorda o Claude com a mensagem de erro. exit 0 = silencioso.

Serializacao JSON segura do output (RESEARCH "Don't Hand-Roll" — obrigatorio para output de tsc):

    /usr/bin/python3 -c "import json,sys; print(json.dumps({'hookSpecificOutput':{'hookEventName':'PostToolUse','additionalContext':sys.argv[1]}}))" "$MSG"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar typecheck-on-edit.sh</name>
  <files>hooks/typecheck-on-edit.sh</files>
  <read_first>
    - .planning/phases/01-quality-memory-hooks/01-RESEARCH.md (Pattern 2 + Code Example "Typecheck assincrono" + Pitfalls 1 e 2)
    - hooks/extract-learnings-reminder.sh (padrao parse stdin)
  </read_first>
  <behavior>
    - file_path .ts/.tsx em projeto com tsc + erro de tipo gera exit 2 com JSON additionalContext contendo "TypeScript" e o trecho do erro do tsc
    - file_path .ts/.tsx valido gera exit 0 silencioso
    - file_path fora de .ts/.tsx gera exit 0 silencioso
    - sem tsc (local nem global) gera exit 0 silencioso (Pitfall 1)
    - output do tsc truncado a ~30 linhas; JSON serializado via python3 (sem quebra por caracteres especiais)
  </behavior>
  <action>
    Criar hooks/typecheck-on-edit.sh (#!/usr/bin/env bash, set -uo pipefail). Seguir Code Example do RESEARCH.
    1. Ler stdin; parsear file_path, cwd via python3. [ -z "$CWD" ] && CWD="$PWD".
    2. Filtrar extensao: case "$FILE_PATH" in *.ts|*.tsx) ;; *) exit 0 ;; esac.
    3. Detectar TSC (padrao das interfaces). Se ausente, exit 0.
    4. Rodar: TSC_OUTPUT="$(cd "$CWD" && "$TSC" --noEmit --incremental 2>&1)" || { ... }. tsc retorna exit !=0 quando ha erros.
    5. No bloco de erro: ERRORS_TRIMMED="$(echo "$TSC_OUTPUT" | head -30)". Montar MSG="TypeScript errors detected:\n$ERRORS_TRIMMED". Serializar JSON via python3 json.dumps (interfaces). echo do JSON. exit 2 (acorda Claude via asyncRewake).
    6. Se tsc retornou 0 (sem erro): exit 0 silencioso.
    NUNCA passar file_path/output via eval. cd "$CWD" em subshell para usar o tsconfig do projeto.
    Nota: o registro async:true + asyncRewake:true + timeout:60 fica no plano 04 (settings.json). Este script apenas implementa o contrato exit 2 = erro.
  </action>
  <verify>
    <automated>echo '{"tool_name":"Edit","tool_input":{"file_path":"/tmp/x.md"},"cwd":"/tmp","session_id":"t"}' | bash hooks/typecheck-on-edit.sh; echo "non-ts exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "node_modules/.bin/tsc" hooks/typecheck-on-edit.sh retorna 0
    - grep -q "noEmit" hooks/typecheck-on-edit.sh retorna 0
    - grep -q "json.dumps" hooks/typecheck-on-edit.sh retorna 0 (serializacao segura do output do tsc)
    - case com *.ts|*.tsx presente; arquivo .md gera exit 0 sem stdout
    - grep -q "exit 2" hooks/typecheck-on-edit.sh retorna 0 (contrato asyncRewake)
    - Smoke: em diretorio temp sem node_modules/.bin/tsc, hook em arquivo .ts sai 0 silencioso
  </acceptance_criteria>
  <done>Hook roda tsc local em .ts/.tsx, acorda Claude com erro via exit 2, silencioso quando OK ou sem tsc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> hook (stdin) | file_path e cwd nao confiaveis |
| hook -> tsc subprocess | execucao de binario do projeto (node_modules/.bin/tsc) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Elevation | typecheck executa tsc do node_modules do cwd | accept | tsc local e o esperado/desejado; mesmo binario que o dev roda. cwd vem do harness confiavel da sessao |
| T-01-08 | Tampering | output do tsc com caracteres especiais quebra JSON | mitigate | Serializar additionalContext via python3 json.dumps, nunca concatenacao manual |
| T-01-09 | DoS | tsc lento bloqueia a UI | mitigate | async:true + asyncRewake:true + timeout:60 na settings.json (plano 04) |
</threat_model>

<verification>
- Hook detecta tsc local, roda --noEmit incremental, acorda Claude via exit 2 com JSON valido
- Silencioso para non-ts, ts valido, e ausencia de tsc
</verification>

<success_criteria>
- Editar .ts com erro -> aviso em segundos (via async)
- .ts valido ou sem tsc -> sem ruido
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/01-quality-memory-hooks/01-03-SUMMARY.md
</output>
