---
phase: "01"
plan: "01-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/precompact-state-save.sh
  - hooks/session-summary.sh
autonomous: true
requirements: [precompact, session-summary]
must_haves:
  truths:
    - "Ao disparar /compact, o STATE.md do projeto (.planning/STATE.md ou STATE.md) ganha/atualiza a secao Compact Snapshot com timestamp"
    - "Ao fim de cada turno (Stop), um arquivo de sessao e escrito em ~/.claude/sessions/YYYY-MM-DD-<slug>.tmp com as 4 secoes ECC"
    - "Em projeto com docs/CONTINUATION_HANDOFF.md, o Stop hook atualiza esse arquivo; em projeto sem ele, nao cria arquivo espurio"
  artifacts:
    - path: "hooks/precompact-state-save.sh"
      provides: "PreCompact hook que faz snapshot no STATE.md antes de compactar"
      min_lines: 35
    - path: "hooks/session-summary.sh"
      provides: "Stop hook que escreve sessao ECC em ~/.claude/sessions/ + atualiza CONTINUATION_HANDOFF.md"
      min_lines: 45
  key_links:
    - from: "hooks/precompact-state-save.sh"
      to: ".planning/STATE.md"
      via: "deteccao de STATE.md no cwd + reescrita da secao Compact Snapshot via python3"
      pattern: "Compact Snapshot"
    - from: "hooks/session-summary.sh"
      to: "~/.claude/sessions/"
      via: "escrita de arquivo .tmp datado a partir do transcript_path"
      pattern: "sessions"
---

<objective>
Entregar os dois hooks de memoria de sessao: precompact-state-save (PreCompact) que garante snapshot no STATE.md antes de compactar, e session-summary (Stop) que persiste a sessao em ~/.claude/sessions/ no padrao ECC de 4 secoes e atualiza o CONTINUATION_HANDOFF.md quando existe.

Purpose: Estado de sessao sobrevive a /compact e fica recuperavel entre sessoes/IDEs.
Output: 2 scripts bash em hooks/ — ambos defensivos quanto a projetos sem STATE.md/CONTINUATION_HANDOFF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-quality-memory-hooks/01-RESEARCH.md
@hooks/ideiaos-detector.sh
@CLAUDE.md

<interfaces>
PreCompact stdin: { session_id, transcript_path, cwd, trigger: "manual"|"auto" }.
Stop stdin: { session_id, transcript_path, cwd }.

Deteccao de STATE.md (RESEARCH Code Examples):

    STATE_FILE=""
    if [ -f "$CWD/.planning/STATE.md" ]; then STATE_FILE="$CWD/.planning/STATE.md"
    elif [ -f "$CWD/STATE.md" ]; then STATE_FILE="$CWD/STATE.md"; fi
    [ -z "$STATE_FILE" ] && exit 0

transcript_path e JSONL: uma linha por mensagem, formato {"role": ..., "message": {...}}.
Formato exato NAO 100% verificado (RESEARCH Open Question 1) — usar parse defensivo com fallback grep.

4 secoes ECC do session summary (RESEARCH Assumption A1):
  1. O que funcionou (com evidencia)
  2. O que falhou
  3. O que nao foi tentado
  4. Proximos passos
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar precompact-state-save.sh</name>
  <files>hooks/precompact-state-save.sh</files>
  <read_first>
    - .planning/phases/01-quality-memory-hooks/01-RESEARCH.md (Pattern 5 + Code Example "PreCompact com snapshot de STATE.md" + Pitfall 4)
    - hooks/ideiaos-detector.sh (padrao de deteccao por cwd)
  </read_first>
  <behavior>
    - cwd com .planning/STATE.md gera reescrita/insercao da secao "## Compact Snapshot" com timestamp; secao antiga substituida (nao duplicada)
    - cwd sem STATE.md gera exit 0 silencioso (sem criar arquivo)
    - conteudo do snapshot e estruturado/minimal (bullets + timestamp), nunca conteudo bruto do transcript (Pitfall 4)
    - STATE.md permanece markdown valido apos escrita (sem caracteres de controle)
  </behavior>
  <action>
    Criar hooks/precompact-state-save.sh (#!/usr/bin/env bash, set -uo pipefail). Seguir Code Example do RESEARCH.
    1. Ler stdin; extrair cwd, trigger via python3. [ -z "$CWD" ] && CWD="$PWD".
    2. Detectar STATE_FILE (padrao das interfaces). Se nenhum, exit 0.
    3. TIMESTAMP="$(date '+%Y-%m-%d %H:%M')".
    4. Via python3: ler conteudo atual; se "## Compact Snapshot" existe, truncar a partir do marcador (remove secao antiga); reanexar nova secao:
       "## Compact Snapshot\n\n**Auto-saved:** $TIMESTAMP (PreCompact hook, trigger: $TRIGGER)\n\n- Snapshot automatico antes do /compact. Detalhes da sessao em ~/.claude/sessions/ (session-summary hook)."
       Escrever de volta o STATE_FILE. NAO inserir conteudo bruto do transcript (Pitfall 4).
    5. Emitir additionalContext informando que STATE.md foi atualizado (preservado no resumo do compact). Truncar a < 5000 chars (Pitfall 6).
    6. exit 0. NAO usar decision:block (compactacao nao deve ser bloqueada).
    Sanitizar variaveis interpoladas no python3 (usar argv, nao f-string com conteudo do usuario).
  </action>
  <verify>
    <automated>mkdir -p /tmp/pcs-test/.planning; printf '# State\n\nconteudo\n' > /tmp/pcs-test/.planning/STATE.md; echo '{"cwd":"/tmp/pcs-test","trigger":"manual","session_id":"t"}' | bash hooks/precompact-state-save.sh >/dev/null; grep -q "Compact Snapshot" /tmp/pcs-test/.planning/STATE.md && echo OK; rm -rf /tmp/pcs-test</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Compact Snapshot" hooks/precompact-state-save.sh retorna 0
    - Projeto com STATE.md ganha secao "## Compact Snapshot" com timestamp apos execucao
    - Rodar o hook 2x no mesmo STATE.md NAO duplica a secao (apenas 1 ocorrencia de "## Compact Snapshot")
    - Projeto sem STATE.md: nenhum arquivo criado (verificar que /tmp sem .planning fica intacto)
    - grep -q "decision.*block" hooks/precompact-state-save.sh retorna != 0 (nao bloqueia)
  </acceptance_criteria>
  <done>PreCompact atualiza STATE.md de forma idempotente apenas onde STATE.md existe; smoke test passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar session-summary.sh</name>
  <files>hooks/session-summary.sh</files>
  <read_first>
    - .planning/phases/01-quality-memory-hooks/01-RESEARCH.md (Pattern 4: leitura transcript_path + Pitfall 3 + Open Question 1)
    - CLAUDE.md (protocolo de fechamento: STATE.md + CONTINUATION_HANDOFF.md)
    - hooks/ideiaos-detector.sh (deteccao por cwd)
  </read_first>
  <behavior>
    - Stop com transcript_path valido gera arquivo ~/.claude/sessions/YYYY-MM-DD-<slug>.tmp com as 4 secoes ECC
    - cwd com docs/CONTINUATION_HANDOFF.md gera atualizacao desse arquivo; cwd sem ele NAO cria o arquivo (Pitfall 3)
    - transcript_path ausente/ilegivel gera escrita de arquivo de sessao minimal (sem crash) ou exit 0 silencioso
    - hook nunca bloqueia (exit 0; sem decision:block)
  </behavior>
  <action>
    Criar hooks/session-summary.sh (#!/usr/bin/env bash, set -uo pipefail).
    1. Ler stdin; extrair session_id, transcript_path, cwd via python3. [ -z "$CWD" ] && CWD="$PWD".
    2. mkdir -p "$HOME/.claude/sessions".
    3. Derivar slug: usar basename do cwd + parte curta do session_id (sanitizado: somente [a-z0-9-]). DATE="$(date '+%Y-%m-%d')". SESSION_FILE="$HOME/.claude/sessions/${DATE}-${SLUG}.tmp".
    4. Extrair ultimo turno assistant do transcript via python3 (Pattern 4), parse defensivo: tentar json.loads por linha; fallback para grep "assistant" se parse falhar (Open Question 1). Truncar a ~2000 chars.
    5. Escrever SESSION_FILE com as 4 secoes ECC (cabecalhos fixos; preencher "O que funcionou" com o resumo extraido; demais secoes com placeholder "(a revisar)" se nao inferivel — NUNCA inventar conteudo). Incluir timestamp e cwd.
    6. Se [ -f "$CWD/docs/CONTINUATION_HANDOFF.md" ]: atualizar/anexar bloco datado "## Ultima sessao automatica ($DATE)" com link para o SESSION_FILE e proximo passo placeholder. Idempotente (substituir bloco anterior do mesmo tipo). Se ausente: NAO criar (Pitfall 3).
    7. exit 0 puro — sem JSON output, sem decision:block (RESEARCH Anti-Pattern Stop hook).
    Sanitizar SLUG e session_id antes de usar em paths.
  </action>
  <verify>
    <automated>echo '{"session_id":"abc12345","transcript_path":"/nonexistent.jsonl","cwd":"/tmp"}' | bash hooks/session-summary.sh; ls "$HOME/.claude/sessions/$(date '+%Y-%m-%d')"-*.tmp >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -qi "funcionou" hooks/session-summary.sh retorna 0 (secao ECC presente)
    - grep -q "CONTINUATION_HANDOFF" hooks/session-summary.sh retorna 0
    - grep -q "sessions" hooks/session-summary.sh retorna 0
    - Apos execucao com cwd=/tmp (sem CONTINUATION_HANDOFF), arquivo de sessao existe em ~/.claude/sessions/ mas /tmp/docs/CONTINUATION_HANDOFF.md NAO foi criado
    - Hook nao emite decision:block (grep -q "block" retorna != 0 OU so em comentario)
  </acceptance_criteria>
  <done>Stop persiste sessao ECC em ~/.claude/sessions/ e atualiza CONTINUATION_HANDOFF.md so onde existe; smoke test passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> hook (stdin) | transcript_path, cwd, session_id nao confiaveis |
| hook -> STATE.md / CONTINUATION_HANDOFF.md | escrita em arquivos de projeto |
| hook -> ~/.claude/sessions/ | escrita em path derivado de slug/session_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | session-summary path do .tmp | mitigate | Sanitizar slug e session_id para [a-z0-9-] antes de montar SESSION_FILE |
| T-01-05 | Tampering | precompact escreve STATE.md com conteudo arbitrario | mitigate | Snapshot estruturado/minimal, nunca conteudo bruto do transcript (Pitfall 4) |
| T-01-06 | Information Disclosure | transcript pode conter segredos copiados pra sessao .tmp | accept | ~/.claude/sessions/ e local ao usuario; truncado; mesmo risco do transcript original |
</threat_model>

<verification>
- precompact-state-save: idempotente, so escreve onde STATE.md existe
- session-summary: arquivo .tmp criado; CONTINUATION_HANDOFF so atualizado onde existe
- Ambos saem 0 e nunca bloqueiam
</verification>

<success_criteria>
- /compact gera snapshot no STATE.md
- Stop gera arquivo de sessao ECC em ~/.claude/sessions/
- Nenhum arquivo espurio criado em projetos sem a estrutura IdeiaOS
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/01-quality-memory-hooks/01-02-SUMMARY.md
</output>
