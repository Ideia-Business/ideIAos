---
phase: 07-contexts-evals
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - setup.sh
  - manifests/modules.json
  - manifests/plugin-membership.md
  - README.md
autonomous: false
requirements:
  - F07-ALIASES
  - F07-STATUSLINE
  - F07-MANIFEST
  - F07-README-SYNC
user_setup: []

must_haves:
  truths:
    - "setup.sh offers claude-dev / claude-review / claude-research aliases without auto-editing dotfiles"
    - "setup.sh deploys the mode contexts to a stable path the aliases read from"
    - "setup.sh offers the IdeiaOS statusline via a settings.json snippet (never auto-edits settings.json)"
    - "manifests/modules.json registers the 3 contexts + statusline; evals is NOT a module"
    - "plugin-membership.md records the contexts/statusline membership decision"
    - "README lists contexts/, the 3 aliases, the statusline, and evals/; check-readme-sync passes"
    - "The full pre-commit gate (bash -n setup.sh + README sync) passes without --no-verify"
  artifacts:
    - path: "setup.sh"
      provides: "Alias offer + context deploy + statusline snippet offer (T-01-10 compliant)"
      contains: "claude-review"
    - path: "manifests/modules.json"
      provides: "Registry entries for contexts + statusline"
      contains: "context"
    - path: "README.md"
      provides: "Discoverable docs for contexts, aliases, statusline, evals"
      contains: "claude-review"
  key_links:
    - from: "setup.sh"
      to: "~/.ideiaos/contexts/<mode>.md"
      via: "copies source/contexts/*.md to a stable read path for aliases"
      pattern: "\\.ideiaos/contexts"
    - from: "aliases (claude-dev/review/research)"
      to: "claude --append-system-prompt"
      via: "shell function cat-ing the deployed context"
      pattern: "append-system-prompt"
    - from: "README.md"
      to: "check-readme-sync.sh"
      via: "all audited scripts/*.sh + new components listed"
      pattern: "ideiaos-statusline|run-evals"
---

<objective>
Wire the Wave 1 deliverables into the IdeiaOS install + catalog: setup.sh offers the three mode
aliases and the statusline (T-01-10 compliant — snippets/offers, never silent dotfile/settings
edits), deploys the contexts to a stable path, registers contexts + statusline in
manifests/modules.json + plugin-membership.md, syncs the README, and runs the full smoke test.

Purpose: Make Fase 07 installable and discoverable on a fresh machine, satisfying the ROADMAP
success criteria ("claude-review abre em modo review; suite de evals roda contra 20+ casos reais")
while passing every repo gate (scan, README sync, pre-commit) without bypass.

Output: edits to setup.sh, manifests/modules.json, manifests/plugin-membership.md, README.md.
This plan owns ALL the shared files for Fase 07 (that is why it is Wave 2, after 07-01 + 07-02).

NOTE: This plan contains a checkpoint:human-verify (alias + statusline visual/functional check),
so `autonomous: false`. Run automation first, then pause for the operator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Depends on Wave 1 outputs (already created by 07-01 and 07-02):
@source/contexts/dev.md
@source/contexts/review.md
@source/contexts/research.md
@source/statusline/ideiaos-statusline.sh
@evals/README.md

# These are the files THIS plan reads to extend correctly:
@setup.sh
@manifests/modules.json
@manifests/plugin-membership.md

# Hard constraints (CRITICAL — gate failures block the commit):
# - T-01-10: setup.sh NEVER auto-edits ~/.claude/settings.json or user dotfiles (.zshrc/.bashrc).
#   It may CREATE files it owns and PRINT snippets/offers, mirroring the EXISTING patterns in
#   setup.sh: e.g. the 'idea-setup' alias is offered via scripts/install-alias.sh (opt-in), and
#   every hook registration prints a `cat <<'SNIPPET'` block + `warn` when not present in
#   settings.json. Follow that EXACT idiom for the new aliases and statusline.
# - Confirmed CLI flag: `claude --append-system-prompt "<text>"` APPENDS to the default system
#   prompt (preserves CLAUDE.md auto-discovery, hooks, memory). Use this — NOT `--system-prompt`
#   (which REPLACES the entire default prompt and would strip IdeiaOS's own context).
# - NEVER use `<!--` in any source/ component or snippet (scan-absorbed.sh Check 2 blocks it).
# - check-readme-sync.sh audits: source/hooks/*.sh, source/skills/*/, source/agents/*.md,
#   scripts/*.sh, source/templates/**/*.tmpl. The new files that COULD be audited:
#     * source/statusline/ideiaos-statusline.sh — it is under source/ but NOT scripts/ and NOT a
#       hook/skill/agent/template glob, so the script does NOT auto-audit it. README must still
#       list it for humans.
#     * source/contexts/*.md — NOT in any audited glob; README lists them for discoverability.
#     * evals/run-evals.sh — under evals/, NOT scripts/; not auto-audited; README lists it.
#   => check-readme-sync.sh should already PASS for existing globs; verify it still exits 0 after
#      edits. If you ADD any `scripts/*.sh` (you should NOT need to), it WOULD be audited.
# - Pre-commit hook guards source/|scripts/|plugins/|manifests/ — since this plan touches
#   manifests/ + setup.sh, the README must be in the same commit and check-readme-sync must pass.

# MEMBERSHIP DECISION (locked for this plan):
#   Contexts and the statusline are NOT Claude Code skills/agents/hooks and do NOT fit the
#   build-plugins.sh copy model (which builds agents/ skills/ hooks/ dirs). DECISION:
#   they are setup.sh-only modules (plugin: null). Contexts install to ~/.ideiaos/contexts/ and
#   are consumed by shell aliases; the statusline is offered via a settings.json snippet. This
#   keeps build-plugins.sh unchanged. Document this rationale in plugin-membership.md. evals/ is a
#   repo-level regression asset and is NOT registered in modules.json at all.

<interfaces>
modules.json schema (existing entries are the contract — match field names exactly):
```json
{
  "id": "context-dev",
  "kind": "context",
  "description": "...",
  "source": "source/contexts/dev.md",
  "targets": ["claude"],
  "deps": [],
  "installStrategy": "manual",
  "plugin": null
}
```
- `kind: "context"` is a NEW kind (extension, backward-compatible — build-plugins.sh ignores
  kinds it does not handle; it only iterates its own CORE_* arrays, so a new kind is inert there).
- statusline entry: `kind: "statusline"`, `source: "source/statusline/ideiaos-statusline.sh"`,
  `installStrategy: "manual"`, `plugin: null`.

setup.sh idiom to mirror (offer-not-edit), from the existing 'idea-setup' + hook blocks:
```bash
if [ -f "$SETTINGS_FILE" ] && grep -q "<marker>" "$SETTINGS_FILE" 2>/dev/null; then
  ok "<thing> já configurado"
else
  warn "<thing> NÃO configurado — adicione:"
  cat <<'SNIPPET'
  ...snippet...
SNIPPET
fi
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy contexts + offer aliases + offer statusline in setup.sh</name>
  <files>setup.sh, source/statusline/ideiaos-statusline.sh</files>
  <action>
Add a new numbered step block to setup.sh (follow the existing `step "5.x) ..."` numbering — use
the next free numbers, e.g. `5.19` and `5.20`; keep them inside the `if [ "$PROJECT_ONLY" -eq 0 ]`
global section alongside the other global hook/skill installs). Reuse the existing color/ok/warn/
step helpers. Mirror the EXISTING offer-not-edit idiom precisely (see <interfaces>). NEVER use
`<!--`. Keep `set -euo pipefail` happy (quote paths, guard greps with `2>/dev/null`, default vars).

**Step A — Contexts deploy + aliases (T-01-10 compliant):**
  1. Deploy the three context files to a stable read path the aliases can `cat`:
     `mkdir -p "$HOME/.ideiaos/contexts"` then copy `source/contexts/{dev,review,research}.md`
     from `$SETUP_DIR/source/contexts/` to `$HOME/.ideiaos/contexts/` (idempotent: copy each time
     so updates propagate; this is a file IdeiaOS OWNS, so copying is allowed — unlike dotfiles).
     `ok` each deploy.
  2. Aliases: do NOT write to .zshrc/.bashrc directly here. Check whether the shell rc already
     contains `alias claude-review=` (probe both ~/.zshrc and ~/.bashrc with `grep -qF … 2>/dev/null`).
     If present → `ok "Aliases de modo já configurados"`. If absent → `warn` + print a `cat
     <<'SNIPPET'` block the user can paste, defining all three as shell functions, e.g.:
        claude-dev()      { claude --append-system-prompt "$(cat "$HOME/.ideiaos/contexts/dev.md")" "$@"; }
        claude-review()   { claude --append-system-prompt "$(cat "$HOME/.ideiaos/contexts/review.md")" "$@"; }
        claude-research() { claude --append-system-prompt "$(cat "$HOME/.ideiaos/contexts/research.md")" "$@"; }
     Use functions (not bare aliases) so extra args (`"$@"`) pass through. Explain in an echo that
     the flag is `--append-system-prompt` (appends; preserves default context) by design.
     OPTIONAL extension (only if it matches repo convention cleanly): mention that an installer
     `scripts/install-alias.sh`-style opt-in could be added later — but do NOT create a new
     scripts/*.sh in THIS plan unless necessary (it would be auto-audited by check-readme-sync).
     Prefer the snippet-offer to keep the gate surface minimal.

**Step B — Statusline offer (T-01-10 compliant — NEVER auto-edit settings.json):**
  1. Deploy the statusline script to a stable path IdeiaOS owns: copy
     `$SETUP_DIR/source/statusline/ideiaos-statusline.sh` → `$HOME/.ideiaos/statusline/ideiaos-statusline.sh`
     (`mkdir -p` first; `chmod +x`). `ok` the deploy.
  2. Probe `~/.claude/settings.json` for `ideiaos-statusline.sh`. If present → `ok "Statusline
     IdeiaOS já registrada"`. If absent → `warn` + print a `cat <<'SNIPPET'` block showing the
     exact `statusLine` field to add, matching the confirmed live shape:
        {
          "statusLine": {
            "type": "command",
            "command": "bash \"$HOME/.ideiaos/statusline/ideiaos-statusline.sh\""
          }
        }
     (Render `$HOME` literally as a path placeholder note, or instruct the user to use their
     absolute home path — mirror how other snippets show `/Users/<você>/...`.) Make crystal clear
     setup.sh did NOT modify settings.json.

After editing, run `bash -n setup.sh` to confirm syntax.
  </action>
  <verify>
    <automated>bash -n setup.sh && grep -q "append-system-prompt" setup.sh && grep -q "claude-review" setup.sh && grep -q "ideiaos-statusline.sh" setup.sh && grep -q ".ideiaos/contexts" setup.sh && ! grep -q '<!--' setup.sh && echo OK</automated>
  </verify>
  <done>
    `bash -n setup.sh` passes. setup.sh deploys contexts to ~/.ideiaos/contexts/, offers the three
    `claude-*` functions via a paste snippet using `--append-system-prompt`, deploys + offers the
    statusline via a settings.json snippet, and NEVER auto-edits .zshrc/.bashrc/settings.json. No
    `<!--`. statusline script is executable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register contexts + statusline in manifests, sync README</name>
  <files>manifests/modules.json, manifests/plugin-membership.md, README.md</files>
  <action>
**manifests/modules.json** — append FOUR new module objects to the `modules` array (valid JSON —
keep trailing-comma discipline; validate with node at the end). Match the existing schema exactly
(see <interfaces>). Do NOT register evals/ (repo-level asset, not a module).
  - context-dev / context-review / context-research:
      kind "context", source "source/contexts/<mode>.md", targets ["claude"], deps [],
      installStrategy "manual", plugin null, description naming the mode.
  - statusline-ideiaos:
      kind "statusline", source "source/statusline/ideiaos-statusline.sh", targets ["claude"],
      deps [], installStrategy "manual", plugin null,
      description "IdeiaOS standard statusline — branch, model, dir, GSD phase, context".
  Update the top-level `generated` date to today if present. Validate:
  `node -e "JSON.parse(require('fs').readFileSync('manifests/modules.json','utf8'))"`.

**manifests/plugin-membership.md** — add a short section documenting the membership DECISION
(line 1 stays `# SOURCE: IdeiaOS v2`; NEVER `<!--`):
  - New subsection e.g. "## Setup-only (não-plugin): contexts + statusline" explaining that the 3
    contexts and the statusline are installed by setup.sh (not by build-plugins.sh), live at
    ~/.ideiaos/, and are `plugin: null` in modules.json. State the rationale: they are not
    Claude skills/agents/hooks and do not fit the plugin copy model; build-plugins.sh is unchanged.
  - One line noting evals/ is a repo-level regression asset, not a module nor a plugin member.

**README.md** — make the new components discoverable so the README-sync gate + human readers are
satisfied (NEVER `<!--`):
  1. Repo-structure tree: replace the placeholder line
     `│   ├── contexts/    ← vazio — para Fase 07 (contexts-evals)` with a populated entry listing
     dev.md/review.md/research.md; add a `│   ├── statusline/  ← ideiaos-statusline.sh` line under
     source/; add a top-level `├── evals/  ← suíte de regressão (≥20 casos reais) + run-evals.sh`
     entry. Bump the source/ summary counts/notes if they mention exact numbers.
  2. Add documentation of the three aliases (`claude-dev`, `claude-review`, `claude-research`) and
     the statusline to an appropriate existing section (e.g. the "Terminal" usage section around
     line 407, or "O que este setup instala"). Mention `--append-system-prompt` and that they are
     offered (snippet), not auto-installed. Reference the evals/ suite + `bash evals/run-evals.sh`.
  3. Ensure the literal strings the sync script and humans look for are present:
     `ideiaos-statusline.sh`, `claude-dev`, `claude-review`, `claude-research`, `run-evals.sh`,
     `source/contexts/`. (run-evals.sh + ideiaos-statusline.sh are not auto-audited, but listing
     them keeps README truthful and future-proof.)

Run `bash scripts/check-readme-sync.sh .` — it MUST exit 0 (it audits the existing globs; confirm
nothing regressed). Then `node -e "JSON.parse(...)"` on modules.json once more.
  </action>
  <verify>
    <automated>node -e "const m=JSON.parse(require('fs').readFileSync('manifests/modules.json','utf8')); const ids=m.modules.map(x=>x.id); const need=['context-dev','context-review','context-research','statusline-ideiaos']; for(const n of need){if(!ids.includes(n)){console.error('MISSING '+n);process.exit(1);}} if(m.modules.some(x=>/eval/i.test(x.id))){console.error('evals should NOT be a module');process.exit(1);} console.log('modules OK');" && grep -q "claude-review" README.md && grep -q "ideiaos-statusline.sh" README.md && grep -q "run-evals.sh" README.md && grep -q "source/contexts/" README.md && head -1 manifests/plugin-membership.md | grep -qx "# SOURCE: IdeiaOS v2" && ! grep -q '<!--' manifests/plugin-membership.md && bash scripts/check-readme-sync.sh . && echo OK</automated>
  </verify>
  <done>
    modules.json contains context-dev/review/research + statusline-ideiaos with the correct schema
    and valid JSON; evals is NOT a module. plugin-membership.md documents the setup.sh-only
    decision. README lists contexts/, the 3 aliases, the statusline, and evals/. `check-readme-sync.sh .`
    exits 0. No `<!--` introduced.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke test — aliases, statusline, evals runner, full gate</name>
  <what-built>
    setup.sh now deploys mode contexts to ~/.ideiaos/contexts/, offers claude-dev/review/research
    via `--append-system-prompt` snippets, and offers the IdeiaOS statusline via a settings.json
    snippet (no auto-edits). modules.json + plugin-membership.md register contexts + statusline.
    README lists everything. Wave 1 produced the contexts, statusline script, and ≥20-case eval
    suite with a runner.
  </what-built>
  <how-to-verify>
Claude runs these automated checks first and reports results, THEN the operator confirms the
interactive bits:

AUTOMATED (Claude runs and pastes output):
  1. `bash -n setup.sh` → exit 0.
  2. `bash scripts/check-readme-sync.sh .` → exit 0 ("✅ README sincronizado.").
  3. `node -e "JSON.parse(require('fs').readFileSync('manifests/modules.json','utf8'))"` → no error.
  4. `printf '%s' '{"model":{"display_name":"Opus 4.8"},"workspace":{"current_dir":"'"$PWD"'"}}' | bash source/statusline/ideiaos-statusline.sh`
     → one line containing "Opus 4.8" and the repo branch.
  5. `bash evals/run-evals.sh --list </dev/null` → enumerates ≥20 cases.
  6. `ls evals/cases/EVAL-*.md | wc -l` → ≥ 20.
  7. Dry-run the pre-commit gate without committing: stage the changed files and run the repo's
     check the hook would run — `bash scripts/check-readme-sync.sh .` (already #2) and `bash -n setup.sh`
     (already #1). Confirm BOTH pass so a real commit will NOT need `--no-verify`.

OPERATOR (manual, in a real shell):
  8. Paste the alias snippet setup.sh printed into your shell (or `source` your rc after adding it),
     then run `claude-review` in a scratch dir and confirm Claude opens in review posture
     (it should refuse to edit files and offer analysis-only) — this is the ROADMAP success check
     "claude-review abre em modo review".
  9. (Optional) Add the statusline snippet to ~/.claude/settings.json and confirm the status bar
     shows branch · model · dir.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all automated checks pass and claude-review opens in review mode; otherwise
    describe what failed (e.g. "statusline empty on garbage", "review mode still edited a file",
    "README sync failed on X").
  </resume-signal>
</task>

</tasks>

<verification>
| Check | Command | Expected |
|-------|---------|----------|
| setup.sh syntax | `bash -n setup.sh` | exit 0 |
| Aliases use correct flag | `grep -c append-system-prompt setup.sh` | ≥ 1 |
| No --system-prompt (replace) misuse for aliases | `grep -n '\-\-system-prompt ' setup.sh \|\| true` | none for aliases |
| Contexts deployed by setup | `grep -c '.ideiaos/contexts' setup.sh` | ≥ 1 |
| Statusline offered, not auto-edited | `grep -c 'ideiaos-statusline.sh' setup.sh` | ≥ 1 |
| modules.json valid + entries | node JSON.parse + id check (Task 2 verify) | OK |
| evals NOT a module | grep modules.json for eval ids | none |
| README sync gate | `bash scripts/check-readme-sync.sh .` | exit 0 |
| No HTML comments added | `grep -rn '<!--' setup.sh manifests/plugin-membership.md README.md` | none new |
| Commit-clean (no bypass) | `bash -n setup.sh && bash scripts/check-readme-sync.sh .` | both exit 0 |
</verification>

<success_criteria>
- setup.sh deploys mode contexts to ~/.ideiaos/contexts/ and offers claude-dev/review/research as
  `--append-system-prompt` shell functions via a paste snippet — never auto-editing dotfiles.
- setup.sh deploys + offers the IdeiaOS statusline via a settings.json snippet — never auto-editing
  settings.json (T-01-10 honored).
- manifests/modules.json registers context-dev/review/research (kind "context") + statusline-ideiaos
  (kind "statusline"), plugin null; evals/ is intentionally not a module. plugin-membership.md
  documents the setup.sh-only decision and that build-plugins.sh is unchanged.
- README lists contexts/, the 3 aliases, the statusline, and evals/; `check-readme-sync.sh .` exits 0.
- Full gate (`bash -n setup.sh` + README sync) passes so a real commit needs NO `--no-verify`.
- Operator confirms `claude-review` opens in review mode (ROADMAP success criterion).
</success_criteria>

<output>
After completion, create `.planning/phases/07-contexts-evals/07-03-SUMMARY.md`.
</output>
