---
phase: 10-token-optimizations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/agents/silent-failure-hunter.md
  - source/hooks/strategic-compact.sh
  - manifests/modules.json
  - setup.sh
autonomous: true
requirements:
  - R3-05
  - R3-06
  - R3-07
must_haves:
  truths:
    - "silent-failure-hunter.md tem model: sonnet e descrição atualizada justificando o downgrade"
    - "strategic-compact.sh não contém nenhuma invocação de python3; contador usa bash puro via /tmp"
    - "manifests/modules.json contém entry typescript-lsp com installStrategy: stack:typescript"
    - "setup.sh instala typescript-lsp condicionalmente quando detect_stack detecta typescript"
    - "build-adapters.sh --target all termina sem erro após todas as mudanças"
  artifacts:
    - path: "source/agents/silent-failure-hunter.md"
      provides: "Agent com model: sonnet"
      contains: "model: sonnet"
    - path: "source/hooks/strategic-compact.sh"
      provides: "Contador bash puro sem python3"
    - path: "manifests/modules.json"
      provides: "Entry typescript-lsp"
      contains: "typescript-lsp"
    - path: "setup.sh"
      provides: "Instalação condicional do typescript-lsp"
  key_links:
    - from: "setup.sh"
      to: "detect_stack()"
      via: "grep typescript nos stacks retornados"
      pattern: "detect_stack.*typescript"
    - from: "manifests/modules.json"
      to: "typescript-lsp"
      via: "installStrategy: stack:typescript"
      pattern: "stack:typescript"
---

<objective>
Aplicar três otimizações de custo de token independentes: (1) downgrade do model do
silent-failure-hunter de opus para sonnet, (2) reescrever contador do strategic-compact.sh
em bash puro eliminando subprocess python3, (3) registrar typescript-lsp no manifesto com
installStrategy condicional e integrar setup.sh como primeira consumidora real de
installStrategy: stack:typescript via detect_stack().

Purpose: Reduz custo de tokens em ~5x por invocação do hunter; elimina ~5ms de overhead em
toda chamada de ferramenta nas sessões longas; habilita LSP TypeScript sob demanda em
projetos do ecossistema Ideia Business (NFideia, IdeiaPartner, Lovable).

Output: Três arquivos modificados + modules.json + setup.sh com bloco condicional novo.

IMPORTANTE: Fase 09 pode estar modificando manifests/modules.json concorrentemente.
Antes de editar modules.json, verificar se há commits recentes de Fase 09 e fazer
git pull / rebase se necessário: `git log --oneline -5` — se ver commits "feat(09-*)"
ou "docs(09-*)", fazer `git pull origin work` antes de editar modules.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/v3/token-economy-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Downgrade silent-failure-hunter opus → sonnet (R3-05)</name>
  <files>source/agents/silent-failure-hunter.md</files>
  <action>
    Editar source/agents/silent-failure-hunter.md:

    1. Frontmatter: alterar `model: opus` para `model: sonnet`.

    2. Na linha description do frontmatter, substituir a justificativa atual
       "Opus — diagnóstico de falha exige raciocínio profundo." pela versão atualizada:
       "Sonnet — segue grep patterns fixos (catch vazio, promise sem await, retornos ignorados);
       processo mecânico validado; downgrade de opus confirmado em token-economy-review.md."

    Não alterar nenhuma outra linha do arquivo (seções Quando usar, Quando NÃO usar,
    Processo, Output permanecem intactas).

    Após a edição, validar manualmente 3 casos representativos do agent (não é execução
    real de API — são casos de inspeção de comportamento esperado):
      Caso 1: `catch {}` vazio — o agent com sonnet deveria identificar pelo grep pattern.
      Caso 2: `.then(fn)` sem `.catch` — detectável por grep, não exige raciocínio aberto.
      Caso 3: retorno de `supabase.from().select()` não checado — segue o mesmo pattern de grep.
    Os três casos confirmam que o processo é grep-based, não requer opus. Documentar essa
    validação no SUMMARY deste plano.
  </action>
  <verify>
    <automated>grep -E "^model: sonnet" source/agents/silent-failure-hunter.md</automated>
    <automated>grep -v "^model: opus" source/agents/silent-failure-hunter.md | grep -c "model:" || true</automated>
    <automated>grep -v "^#" source/agents/silent-failure-hunter.md | grep -c "opus" && echo "FAIL: opus ainda presente" || echo "OK: opus removido"</automated>
  </verify>
  <done>
    `grep "^model:" source/agents/silent-failure-hunter.md` retorna `model: sonnet`.
    Nenhuma ocorrência de "opus" permanece no arquivo.
    Description atualizada com justificativa de downgrade.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reescrever contador strategic-compact.sh em bash puro (R3-06)</name>
  <files>source/hooks/strategic-compact.sh</files>
  <action>
    Reescrever source/hooks/strategic-compact.sh eliminando TODOS os subprocessos python3.
    O comportamento observável deve ser idêntico ao atual:
      - Session_id vazio → exit 0 silencioso
      - Path traversal no session_id → exit 0 silencioso
      - Conta tool calls por session_id usando arquivo em /tmp
      - A cada 50 chamadas (50, 100, 150, ...) emite hookSpecificOutput.additionalContext
      - Nunca emite exit code diferente de 0

    Estratégia de substituição:

    BLOCO 1 — Parse do session_id (substitui python3 -c "json.load..."):
      Usar `jq -r '.session_id // ""' 2>/dev/null` como primeiro choice.
      Fallback se jq não disponível: `grep -o '"session_id"\s*:\s*"[^"]*"' | sed 's/.*"\([^"]*\)"$/\1/'`
      Pattern completo com fallback:
      ```
      SESSION_ID="$(echo "$INPUT" | \
        (command -v jq &>/dev/null && jq -r '.session_id // ""' 2>/dev/null) || \
        echo "$INPUT" | grep -o '"session_id"[[:space:]]*:[[:space:]]*"[^"]*"' | \
          sed 's/.*"session_id"[[:space:]]*:[[:space:]]*"\([^"]*\)"/\1/' 2>/dev/null || \
        echo "")"
      ```

    BLOCO 2 — Leitura do contador (substitui python3 -c "json.load(open(...))..."):
      Counter file muda de JSON para plain text (um número por linha):
        COUNTER_FILE="/tmp/claude-compact-counter-${SESSION_ID}"  # sem .json
        COUNT=0
        if [ -f "$COUNTER_FILE" ]; then
          COUNT="$(cat "$COUNTER_FILE" 2>/dev/null || echo 0)"
          COUNT="${COUNT:-0}"
          # Garantir que é número
          [[ "$COUNT" =~ ^[0-9]+$ ]] || COUNT=0
        fi

    BLOCO 3 — Escrita do contador (substitui python3 -c "json.dump..."):
        COUNT=$((COUNT + 1))
        echo "$COUNT" > "$COUNTER_FILE" || true

    BLOCO 4 — Emissão do JSON de saída (substitui python3 -c "json.dumps..."):
      Construir o JSON inline em bash usando printf. A mensagem e estrutura do JSON
      devem ser idênticas ao original:
        MSG="~${COUNT} tool calls nesta sessao — considere rodar /compact para preservar qualidade. O hook precompact-state-save salvara o STATE.md automaticamente."
        printf '{"hookSpecificOutput":{"hookEventName":"PreToolUse","additionalContext":"%s"}}\n' "$MSG"

    IMPORTANTE: Manter o cabeçalho de documentação do arquivo (linhas 1-18) intacto.
    Manter a lógica de guards (empty session_id, path traversal) exatamente como está.
    O arquivo resultante NÃO deve conter nenhuma ocorrência de "python3".
    O COUNTER_FILE muda de sufixo .json para sem sufixo — isso é intencional (formato
    plain text é mais simples e mais eficiente que JSON para um único inteiro).

    Testar comportamento após reescrita:
      echo '{"session_id":"test-abc-123","tool_name":"Read"}' | bash source/hooks/strategic-compact.sh
      Resultado esperado: silencioso (exit 0) para chamadas 1-49.
      Para simular 50ª chamada: criar /tmp/claude-compact-counter-test-abc-123 com valor 49,
      então rodar o hook — deve emitir o JSON de additionalContext.
  </action>
  <verify>
    <automated>grep -c "python3" source/hooks/strategic-compact.sh && echo "FAIL: python3 ainda presente" || echo "OK: sem python3"</automated>
    <automated>bash -n source/hooks/strategic-compact.sh && echo "OK: sintaxe bash válida"</automated>
    <automated>echo '{"session_id":"plantest-$$","tool_name":"Read"}' | bash source/hooks/strategic-compact.sh; echo "exit=$?"</automated>
    <automated>echo 49 > /tmp/claude-compact-counter-plantest-50; echo '{"session_id":"plantest-50","tool_name":"Read"}' | bash source/hooks/strategic-compact.sh | grep -c "additionalContext" && echo "OK: emite no 50" || echo "FAIL: não emitiu"</automated>
  </verify>
  <done>
    `grep -c "python3" source/hooks/strategic-compact.sh` retorna 0.
    `bash -n source/hooks/strategic-compact.sh` retorna 0 (sintaxe válida).
    Hook é silencioso para chamadas 1-49 e emite JSON additionalContext na 50ª chamada.
    Comportamento com session_id vazio: exit 0 sem output.
    Comportamento com session_id contendo "/": exit 0 sem output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Registrar typescript-lsp no manifesto e wiring em setup.sh (R3-07)</name>
  <files>manifests/modules.json, setup.sh</files>
  <action>
    PASSO 0 — Sincronizar com Fase 09 antes de editar modules.json:
      git log --oneline -5
      Se aparecerem commits com prefixo "feat(09-" ou "docs(09-", executar:
        git pull origin work
      Isso garante que a entry ideiaos-checker de Fase 09 está presente antes de adicionar
      a entry typescript-lsp.

    PASSO 1 — Adicionar entry typescript-lsp em manifests/modules.json:
      Inserir ANTES do bloco `"statusline-ideiaos"` (penúltima entry antes de fechar o array)
      o seguinte objeto JSON (respeitar vírgula separadora do objeto anterior):

      {
        "id": "typescript-lsp",
        "kind": "lsp",
        "description": "TypeScript Language Server — find-references semântico, hover types e rename-symbol para projetos TypeScript. Instalado apenas quando detect_stack detecta tsconfig.json.",
        "source": null,
        "targets": ["claude"],
        "deps": [],
        "installStrategy": "stack:typescript",
        "plugin": "ideiaos-core",
        "config": {
          "tsconfig_path": "$(find . -name tsconfig.json -maxdepth 2 | head -1)"
        }
      }

      Validar JSON após edição:
        python3 -m json.tool manifests/modules.json > /dev/null && echo "OK: JSON válido"

    PASSO 2 — Adicionar bloco de instalação condicional em setup.sh:
      Localizar o bloco de `setup_project()` ou a seção de steps do projeto (após step "5")
      — especificamente localizar o ponto após setup_lovable_project e antes de
      setup_learnings_layer em torno da linha 1537-1541.

      Na função setup_ideiaos_layer (ou no bloco PROJECT_ONLY, após a chamada a
      setup_lovable_project), adicionar novo step condicional:

        # Camada TypeScript LSP (stack:typescript — primeira consumidora real de detect_stack)
        if [[ "${PROJECT_ONLY}" -eq 1 || "${SETUP_DIR}" != "" ]]; then
          TARGET_PROJ="${PROJECT_DIR:-$PWD}"
          DETECTED_STACKS="$(detect_stack "$TARGET_PROJ")"
          if echo "$DETECTED_STACKS" | grep -qw "typescript"; then
            TSCONFIG_PATH="$(find "$TARGET_PROJ" -name tsconfig.json -maxdepth 2 2>/dev/null | head -1)"
            if [ -n "$TSCONFIG_PATH" ]; then
              ok "TypeScript LSP: tsconfig.json encontrado em $TSCONFIG_PATH (módulo typescript-lsp ativo)"
            else
              warn "TypeScript LSP: stack typescript detectado mas tsconfig.json não encontrado — LSP não ativado"
            fi
          fi
        fi

      Posição exata de inserção: imediatamente após a linha que contém
      `setup_lovable_project "$PROJECT_DIR" "$PROJECT_NAME"` (cerca de linha 1538).

      NOTA: O bloco acima é a primeira consumidora real de detect_stack() para
      installStrategy: stack:typescript. A função detect_stack() já existe desde Fase 03
      (linha ~438 do setup.sh) e detecta typescript via presença de tsconfig.json.

    PASSO 3 — Rodar build-adapters após todas as mudanças:
      bash scripts/build-adapters.sh --target all
      Deve terminar com exit 0. Se falhar, inspecionar o erro — pode ser que Fase 09
      adicionou validação de model/tools nos agents e os arquivos já estão corretos.
  </action>
  <verify>
    <automated>python3 -m json.tool manifests/modules.json > /dev/null && echo "OK: JSON válido"</automated>
    <automated>grep -c '"id": "typescript-lsp"' manifests/modules.json && echo "OK: entry presente" || echo "FAIL: entry ausente"</automated>
    <automated>grep -c '"installStrategy": "stack:typescript"' manifests/modules.json && echo "OK: installStrategy correto" || echo "FAIL"</automated>
    <automated>grep -c "detect_stack.*typescript\|typescript.*detect_stack\|LSP.*typescript\|typescript.*LSP" setup.sh || grep -c "TypeScript LSP" setup.sh</automated>
    <automated>bash -n setup.sh && echo "OK: sintaxe setup.sh válida"</automated>
    <automated>bash scripts/build-adapters.sh --target all && echo "OK: build-adapters passou" || echo "WARN: build-adapters falhou — ver saída"</automated>
  </verify>
  <done>
    `grep '"id": "typescript-lsp"' manifests/modules.json` retorna 1 match.
    `grep '"installStrategy": "stack:typescript"' manifests/modules.json` retorna 1 match.
    `python3 -m json.tool manifests/modules.json` termina sem erro.
    setup.sh contém bloco condicional que invoca detect_stack() e verifica "typescript".
    `bash -n setup.sh` retorna 0.
    `bash scripts/build-adapters.sh --target all` retorna 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin → strategic-compact.sh | JSON não confiável lido do hook event; session_id pode conter path traversal |
| /tmp/claude-compact-counter-{id} | Arquivo de estado mutável lido/escrito por bash puro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | strategic-compact.sh counter file | mitigate | Manter guard de path traversal existente (reject session_id com `/`, `\`, `..`); counter file em /tmp isolado por session_id |
| T-10-02 | Information Disclosure | strategic-compact.sh MSG emit | accept | MSG contém apenas contagem numérica; não expõe conteúdo de ferramentas; risco baixo |
| T-10-03 | Spoofing | session_id em counter file name | mitigate | Guard regex existente `grep -qE '[/\\]|\.\.'` mantido intacto na reescrita bash |
| T-10-04 | Denial of Service | /tmp write fail no contador | accept | `echo "$COUNT" > "$COUNTER_FILE" || true` — falha silenciosa; counter não crítico para operação |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Fase 10 não instala nenhum pacote externo; typescript-lsp registrado no manifesto como referência de configuração, não como pacote npm a instalar |
</threat_model>

<verification>
## Acceptance Gates

| Requisito | Verificação | Comando |
|-----------|-------------|---------|
| R3-05 | model: sonnet no frontmatter | `grep "^model: sonnet" source/agents/silent-failure-hunter.md` |
| R3-05 | opus removido do arquivo | `grep -v "^#" source/agents/silent-failure-hunter.md | grep -c "opus"` → 0 |
| R3-06 | Sem python3 no hook | `grep -c "python3" source/hooks/strategic-compact.sh` → 0 |
| R3-06 | Sintaxe bash válida | `bash -n source/hooks/strategic-compact.sh` → exit 0 |
| R3-06 | Emite na 50ª chamada | Teste manual descrito em Task 2 verify |
| R3-07 | Entry typescript-lsp presente | `grep -c '"id": "typescript-lsp"' manifests/modules.json` → 1 |
| R3-07 | installStrategy correto | `grep '"installStrategy": "stack:typescript"' manifests/modules.json` → 1 match |
| R3-07 | JSON válido | `python3 -m json.tool manifests/modules.json > /dev/null` → exit 0 |
| R3-07 | setup.sh condicional | `grep -c "TypeScript LSP" setup.sh` → ≥1 |
| R3-07 | build-adapters passa | `bash scripts/build-adapters.sh --target all` → exit 0 |
</verification>

<success_criteria>
1. `source/agents/silent-failure-hunter.md` tem `model: sonnet`; nenhuma ocorrência de "opus"; description documenta justificativa do downgrade.
2. `source/hooks/strategic-compact.sh` não contém "python3"; `bash -n` passa; contador bash puro funciona corretamente (silent 1-49, emite na 50ª).
3. `manifests/modules.json` contém entry `typescript-lsp` com `"installStrategy": "stack:typescript"` e `"kind": "lsp"`; JSON válido.
4. `setup.sh` contém bloco condicional que chama `detect_stack()` e verifica "typescript" antes de ativar o LSP; `bash -n setup.sh` passa.
5. `bash scripts/build-adapters.sh --target all` retorna exit 0 após todas as mudanças.
</success_criteria>

<output>
Criar `.planning/phases/10-token-optimizations/10-01-SUMMARY.md` ao concluir,
seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
