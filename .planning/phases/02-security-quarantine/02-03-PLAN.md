---
phase: 02-security-quarantine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - setup-dev-machine.sh
  - docs/security/memory-hygiene.md
  - AGENTS.md
autonomous: true
requirements: [REQ-05]
must_haves:
  truths:
    - "O plist gerado pelo setup-dev-machine.sh tem timeout wrapper no ProgramArguments"
    - "Rodar setup-dev-machine.sh recria o LaunchAgent com kill-switch sem quebrar o autosync"
    - "docs/security/memory-hygiene.md formaliza a regra (sem secrets, memória projeto≠global, reset pós-run)"
    - "AGENTS.md referencia a regra de memory hygiene"
  artifacts:
    - path: "setup-dev-machine.sh"
      provides: "Plist com timeout 120 wrapper no ProgramArguments"
      contains: "timeout"
    - path: "docs/security/memory-hygiene.md"
      provides: "Regra formal de higiene de memória"
      min_lines: 25
    - path: "AGENTS.md"
      provides: "Referência à regra de memory hygiene"
      contains: "memory-hygiene"
  key_links:
    - from: "setup-dev-machine.sh plist"
      to: "~/.local/bin/timeout"
      via: "ProgramArguments wrapper"
      pattern: "timeout"
    - from: "AGENTS.md"
      to: "docs/security/memory-hygiene.md"
      via: "referência de leitura"
      pattern: "memory-hygiene"
---

<objective>
Adicionar (1) **kill-switch via timeout wrapper** ao LaunchAgent do autosync (evita git-autosync travado indefinidamente — lição OpenClaw), e (2) formalizar **memory hygiene** como regra documentada em `docs/security/memory-hygiene.md`, referenciada no AGENTS.md.

Purpose: Endurece o daemon de background (DoS de processo travado) e formaliza a regra "sem secrets em memória" que o idea-doctor (Plano 02) já passa a checar.
Output: Plist com timeout 120 + doc de memory hygiene + referência no AGENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-security-quarantine/02-RESEARCH.md
@setup-dev-machine.sh
@AGENTS.md

<interfaces>
<!-- Bloco do plist a modificar (setup-dev-machine.sh linhas 210-226, heredoc PLIST_EOF) -->
ProgramArguments ATUAL (linha 217):
  <array><string>$SCRIPT_PATH</string><string>--all</string></array>
SCRIPT_PATH = ~/.local/bin/git-autosync (linha 22)
Shim timeout JÁ instalado em $BIN_DIR/timeout = ~/.local/bin/timeout (linhas 67-90)
LABEL = com.ideiaos.gitautosync; recarga via bootout + bootstrap + kickstart (linhas 227-230)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill-switch — timeout wrapper no plist</name>
  <read_first>setup-dev-machine.sh (linhas 22-26 vars, 62-90 shim timeout, 208-231 bloco plist), .planning/phases/02-security-quarantine/02-RESEARCH.md (Pattern 4, Pitfall 5, Assumption A3)</read_first>
  <files>setup-dev-machine.sh</files>
  <action>
Editar o heredoc do plist (linha 217) para envolver o git-autosync com o shim `timeout 120`. O shim já está em `$BIN_DIR/timeout` (= `~/.local/bin/timeout`), instalado mais cedo no mesmo script.

Definir uma variável `TIMEOUT_BIN="$BIN_DIR/timeout"` perto das outras vars (após linha 22, onde SCRIPT_PATH é definido). Trocar a linha 217:

DE:
```
    <array><string>$SCRIPT_PATH</string><string>--all</string></array>
```
PARA:
```
    <array><string>$TIMEOUT_BIN</string><string>120</string><string>$SCRIPT_PATH</string><string>--all</string></array>
```

Adicionar também ao plist (dentro do `<dict>`, antes de `</dict>` na linha 224) a chave que garante kill de subprocessos (Assumption A4 / Anti-pattern do RESEARCH):
```
    <key>AbandonProcessGroup</key><false/>
```

Como `$TIMEOUT_BIN` é caminho absoluto, não depende do PATH do plist (mitiga Assumption A3). Usar Edit cirúrgico — NÃO reescrever o arquivo. Manter o heredoc `PLIST_EOF` intacto no resto.
  </action>
  <acceptance_criteria>
- `grep -q 'TIMEOUT_BIN=' setup-dev-machine.sh` → exit 0
- `grep -q '<string>120</string>' setup-dev-machine.sh` → exit 0
- `grep -q 'AbandonProcessGroup' setup-dev-machine.sh` → exit 0
- `bash -n setup-dev-machine.sh` → exit 0
- O ProgramArguments contém timeout ANTES do git-autosync (ordem: timeout, 120, script, --all)
  </acceptance_criteria>
  <verify>
    <automated>bash -n setup-dev-machine.sh && grep -q 'TIMEOUT_BIN=' setup-dev-machine.sh && grep -q '<string>120</string>' setup-dev-machine.sh && grep -q 'AbandonProcessGroup' setup-dev-machine.sh && echo OK</automated>
  </verify>
  <done>Plist gera ProgramArguments com timeout 120 wrapper + AbandonProcessGroup false; sintaxe válida.</done>
</task>

<task type="auto">
  <name>Task 2: docs/security/memory-hygiene.md + referência no AGENTS.md</name>
  <read_first>AGENTS.md, .planning/phases/02-security-quarantine/02-RESEARCH.md (Open Question 3, Security Domain), .planning/research/ECC-ABSORPTION-PLAN.md (Fase 2 item 3)</read_first>
  <files>docs/security/memory-hygiene.md, AGENTS.md</files>
  <action>
1. Criar `docs/security/memory-hygiene.md` formalizando as 3 regras (Fase 2 item 3 do ECC-ABSORPTION-PLAN + Security Domain do RESEARCH):
   - **Sem secrets em memória/vault**: nunca gravar API keys, service_role, tokens em `~/.claude/projects/`, no vault Obsidian ou em SUMMARY/STATE. Usar referências ("a chave está no 1Password/env"), não o valor.
   - **Memória de projeto ≠ global**: `~/.claude/projects/<projeto>/` é isolado por projeto; não copiar contexto sensível de um projeto para o global. (Já é assim por design — esta regra formaliza.)
   - **Reset após runs não-confiáveis**: depois de absorver/testar conteúdo de terceiros (skills ECC em quarentena), limpar a memória da sessão antes de continuar trabalho confiável.
   - Incluir como verificar: `bash scripts/idea-doctor.sh` (Seção 7c audita secrets em memória).
   - Header de atribuição: regra derivada do ECC the-security-guide.md.
2. Adicionar ao AGENTS.md uma linha/seção curta referenciando a regra: que toda sessão deve respeitar `docs/security/memory-hygiene.md` (sem secrets em memória; reset pós-quarentena). Usar Edit cirúrgico no AGENTS.md.
  </action>
  <acceptance_criteria>
- `test -f docs/security/memory-hygiene.md` → exit 0
- `grep -qi 'secret' docs/security/memory-hygiene.md` → exit 0
- `grep -qi 'reset\|não-confiáv\|quarent' docs/security/memory-hygiene.md` → exit 0
- `grep -q 'memory-hygiene' AGENTS.md` → exit 0
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/security/memory-hygiene.md && grep -qi 'secret' docs/security/memory-hygiene.md && grep -q 'memory-hygiene' AGENTS.md && echo OK</automated>
  </verify>
  <done>Regra de memory hygiene documentada com as 3 sub-regras e como verificar; AGENTS.md a referencia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon background→git | git-autosync roda sem supervisão; pull pode travar em conflito |
| sessão→memória persistida | conteúdo de quarentena pode contaminar memória da sessão seguinte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | DoS | git-autosync travado indefinidamente (git pull em conflito) | mitigate | timeout 120 wrapper no plist mata o processo; AbandonProcessGroup false mata filhos |
| T-02-12 | Information Disclosure | secrets gravados em ~/.claude/projects ou vault | mitigate | regra formal memory-hygiene.md + check idea-doctor 7c |
| T-02-13 | Tampering | memória de sessão contaminada por conteúdo de quarentena | mitigate | regra "reset após runs não-confiáveis" documentada |
</threat_model>

<verification>
- `bash -n setup-dev-machine.sh` passa.
- Plist gerado tem `timeout 120` no ProgramArguments + AbandonProcessGroup false.
- docs/security/memory-hygiene.md existe com as 3 regras; AGENTS.md referencia.
</verification>

<success_criteria>
LaunchAgent regenerado nunca trava >2min (timeout 120); memory hygiene formalizada e referenciada no AGENTS.md, alinhada ao check do idea-doctor (Plano 02).
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-quarantine/02-03-SUMMARY.md`
</output>
