---
phase: 02-security-quarantine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - security/scan-absorbed.sh
  - security/quarantine/.gitkeep
  - security/README.md
autonomous: true
requirements: [REQ-01, REQ-02]
must_haves:
  truths:
    - "Rodar scan-absorbed.sh num arquivo com unicode invisível retorna exit 1 (FAIL)"
    - "Rodar scan-absorbed.sh num arquivo com payload <script> retorna exit 1 (FAIL)"
    - "Rodar scan-absorbed.sh num arquivo limpo retorna exit 0 (PASS)"
    - "Comandos suspeitos (curl/wget/ssh) geram WARN, não FAIL"
    - "AgentShield indisponível (offline) não bloqueia o scan (WARN, exit 0)"
  artifacts:
    - path: "security/scan-absorbed.sh"
      provides: "Pipeline de quarentena: unicode + payloads + comandos + AgentShield"
      min_lines: 50
    - path: "security/quarantine/.gitkeep"
      provides: "Staging area versionada para conteúdo de terceiros"
    - path: "security/README.md"
      provides: "Doc de uso do pipeline de quarentena"
  key_links:
    - from: "security/scan-absorbed.sh"
      to: "npx ecc-agentshield"
      via: "npx --yes invocation com fallback"
      pattern: "npx --yes ecc-agentshield"
    - from: "security/scan-absorbed.sh"
      to: "ripgrep unicode codepoints"
      via: "rg -nP com \\x{...}"
      pattern: "rg -nP"
---

<objective>
Criar `security/scan-absorbed.sh` — o pipeline de quarentena obrigatório que TODO conteúdo de terceiros (skills/agents/rules do ECC) deve passar antes de ser absorvido em `source/`. Implementa a decisão travada "Quarentena obrigatória" do PROJECT.md.

Purpose: É o PRÉ-REQUISITO técnico das fases 04-06 (absorção do catálogo ECC). Sem ele, conteúdo malicioso (36% das skills públicas têm prompt injection — estudo ToxicSkills) entra sem scan.
Output: Script executável + staging dir `quarantine/` + doc de uso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-security-quarantine/02-RESEARCH.md

<interfaces>
<!-- Comportamento de exit codes que consumidores (idea-doctor, fluxo de absorção) esperam -->
scan-absorbed.sh:
  Uso:  bash security/scan-absorbed.sh <arquivo-ou-diretório>
  Exit: 0 = PASS (limpo, OU só WARNs); 1 = FAIL (unicode invisível ou payload ativo); 2 = erro de invocação (target não existe)

Dependências verificadas (02-RESEARCH.md Environment Availability):
  rg 14.1.1 (requer flag -P para \x{...})
  npx ecc-agentshield 1.5.0 (download na 1ª vez; --yes suprime prompt)
  AgentShield OFFLINE → WARN, não FAIL (não bloquear fluxo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Criar staging dir + test fixtures (Wave 0)</name>
  <read_first>.planning/phases/02-security-quarantine/02-RESEARCH.md (seção "Validation Architecture", "Wave 0 Gaps")</read_first>
  <files>security/quarantine/.gitkeep, /tmp/payload-unicode.md, /tmp/payload-html.md, /tmp/payload-clean.md</files>
  <action>
1. Criar `security/quarantine/.gitkeep` (arquivo vazio) — staging area versionada.
2. Criar fixtures de teste (NÃO versionar — ficam em /tmp):
   - `/tmp/payload-unicode.md`: linha normal + uma linha com Zero-Width Space (U+200B). Gere com: `printf 'skill normal\nlinha com\xE2\x80\x8Bcaractere invisivel\n' > /tmp/payload-unicode.md`
   - `/tmp/payload-html.md`: `printf 'skill doc\n<script>fetch("http://evil")</script>\ndata:text/html,x\n' > /tmp/payload-html.md`
   - `/tmp/payload-clean.md`: `printf '# Skill limpa\nApenas markdown normal sem nada suspeito.\n' > /tmp/payload-clean.md`
  </action>
  <acceptance_criteria>
- `test -f security/quarantine/.gitkeep` → exit 0
- `rg -nP '[\x{200B}]' /tmp/payload-unicode.md` encontra match (confirma fixture contaminada)
- `grep -q '<script>' /tmp/payload-html.md` → exit 0
  </acceptance_criteria>
  <verify>
    <automated>test -f security/quarantine/.gitkeep && rg -nP '[\x{200B}]' /tmp/payload-unicode.md && grep -q '<script>' /tmp/payload-html.md && echo OK</automated>
  </verify>
  <done>Staging dir versionado existe; 3 fixtures de teste em /tmp prontas para validar o script.</done>
</task>

<task type="auto">
  <name>Task 1: Escrever security/scan-absorbed.sh</name>
  <read_first>.planning/phases/02-security-quarantine/02-RESEARCH.md (Pattern 2, Pitfall 1-4, Common Pitfalls)</read_first>
  <files>security/scan-absorbed.sh</files>
  <action>
Criar `security/scan-absorbed.sh` (chmod +x) seguindo Pattern 2 do RESEARCH, COM as correções dos Pitfalls. Estrutura exata:

```bash
#!/usr/bin/env bash
# security/scan-absorbed.sh — pipeline de quarentena para conteúdo de terceiros (ECC).
# Uso: bash security/scan-absorbed.sh <arquivo-ou-diretório>
# Exit: 0 = PASS (limpo ou só WARN); 1 = FAIL (payload ativo); 2 = erro de invocação.
# Decisão travada PROJECT.md: NENHUM conteúdo de terceiros é absorvido sem passar aqui.
set -uo pipefail

TARGET="${1:-security/quarantine}"
[ -e "$TARGET" ] || { echo "ERRO: target não existe: $TARGET" >&2; exit 2; }
PASS=0; WARN=0; FAIL=0

# Check 1 — Unicode invisível (prompt injection oculto). FAIL automático.
if rg -nP '[\x{200B}\x{200C}\x{200D}\x{2060}\x{FEFF}\x{202A}-\x{202E}]' "$TARGET" 2>/dev/null; then
  echo "  ✗ FAIL: caractere(s) Unicode invisível(eis) detectado(s)"; FAIL=$((FAIL+1))
else echo "  ✓ sem unicode invisível"; PASS=$((PASS+1)); fi

# Check 2 — Payloads HTML/JS inline. FAIL automático.
if rg -n '<!--|<script|data:text/html|base64,' "$TARGET" 2>/dev/null; then
  echo "  ✗ FAIL: payload HTML/JS/base64 detectado"; FAIL=$((FAIL+1))
else echo "  ✓ sem payloads HTML/JS"; PASS=$((PASS+1)); fi

# Check 3 — Comandos suspeitos. WARN (curl/ssh aparecem em docs legítimas) — inspeção manual.
if rg -n 'curl|wget|nc |scp |ssh |enableAllProjectMcpServers|ANTHROPIC_BASE_URL' "$TARGET" 2>/dev/null; then
  echo "  ⚠ WARN: comandos suspeitos — INSPEÇÃO MANUAL obrigatória antes de promover"; WARN=$((WARN+1))
else echo "  ✓ sem comandos suspeitos"; PASS=$((PASS+1)); fi

# Check 4 — AgentShield (best-effort; offline não bloqueia).
if command -v npx >/dev/null 2>&1 && npx --yes ecc-agentshield scan --path "$TARGET" --format json \
     --output "/tmp/agentshield-scan-$(date +%s).json" 2>/dev/null; then
  echo "  ✓ AgentShield: sem findings críticos"; PASS=$((PASS+1))
else
  echo "  ⚠ WARN: AgentShield indisponível/offline — scan parcial"; WARN=$((WARN+1))
fi

echo ""
echo "Scan: PASS=$PASS WARN=$WARN FAIL=$FAIL  (target: $TARGET)"
if [ "$FAIL" -gt 0 ]; then echo "RESULTADO: BLOQUEADO — não absorver."; exit 1; fi
[ "$WARN" -gt 0 ] && echo "RESULTADO: APROVADO COM RESSALVA — revisar WARNs manualmente."
[ "$WARN" -eq 0 ] && echo "RESULTADO: LIMPO — aprovado para absorção."
exit 0
```

NÃO usar `exit 2` por WARN (Pitfall 4: bloqueia skills legítimas). NÃO usar `grep -P` para unicode (Pitfall 2: usar `rg -nP`). AgentShield offline = WARN + exit 0 (Pitfall 3).
Aplicar: `chmod +x security/scan-absorbed.sh`.
  </action>
  <acceptance_criteria>
- `test -x security/scan-absorbed.sh` → exit 0
- `grep -q 'rg -nP' security/scan-absorbed.sh` → exit 0 (Pitfall 2 honrado)
- `grep -q 'npx --yes ecc-agentshield' security/scan-absorbed.sh` → exit 0
- `grep -q 'exit 2' security/scan-absorbed.sh` aparece SÓ no check de invocação (não em WARN)
- `bash -n security/scan-absorbed.sh` (syntax check) → exit 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n security/scan-absorbed.sh && test -x security/scan-absorbed.sh && grep -q 'rg -nP' security/scan-absorbed.sh && grep -q 'npx --yes ecc-agentshield' security/scan-absorbed.sh && echo OK</automated>
  </verify>
  <done>Script executável, sintaxe válida, usa rg -nP e npx --yes, sem exit 2 por WARN.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test do pipeline + security/README.md</name>
  <read_first>security/scan-absorbed.sh, .planning/phases/02-security-quarantine/02-RESEARCH.md (Code Examples)</read_first>
  <files>security/README.md</files>
  <action>
1. Rodar o smoke test contra as 3 fixtures e confirmar exit codes:
   - `bash security/scan-absorbed.sh /tmp/payload-unicode.md` → DEVE exit 1
   - `bash security/scan-absorbed.sh /tmp/payload-html.md` → DEVE exit 1
   - `bash security/scan-absorbed.sh /tmp/payload-clean.md` → DEVE exit 0
   - `bash security/scan-absorbed.sh /caminho/inexistente` → DEVE exit 2
   Se algum exit code divergir, corrigir o script antes de prosseguir.
2. Criar `security/README.md` documentando:
   - Propósito (decisão travada "Quarentena obrigatória" do PROJECT.md)
   - Fluxo: colocar conteúdo de terceiros em `security/quarantine/` → rodar `bash security/scan-absorbed.sh security/quarantine/` → só promover para `source/` se exit 0
   - Significado dos exit codes (0/1/2) e da distinção WARN vs FAIL
   - Nota: AgentShield via npx baixa na 1ª execução (10-30s); offline = scan parcial
  </action>
  <acceptance_criteria>
- `bash security/scan-absorbed.sh /tmp/payload-unicode.md; [ $? -eq 1 ]` → verdadeiro
- `bash security/scan-absorbed.sh /tmp/payload-html.md; [ $? -eq 1 ]` → verdadeiro
- `bash security/scan-absorbed.sh /tmp/payload-clean.md; [ $? -eq 0 ]` → verdadeiro
- `grep -qi 'quarentena\|quarantine' security/README.md` → exit 0
  </acceptance_criteria>
  <verify>
    <automated>bash security/scan-absorbed.sh /tmp/payload-unicode.md; [ $? -eq 1 ] && bash security/scan-absorbed.sh /tmp/payload-clean.md; [ $? -eq 0 ] && grep -qi 'quarant' security/README.md && echo OK</automated>
  </verify>
  <done>Pipeline detecta unicode e payload (exit 1), passa em arquivo limpo (exit 0); README documenta o fluxo obrigatório.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terceiro→source/ | Conteúdo ECC (skills/agents/rules) entra como input não-confiável; scan-absorbed.sh é o gate |
| script→AgentShield (npx) | Download de pacote npm na 1ª execução; rede não-confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | scan-absorbed.sh (unicode check) | mitigate | rg -nP detecta U+200B/U+202A-E etc.; FAIL automático bloqueia absorção |
| T-02-02 | Tampering | scan-absorbed.sh (payload check) | mitigate | rg detecta `<script>`, `data:text/html`, `base64,`; FAIL automático |
| T-02-03 | Information Disclosure | conteúdo com ANTHROPIC_BASE_URL/enableAllProjectMcpServers | mitigate | check 3 (WARN) + inspeção manual obrigatória; AgentShield reforça |
| T-02-04 | DoS | npx ecc-agentshield offline trava o script | accept | offline → WARN + exit 0; não bloqueia fluxo (Pitfall 3) |
| T-02-05 | Tampering | npx baixa pacote ecc-agentshield comprometido | accept | pacote do mesmo autor do ECC (affaan-m), MIT; risco baixo, scan é best-effort |
</threat_model>

<verification>
- `bash -n security/scan-absorbed.sh` passa (sintaxe).
- Smoke test: unicode → exit 1, html → exit 1, clean → exit 0, inexistente → exit 2.
- `security/quarantine/.gitkeep` e `security/README.md` existem.
</verification>

<success_criteria>
scan-absorbed.sh detecta payload de teste (exit 1) e passa em conteúdo limpo (exit 0), cumprindo o critério de sucesso da fase ("scan-absorbed.sh detecta payload de teste").
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-quarantine/02-01-SUMMARY.md`
</output>
