---
phase: 08-ideiaos-v3-review
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v3/token-economy-review.md
autonomous: true
requirements:
  - F08-TOKEN-ECONOMY
user_setup: []

must_haves:
  truths:
    - "An action→model matrix maps each major IdeiaOS action to its model with relative cost"
    - "Per-hook overhead is characterized (which events fire, how heavy)"
    - "Subagent-spawn vs inline guidance exists (when a separate agent is worth the context cost)"
    - "The mgrep + LSP re-evaluation reaches a FINAL decision (adopt / defer / reject, with the trigger condition)"
    - "Concrete token-reduction opportunities are listed (e.g. contexts via --append-system-prompt vs always-loaded CLAUDE.md)"
  artifacts:
    - path: "docs/v3/token-economy-review.md"
      provides: "Updated model/action matrix, hook overhead, spawn-vs-inline rule, mgrep/LSP final decision, reduction opportunities"
      contains: "# SOURCE: IdeiaOS v2"
      min_lines: 100
  key_links:
    - from: "docs/v3/token-economy-review.md"
      to: "source/agents/*.md + source/hooks/ + docs/decisions/mgrep-lsp-evaluation.md"
      via: "model frontmatter, hook event triggers, and the deferred mgrep/LSP decision inspected on disk"
      pattern: "model:|PostToolUse|PreCompact|mgrep|lsp"
---

<objective>
Review IdeiaOS token economy and produce the updated model/action matrix, hook overhead characterization, a subagent-spawn-vs-inline rule, the FINAL mgrep + LSP decision (the one deferred from Phase 04), and a concrete list of token-reduction opportunities.

Purpose: Cost and quality both depend on routing the right model to the right action and not burning context on overhead. This is the quantitative backbone of the v3 review — it must close the open mgrep/LSP decision and surface where IdeiaOS spends tokens it doesn't need to.

Output: `docs/v3/token-economy-review.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/decisions/mgrep-lsp-evaluation.md

<audit_inputs>
- Model routing per agent (the 15 in source/agents/) — confirm `model:` on disk; note the 2 with NONE.
- Hooks to characterize (inspect `source/hooks/` and the project hook configs): typecheck-on-edit (PostToolUse .ts/.tsx), console-log-guard (PostToolUse), precompact-state-save (PreCompact), session-summary (Stop), strategic-compact (PreToolUse counter), plus the Phase 05 observation hooks (UserPromptSubmit/PostToolUse → observations.jsonl). For each: which event, how often it fires, rough overhead (cheap echo vs spawns a process vs runs tsc).
- mgrep + LSP: docs/decisions/mgrep-lsp-evaluation.md is documentation-only and explicitly says "re-evaluate in Phase 08". This plan must produce the FINAL decision.
- Context delivery: source/contexts/ (dev/review/research) injected via --append-system-prompt (07-01) vs CLAUDE.md / rules always loaded — a real reduction lever.
</audit_inputs>

<known_facts>
From STATE.md (verify on disk):
- 07-01: `--append-system-prompt` is the confirmed mechanism for contexts (preserves CLAUDE.md + hooks); ctx statusline segment derived from cost.total_tokens.
- 04-04: mgrep/LSP documented as Phase 08 candidates; nothing installed. The decision doc lists adoption conditions (mgrep: >30% measured reduction in IdeiaOS context; LSP: >50k LOC TS / significant Python, installStrategy stack-scoped).
- 05-01: observation hooks capture only the first token of Bash commands (privacy by design) — relevant to per-event overhead.
</known_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the action→model matrix and characterize hook overhead</name>
  <files>docs/v3/token-economy-review.md (scratch/notes — finalized in Task 2)</files>
  <action>
    1. **Action→model matrix.** Enumerate the major IdeiaOS actions (drive from the 15 agents + the main skill workflows). For each action, record the model it currently uses and a RELATIVE cost label (haiku=baixo / sonnet=médio / opus=alto). Flag any action that is over-modeled (opus where sonnet suffices) or under-modeled (haiku where the job needs reasoning). Cross-reference 08-01 routing verdicts if the executor has them, but this plan is self-sufficient: re-read `model:` from source/agents/ on disk.
    2. **Hook overhead.** For each hook (list in audit_inputs), inspect the script in `source/hooks/` and record: event it binds to, firing frequency (every edit / every tool call / only on compact / only on stop), and overhead class (cheap echo, spawns subprocess, runs tsc, appends to jsonl). Identify any hook that fires hot (e.g. PostToolUse on every edit) and whether its cost is justified.
    3. **Spawn vs inline.** Draft the rule for when spawning a subagent pays for itself vs doing the work inline (consider: fresh context window benefit, the spawn's own system-prompt overhead, parallelism). Anchor to real agents (e.g. code-explorer haiku spawn is cheap and isolates search; opus reviewers are worth a spawn for isolation).

    Verify every model value and hook binding against the real files.
  </action>
  <verify>
    <automated>test -d /Users/gustavolopespaiva/dev/IdeiaOS/source/hooks && test $(ls /Users/gustavolopespaiva/dev/IdeiaOS/source/agents/*.md | wc -l | tr -d ' ') -eq 15</automated>
  </verify>
  <done>Action→model matrix drafted with relative costs; every hook's event+frequency+overhead recorded from disk; spawn-vs-inline rule drafted.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve mgrep/LSP and write docs/v3/token-economy-review.md</name>
  <files>docs/v3/token-economy-review.md</files>
  <action>
    Create `docs/v3/token-economy-review.md`. First line MUST be `# SOURCE: IdeiaOS v2`. Never use literal `<!--`. Structure:

    1. `## Resumo` — 2-3 sentences: state of token economy + the headline mgrep/LSP decision.
    2. `## Matriz Modelo × Ação` — table: Ação | Modelo atual | Custo relativo | Veredito (manter / trocar para X) | Justificativa. Cover the major actions; flag every over/under-model.
    3. `## Overhead de Hooks` — table: Hook | Evento | Frequência | Overhead | Justificado?. One row per hook inspected.
    4. `## Subagent: Spawn vs Inline` — the rule + 2-3 worked cases (when to spawn, when to inline).
    5. `## Decisão Final: mgrep + LSP` — re-read docs/decisions/mgrep-lsp-evaluation.md and reach a FINAL disposition for each tool: **adotar / adiar / rejeitar**, with the explicit trigger condition and what evidence would change it. This closes the Phase-04 deferral — do not leave it open.
    6. `## Oportunidades de Redução` — concrete, ranked list (e.g. contexts via --append-system-prompt to avoid always-loaded weight; trim over-granted agent tools; demote any over-modeled action; mute/lighten a hot hook). Each item: lever + estimated direction of saving. These feed 08-04.

    Be quantitative where possible (relative cost labels are fine; absolute token counts only if measurable). The mgrep/LSP section MUST land a decision word (adotar/adiar/rejeitar).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && head -1 docs/v3/token-economy-review.md | grep -q "# SOURCE: IdeiaOS v2" && ! grep -q '<!--' docs/v3/token-economy-review.md && grep -q 'Matriz Modelo' docs/v3/token-economy-review.md && grep -q 'Overhead de Hooks' docs/v3/token-economy-review.md && grep -qi 'mgrep' docs/v3/token-economy-review.md && grep -qiE 'adotar|adiar|rejeitar' docs/v3/token-economy-review.md && grep -qi 'Oportunidades de Redução' docs/v3/token-economy-review.md</automated>
  </verify>
  <done>docs/v3/token-economy-review.md exists; SOURCE header; no HTML comment; model×action matrix + hook overhead table present; mgrep/LSP final decision word present; reduction opportunities listed.</done>
</task>

</tasks>

<verification>
| Check | Command | Expect |
|-------|---------|--------|
| Doc exists | `test -f docs/v3/token-economy-review.md` | exit 0 |
| SOURCE header | `head -1 docs/v3/token-economy-review.md` | `# SOURCE: IdeiaOS v2` |
| No HTML comment | `grep -c '<!--' docs/v3/token-economy-review.md` | 0 |
| Model matrix | `grep -c 'Matriz Modelo' docs/v3/token-economy-review.md` | ≥1 |
| Hook overhead | `grep -c 'Overhead de Hooks' docs/v3/token-economy-review.md` | ≥1 |
| mgrep/LSP decision | `grep -ciE 'adotar|adiar|rejeitar' docs/v3/token-economy-review.md` | ≥1 |
| Reduction list | `grep -ci 'Oportunidades de Redução' docs/v3/token-economy-review.md` | ≥1 |
</verification>

<success_criteria>
- Action→model matrix with relative costs + over/under-model flags.
- Hook overhead characterized per event with a justification verdict.
- Spawn-vs-inline rule stated with worked cases.
- mgrep + LSP FINAL decision reached (adotar/adiar/rejeitar + trigger).
- Concrete reduction opportunities ready for 08-04.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ideiaos-v3-review/08-03-SUMMARY.md`
</output>
