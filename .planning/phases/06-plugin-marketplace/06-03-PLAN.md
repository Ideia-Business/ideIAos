---
phase: "06"
plan: "06-03"
wave: 3
title: "README: seção 'Instalação via Plugin' + árvore atualizada (pós-remoção + plugins) + smoke test integrado + commit conjunto"
goal: "Fechar a Fase 06: adicionar ao README a seção 'Instalação via Plugin' (/plugin marketplace add + install dos 3 sub-plugins), atualizar a árvore 'Estrutura do repositório' para refletir os dirs-raiz removidos (06-02) e os novos .claude-plugin/ + plugins/ + scripts/build-plugins.sh (06-01), rodar o smoke test de integração do plugin (marketplace + plugin.json + hooks.json válidos, build idempotente, check-readme-sync verde), e fazer o commit conjunto que cobre remoção (06-02) + README, passando no pre-commit hook SEM --no-verify."
subsystem: readme-integration
tags: [readme, plugin-install-docs, file-tree, smoke-test, integration, commit, wave3]
depends_on: ["06-01", "06-02"]
provides:
  - readme-plugin-install-section
  - readme-tree-updated-post-removal
  - phase-06-integration-verified
  - phase-06-commit
affects:
  modified:
    - README.md
estimated_duration: "~60min"
checkpoint: none

must_haves:
  truths:
    - "O README tem uma seção 'Instalação via Plugin' com os comandos /plugin marketplace add Ideia-Business/IdeiaOS e /plugin install <plugin>@ideiaos para os 3 sub-plugins."
    - "A árvore 'Estrutura do repositório' do README NÃO lista mais skills/, agents/, hooks/, templates/ na raiz (removidos em 06-02) e LISTA .claude-plugin/, plugins/ e scripts/build-plugins.sh."
    - "scripts/check-readme-sync.sh (já apontando para source/, de 06-02) roda e retorna sincronizado (exit 0)."
    - "O smoke test de integração confirma: marketplace.json válido, 3 plugin.json válidos, hooks.json com ${CLAUDE_PLUGIN_ROOT}, build-plugins.sh idempotente, contagens de componentes corretas."
    - "Existe um commit conjunto (remoção 06-02 + README) que passa no pre-commit README-sync hook sem --no-verify."
  artifacts:
    - path: "README.md"
      provides: "Doc de instalação via plugin + árvore coerente com o repo pós-Fase 06"
      contains: "/plugin marketplace add"
  key_links:
    - from: "README.md (seção Instalação via Plugin)"
      to: ".claude-plugin/marketplace.json"
      via: "comando /plugin marketplace add Ideia-Business/IdeiaOS"
      pattern: "/plugin marketplace add Ideia-Business/IdeiaOS"
    - from: "README.md (árvore)"
      to: "plugins/ e .claude-plugin/"
      via: "entradas na seção Estrutura do repositório"
      pattern: "plugins/"
---

# Phase 06 Plan 03 — README (instalação via plugin + árvore) + integração + commit

**Goal:** Consolidar a Fase 06. Esta é a ÚNICA plan que edita `README.md`. Roda na Wave 3 (após 06-01 criar a infra e 06-02 remover os fallbacks) porque a árvore do README precisa refletir o estado FINAL do repo: sem dirs-raiz, com `.claude-plugin/` + `plugins/`. Faz o commit conjunto que destrava o pre-commit hook (remoção + README coerentes num passo).

## Context

@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@.claude-plugin/marketplace.json
@scripts/check-readme-sync.sh

Depends on: 06-01 (marketplace.json, plugins/, build-plugins.sh, plugin-membership.md) e 06-02 (dirs-raiz removidos, check-readme-sync.sh reescrito para source/, AGENTS.md atualizado). Esta plan assume os dois estados aplicados no working tree.

### O que a árvore do README mostra HOJE (a corrigir)

Linha ~452 (seção "Arquitetura Multi-Harness"): bloco já usa `source/` (451-463); ajustar a linha `├── skills/  (catálogo ECC — 33 módulos)` para refletir contagem real (66 módulos / 34 skills em source/).

Linhas ~667-760 (seção "Estrutura do repositório"): a ÁRVORE lista `agents/` (674), `skills/` (677), `hooks/` (690), `templates/` (712) como dirs de RAIZ — REMOVIDOS em 06-02. A árvore também já documenta `source/` (740-753) corretamente. **Ação:** remover os blocos de raiz `agents/`, `skills/`, `hooks/`, `templates/` (eram duplicação dos de `source/`) e adicionar `.claude-plugin/`, `plugins/` e a linha `scripts/build-plugins.sh`.

Linha ~755: `modules.json ... 33 módulos` → atualizar para 66 módulos + campo `plugin`.

Linhas ~97, 605, 936-939: referências a `templates/ideiaos/...` como link relativo — agora vivem em `source/templates/ideiaos/...`. Atualizar para `source/templates/...`.

### Esquema de instalação confirmado (de 06-01, fonte code.claude.com/docs)

```
# Máquina nova — instalar IdeiaOS via plugin:
/plugin marketplace add Ideia-Business/IdeiaOS
/plugin install ideiaos-core@ideiaos
/plugin install ideiaos-design-suite@ideiaos   # opcional (perfil design)
/plugin install ideiaos-lovable@ideiaos        # opcional (projetos Lovable)
```

`@ideiaos` = nome do marketplace. setup.sh permanece o caminho documentado para bootstrap de máquina (working-dirs, LaunchAgent, vault, git hooks); o plugin é um caminho ADICIONAL que entrega skills/agents/hooks versionados com update nativo (`/plugin update`).

### Constraints travadas

- README é doc humano em PT — NÃO usar header `# SOURCE: IdeiaOS v2` no corpo (esse constraint vale para docs novos de componente; README é documento de produto pré-existente). Editar normalmente.
- Pre-commit README-sync hook DEVE passar SEM `--no-verify` no commit final.
- `check-readme-sync.sh` (versão de 06-02, que audita source/) deve retornar exit 0.
- Commits convencionais, branch work.

## Tasks

<task type="auto">
  <name>Task 1: README — seção 'Instalação via Plugin' + atualização da árvore (pós-remoção + plugins)</name>
  <files>README.md</files>
  <action>
**A) Nova seção "Instalação via Plugin (marketplace privado)":** Inserir logo após o Quickstart (após linha ~42, antes de "## 🧠 O que é o ideIAos") OU como subseção do Quickstart. Conteúdo:
- Frase de abertura: máquina nova pode instalar via plugin nativo, versionado, com `/plugin update`.
- Bloco de comandos (o da seção Context): `/plugin marketplace add Ideia-Business/IdeiaOS` + os 3 `/plugin install <nome>@ideiaos` (core obrigatório; design-suite e lovable opcionais por perfil).
- Tabela dos 3 plugins: nome | conteúdo | quando instalar (core = sempre; design-suite = quem faz UI/design; lovable = projetos Lovable).
- Nota: setup.sh continua o bootstrap de máquina (working-dirs, LaunchAgent, vault, git hooks) — plugin e setup.sh são COMPLEMENTARES. Plugin entrega skills/agents/hooks versionados; setup.sh entrega ambiente de máquina + config de projeto.

**B) Atualizar a árvore "Estrutura do repositório" (linhas ~667-760):**
- REMOVER os blocos de raiz: `├── agents/` (674-676), `├── skills/` (677-689), `├── hooks/` (690-701), `├── templates/` (712-739) — eram espelho de source/ e os dirs não existem mais.
- ADICIONAR no topo da árvore (perto de setup.sh/versions.lock):
  - `├── .claude-plugin/` → `│   └── marketplace.json   ← marketplace 'ideiaos' (3 plugins: core/design-suite/lovable)`
  - `├── plugins/` → 3 subentradas `ideiaos-core/`, `ideiaos-design-suite/`, `ideiaos-lovable/` com nota "GERADO por scripts/build-plugins.sh — não editar à mão (edite source/)"
- ADICIONAR em `scripts/` a linha `│   └── build-plugins.sh   ← Gera plugins/ a partir de source/ (marketplace)`.
- ADICIONAR em `manifests/` a linha `plugin-membership.md ← mapeamento módulo → plugin`.
- Atualizar `modules.json` (linha ~755): "catálogo ECC de **66** módulos (hooks/agents/skills/templates) + campo `plugin` de membership".
- A seção `source/` (740-753) permanece (é a fonte real); só conferir que descreve corretamente.

**C) Atualizar links/refs órfãos para source/:**
- Linha ~97: `templates/ideiaos/DECISION-MATRIX.md.tmpl` → `source/templates/ideiaos/DECISION-MATRIX.md.tmpl`; idem GUIDE-AI.
- Linha ~605: "edite em `skills/`" → "edite em `source/skills/`".
- Linhas ~936-939: `templates/ideiaos/*.tmpl` → `source/templates/ideiaos/*.tmpl` (4 linhas).
- Linha ~452 (bloco multi-harness): `├── skills/  (catálogo ECC — 33 módulos)` → contagem real (66 módulos / 34 skills em source/).

**D) Componentes ausentes:** rodar a auditoria (Task 2 passo 6) e adicionar às tabelas "O que este setup instala" qualquer skill/agent/hook que a check-readme-sync.sh (source/) apontar como faltando — provavelmente skills das Fases 04/05 (ideiaos-catalog, instinct-analyze, instinct-status, learn, evolve, deep-research, codebase-onboarding, code-tour, database-migrations, api-design, accessibility, benchmark-optimization-loop, cost-tracking, tdd, e2e-testing, two-instance-kickoff, llms-txt, mcp-to-cli) e agents (build-error-resolver, silent-failure-hunter, code-simplifier, refactor-cleaner, planner, code-explorer, doc-updater, pr-test-analyzer, performance-optimizer, security-reviewer, typescript-reviewer, react-reviewer, rls-reviewer) e hooks observe-tool-use/observe-session-end.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && grep -q '/plugin marketplace add Ideia-Business/IdeiaOS' README.md && grep -q 'ideiaos-core@ideiaos' README.md && grep -q 'build-plugins.sh' README.md && grep -q 'plugins/' README.md && ! grep -qE '^├── skills/' README.md && ! grep -qE '^├── agents/' README.md && ! grep -qE '^├── hooks/' README.md && ! grep -qE '^├── templates/' README.md && echo "README_UPDATED"</automated>
  </verify>
  <done>README tem seção de instalação via plugin com os comandos dos 3 plugins; árvore sem dirs-raiz removidos e com .claude-plugin/+plugins/+build-plugins.sh; links órfãos apontam para source/.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test de integração do plugin + check-readme-sync verde</name>
  <files>(verificação; correções pontuais em README.md se a auditoria apontar)</files>
  <action>
Rodar a bateria de integração que prova que a Fase 06 está coerente de ponta a ponta:

1. **marketplace.json válido + 3 plugins:** parse, name=ideiaos, plugins.length===3, cada um com name+source relativo e `test -d plugins/<nome>`.
2. **3 plugin.json válidos:** loop parse, name+version presentes.
3. **hooks.json íntegro:** parse, 6 eventos, 11 hooks, todos os commands com `${CLAUDE_PLUGIN_ROOT}`; cada `<hook>.sh` referenciado EXISTE em `plugins/ideiaos-core/hooks/`.
4. **build-plugins.sh idempotente:** rodar `bash scripts/build-plugins.sh` e conferir `git diff --name-only plugins/` VAZIO (conteúdo já gerado em 06-01; re-rodar não muda nada). Se mudar, há não-determinismo a corrigir em 06-01 (reportar como gap).
5. **Componentes completos:** core = 15 agents + 23 skills + 11 hooks; design-suite = 10 skills; lovable = skill lovable-handoff + templates/lovable. Contagens via `ls | wc -l`.
6. **check-readme-sync.sh verde:** `bash scripts/check-readme-sync.sh` → exit 0. Se exit 1, adicionar ao README (Task 1 D) os componentes faltantes que a auditoria listar e re-rodar até verde.
7. **setup.sh íntegro:** `bash -n setup.sh` → exit 0 (sem regressão de 06-02).
8. **Sem dir-raiz residual:** `test ! -d skills && test ! -d agents && test ! -d hooks && test ! -d templates`.

Documentar a saída no SUMMARY (validação one-shot via comandos; não criar arquivo de teste).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && bash scripts/build-plugins.sh >/dev/null 2>&1; test -z "$(git diff --name-only plugins/ 2>/dev/null)" && bash -n setup.sh && bash scripts/check-readme-sync.sh >/dev/null 2>&1 && C=$(ls plugins/ideiaos-core/agents/*.md 2>/dev/null | wc -l | tr -d ' ') && S=$(ls -d plugins/ideiaos-core/skills/*/ 2>/dev/null | wc -l | tr -d ' ') && H=$(ls plugins/ideiaos-core/hooks/*.sh 2>/dev/null | wc -l | tr -d ' ') && D=$(ls -d plugins/ideiaos-design-suite/skills/*/ 2>/dev/null | wc -l | tr -d ' ') && [ "$C" = "15" ] && [ "$S" = "23" ] && [ "$H" = "11" ] && [ "$D" = "10" ] && echo "INTEGRATION_OK core=$C/$S/$H design=$D"</automated>
  </verify>
  <done>marketplace+plugin.json+hooks.json válidos; build idempotente (diff vazio); contagens corretas (15/23/11 core, 10 design); check-readme-sync.sh verde; setup.sh íntegro; sem dir-raiz residual.</done>
</task>

<task type="auto">
  <name>Task 3: Commit conjunto da Fase 06 (remoção 06-02 + plugins 06-01 + README) passando no pre-commit hook</name>
  <files>(git — commit conjunto; nenhum arquivo novo)</files>
  <action>
Fazer o(s) commit(s) que fecham a Fase 06, garantindo que o pre-commit README-sync hook passe SEM `--no-verify`.

**Estratégia de commit (a remoção de componentes exige README atualizado no mesmo commit):**
- Verificar o estado: `git status` deve mostrar removidos (skills/ agents/ hooks/ templates/), novos (.claude-plugin/, plugins/, scripts/build-plugins.sh, manifests/plugin-membership.md) e modificados (setup.sh, scripts/*, AGENTS.md, adapters/_scaffold/README.md, manifests/modules.json, versions.lock, .gitignore, README.md).
- Como o pre-commit hook bloqueia commits que tocam componentes (source/, scripts/, plugins/, manifests/ — pós-06-02) sem atualizar README: o README JÁ está atualizado (Task 1). Então um commit que inclua README + remoção + plugins passa no hook.
- Fazer commits convencionais. Sugestão de sequência (cada um deve passar no hook — todos incluem ou não tocam README conforme a regra do hook):
  1. `feat(06): plugin marketplace — marketplace.json + 3 sub-plugins + build-plugins.sh` (06-01: .claude-plugin/, plugins/, scripts/build-plugins.sh, manifests/modules.json, manifests/plugin-membership.md, versions.lock, .gitignore) — se o hook reclamar por tocar manifests/scripts sem README, incluir README neste commit OU fazer um único commit conjunto (ver abaixo).
  2. `refactor(06): remover dirs-fallback skills/agents/hooks/templates + atualizar referências para source/` (06-02: git rm + setup.sh, scripts/*, AGENTS.md, adapters/_scaffold/README.md) + README árvore — DEVE incluir README.md (a remoção dispara o hook).
- **Caminho mais seguro (recomendado):** UM commit conjunto cobrindo 06-01+06-02+06-03 quando a granularidade do hook tornar commits separados inviáveis: `feat(06): IdeiaOS como plugin/marketplace + remoção de dirs-fallback`. Decidir em runtime conforme o que o hook aceitar; o objetivo é zero `--no-verify`.
- Mensagem de commit termina com `Co-Authored-By: Claude <noreply@anthropic.com>`.

**Validação pós-commit:** `git log --oneline -3` mostra o(s) commit(s) da Fase 06; `git status` limpo (working tree clean); nenhum uso de `--no-verify` (o hook rodou e passou).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && git status --porcelain | grep -qE 'README|plugins|setup' && echo "WARN: working tree não commitado ainda" || (git log --oneline -1 | grep -iqE '06|plugin' && test -z "$(git status --porcelain)" && echo "COMMITTED_CLEAN")</automated>
  </verify>
  <done>Fase 06 commitada (working tree limpo); commit(s) convencional(is) com escopo 06/plugin; pre-commit README-sync hook passou sem --no-verify.</done>
</task>

## Verification

| # | Verificação | Comando | Esperado |
|---|-------------|---------|----------|
| 1 | seção de instalação via plugin | `grep '/plugin marketplace add Ideia-Business/IdeiaOS' README.md` | match |
| 2 | comandos dos 3 plugins | `grep -E 'ideiaos-(core\|design-suite\|lovable)@ideiaos' README.md` | 3 matches |
| 3 | árvore sem dirs-raiz | `grep -E '^├── (skills\|agents\|hooks\|templates)/' README.md` | vazio |
| 4 | árvore com plugins + build-plugins | `grep -E 'plugins/\|build-plugins.sh' README.md` | match |
| 5 | check-readme-sync verde | `bash scripts/check-readme-sync.sh; echo $?` | 0 |
| 6 | marketplace.json válido | node parse | OK |
| 7 | build idempotente | `bash scripts/build-plugins.sh && git diff --name-only plugins/` | vazio |
| 8 | contagens de componentes | ls\|wc nas 3 pastas | 15/23/11 core, 10 design |
| 9 | setup.sh íntegro | `bash -n setup.sh` | exit 0 |
| 10 | commit limpo sem --no-verify | `git status --porcelain` | vazio (pós-commit) |

## Notes

- **Commit conjunto é intencional:** o pre-commit README-sync hook acopla remoção de componentes à atualização do README. Por isso 06-02 deixa o working tree pronto (sem commit) e 06-03 atualiza o README e faz o commit que cobre ambos — garantindo coerência e zero `--no-verify`. Se commits separados forem viáveis sob a regra do hook, mantê-los; senão, um commit conjunto da Fase 06.
- **setup.sh permanece canônico para bootstrap:** a seção de plugin deixa explícito que plugin e setup.sh são complementares — plugin = skills/agents/hooks versionados com update nativo; setup.sh = ambiente de máquina (working-dirs, LaunchAgent, vault, git hooks) + config de projeto. Não há remoção de setup.sh.
- **Contagem de skills:** 34 em source/skills (23 core + 10 design + 1 lovable). modules.json = 66 módulos. Esses números devem aparecer corretos no README após esta plan.
- **Pós-Fase 06 — fechamento de sessão (CLAUDE.md):** atualizar STATE.md (marcar Fase 06 completa + decisões: plugins versionados, dirs-raiz removidos, membership em modules.json) e docs/CONTINUATION_HANDOFF.md. Não faz parte das tasks de execução, mas é o protocolo de fechamento.
