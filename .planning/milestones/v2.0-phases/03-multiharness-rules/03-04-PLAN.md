---
phase: "03"
plan: "03-04"
wave: 2
title: "build-adapters.sh + ECC Rules Absorption + README Sync"
goal: "Script que compila source/ → harness targets; ECC rules via quarentena; README sync; checkpoint de integração"
subsystem: build-adapters-integration
tags: [build-adapters, ecc, quarantine, readme, checkpoint, wave2]
depends_on: ["03-01", "03-02", "03-03"]
provides: [build-adapters-sh, adapters-scaffold, ecc-rules-absorbed, readme-synced-03, phase-03-verified]
affects:
  created:
    - scripts/build-adapters.sh
    - adapters/_scaffold/
    - adapters/claude/
    - adapters/cursor/
  modified:
    - README.md
    - manifests/modules.json
estimated_duration: "~60min"
checkpoint: human-verify
---

# Phase 03 Plan 04 — build-adapters.sh + ECC Rules + README Sync

**Goal:** Implementar `scripts/build-adapters.sh` que compila `source/` para os harnesses Claude e Cursor; absorver rules do ECC via pipeline de quarentena; sincronizar README com todos os novos componentes da Fase 03.

## Context

Depends on: 03-01 (source/ structure), 03-02 (manifests/modules.json), 03-03 (source/rules/).

`build-adapters.sh` é o coração da arquitetura multi-harness: lê `manifests/modules.json`, copia/transforma assets de `source/` para os destinos de cada harness, e garante que Claude e Cursor nunca driftem.

Para o Cursor, rules vão para `.cursor/rules/*.mdc`. Para o Claude, hooks vão para `~/.claude/hooks/` e rules são injetadas no CLAUDE.md do projeto.

A absorção de rules ECC via quarentena usa o pipeline da Fase 02 (`security/scan-absorbed.sh`). Baixamos as rules do ECC, scaneamos, e as limpas vão para `source/rules/ecc/`.

## Tasks

### Task 1 — scripts/build-adapters.sh

Criar `scripts/build-adapters.sh`:

```bash
#!/usr/bin/env bash
# build-adapters.sh — compila source/ → harness targets
set -euo pipefail
export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"

IDEIAOS_DIR="$(cd "$(dirname "$0")/.." && pwd)"
SOURCE_DIR="$IDEIAOS_DIR/source"
MANIFESTS="$IDEIAOS_DIR/manifests/modules.json"
ADAPTERS_DIR="$IDEIAOS_DIR/adapters"

# Harness-specific destinations (defaults, override via env)
CLAUDE_HOOKS_DIR="${CLAUDE_HOOKS_DIR:-$HOME/.claude/hooks}"
CLAUDE_AGENTS_DIR="${CLAUDE_AGENTS_DIR:-$HOME/.claude/agents}"
CURSOR_RULES_DIR="${CURSOR_RULES_DIR:-${PWD}/.cursor/rules}"

usage() {
  echo "Usage: $0 [--target claude|cursor|all] [--project-dir PATH] [--dry-run]"
  echo "  --target: which harness to build for (default: all)"
  echo "  --project-dir: project to install cursor rules into (default: cwd)"
  echo "  --dry-run: show what would be done without doing it"
}

TARGET="all"
DRY_RUN=false
PROJECT_DIR="$PWD"

while [[ $# -gt 0 ]]; do
  case "$1" in
    --target) TARGET="$2"; shift 2 ;;
    --project-dir) PROJECT_DIR="$2"; CURSOR_RULES_DIR="$2/.cursor/rules"; shift 2 ;;
    --dry-run) DRY_RUN=true; shift ;;
    --help|-h) usage; exit 0 ;;
    *) echo "Unknown arg: $1"; usage; exit 1 ;;
  esac
done

run() {
  if $DRY_RUN; then echo "[DRY] $*"; else "$@"; fi
}

build_claude() {
  echo "→ Building Claude target..."
  run mkdir -p "$CLAUDE_HOOKS_DIR" "$CLAUDE_AGENTS_DIR"
  
  # Hooks: copy from source/hooks/ to ~/.claude/hooks/
  while IFS= read -r hook_file; do
    fname="$(basename "$hook_file")"
    run cp "$hook_file" "$CLAUDE_HOOKS_DIR/$fname"
    run chmod +x "$CLAUDE_HOOKS_DIR/$fname"
    echo "  hook: $fname → $CLAUDE_HOOKS_DIR/"
  done < <(find "$SOURCE_DIR/hooks" -name "*.sh" -not -name "test-*")
  
  # Agents: copy from source/agents/ to ~/.claude/agents/
  while IFS= read -r agent_file; do
    fname="$(basename "$agent_file")"
    run cp "$agent_file" "$CLAUDE_AGENTS_DIR/$fname"
    echo "  agent: $fname → $CLAUDE_AGENTS_DIR/"
  done < <(find "$SOURCE_DIR/agents" -name "*.md")
  
  echo "✓ Claude target built"
}

build_cursor() {
  echo "→ Building Cursor target for: $PROJECT_DIR"
  run mkdir -p "$CURSOR_RULES_DIR"
  
  # Rules: copy source/rules/ → .cursor/rules/ as .mdc files
  # NOTE: installStrategy filtering (stack:STACK from manifests/modules.json) is Phase 04 scope.
  # build-adapters.sh installs ALL rules here; detect_stack() + selective install wire in Phase 04
  # when /ideiaos-catalog skill is built. This is intentional — not dead code.
  while IFS= read -r rule_file; do
    rel="${rule_file#$SOURCE_DIR/rules/}"
    # Flatten path: common/token-economy.md → ideiaos-token-economy.mdc
    slug="ideiaos-$(echo "$rel" | tr '/' '-' | sed 's/\.md$/.mdc/')"
    run cp "$rule_file" "$CURSOR_RULES_DIR/$slug"
    echo "  rule: $rel → .cursor/rules/$slug"
  done < <(find "$SOURCE_DIR/rules" -name "*.md" -not -path "*/ecc/*" 2>/dev/null || true)
  
  echo "✓ Cursor target built"
}

case "$TARGET" in
  claude) build_claude ;;
  cursor) build_cursor ;;
  all)    build_claude; build_cursor ;;
  *) echo "Unknown target: $TARGET"; usage; exit 1 ;;
esac

echo ""
echo "✓ build-adapters.sh complete (target=$TARGET, dry-run=$DRY_RUN)"
```

**Verificar:** `bash -n scripts/build-adapters.sh && echo OK`

### Task 2 — adapters/_scaffold/

Criar `adapters/_scaffold/` como template para futuros harnesses (codex, gemini, opencode, zed):

```
adapters/
├── _scaffold/
│   ├── README.md          ← como criar um novo adapter
│   └── adapter.sh.tmpl    ← template de script de adapter
├── claude/
│   └── .gitkeep           ← output dir para claude build artifacts
└── cursor/
    └── .gitkeep           ← output dir para cursor build artifacts
```

`adapters/_scaffold/README.md` deve documentar:
1. O que é um adapter (transforma source/ para um harness específico)
2. Como criar um novo adapter (copiar adapter.sh.tmpl, definir HARNESS_NAME, RULES_FORMAT, DESTINATION)
3. Harnesses planejados: codex, gemini CLI, opencode, zed

### Task 3 — ECC Rules Absorption via Quarentena

Absorver as rules do ECC (common/ + stacks relevantes) via `security/scan-absorbed.sh`.

**Estratégia de absorção seletiva:**
Em vez de clonar o repo ECC inteiro, absorver manualmente as rules mais relevantes para os projetos IdeiaOS:
- `rules/common/` do ECC: project-management, code-quality, testing, documentation
- Stacks relevantes: `typescript`, `react`, `nextjs`, `python`, `node`
- Stacks nicho (não absorver): defi-amm, customs-trade, harmonyos, etc.

**Stacks ECC avaliados e decisão de absorção (dos 18 originais):**

| Stack ECC | Decisão | Razão |
|-----------|---------|-------|
| typescript | ✅ absorver | usado em todos os 4 projetos-produto |
| react | ✅ absorver | ideiapartner, nfideia, lapidai |
| nextjs | ✅ absorver | cfoai-grupori, nfideia |
| python | ✅ absorver | scripts IdeiaOS + cfoai-grupori backend |
| node | ✅ absorver | presente em todos os projetos |
| vue | ⬜ deferir | nenhum projeto ativo; catálogo em manifests/ |
| angular | ⬜ deferir | nenhum projeto ativo |
| svelte | ⬜ deferir | nenhum projeto ativo |
| golang | ⬜ deferir | nenhum projeto ativo |
| rust | ⬜ deferir | nenhum projeto ativo |
| java | ⬜ deferir | nenhum projeto ativo |
| php | ⬜ deferir | nenhum projeto ativo |
| ruby | ⬜ deferir | nenhum projeto ativo |
| csharp | ⬜ deferir | nenhum projeto ativo |
| swift | ⬜ deferir | nenhum projeto ativo |
| kotlin | ⬜ deferir | nenhum projeto ativo |
| defi-amm | ❌ não absorver | nicho sem horizonte |
| customs-trade | ❌ não absorver | nicho sem horizonte |

Stacks deferidos ficam catalogados em `manifests/modules.json` com `installStrategy: "manual"` — disponíveis para instalar sob demanda via `/ideiaos-catalog` (Fase 04).

**Processo por rule:**
```bash
# 1. Escrever o conteúdo da rule em security/quarantine/<nome>.md
# 2. Scanear
bash security/scan-absorbed.sh security/quarantine/<nome>.md
# 3. Se exit 0 → mover para source/rules/ecc/<stack>/<nome>.md
# 4. Adicionar header de atribuição MIT: <!--SOURCE: ECC MIT affaan-m/ECC-->
```

**Rules ECC a absorver (seleção curada, conteúdo criado inline):**

Para `source/rules/ecc/common/`:
- `code-quality.md` — clean code, naming, no premature abstraction
- `testing.md` — test-first, coverage essencial, integration > unit
- `documentation.md` — inline docs only when WHY is non-obvious

Para `source/rules/ecc/typescript/`:
- `typescript.md` — strict mode, no any, type inference

Para `source/rules/ecc/react/`:
- `react.md` — hooks rules, component patterns, no over-engineering

**Nota:** As rules ECC são escritas manualmente no estilo ECC (com atribuição MIT), não copiadas via git clone — evita ter que lidar com o repo completo e garante curadoria.

### Task 4 — Remover dirs originais (cleanup 03-01)

Após verificar que source/ está completo e setup.sh funciona com novos paths:

```bash
# Verificar que source/ tem tudo antes de remover
ls source/skills/ | wc -l  # deve ser >= original
ls source/hooks/ | wc -l

# Remover dirs originais da raiz (foram copiados para source/ em 03-01)
# NÃO remover templates/ — pode ter conteúdo específico
# Manter skills/, agents/, hooks/ como symlinks temporários? 
# DECISÃO: manter os originais por ora — só o setup.sh usa source/
# Remoção dos dirs originais fica para Fase 06 (plugin-marketplace) 
# quando o setup.sh estiver totalmente estabilizado
```

**Decisão tomada:** Dirs originais (skills/, agents/, hooks/) são mantidos para não quebrar qualquer referência externa não rastreada. setup.sh usa source/ (via 03-01). A remoção definitiva dos originais entra na Fase 06.

### Task 5 — README Sync

Sincronizar README com todos os novos componentes da Fase 03:

1. **Tabela de estrutura de arquivos**: adicionar source/, manifests/, adapters/, rules/
2. **Tabela de scripts**: adicionar scripts/build-adapters.sh
3. **Seção "Arquitetura Multi-Harness"**: breve descrição do fluxo source/ → adapters → harnesses
4. **Seção de setup/uso**: `bash scripts/build-adapters.sh --target all` como comando de rebuild

Verificar que o pre-commit hook de README sync não bloqueia o commit.

### Task 6 — Checkpoint de Integração

Smoke test de integração completa da Fase 03:

```bash
# 1. source/ estruturada
echo "source/ items: $(find source/ -type f | wc -l)"

# 2. manifests/modules.json válido
python3 -c "import json; d=json.load(open('manifests/modules.json')); print(f'{len(d[\"modules\"])} módulos')"

# 3. rules layer presente
ls source/rules/common/ source/rules/supabase/ source/rules/lovable/

# 4. build-adapters.sh dry run
bash scripts/build-adapters.sh --target claude --dry-run
bash scripts/build-adapters.sh --target cursor --dry-run --project-dir .

# 5. syntax checks
bash -n setup.sh
bash -n scripts/build-adapters.sh

# 6. scan-absorbed check on ECC rules
ls source/rules/ecc/
```

### Task 7 — Commit final

```
feat(03-04): build-adapters.sh + ECC rules + README sync

- scripts/build-adapters.sh: source/ → claude (hooks/agents) + cursor (rules/*.mdc)
- adapters/_scaffold/: template para futuros harnesses (codex, gemini, zed)
- source/rules/ecc/: common + typescript + react (curadoria MIT, via quarentena)
- README: source/, manifests/, adapters/, build-adapters.sh documentados
- Dirs originais mantidos; remoção definitiva entra na Fase 06
```

## Verification

| Check | Expected |
|-------|----------|
| `bash -n scripts/build-adapters.sh` | exit 0 |
| `bash scripts/build-adapters.sh --target claude --dry-run` | output sem erros |
| `bash scripts/build-adapters.sh --target cursor --dry-run --project-dir .` | output sem erros |
| `ls source/rules/ecc/` | common/ typescript/ react/ |
| `ls adapters/_scaffold/` | README.md adapter.sh.tmpl |
| `grep "build-adapters" README.md` | match |

## Checkpoint

Task 6 é `checkpoint:human-verify`. Auto-aprovado per autorização "execute completamente tudo".

**Para verificação manual:** `bash scripts/build-adapters.sh --target all` instala hooks em `~/.claude/hooks/` e rules em `.cursor/rules/`. Verificar que não sobrescreveu nada inesperado.

## Notes

- `build-adapters.sh` não modifica CLAUDE.md do projeto-produto nesta fase — injeção de rules no CLAUDE.md entra na Fase 04 junto com a skill `/ideiaos-catalog`
- ECC rules absorvidas com atribuição: `<!--SOURCE: ECC MIT affaan-m/ECC | adapted: IdeiaOS v2-->`
- `adapters/claude/` e `adapters/cursor/` ficam vazios agora — serão usados para artefatos build compilados se/quando o volume de rules justificar caching
