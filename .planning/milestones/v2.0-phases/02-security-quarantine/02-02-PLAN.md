---
phase: 02-security-quarantine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install-global-patches.sh
  - scripts/idea-doctor.sh
autonomous: true
requirements: [REQ-03, REQ-04]
must_haves:
  truths:
    - "Rodar install-global-patches.sh adiciona deny rules ao ~/.claude/settings.json sem duplicar"
    - "Re-rodar install-global-patches.sh não duplica deny rules (idempotente)"
    - "idea-doctor reporta FAIL quando uma deny rule baseline está ausente"
    - "idea-doctor reporta hooks com curl|bash pipe e secrets em memória de projeto"
    - "Bash(ssh *) e Bash(scp *) usam 'ask' não 'deny' (não bloqueiam SSH legítimo)"
  artifacts:
    - path: "scripts/install-global-patches.sh"
      provides: "Patch 10 — deny rules baseline (python3 idempotente)"
      contains: "Patch 10"
    - path: "scripts/idea-doctor.sh"
      provides: "Seção 7 Security Audit (read-only)"
      contains: "7) Security Audit"
  key_links:
    - from: "scripts/install-global-patches.sh"
      to: "~/.claude/settings.json permissions.deny"
      via: "python3 inline merge idempotente"
      pattern: "permissions.*deny"
    - from: "scripts/idea-doctor.sh"
      to: "deny rules + hooks + memória"
      via: "checks pass/warn/fail"
      pattern: "Security Audit"
---

<objective>
Adicionar (1) **Patch 10** ao install-global-patches.sh injetando as 6 deny rules baseline no settings.json global, e (2) **Seção 7 "Security Audit"** ao idea-doctor.sh que audita config insegura (deny rules ausentes, hooks perigosos, secrets em memória).

Purpose: Fecha a superfície de ataque da config do próprio IdeiaOS (CVE-2025-59536, CVE-2026-21852). O par é acoplado: a Seção 7 do doctor VERIFICA exatamente as deny rules que o Patch 10 instala.
Output: Patch 10 (idempotente) + seção de auditoria no doctor existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-security-quarantine/02-RESEARCH.md
@scripts/install-global-patches.sh
@scripts/idea-doctor.sh

<interfaces>
<!-- Padrões existentes que o executor DEVE seguir (extraídos do codebase) -->
install-global-patches.sh:
  - Helpers: ok() skip() warn() err() step()  | Contadores: APPLIED SKIPPED FAILED
  - Patches são funções patch_*() chamadas no final via `step "Patch N/9: ..."` + `patch_xxx`
  - Patch 4 e 8 já usam python3 inline para editar ~/.claude/settings.json (padrão a copiar)
  - Atualmente vai até Patch 9 (gitignore global). Adicionar Patch 10 = renumerar para "Patch 10/10".

idea-doctor.sh:
  - Helpers: pass() warn() fail() info() step()  | Contadores: PASS WARN FAIL
  - Atualmente 6 seções (step "6) Autosync"). Adicionar `step "7) Security Audit"` ANTES do "━━━ Resumo ━━━".
  - fail() incrementa FAIL → causa exit 1 no final. Usar fail() para deny rules ausentes.
  - SETTINGS="$HOME/.claude/settings.json"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch 10 — deny rules baseline em install-global-patches.sh</name>
  <read_first>scripts/install-global-patches.sh (linhas 655-723: Patch 9 + bloco de invocação + Resumo), .planning/phases/02-security-quarantine/02-RESEARCH.md (Pattern 1, Open Question 1)</read_first>
  <files>scripts/install-global-patches.sh</files>
  <action>
1. Adicionar a função `patch_deny_rules()` LOGO APÓS `patch_global_gitignore()` (após linha 676), seguindo o padrão das funções existentes (ok/skip/warn/err + contadores APPLIED/SKIPPED/FAILED):

```bash
# ── PATCH 10: deny rules baseline em settings.json ───────────────────────────
# Fecha superfície de ataque da config (CVE-2025-59536, CVE-2026-21852).
# Decisão PROJECT.md: security como infra. ssh/scp = "ask" (não bloquear SSH legítimo).
patch_deny_rules() {
  local target="$HOME/.claude/settings.json"
  if [ ! -f "$target" ]; then
    warn "Patch 10: ~/.claude/settings.json não existe — pular"
    SKIPPED=$((SKIPPED+1)); return 0
  fi
  local result_str
  result_str=$(python3 - "$target" <<'PY'
import json, sys
path = sys.argv[1]
DENY = ["Read(~/.ssh/**)","Read(~/.aws/**)","Read(**/.env*)",
        "Write(~/.ssh/**)","Bash(curl * | bash)","Bash(nc *)"]
ASK  = ["Bash(ssh *)","Bash(scp *)"]  # ambíguos → ask, não deny (Open Question 1)
with open(path) as f: cfg = json.load(f)
perms = cfg.setdefault("permissions", {})
deny = perms.setdefault("deny", [])
ask  = perms.setdefault("ask", [])
added = 0
for r in DENY:
    if r not in deny: deny.append(r); added += 1
for r in ASK:
    if r not in ask and r not in deny: ask.append(r); added += 1
if added:
    with open(path, "w") as f:
        json.dump(cfg, f, indent=2, ensure_ascii=False); f.write("\n")
    print(f"APPLIED:{added}")
else:
    print("SKIPPED")
PY
)
  case "$result_str" in
    APPLIED:*) ok "Patch 10: deny rules baseline (${result_str#APPLIED:} regra(s))"; APPLIED=$((APPLIED+1)) ;;
    SKIPPED)   skip "Patch 10: deny rules já presentes"; SKIPPED=$((SKIPPED+1)) ;;
    *)         err "Patch 10: Python falhou ($result_str)"; FAILED=$((FAILED+1)) ;;
  esac
}
```

2. Renumerar os `step "Patch N/9"` existentes para `N/10` (linhas ~678-702: são 9 chamadas) e adicionar ao final, ANTES do bloco "━━━ Resumo ━━━" (linha 705):

```bash
step "Patch 10/10: deny rules baseline em settings.json"
patch_deny_rules
```

Usar Edit cirúrgico — NÃO reescrever o arquivo inteiro. As 6 deny rules são EXATAMENTE: `Read(~/.ssh/**)`, `Read(~/.aws/**)`, `Read(**/.env*)`, `Write(~/.ssh/**)`, `Bash(curl * | bash)`, `Bash(nc *)`. ssh/scp vão em `permissions.ask` (Open Question 1 do RESEARCH).
  </action>
  <acceptance_criteria>
- `grep -q 'patch_deny_rules' scripts/install-global-patches.sh` → exit 0
- `grep -q 'Patch 10/10' scripts/install-global-patches.sh` → exit 0
- `grep -q 'Read(~/.ssh/\*\*)' scripts/install-global-patches.sh` → exit 0
- `grep -q 'permissions' scripts/install-global-patches.sh` e `grep -q '"ask"' scripts/install-global-patches.sh` → exit 0 (ssh/scp em ask)
- `bash -n scripts/install-global-patches.sh` → exit 0
- Idempotência: rodar `python3` do patch 2x sobre um settings.json de teste não duplica entradas no array deny
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/install-global-patches.sh && grep -q 'patch_deny_rules' scripts/install-global-patches.sh && grep -q 'Patch 10/10' scripts/install-global-patches.sh && grep -qF 'Read(~/.ssh/**)' scripts/install-global-patches.sh && echo OK</automated>
  </verify>
  <done>Patch 10 adiciona 6 deny rules + 2 ask rules de forma idempotente; sintaxe válida; numeração 10/10.</done>
</task>

<task type="auto">
  <name>Task 2: Seção 7 Security Audit em idea-doctor.sh</name>
  <read_first>scripts/idea-doctor.sh (linhas 126-148: seção 6 + Resumo), .planning/phases/02-security-quarantine/02-RESEARCH.md (Pattern 3)</read_first>
  <files>scripts/idea-doctor.sh</files>
  <action>
Inserir a Seção 7 ENTRE a seção "6) Autosync" e o bloco "━━━ Resumo ━━━" (entre linhas 135 e 137), usando os helpers existentes pass/warn/fail/step. Seguir Pattern 3 do RESEARCH:

```bash
# ── 7) Security Audit ─────────────────────────────────────────────────────────
step "7) Security Audit"
SETTINGS="$HOME/.claude/settings.json"

# 7a) Deny rules baseline presentes?
REQUIRED_DENY=("Read(~/.ssh/**)" "Read(~/.aws/**)" "Read(**/.env*)" "Write(~/.ssh/**)" "Bash(curl * | bash)" "Bash(nc *)")
if [ -f "$SETTINGS" ]; then
  for rule in "${REQUIRED_DENY[@]}"; do
    if python3 -c "import json,sys; d=json.load(open('$SETTINGS')).get('permissions',{}).get('deny',[]); sys.exit(0 if '$rule' in d else 1)" 2>/dev/null; then
      pass "deny: $rule"
    else
      fail "deny rule ausente: $rule — rode: bash scripts/install-global-patches.sh"
    fi
  done
else
  fail "settings.json não encontrado em $SETTINGS"
fi

# 7b) Hooks com curl|bash pipe (comando perigoso)
if [ -d "$HOME/.claude/hooks" ]; then
  if rg -ln 'curl.*\|.*bash|bash.*<.*curl' "$HOME/.claude/hooks/" 2>/dev/null | grep -q .; then
    fail "Hooks contêm curl|bash pipe — inspeção manual necessária"
  else
    pass "Hooks sem curl|bash pipe"
  fi
fi

# 7c) Secrets em texto plano na memória de projeto
MEM_DIR="$HOME/.claude/projects"
if [ -d "$MEM_DIR" ]; then
  if rg -l 'sk-[a-zA-Z0-9]{40,}|ANTHROPIC_API_KEY|service_role.*[a-zA-Z0-9]{30,}' "$MEM_DIR" 2>/dev/null | grep -q .; then
    fail "POSSÍVEL secret em memória de projeto — checar $MEM_DIR"
  else
    pass "Memória de projeto sem secrets aparentes"
  fi
fi

# 7d) scan-absorbed.sh presente (pipeline de quarentena)
if [ -x "$SETUP_DIR/security/scan-absorbed.sh" ]; then
  pass "pipeline de quarentena (security/scan-absorbed.sh) presente"
else
  warn "security/scan-absorbed.sh ausente — quarentena obrigatória não disponível"
fi
```

Atualizar o comentário-cabeçalho do arquivo (linhas 5-15) para mencionar "7. Security Audit (deny rules, hooks perigosos, secrets, quarentena)". Usar Edit cirúrgico — NÃO reescrever o arquivo inteiro.
  </action>
  <acceptance_criteria>
- `grep -q '7) Security Audit' scripts/idea-doctor.sh` → exit 0
- `grep -q 'deny rule ausente' scripts/idea-doctor.sh` → exit 0
- `grep -q 'curl|bash pipe' scripts/idea-doctor.sh` → exit 0
- `grep -q 'scan-absorbed.sh' scripts/idea-doctor.sh` → exit 0
- `bash -n scripts/idea-doctor.sh` → exit 0
- A inserção está ANTES de `━━━ Resumo ━━━` (a seção 7 conta para PASS/WARN/FAIL do resumo)
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/idea-doctor.sh && grep -q '7) Security Audit' scripts/idea-doctor.sh && grep -q 'deny rule ausente' scripts/idea-doctor.sh && grep -q 'scan-absorbed.sh' scripts/idea-doctor.sh && echo OK</automated>
  </verify>
  <done>idea-doctor audita deny rules (FAIL se ausente), hooks perigosos, secrets em memória e presença do pipeline; sintaxe válida; seção contabilizada no resumo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config→Claude Code | settings.json é superfície de ataque (CVE-2025-59536 pré-trust, CVE-2026-21852 base URL override) |
| memória→disco | ~/.claude/projects pode conter secrets vazados em texto plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | Read(~/.ssh, ~/.aws, .env) | mitigate | deny rules no settings.json global via Patch 10 |
| T-02-07 | Elevation of Privilege | Bash(curl * \| bash), Bash(nc *) | mitigate | deny rules bloqueiam execução remota/reverse shell |
| T-02-08 | Information Disclosure | secrets em ~/.claude/projects (memória) | mitigate | idea-doctor 7c faz grep de padrões sk-/ANTHROPIC_API_KEY/service_role |
| T-02-09 | Tampering | hooks com curl\|bash pipe | mitigate | idea-doctor 7b detecta e marca FAIL |
| T-02-10 | DoS | deny Bash(ssh *) bloqueia deploy legítimo dos 4 projetos | accept | ssh/scp vão em `ask` (prompt), não `deny` — Open Question 1 |
</threat_model>

<verification>
- `bash -n` passa em ambos os arquivos.
- Patch 10 idempotente (2 execuções → mesmo array deny).
- idea-doctor seção 7 existe e contabiliza no resumo final.
- ssh/scp em `permissions.ask`, não em `deny`.
</verification>

<success_criteria>
idea-doctor reporta config insegura (deny rules ausentes geram FAIL), cumprindo o critério de sucesso da fase ("idea-doctor reporta config insegura"). Patch 10 instala as 6 deny rules de forma idempotente.
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-quarantine/02-02-SUMMARY.md`
</output>
