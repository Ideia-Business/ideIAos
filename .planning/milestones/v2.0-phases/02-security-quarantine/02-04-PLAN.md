---
phase: 02-security-quarantine
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - README.md
autonomous: false
requirements: [REQ-01, REQ-02, REQ-03, REQ-04, REQ-05]
must_haves:
  truths:
    - "README documenta security/scan-absorbed.sh (pipeline de quarentena obrigatório)"
    - "README documenta a nova Seção 7 Security Audit do idea-doctor"
    - "README documenta as deny rules baseline (Patch 10) e o kill-switch do autosync"
    - "README referencia docs/security/memory-hygiene.md"
    - "Smoke test integrado de toda a fase passa (scan + doctor)"
  artifacts:
    - path: "README.md"
      provides: "Documentação dos 5 entregáveis de segurança (regra de sync obrigatória)"
      contains: "scan-absorbed"
  key_links:
    - from: "README.md"
      to: "security/scan-absorbed.sh + idea-doctor seção 7 + memory-hygiene"
      via: "seção de segurança"
      pattern: "scan-absorbed|Security Audit|memory-hygiene"
---

<objective>
Sincronizar o README.md com os 5 entregáveis da Fase 02 (regra travada "README sync obrigatório" do PROJECT.md) e rodar o smoke test integrado de toda a fase.

Purpose: O README é o índice de verdade do ambiente IdeiaOS. Todo componente novo (decisão PROJECT.md) precisa estar nele. Sem isso, o pipeline de quarentena e a auditoria de segurança ficam invisíveis.
Output: README atualizado + validação end-to-end + checkpoint humano de confirmação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@README.md
@.planning/phases/02-security-quarantine/02-01-SUMMARY.md
@.planning/phases/02-security-quarantine/02-02-SUMMARY.md
@.planning/phases/02-security-quarantine/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar README com os 5 entregáveis de segurança</name>
  <read_first>README.md (linhas 220-230 tabela de scripts; linhas 480-510 comandos de manutenção; linha 567 estrutura de arquivos), os 3 SUMMARYs da fase</read_first>
  <files>README.md</files>
  <action>
Atualizar o README.md cobrindo os 5 entregáveis. Pontos mínimos a inserir (Edit cirúrgico, manter a estrutura existente):

1. **Tabela de scripts** (~linha 223): adicionar linha para `security/scan-absorbed.sh` — "Pipeline de quarentena: scan unicode/payloads/comandos + AgentShield antes de absorver conteúdo de terceiros. Exit 1 = bloqueado."
2. **idea-doctor** (linhas 223/488/694): atualizar a descrição para incluir a **Seção 7 Security Audit** (deny rules, hooks perigosos, secrets em memória, presença do pipeline).
3. **Estrutura de arquivos** (~linha 567): adicionar `security/` (scan-absorbed.sh + quarantine/) e `docs/security/memory-hygiene.md`.
4. **Patch 10**: onde o README cita "7 patches"/"9 patches" do overlay, atualizar para incluir o Patch 10 (deny rules baseline). Buscar todas as ocorrências de contagem de patches e ajustar.
5. **Kill-switch**: na seção do autosync/setup-dev-machine, mencionar que o LaunchAgent agora roda sob `timeout 120` (kill-switch).
6. **Memory hygiene**: referenciar `docs/security/memory-hygiene.md` na seção de segurança/manutenção.

Garantir consistência: a contagem de patches deve bater em TODAS as menções (grep por "patches" e "/9" no README).
  </action>
  <acceptance_criteria>
- `grep -q 'scan-absorbed' README.md` → exit 0
- `grep -qi 'Security Audit' README.md` → exit 0
- `grep -q 'memory-hygiene' README.md` → exit 0
- `grep -qi 'timeout\|kill-switch' README.md` → exit 0
- Nenhuma menção residual a "7 patches" inconsistente com o total real (10) — `grep -n 'patches' README.md` revisado manualmente
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'scan-absorbed' README.md && grep -qi 'Security Audit' README.md && grep -q 'memory-hygiene' README.md && echo OK</automated>
  </verify>
  <done>README documenta os 5 entregáveis; contagem de patches consistente; estrutura de arquivos atualizada.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test integrado da fase</name>
  <read_first>security/scan-absorbed.sh, scripts/idea-doctor.sh</read_first>
  <files>(nenhum — apenas validação)</files>
  <action>
Rodar a validação end-to-end de toda a Fase 02 e registrar os resultados no SUMMARY:
1. `printf 'clean\n' > /tmp/clean.md && bash security/scan-absorbed.sh /tmp/clean.md` → exit 0
2. `printf 'x\xE2\x80\x8By\n' > /tmp/inv.md && bash security/scan-absorbed.sh /tmp/inv.md; echo "exit=$?"` → exit 1
3. `bash scripts/idea-doctor.sh; echo "doctor exit=$?"` — confirmar que a Seção 7 Security Audit aparece no output. (Pode dar FAIL se as deny rules ainda não foram instaladas via install-global-patches.sh — isso é esperado e prova que o check funciona.)
4. `bash -n setup-dev-machine.sh && bash -n scripts/install-global-patches.sh && bash -n scripts/idea-doctor.sh` → todos exit 0.
Registrar exit codes observados no SUMMARY.
  </action>
  <acceptance_criteria>
- scan-absorbed.sh: limpo → exit 0, unicode → exit 1
- idea-doctor.sh roda e mostra "7) Security Audit" no output
- `bash -n` passa nos 3 scripts modificados na fase
  </acceptance_criteria>
  <verify>
    <automated>printf 'clean\n' > /tmp/clean.md; bash security/scan-absorbed.sh /tmp/clean.md; [ $? -eq 0 ] && bash scripts/idea-doctor.sh 2>&1 | grep -q 'Security Audit' && bash -n setup-dev-machine.sh && echo OK</automated>
  </verify>
  <done>Validação end-to-end confirma scan funcional, doctor com seção 7 e sintaxe válida em todos os scripts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — validação humana da Fase 02</name>
  <action>Pausar para o usuário validar os 5 entregáveis de segurança e confirmar que as deny rules globais não quebram os 4 projetos-produto. Ver what-built / how-to-verify abaixo.</action>
  <what-built>Fase 02 completa: pipeline de quarentena (scan-absorbed.sh), deny rules baseline (Patch 10), idea-doctor Seção 7, kill-switch do autosync, memory-hygiene doc, README sincronizado.</what-built>
  <how-to-verify>
1. Rodar `bash scripts/install-global-patches.sh` e confirmar "Patch 10: deny rules baseline" aplicado (ou já presente). Cuidado: isso adiciona deny rules GLOBAIS ao seu ~/.claude/settings.json que afetam TODAS as sessões (Pitfall 1 do RESEARCH) — confirme que aceita bloquear Read de ~/.ssh, ~/.aws, .env.
2. Rodar `bash scripts/idea-doctor.sh` e confirmar Seção 7 Security Audit com todas as deny rules em PASS.
3. (Opcional, afeta o daemon) Rodar `bash setup-dev-machine.sh` para recriar o LaunchAgent com timeout, e checar: `launchctl list | grep gitautosync`.
4. Conferir que o README reflete os 5 entregáveis.
5. Confirmar que os 4 projetos-produto (ideiapartner, nfideia, cfoai-grupori, lapidai) não dependem de Read(~/.ssh)/SSH automático que seria bloqueado (ssh/scp estão em "ask", não "deny" — devem só pedir confirmação).
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva problemas (ex: alguma deny rule quebra um fluxo dos projetos-produto)</resume-signal>
</task>

</tasks>

<verification>
- README cita scan-absorbed, Security Audit, memory-hygiene, kill-switch.
- Smoke test integrado passa (scan limpo→0, unicode→1, doctor mostra seção 7).
- Checkpoint humano confirma deny rules aceitas e projetos-produto não quebrados.
</verification>

<success_criteria>
README sincronizado (regra travada cumprida); validação end-to-end da fase passa; usuário confirma que as deny rules globais são aceitáveis e os 4 projetos-produto seguem funcionando.
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-quarantine/02-04-SUMMARY.md`
</output>
