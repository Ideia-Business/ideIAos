---
phase: "01"
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/test-hooks.sh
  - hooks/console-log-guard.sh
  - hooks/strategic-compact.sh
autonomous: true
requirements: [console-log, strategic-compact]
must_haves:
  truths:
    - "Editar arquivo .ts/.tsx/.js com console.log gera additionalContext alertando deploy Lovable"
    - "A cada ~50 tool calls na sessão, um aviso sugere /compact"
    - "Existe um harness de smoke test que exercita cada hook via echo JSON | bash"
  artifacts:
    - path: "hooks/console-log-guard.sh"
      provides: "PostToolUse guard que detecta console.log em arquivos editados"
      min_lines: 30
    - path: "hooks/strategic-compact.sh"
      provides: "PreToolUse counter em /tmp por session_id, sugere /compact a cada 50"
      min_lines: 30
    - path: "hooks/test-hooks.sh"
      provides: "Smoke tests dos 5 hooks da fase"
      min_lines: 40
  key_links:
    - from: "hooks/strategic-compact.sh"
      to: "/tmp/claude-compact-counter-{session_id}.json"
      via: "leitura/escrita de contador via python3"
      pattern: "claude-compact-counter"
    - from: "hooks/console-log-guard.sh"
      to: "tool_input.file_path"
      via: "parse python3 do stdin JSON"
      pattern: "console"
---

<objective>
Entregar o harness de smoke test da fase e os dois hooks mais simples: console-log-guard (PostToolUse) e strategic-compact (PreToolUse counter). Estes hooks nao escrevem em disco de projeto e tem logica autocontida, entao servem de base validavel para os demais.

Purpose: Estabelecer o padrao de hook + teste que os planos 02 e 03 replicam, e fechar o gap de Wave 0 do RESEARCH (nao havia test harness).
Output: 3 scripts bash em hooks/ — todos com smoke test passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-quality-memory-hooks/01-RESEARCH.md
@hooks/extract-learnings-reminder.sh
@hooks/ideiaos-detector.sh

<interfaces>
Padrao de parse stdin (de extract-learnings-reminder.sh, verificado):

    INPUT="$(cat 2>/dev/null || echo '{}')"
    PARSED="$(echo "$INPUT" | /usr/bin/python3 -c "
    import json, sys
    try:
        d = json.load(sys.stdin)
        print(d.get('tool_name',''))
        print((d.get('tool_input',{}) or {}).get('file_path',''))
        print(d.get('session_id',''))
        print(d.get('cwd',''))
    except Exception: pass
    " 2>/dev/null)"

Output JSON com additionalContext (PostToolUse e PreToolUse suportam):

    printf '{"hookSpecificOutput":{"hookEventName":"PostToolUse","additionalContext":"%s"}}\n' "$MSG"

Sanitizacao de session_id obrigatoria antes de usar em path (RESEARCH Security Domain):

    if echo "$SESSION_ID" | grep -qE '[/\\]|\.\.' 2>/dev/null; then exit 0; fi
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar harness de smoke test test-hooks.sh</name>
  <files>hooks/test-hooks.sh</files>
  <read_first>
    - hooks/extract-learnings-reminder.sh (padrao de parse stdin a ser exercitado)
    - .planning/phases/01-quality-memory-hooks/01-RESEARCH.md (secao "Validation Architecture")
  </read_first>
  <behavior>
    - bash hooks/test-hooks.sh com console-log-guard ou strategic-compact ausentes falha (exit 1) listando quais faltam
    - console-log-guard: JSON com file_path=/tmp/t.ts (contendo console.log) gera output contendo "console.log"; arquivo limpo gera output vazio
    - strategic-compact: 50 execucoes mesmo session_id, 50a emite "/compact"; 1a sai silenciosa
    - precompact-state-save, session-summary, typecheck: assertions presentes mas SKIP (sem falhar) se hook ausente
  </behavior>
  <action>
    Criar hooks/test-hooks.sh (bash, set -uo pipefail). Estrutura:
    - Funcao assert_contains "$output" "$expected" "$test_name" que imprime PASS/FAIL e incrementa contador global FAILS.
    - Funcao assert_empty "$output" "$test_name".
    - Uma secao por hook. Para hooks dos planos 02/03 (precompact-state-save, session-summary, typecheck-on-edit): checar [ -f hooks/<hook>.sh ]; se ausente imprimir "SKIP: <hook>" e continuar (nao conta falha). console-log-guard e strategic-compact SAO deste plano, DEVEM existir e passar.
    - console-log-guard: criar /tmp/clg-test.ts com console.log("debug"), montar JSON com file_path=/tmp/clg-test.ts, pipe ao hook, assert_contains output "console.log". Criar /tmp/clg-clean.ts sem console.log, assert_empty.
    - strategic-compact: rm -f /tmp/claude-compact-counter-smoketest-sc.json; loop 1..50 enviando {"session_id":"smoketest-sc","tool_name":"Read"}; capturar saida da 1a (assert_empty) e da 50a (assert_contains "/compact"); limpar arquivo ao final.
    - Ao final: imprimir total FAILS; exit 1 se FAILS>0, senao exit 0.
    Bash puro, sem frameworks. Rodar a partir da raiz do IdeiaOS (caminhos relativos).
  </action>
  <verify>
    <automated>bash hooks/test-hooks.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "assert_contains" hooks/test-hooks.sh retorna 0
    - grep -q "claude-compact-counter-smoketest-sc" hooks/test-hooks.sh retorna 0
    - Rodar o harness antes de criar os hooks deste plano resulta em exit 1 citando console-log-guard e strategic-compact
    - Mensagens "SKIP" aparecem para precompact-state-save, session-summary, typecheck-on-edit
  </acceptance_criteria>
  <done>Harness existe, falha quando hooks deste plano ausentes, SKIPa hooks de outros planos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar console-log-guard.sh</name>
  <files>hooks/console-log-guard.sh</files>
  <read_first>
    - hooks/extract-learnings-reminder.sh (padrao parse + JSON output)
    - hooks/test-hooks.sh (assertions que este hook deve satisfazer)
  </read_first>
  <behavior>
    - file_path .ts/.tsx/.js/.jsx contendo console.log( gera JSON additionalContext mencionando "console.log" e producao/Lovable
    - extensao fora da lista gera exit 0 silencioso
    - file_path inexistente/vazio gera exit 0 silencioso
    - arquivo sem console.log gera exit 0 silencioso
  </behavior>
  <action>
    Criar hooks/console-log-guard.sh (#!/usr/bin/env bash, set -uo pipefail).
    1. Ler stdin, parsear tool_name/file_path/cwd via python3 (padrao das interfaces).
    2. [ -z "$FILE_PATH" ] && exit 0. Filtrar: case "$FILE_PATH" in *.ts|*.tsx|*.js|*.jsx) ;; *) exit 0 ;; esac.
    3. [ -f "$FILE_PATH" ] || exit 0.
    4. MATCHES="$(grep -nE 'console\.(log|debug|info)\(' "$FILE_PATH" 2>/dev/null | head -10)". Se vazio, exit 0.
    5. Serializar JSON final via python3 json.dumps (NUNCA concatenacao manual — RESEARCH "Don't Hand-Roll" / Anti-Patterns). Texto truncado a ~1500 chars:
       "console.log detectado em <basename> — vai pra producao Lovable. Remova ou troque por logger antes do deploy. Linhas: <numeros>".
       Gerar com: /usr/bin/python3 -c "import json,sys; print(json.dumps({'hookSpecificOutput':{'hookEventName':'PostToolUse','additionalContext':sys.argv[1]}}))" "$TEXTO".
    6. echo do JSON, exit 0. NUNCA exit 2 (nao bloqueia).
    Passar file_path como argumento, nunca via shell expansion nao citada.
  </action>
  <verify>
    <automated>printf 'console.log("x")' > /tmp/clg.ts; echo '{"tool_name":"Edit","tool_input":{"file_path":"/tmp/clg.ts"},"cwd":"/tmp","session_id":"t"}' | bash hooks/console-log-guard.sh | grep -q console.log && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "json.dumps" hooks/console-log-guard.sh retorna 0 (serializacao segura)
    - case com *.ts|*.tsx presente em hooks/console-log-guard.sh
    - JSON com console.log produz stdout contendo "console.log"; arquivo limpo produz stdout vazio
    - grep -q "exit 2" hooks/console-log-guard.sh retorna != 0 (nunca bloqueia)
    - Secao correspondente de hooks/test-hooks.sh passa
  </acceptance_criteria>
  <done>Hook detecta console.log em arquivos JS/TS editados sem bloquear; smoke test passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar strategic-compact.sh</name>
  <files>hooks/strategic-compact.sh</files>
  <read_first>
    - .planning/phases/01-quality-memory-hooks/01-RESEARCH.md (Pattern 3: Contador de sessao em /tmp; Pitfall 5)
    - hooks/test-hooks.sh (assertions de contador)
  </read_first>
  <behavior>
    - 50a chamada (count % 50 == 0) com mesmo session_id gera additionalContext com "/compact"
    - chamadas 1-49 geram exit 0, stdout vazio, contador persistido
    - session_id com / \ ou .. gera exit 0 silencioso (path traversal rejeitado)
    - session_id vazio gera exit 0 silencioso (sem arquivo de contador global)
  </behavior>
  <action>
    Criar hooks/strategic-compact.sh (#!/usr/bin/env bash, set -uo pipefail). Seguir Pattern 3 do RESEARCH.
    1. Ler stdin, extrair session_id via python3.
    2. Sanitizar: if echo "$SESSION_ID" | grep -qE '[/\\]|\.\.' 2>/dev/null; then exit 0; fi. [ -z "$SESSION_ID" ] && exit 0 (NAO usar arquivo global — Pitfall 5).
    3. COUNTER_FILE="/tmp/claude-compact-counter-${SESSION_ID}.json".
    4. Ler count atual: COUNT=0; [ -f "$COUNTER_FILE" ] && COUNT="$(python3 -c "import json; print(json.load(open('$COUNTER_FILE')).get('count',0))" 2>/dev/null || echo 0)".
    5. COUNT=$((COUNT + 1)). Persistir: python3 -c "import json; json.dump({'count': $COUNT}, open('$COUNTER_FILE','w'))" 2>/dev/null || true.
    6. if [ $((COUNT % 50)) -eq 0 ]; then emitir JSON via printf com texto: "~$COUNT tool calls nesta sessao — considere rodar /compact para preservar qualidade. O hook precompact-state-save salvara o STATE.md automaticamente." fi
    7. exit 0 sempre (nunca bloqueia tool call).
  </action>
  <verify>
    <automated>rm -f /tmp/claude-compact-counter-vt.json; for i in $(seq 1 50); do OUT=$(echo '{"session_id":"vt","tool_name":"Read"}' | bash hooks/strategic-compact.sh); done; echo "$OUT" | grep -q "/compact" && echo OK; rm -f /tmp/claude-compact-counter-vt.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "claude-compact-counter" hooks/strategic-compact.sh retorna 0
    - grep -qE '\[/\\\\\]' hooks/strategic-compact.sh (sanitizacao de session_id presente)
    - 50a chamada com mesmo session_id produz stdout contendo "/compact"; 1a chamada produz stdout vazio
    - grep -c "exit 0" hooks/strategic-compact.sh retorna >= 1; nao ha exit 2
    - Secao correspondente de hooks/test-hooks.sh passa
  </acceptance_criteria>
  <done>Contador por session_id sugere /compact a cada 50 calls; sem disparo precoce entre sessoes; smoke test passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness CC -> hook (stdin JSON) | session_id e file_path vem do harness e sao tratados como nao confiaveis |
| hook -> filesystem (/tmp counter) | path de arquivo derivado de session_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | strategic-compact.sh path do counter | mitigate | Rejeitar session_id com /, \\ ou .. antes de montar COUNTER_FILE (grep -qE) |
| T-01-02 | Elevation | console-log-guard.sh file_path | mitigate | file_path passado como argumento ao grep/python3, nunca via eval/expansion nao citada |
| T-01-03 | DoS | console-log-guard grep em arquivo grande | accept | timeout: 5 na settings.json (plano 04); arquivos editados sao pequenos |
</threat_model>

<verification>
- bash hooks/test-hooks.sh sai 0 com console-log-guard e strategic-compact implementados
- chmod +x aplicado nos 3 scripts (validado no plano 04 deploy, mas scripts ja devem ter shebang)
</verification>

<success_criteria>
- Editar .ts com console.log produz aviso (sem bloquear)
- 50 tool calls produzem sugestao de /compact
- Harness de smoke test passa para ambos os hooks deste plano
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/01-quality-memory-hooks/01-01-SUMMARY.md
</output>
