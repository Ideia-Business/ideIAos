---
phase: "01"
plan: "01-04"
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - setup.sh
  - README.md
  - hooks/test-hooks.sh
autonomous: false
requirements: [typecheck, console-log, precompact, session-summary, strategic-compact]
must_haves:
  truths:
    - "Rodar setup.sh instala/atualiza os 5 novos hooks em ~/.claude/hooks/ (cp + chmod +x), idempotente"
    - "setup.sh avisa (warn) se cada um dos 5 hooks nao esta registrado em ~/.claude/settings.json, mostrando o snippet exato do evento correto"
    - "README.md lista os 5 novos hooks (tabela de componentes + arvore de arquivos + secao settings.json)"
    - "O harness test-hooks.sh exercita typecheck-on-edit (nao mais SKIP) e a suite completa passa"
  artifacts:
    - path: "setup.sh"
      provides: "5 blocos de deploy idempotente (padrao step 5.x existente) para os novos hooks"
      contains: "typecheck-on-edit.sh"
    - path: "README.md"
      provides: "Documentacao dos 5 hooks (constraint: README sync obrigatorio)"
      contains: "strategic-compact"
  key_links:
    - from: "setup.sh"
      to: "~/.claude/hooks/"
      via: "cp template + chmod +x + grep em settings.json"
      pattern: "HOOK_DIR"
    - from: "README.md"
      to: "hooks/"
      via: "tabela de componentes + arvore de arquivos"
      pattern: "PostToolUse"
---

<objective>
Integrar os 5 hooks da fase ao deployment (setup.sh) e a documentacao (README.md), e fechar o harness de smoke test. setup.sh copia os scripts para ~/.claude/hooks/ de forma idempotente e avisa o registro manual em settings.json (regra de seguranca: setup.sh NAO auto-modifica settings.json). Inclui um checkpoint humano para o usuario aplicar o registro em settings.json e validar os 3 criterios de sucesso da fase ao vivo.

Purpose: Sem deploy + registro, os hooks existem mas nao disparam. Sem README sync, viola constraint do projeto.
Output: setup.sh com 5 blocos novos, README atualizado, harness completo, e checkpoint de validacao end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-quality-memory-hooks/01-RESEARCH.md
@setup.sh
@README.md
@hooks/test-hooks.sh

<interfaces>
Padrao de bloco de deploy idempotente em setup.sh (extraido de setup.sh linhas 596-680, step 5.5/5.6):

    HOOK_FILE="$HOOK_DIR/<nome>.sh"
    HOOK_TEMPLATE="$SETUP_DIR/hooks/<nome>.sh"
    if [ -f "$HOOK_FILE" ]; then
      if diff -q "$HOOK_TEMPLATE" "$HOOK_FILE" &>/dev/null; then ok "...ja na versao mais recente"
      else cp "$HOOK_TEMPLATE" "$HOOK_FILE"; chmod +x "$HOOK_FILE"; ok "...atualizado"; fi
    else cp "$HOOK_TEMPLATE" "$HOOK_FILE"; chmod +x "$HOOK_FILE"; ok "...instalado"; fi
    # Verificacao de registro (NAO modifica settings.json — regra de seguranca, README linha 642):
    if [ -f "$SETTINGS_FILE" ] && grep -q "<nome>.sh" "$SETTINGS_FILE" 2>/dev/null; then ok "registrado"
    else warn "NAO registrado — adicione em hooks.<Evento>:"; cat <<'SNIPPET' ... SNIPPET; fi

Variaveis ja definidas no setup.sh antes do step 5.5: HOOK_DIR, SETTINGS_FILE, SETUP_DIR. Usar step "5.14)..." em diante (ultimo step atual e 5.13).

Eventos/registro de cada hook (RESEARCH "Settings.json entries"):
  - typecheck-on-edit.sh   -> PostToolUse, matcher "Edit|Write", timeout 60, async:true, asyncRewake:true
  - console-log-guard.sh   -> PostToolUse, matcher "Edit|Write", timeout 5
  - strategic-compact.sh   -> PreToolUse, sem matcher, timeout 3
  - precompact-state-save.sh -> PreCompact, timeout 10
  - session-summary.sh     -> Stop, timeout 30

README — 3 pontos de sync (verificados): tabela de componentes (~linha 208-211), arvore de arquivos (~linha 585-589), secao settings.json troubleshooting (~linha 648).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar 5 blocos de deploy em setup.sh</name>
  <files>setup.sh</files>
  <read_first>
    - setup.sh (linhas 590-810 — steps 5.5 a 5.13, padrao de deploy idempotente a replicar)
    - .planning/phases/01-quality-memory-hooks/01-RESEARCH.md (secao "Settings.json entries para os 5 novos hooks")
  </read_first>
  <action>
    Inserir 5 novos blocos de deploy em setup.sh apos o step 5.13 (linha ~808, antes do step 5.13 MCP chrome-devtools OU logo apos ele — escolher posicao que nao quebre numeracao; usar steps 5.14 a 5.18). Para cada hook (typecheck-on-edit, console-log-guard, strategic-compact, precompact-state-save, session-summary):
    - Replicar exatamente o padrao das interfaces: definir HOOK_FILE/HOOK_TEMPLATE, cp+chmod idempotente com diff -q.
    - Bloco de verificacao de registro com grep "<nome>.sh" em SETTINGS_FILE; se ausente, warn + cat heredoc SNIPPET com a entrada JSON do evento correto (valores da tabela das interfaces — incluir async:true/asyncRewake:true/timeout:60 para typecheck, timeout proprio para cada um).
    - NAO modificar settings.json automaticamente (regra de seguranca — README linha 642). Apenas warn + snippet.
    - Um echo descritivo do comportamento de cada hook (padrao dos steps existentes).
    Manter consistencia com helpers ok/warn/step ja usados.
  </action>
  <verify>
    <automated>bash -n setup.sh && echo "syntax OK"; for h in typecheck-on-edit console-log-guard strategic-compact precompact-state-save session-summary; do grep -q "$h.sh" setup.sh && echo "$h: present" || echo "$h: MISSING"; done</automated>
  </verify>
  <acceptance_criteria>
    - bash -n setup.sh passa (sem erro de sintaxe)
    - Os 5 nomes de hook aparecem em setup.sh com cp + chmod +x
    - grep -q "async" setup.sh retorna 0 (snippet do typecheck inclui async:true/asyncRewake:true)
    - grep -c "warn" no trecho novo >= 5 (aviso de registro manual por hook)
    - Nenhum bloco novo escreve em "$SETTINGS_FILE" (apenas grep de leitura) — confirmar com grep -n "SETTINGS_FILE" nos blocos novos
  </acceptance_criteria>
  <done>setup.sh instala os 5 hooks idempotentemente e avisa o registro manual; sintaxe valida.</done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar README.md e fechar o harness</name>
  <files>README.md, hooks/test-hooks.sh</files>
  <read_first>
    - README.md (tabela de componentes linhas ~208-211; arvore de arquivos ~585-589; secao settings.json ~648)
    - hooks/test-hooks.sh (secao typecheck atualmente em SKIP)
    - hooks/typecheck-on-edit.sh (hook agora implementado pelo plano 01-03)
  </read_first>
  <action>
    Parte A — README.md (constraint: todo componente novo precisa estar no README):
    - Tabela de componentes (~linha 208-211): adicionar 5 linhas, uma por hook, com path ~/.claude/hooks/ e descricao curta do evento/comportamento.
    - Arvore de arquivos (~linha 585-589): adicionar os 5 .sh com comentario do evento (ex: "typecheck-on-edit.sh  <- Claude PostToolUse .ts/.tsx async").
    - Secao settings.json troubleshooting (~linha 648): acrescentar os blocos JSON dos 5 novos hooks (PostToolUse Edit|Write para typecheck+console-log, PreToolUse, PreCompact, Stop), espelhando os snippets do setup.sh.
    Parte B — hooks/test-hooks.sh: trocar a secao typecheck de SKIP por assertions reais (agora que o hook existe):
    - non-ts: JSON com file_path=/tmp/z.md -> assert_empty + exit 0.
    - sem tsc: dir temp sem node_modules, file_path .ts -> assert_empty (silencioso). Limpar.
    - Manter o caso de erro real de tsc como verificacao do checkpoint (Task 3), nao automatizado aqui (ambiente pode nao ter tsc).
  </action>
  <verify>
    <automated>for h in typecheck-on-edit console-log-guard strategic-compact precompact-state-save session-summary; do grep -q "$h" README.md && echo "$h: in README" || echo "$h: MISSING"; done; bash hooks/test-hooks.sh; echo "harness exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Os 5 nomes de hook aparecem em README.md
    - grep -q "PreCompact" README.md e grep -q "Stop" README.md retornam 0 (eventos documentados)
    - hooks/test-hooks.sh nao tem mais "SKIP" para typecheck-on-edit
    - bash hooks/test-hooks.sh sai 0 com todos os 5 hooks implementados
  </acceptance_criteria>
  <done>README documenta os 5 hooks nos 3 pontos de sync; harness exercita typecheck e a suite passa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>5 hooks instalados via setup.sh + documentados no README + harness de smoke test passando. Falta o registro manual em ~/.claude/settings.json (setup.sh nao auto-modifica config por regra de seguranca) e a validacao ao vivo dos 3 criterios de sucesso da fase.</what-built>
  <how-to-verify>
    1. Rodar: bash setup.sh --global-only (ou bash setup.sh) — confirmar que os 5 hooks foram instalados em ~/.claude/hooks/ e que apareceram os 5 avisos de registro manual.
    2. Aplicar os snippets dos 5 hooks em ~/.claude/settings.json (PostToolUse: typecheck-on-edit + console-log-guard; PreToolUse: strategic-compact; PreCompact: precompact-state-save; Stop: session-summary). Reiniciar a sessao Claude Code.
    3. Criterio typecheck: em um projeto TS (ex: ideiapartner), editar um .ts introduzindo um erro de tipo -> confirmar aviso em segundos.
    4. Criterio console-log: adicionar console.log("x") num .ts -> confirmar aviso de produacao Lovable.
    5. Criterio precompact: rodar /compact -> abrir .planning/STATE.md e confirmar a secao "## Compact Snapshot" com timestamp.
    6. Criterio strategic-compact: apos ~50 tool calls na sessao -> confirmar a sugestao de /compact.
    7. Criterio session-summary: encerrar um turno -> confirmar arquivo novo em ~/.claude/sessions/YYYY-MM-DD-*.tmp; se no IdeiaOS, confirmar docs/CONTINUATION_HANDOFF.md atualizado.
    8. Observacao A5 do RESEARCH: se o evento PreCompact nao disparar, testar a chave "Compact" em settings.json.
  </how-to-verify>
  <resume-signal>Digite "approved" se os 5 hooks dispararam corretamente, ou descreva quais falharam (e qual evento/chave precisou ajustar em settings.json).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup.sh -> ~/.claude/settings.json | setup.sh LE mas nao escreve (regra de seguranca: IA nao auto-modifica config) |
| setup.sh -> ~/.claude/hooks/ | cp de scripts versionados do repo (fonte confiavel) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | setup.sh auto-modificando settings.json | mitigate | Manter regra existente: apenas grep de leitura + warn com snippet; registro e manual pelo usuario |
| T-01-11 | Repudiation | hooks instalados sem trilha no README | mitigate | README sync obrigatorio (Task 2); pre-commit hook do IdeiaOS bloqueia componente sem README |
</threat_model>

<verification>
- bash -n setup.sh passa; 5 hooks com deploy idempotente
- README lista os 5 hooks nos 3 pontos de sync
- harness completo passa (typecheck nao mais SKIP)
- Checkpoint humano valida os 3 criterios de sucesso da fase ao vivo
</verification>

<success_criteria>
- setup.sh instala os 5 hooks e avisa registro manual (sem auto-modificar settings.json)
- README documenta todos os 5 hooks
- Os 3 criterios da fase validados ao vivo: editar .ts com erro -> aviso; /compact -> snapshot; 50 calls -> sugestao
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/01-quality-memory-hooks/01-04-SUMMARY.md
</output>
