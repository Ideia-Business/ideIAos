---
phase: 08-ideiaos-v3-review
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - docs/v3/v3-review.md
  - docs/v3/v3-roadmap.md
  - README.md
autonomous: true
requirements:
  - F08-V3-SYNTHESIS
  - F08-V3-ROADMAP
user_setup: []

must_haves:
  truths:
    - "v3-review.md consolidates the three Wave-1 audits (agents, skills, token economy)"
    - "v3-review.md lists at least 10 prioritized gaps, each with priority (P1/P2/P3), effort, and value"
    - "Orchestration gaps not surfaced by the per-component audits are added (e.g. CI/CD, multi-repo, evals automation, manual instinct loop)"
    - "v3-roadmap.md proposes candidate v3 phases derived from the prioritized gaps"
    - "README.md mentions docs/v3/ and the commit passes check-readme-sync.sh (no --no-verify)"
  artifacts:
    - path: "docs/v3/v3-review.md"
      provides: "Consolidated v3 review: synthesis of 08-01/02/03 + orchestration gaps + ≥10 prioritized gaps"
      contains: "# SOURCE: IdeiaOS v2"
      min_lines: 120
    - path: "docs/v3/v3-roadmap.md"
      provides: "Candidate v3 phases derived from prioritized gaps"
      contains: "# SOURCE: IdeiaOS v2"
      min_lines: 40
  key_links:
    - from: "docs/v3/v3-review.md"
      to: "docs/v3/agents-audit.md + docs/v3/skills-guide.md + docs/v3/token-economy-review.md"
      via: "gaps sections of all three Wave-1 docs are pulled into the consolidated prioritized list"
      pattern: "agents-audit|skills-guide|token-economy"
    - from: "README.md"
      to: "docs/v3/"
      via: "README references the v3 review docs directory"
      pattern: "docs/v3"
---

<objective>
Synthesize the three Wave-1 audits into `docs/v3/v3-review.md` with at least 10 PRIORITIZED gaps (P1/P2/P3 + effort + value), add the orchestration-level gaps the per-component audits don't surface, derive `docs/v3/v3-roadmap.md` (candidate v3 phases), and update README.md to point at docs/v3/.

Purpose: This is the deliverable the whole phase exists for — the single document that tells "what v3 should fix and in what order", grounded in the three component audits plus the cross-cutting orchestration gaps.

Output: `docs/v3/v3-review.md` + `docs/v3/v3-roadmap.md` + README sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/v3/agents-audit.md
@docs/v3/skills-guide.md
@docs/v3/token-economy-review.md

<orchestration_gap_candidates>
Cross-cutting gaps the per-component audits will NOT surface — assess each against the real system, include the ones that are genuinely uncovered (honest audit — verify, don't assume):
- CI/CD: is there any automated pipeline, or is everything local hooks + manual commits?
- Multi-repo / multi-machine: autosync exists (LaunchAgent); is there a story for applying IdeiaOS across many target repos at once?
- Evals automation: run-evals.sh is manual by design (no API key, per 07-02) — automated eval runs are an open gap.
- Instinct loop: /instinct-analyze requires MANUAL invocation (no scheduler) — the "Fase A automática" goal is only partially met. This is a real, known gap.
- README/docs sync: docs/ is NOT covered by the pre-commit README-sync hook (only source/scripts/plugins/manifests) — docs can drift silently.
- Any false-positive friction already logged (scan-absorbed `<!--` HTML payload check, `nc ` substring warns) — note as DX gaps if still unresolved.
</orchestration_gap_candidates>

<readme_sync_note>
The pre-commit hook (.git/hooks/pre-commit -> scripts/check-readme-sync.sh) fires only when STAGED files touch source/, scripts/, plugins/, or manifests/. Editing docs/v3/ alone does NOT trip it. BUT this plan edits README.md to mention docs/v3/, so the commit will run check-readme-sync.sh — the README change MUST keep the sync check green. Commit WITHOUT --no-verify. If the sync script fails, read /tmp/readme-sync-check.log and fix the README to satisfy it before committing.
</readme_sync_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate the three audits + orchestration gaps into a prioritized gap list</name>
  <files>docs/v3/v3-review.md (gap list assembled here, doc finalized in Task 2)</files>
  <action>
    Read the three Wave-1 docs end to end: `docs/v3/agents-audit.md`, `docs/v3/skills-guide.md`, `docs/v3/token-economy-review.md`. Pull every "Gaps" / "AJUSTAR / RETRABALHAR" / "Oportunidades de Redução" / redundancy item into one working list. Then assess each `orchestration_gap_candidate` against the real system and add the ones that are genuinely uncovered.

    Deduplicate and normalize into a single prioritized gap list. There MUST be at least 10 gaps. For EACH gap record:
    - **ID** (G-01, G-02, …)
    - **Gap** (one line)
    - **Origem** (which audit / orchestration)
    - **Prioridade** (P1 / P2 / P3)
    - **Esforço** (baixo / médio / alto)
    - **Valor** (baixo / médio / alto)

    Prioritize honestly: P1 = correctness/cost wins or known broken-ish behavior (e.g. agents with no model declared; manual-only instinct loop if the "automática" goal matters); P2 = meaningful improvements; P3 = polish. Do NOT pad — if there are more than 10 real gaps, list them all; the floor is 10.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && test -f docs/v3/agents-audit.md && test -f docs/v3/skills-guide.md && test -f docs/v3/token-economy-review.md</automated>
  </verify>
  <done>All three Wave-1 docs read; ≥10 deduplicated, prioritized gaps assembled with ID/origem/prioridade/esforço/valor.</done>
</task>

<task type="auto">
  <name>Task 2: Write v3-review.md + v3-roadmap.md and sync README</name>
  <files>docs/v3/v3-review.md, docs/v3/v3-roadmap.md, README.md</files>
  <action>
    **A. docs/v3/v3-review.md** — first line `# SOURCE: IdeiaOS v2`, never literal `<!--`. Sections:
    1. `## Resumo` — what v2 got right, headline gap count, the top P1s.
    2. `## Síntese das Auditorias` — short subsection each summarizing agents-audit / skills-guide / token-economy findings (link by filename).
    3. `## Gaps de Orquestração` — the cross-cutting gaps assessed in Task 1 (CI/CD, multi-repo, evals automation, manual instinct loop, docs-sync, residual false-positive friction).
    4. `## Gaps Priorizados` — THE table: ID | Gap | Origem | Prioridade | Esforço | Valor. At least 10 rows, each ID in `G-NN` form so it greps as `| G-01`.
    5. `## Recomendação v3` — 3-5 sentences pointing at the roadmap.

    **B. docs/v3/v3-roadmap.md** — first line `# SOURCE: IdeiaOS v2`, no `<!--`. Group the P1/P2 gaps into 3-6 candidate v3 phases (each: name, goal, which G-IDs it closes, rough esforço/valor). This is the `/ideiaos-v3-roadmap` output the ROADMAP calls for.

    **C. README.md** — add a short reference so docs/v3/ is discoverable (e.g. a line/bullet in the docs section pointing to `docs/v3/v3-review.md` as the v3 review). Keep it minimal. Then ensure `scripts/check-readme-sync.sh` passes (the commit step runs it). manifests/modules.json does NOT change in this phase — do not touch it.

    Confirm the gap table has ≥10 data rows (lines matching `| G-NN`) before finishing.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && head -1 docs/v3/v3-review.md | grep -q "# SOURCE: IdeiaOS v2" && head -1 docs/v3/v3-roadmap.md | grep -q "# SOURCE: IdeiaOS v2" && ! grep -q '<!--' docs/v3/v3-review.md && ! grep -q '<!--' docs/v3/v3-roadmap.md && grep -q '## Gaps Priorizados' docs/v3/v3-review.md && test $(grep -cE '\| G-[0-9]' docs/v3/v3-review.md) -ge 10 && grep -q 'docs/v3' README.md && bash scripts/check-readme-sync.sh . > /tmp/readme-sync-check.log 2>&1</automated>
  </verify>
  <done>v3-review.md + v3-roadmap.md exist with SOURCE header and no HTML comment; ≥10 prioritized gap rows; README references docs/v3/; check-readme-sync.sh passes.</done>
</task>

</tasks>

<verification>
| Check | Command | Expect |
|-------|---------|--------|
| Review doc exists | `test -f docs/v3/v3-review.md` | exit 0 |
| Roadmap doc exists | `test -f docs/v3/v3-roadmap.md` | exit 0 |
| SOURCE headers | `head -1 docs/v3/v3-review.md; head -1 docs/v3/v3-roadmap.md` | both `# SOURCE: IdeiaOS v2` |
| No HTML comment | `grep -c '<!--' docs/v3/v3-review.md docs/v3/v3-roadmap.md` | 0 each |
| ≥10 prioritized gaps | `grep -cE '\| G-[0-9]' docs/v3/v3-review.md` | ≥10 |
| Orchestration gaps | `grep -c '## Gaps de Orquestração' docs/v3/v3-review.md` | ≥1 |
| README points at docs/v3 | `grep -c 'docs/v3' README.md` | ≥1 |
| README sync green | `bash scripts/check-readme-sync.sh .` | exit 0 |
</verification>

<success_criteria>
- v3-review.md consolidates all three Wave-1 audits + orchestration gaps.
- ≥10 prioritized gaps with priority/effort/value.
- v3-roadmap.md proposes candidate v3 phases mapped to gap IDs.
- README references docs/v3/ and check-readme-sync.sh passes (commit without --no-verify).
</success_criteria>

<output>
After completion, create `.planning/phases/08-ideiaos-v3-review/08-04-SUMMARY.md`
</output>
