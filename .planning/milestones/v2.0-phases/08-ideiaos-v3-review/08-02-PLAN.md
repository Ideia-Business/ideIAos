---
phase: 08-ideiaos-v3-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v3/skills-guide.md
autonomous: true
requirements:
  - F08-SKILLS-GUIDE
user_setup: []

must_haves:
  truths:
    - "All 34 skills in source/skills/ are grouped into workflow clusters with none left uncategorized"
    - "Each cluster has a recommended sequence for a concrete scenario"
    - "Each cluster has an anti-patterns / when-NOT-to-use note"
    - "Each cluster has exactly one canonical example"
    - "Redundant or overlapping skills are explicitly identified"
  artifacts:
    - path: "docs/v3/skills-guide.md"
      provides: "Usage guide for 34 skills: workflow grouping, per-scenario sequence, anti-patterns, canonical examples, redundancy map"
      contains: "# SOURCE: IdeiaOS v2"
      min_lines: 120
  key_links:
    - from: "docs/v3/skills-guide.md"
      to: "source/skills/*/"
      via: "each skill inspected on disk (SKILL.md description + triggers)"
      pattern: "name:|description:"
---

<objective>
Produce a usage guide for all 34 skills in `source/skills/`: group them by workflow, give a recommended sequence per scenario, document anti-patterns (when NOT to use a skill), provide one canonical example per group, and flag redundant/overlapping skills.

Purpose: 34 skills is past the point where a user can hold the map in their head. v3 needs a "which skill, when, in what order" guide so the right skill fires for the right job — and so overlapping skills can be merged or retired in the v3 roadmap.

Output: `docs/v3/skills-guide.md` — grouped usage guide + redundancy map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<audit_inputs>
The 34 skill directories in source/skills/ (read each SKILL.md to confirm purpose):

accessibility · api-design · banner-design · benchmark-optimization-loop · brand · code-tour · codebase-onboarding · cost-tracking · cursor-continuation · database-migrations · deep-research · design · design-system · e2e-testing · evolve · extract-learnings · frontend-visual-loop · idea · ideiaos-catalog · ideiaos-setup · instinct-analyze · instinct-status · learn · llms-txt · lovable-handoff · mcp-to-cli · motion · recall-learnings · slides · tdd · two-instance-kickoff · ui-styling · ui-ux-pro-max · web-quality

Suggested clusters (refine against what you read — every skill lands in exactly one primary cluster):
- **Dev diário:** idea, tdd, e2e-testing, code-tour, codebase-onboarding, web-quality, api-design, database-migrations
- **Design / visual:** design, design-system, ui-styling, ui-ux-pro-max, frontend-visual-loop, motion, banner-design, brand, slides, accessibility
- **Learning loop:** instinct-analyze, instinct-status, learn, evolve, recall-learnings, extract-learnings
- **ECC / handoff / receitas:** lovable-handoff, two-instance-kickoff, llms-txt, mcp-to-cli, deep-research, benchmark-optimization-loop, cost-tracking
- **Meta / setup:** ideiaos-catalog, ideiaos-setup, cursor-continuation
</audit_inputs>

<known_facts>
From STATE.md (verify against disk):
- 04-04: receita skills (two-instance-kickoff, llms-txt, mcp-to-cli) are installStrategy:manual (on-demand); ideiaos-catalog is installStrategy:always.
- 04-04 had a frontmatter-inverted incident on skills (corrected) — sanity-check skill frontmatter while reading.
- 05: learning-loop skills (instinct-analyze, instinct-status, learn, evolve, recall-learnings) and the manual-invocation gap of /instinct-analyze (no scheduler) are real and relevant to anti-patterns.
</known_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect all 34 skills and assign each to a workflow cluster</name>
  <files>docs/v3/skills-guide.md (scratch/notes — finalized in Task 2)</files>
  <action>
    For each of the 34 directories in `source/skills/`, read its `SKILL.md` (at minimum the frontmatter `name`/`description` and the opening section). Capture:
    1. **Purpose** — one line.
    2. **Primary cluster** — assign to exactly ONE cluster (dev diário / design / learning loop / ECC-receitas / meta-setup). Refine cluster names if the suggested map is wrong.
    3. **Overlap signal** — does this skill substantially overlap another (e.g. design vs design-system vs ui-styling vs ui-ux-pro-max; codebase-onboarding vs code-tour; instinct-status vs recall-learnings)? Note candidate overlaps.
    4. **When NOT to use** — one concrete anti-pattern (e.g. "don't run benchmark-optimization-loop before you have a passing baseline").

    Verify each claim against the real SKILL.md. While reading, sanity-check that no frontmatter is inverted (04-04 incident). Flag any skill whose purpose is unclear from its own SKILL.md as a documentation gap.
  </action>
  <verify>
    <automated>test $(ls -1d /Users/gustavolopespaiva/dev/IdeiaOS/source/skills/*/ | wc -l | tr -d ' ') -eq 34</automated>
  </verify>
  <done>All 34 SKILL.md files read; each skill assigned a primary cluster; overlap candidates and anti-patterns noted.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/v3/skills-guide.md (clusters, sequences, anti-patterns, canonical examples, redundancy map)</name>
  <files>docs/v3/skills-guide.md</files>
  <action>
    Create `docs/v3/skills-guide.md`. First line MUST be `# SOURCE: IdeiaOS v2`. Never use literal `<!--` (scan-absorbed false positive). Structure:

    1. `## Resumo` — 2-3 sentences: how the 34 skills organize, headline count of redundancy candidates.
    2. `## Skills por Workflow` — one `### {Cluster}` per cluster. Each cluster contains:
       - **Skills do grupo:** the list (every skill appears in exactly one cluster; the union across clusters = all 34).
       - **Sequência recomendada (cenário X):** an ordered sequence for ONE concrete scenario (e.g. dev diário: idea → tdd → e2e-testing → web-quality).
       - **Anti-patterns / quando NÃO usar:** concrete bullets.
       - **Exemplo canônico:** exactly one worked example for the cluster (a realistic invocation + expected outcome).
    3. `## Mapa de Redundância` — a table: Skill A | Skill B | Sobreposição | Recomendação (manter ambas / fundir / aposentar / esclarecer escopo). At least the design-family and the onboarding-family overlaps must appear if confirmed.
    4. `## Gaps de Documentação` — skills whose purpose is unclear from their own SKILL.md, the /instinct-analyze manual-invocation gap, and any frontmatter issues spotted. These feed 08-04.

    The sum of skills listed across clusters MUST equal 34 (no skill dropped, none double-counted as primary). State the count explicitly in Resumo.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && head -1 docs/v3/skills-guide.md | grep -q "# SOURCE: IdeiaOS v2" && ! grep -q '<!--' docs/v3/skills-guide.md && grep -q '## Skills por Workflow' docs/v3/skills-guide.md && grep -q '## Mapa de Redundância' docs/v3/skills-guide.md && grep -qiE 'anti-pattern|quando NÃO usar' docs/v3/skills-guide.md && grep -qi 'Exemplo canônico' docs/v3/skills-guide.md</automated>
  </verify>
  <done>docs/v3/skills-guide.md exists; SOURCE header; no HTML comment; workflow clusters cover all 34 skills; redundancy map present; per-cluster anti-patterns + one canonical example each.</done>
</task>

</tasks>

<verification>
| Check | Command | Expect |
|-------|---------|--------|
| Doc exists | `test -f docs/v3/skills-guide.md` | exit 0 |
| SOURCE header | `head -1 docs/v3/skills-guide.md` | `# SOURCE: IdeiaOS v2` |
| No HTML comment | `grep -c '<!--' docs/v3/skills-guide.md` | 0 |
| Workflow section | `grep -c '## Skills por Workflow' docs/v3/skills-guide.md` | ≥1 |
| Redundancy map | `grep -c '## Mapa de Redundância' docs/v3/skills-guide.md` | ≥1 |
| Anti-patterns | `grep -ciE 'anti-pattern|quando NÃO usar' docs/v3/skills-guide.md` | ≥1 |
| Canonical example | `grep -ci 'Exemplo canônico' docs/v3/skills-guide.md` | ≥1 |
</verification>

<success_criteria>
- All 34 skills grouped into workflow clusters (union = 34, no skill lost).
- Each cluster: recommended sequence + anti-patterns + one canonical example.
- Redundancy map identifies overlapping skills with a recommendation.
- Documentation gaps captured for 08-04.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ideiaos-v3-review/08-02-SUMMARY.md`
</output>
