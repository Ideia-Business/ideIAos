---
phase: "14.0"
plan: "14.0-04"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/cockpit/package.json
  - apps/cockpit/vite.config.ts
  - apps/cockpit/tailwind.config.ts
  - apps/cockpit/index.html
  - apps/cockpit/src/main.tsx
  - apps/cockpit/src/App.tsx
  - apps/cockpit/src/index.css
  - apps/cockpit/components.json
autonomous: true
requirements: [R14-04]
user_setup: []
must_haves:
  truths:
    - "apps/cockpit/ compila com Vite (npm run build) e produz dist/index.html"
    - "O dev server serve em loopback http://127.0.0.1 sem login e responde com a raiz React (#root)"
    - "Tema black-gold OKLCH com --brand-hue:75 presente no CSS base"
  artifacts:
    - path: "apps/cockpit/package.json"
      provides: "Scaffold Vite 7 + React 18 + TS + Tailwind + shadcn/ui"
      min_lines: 15
    - path: "apps/cockpit/src/index.css"
      provides: "Tema black-gold OKLCH (--brand-hue:75)"
      min_lines: 5
  key_links:
    - from: "apps/cockpit/index.html"
      to: "src/main.tsx"
      via: "div#root + script module"
      pattern: "id=\"root\""
    - from: "apps/cockpit/src/index.css"
      to: "tema black-gold"
      via: "custom property OKLCH"
      pattern: "--brand-hue"
---

<objective>
Scaffold da SPA do Cockpit em `apps/cockpit/` (Vite 7 + React 18 + TS + Tailwind + shadcn/ui), tema
black-gold OKLCH (`--brand-hue:75`), servida em `http://127.0.0.1` loopback sem login. Esta fase entrega
**so o scaffold + o serve em loopback** — a leitura do read-model e o card de maquina sao do plano 07 (Wave 3),
quando o read-model (plano 06) ja existe.

Purpose: a Espinha visual minima. Nao ha UI de valor ainda — so prova que o app builda e serve. Plano
independente (Wave 1, doc 72: Grupo 6.1/6.2). Reuso do padrao shadcn do nfideia (Card/Badge/lucide) e
confirmado em 14.0-PATTERNS.md; o tema black-gold e proprio do cockpit (nao herda o tema fiscal do nfideia).
Output: `apps/cockpit/` que `npm run build` -> `dist/index.html` e `npm run dev` serve em 127.0.0.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-CONTEXT.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-PATTERNS.md
@docs/ideiaos-console/72-phase-v14_0-buildable.md
@specs/cockpit/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (6.1): Scaffold Vite 7 + React 18 + TS + Tailwind + shadcn + tema black-gold OKLCH</name>
  <files>apps/cockpit/package.json, apps/cockpit/vite.config.ts, apps/cockpit/tailwind.config.ts, apps/cockpit/index.html, apps/cockpit/src/main.tsx, apps/cockpit/src/App.tsx, apps/cockpit/src/index.css, apps/cockpit/components.json</files>
  <read_first>
    - 14.0-PATTERNS.md secao "apps/cockpit/ SPA" (padrao de imports shadcn alias @/ absoluto: Card/CardHeader/CardTitle/CardContent + Badge + lucide; reuso confirmado do nfideia; tema black-gold proprio)
    - /Users/gustavolopespaiva/dev/nfideia/src/components/clientes/NotaGatewayHealthCard.tsx:8-12 (imports shadcn Card/Badge/Skeleton + lucide — padrao de health-card a copiar a ESTRUTURA, nao o dominio fiscal)
    - /Users/gustavolopespaiva/dev/nfideia/components.json e tailwind.config.ts (alias @/ e config base — referencia de convencao)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 6.1 (Vite 7 + React 18 + TS + Tailwind + shadcn; tema black-gold OKLCH --brand-hue:75; npm run build exit 0 + test -s dist/index.html)
  </read_first>
  <action>
    Tarefa 6.1: scaffold em apps/cockpit/. package.json com Vite 7, React 18, TypeScript, Tailwind, shadcn/ui
    (deps minimas — token-economy; nada de framework pesado). vite.config.ts com alias `@/` -> ./src e plugin react.
    tailwind.config.ts apontando para ./src e ./index.html. index.html com `<div id="root">` + script module main.tsx.
    src/main.tsx monta o App no #root. src/App.tsx renderiza um shell minimo (header "IdeiaOS Cockpit"); NAO le
    o read-model ainda (isso e o plano 07). src/index.css com a base Tailwind e o tema black-gold OKLCH:
    custom property `--brand-hue: 75` + tokens OKLCH derivados (fundo preto, dourado de marca). components.json no
    estilo shadcn (alias @/components, @/lib). Header de proveniencia onde aplicavel.
    Instalar deps (`npm install` em apps/cockpit) e provar o build.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS/apps/cockpit && npm install >/dev/null 2>&1 && npm run build >/dev/null 2>&1 && test -s dist/index.html; echo "build_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/cockpit && npm run build` exit 0 (tarefa 6.1).
    - `test -s apps/cockpit/dist/index.html` exit 0 (tarefa 6.1 — artefato de build nao-vazio).
    - `grep -q -- '--brand-hue' apps/cockpit/src/index.css` exit 0 (tema black-gold OKLCH presente).
    - `grep -q 'id="root"' apps/cockpit/index.html` exit 0 (ponto de montagem React).
  </acceptance_criteria>
  <done>apps/cockpit/ builda (dist/index.html nao-vazio) com tema black-gold OKLCH (--brand-hue:75) e #root.</done>
</task>

<task type="auto">
  <name>Task 2 (6.2): Servir em loopback http://127.0.0.1 sem login</name>
  <files>apps/cockpit/vite.config.ts</files>
  <read_first>
    - apps/cockpit/vite.config.ts (o config da Task 1 — ajustar server.host para 127.0.0.1)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 6.2 (npm run dev + curl http://127.0.0.1:<port>/ contendo `<div id="root"`; processo encerrado apos o gate; sem login)
  </read_first>
  <action>
    Tarefa 6.2: configurar o dev server para servir em loopback `127.0.0.1` (vite server.host = '127.0.0.1',
    porta fixa documentada). Sem nenhuma camada de login — e loopback puro. O gate sobe o dev server em background,
    faz curl na raiz e confirma o markup do #root, depois encerra o processo. Documentar a porta no SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS/apps/cockpit && (npm run dev >/tmp/cockpit-dev.log 2>&1 &) ; sleep 4; PORT=$(grep -oE '127.0.0.1:[0-9]+' /tmp/cockpit-dev.log | head -1 | grep -oE '[0-9]+$'); curl -sf "http://127.0.0.1:${PORT:-5173}/" | grep -qi 'id="root"'; R=$?; pkill -f 'vite' 2>/dev/null; echo "serve_exit=$R"</automated>
  </verify>
  <acceptance_criteria>
    - O dev server sobe em `127.0.0.1` (loopback): o log do dev contem `127.0.0.1:<port>`.
    - `curl -sf http://127.0.0.1:<port>/ | grep -qi 'id="root"'` exit 0 (tarefa 6.2 — raiz React servida sem login).
    - O processo do dev server e encerrado apos o gate (pkill -f vite).
  </acceptance_criteria>
  <done>Dev server serve a raiz React em loopback 127.0.0.1 sem login; curl confirma #root; processo encerrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser local -> SPA em 127.0.0.1 | a SPA serve so em loopback; nunca exposta fora de 127.0.0.1 (mitiga o risco "mapa do tesouro") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.0-04-I | Information Disclosure | superficie web do cockpit | mitigate | server.host fixo em 127.0.0.1 (loopback) — nunca 0.0.0.0; sem exposicao de rede |
| T-14.0-04-S | Spoofing | acesso sem login | accept | loopback-only e local-first por design (ADR); nao ha multi-tenant nesta superficie |
| T-14.0-04-SC | Tampering | npm install (Vite/React/Tailwind/shadcn) | mitigate | usar so pacotes mainstream pinados no package.json; auditar lockfile antes do build (sem [ASSUMED]/[SUS] — stack padrao do ecossistema) |
</threat_model>

<verification>
- `npm run build` exit 0 + `test -s dist/index.html`.
- Dev server em 127.0.0.1; curl da raiz contem #root; processo encerrado.
- `--brand-hue` presente no CSS.
</verification>

<success_criteria>
- Scaffold apps/cockpit/ builda e serve em loopback sem login.
- Tema black-gold OKLCH (--brand-hue:75) aplicado.
- Estrutura pronta para o card de maquina do plano 07 (Wave 3).
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.0-substrate-spine/14.0-04-SUMMARY.md` when done
</output>
