---
phase: "14.0"
plan: "14.0-05"
type: execute
wave: 2
depends_on: ["14.0-01", "14.0-02"]
files_modified:
  - source/agentd/collect.js
  - source/agentd/agentd.js
  - source/agentd/zeroleak-snapshot.sh
  - infra/launchd/com.ideiaos.cockpit.plist
autonomous: true
requirements: [R14-02, R14-CTX-A1, R14-CTX-A2]
must_haves:
  truths:
    - "O coletor le so metadata: nenhum objeto retornado tem campo value (RHS de = descartado)"
    - "O snapshot ideiaos-cockpit-snapshot/v1 e gravado no ref cockpit via cockpit_write_snapshot (plano 02)"
    - "Zero-Leak: o snapshot gerado nao contem nenhum valor de segredo (sk-/gho_/eyJ/service_role/hex>40) — gate de release"
    - "O agentd e fail-silent: fonte ausente -> exit 0 com warning no stderr (hook-contract de daemon)"
    - "com.ideiaos.cockpit.plist e o 4o LaunchAgent com StartInterval 900"
  artifacts:
    - path: "source/agentd/collect.js"
      provides: "Leitores por fonte (read-only) retornando metadata sem value"
      min_lines: 60
    - path: "source/agentd/zeroleak-snapshot.sh"
      provides: "Scan de Zero-Leak do snapshot (exit 0 = sem segredo)"
      min_lines: 15
    - path: "infra/launchd/com.ideiaos.cockpit.plist"
      provides: "4o LaunchAgent, StartInterval 900"
      min_lines: 15
  key_links:
    - from: "source/agentd/agentd.js"
      to: "refs/heads/cockpit"
      via: "cockpit_write_snapshot (source/lib/cockpit.sh)"
      pattern: "cockpit_write_snapshot"
    - from: "source/agentd/collect.js"
      to: "idea-doctor --json"
      via: "summary.{ok,warn,fail} -> MachineSnapshot.doctor"
      pattern: "--json"
    - from: "infra/launchd/com.ideiaos.cockpit.plist"
      to: "StartInterval"
      via: "agendamento 900s"
      pattern: "<integer>900</integer>"
---

<objective>
Construir o `ideiaos-agentd`: coletor **read-only** (Node 18) que normaliza so **metadata** (zero valor
de segredo) das fontes do substrato, monta o snapshot `ideiaos-cockpit-snapshot/v1` e o grava no ref
`cockpit` via `cockpit_write_snapshot` (plano 02). Gate **Zero-Leak** sobre o snapshot (DoD #1) + o 4o
LaunchAgent `com.ideiaos.cockpit` (StartInterval 900). Fail-silent (hook-contract de daemon).

Purpose: e a coleta nao-mutante (DoD #3) que torna o substrato federavel. Consome `idea-doctor --json`
(plano 01) para `doctor{}` e `cockpit_write_snapshot` (plano 02) para gravar — por isso Wave 2. Descoberta
de produtos dinamica (nunca N=5). machine_id = sha256(IOPlatformUUID)[:12], NUNCA hostname.
Output: `source/agentd/{collect,agentd}.js` + `zeroleak-snapshot.sh` + `com.ideiaos.cockpit.plist`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-CONTEXT.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-PATTERNS.md
@docs/ideiaos-console/72-phase-v14_0-buildable.md
@specs/cockpit/spec.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-01-idea-doctor-json-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-02-cockpit-ref-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (3.1 + 3.2): collect.js — leitores read-only por fonte + descoberta dinamica de produtos</name>
  <files>source/agentd/collect.js</files>
  <read_first>
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 3 (tabela fonte->entidade; readEnvKeys descarta RHS de =; descoberta dinamica iterando ~/dev/*/.git; classificacao produto vs test-dir por heuristica determinista; machine_id = sha256(IOPlatformUUID)[:12])
    - 14.0-PATTERNS.md secao "source/agentd/collect.js" (greenfield-with-convention: Node 18; header `// SOURCE: IdeiaOS v14 | kind: agentd`; fail-silent process.exit(0); equivalente Node de `grep '^[A-Z_]*=' | sed 's/=.*//'`; nenhum objeto com campo value)
    - scripts/check-soak.sh:50-65 (parse awk -F'|' dos ledgers soak — referencia conceitual para Node ler `.planning/soak/*.log`)
  </read_first>
  <action>
    Tarefa 3.1: criar source/agentd/collect.js (Node 18, header de proveniencia). Funcoes de leitura por fonte
    (tabela do doc 72 secao 3): readSoakHeartbeats (parse pipe-delimited de .planning/soak/*.log), readDaemons
    (`launchctl list | grep ideiaos`), readDoctor (consome `idea-doctor --json` do plano 01 -> summary), readCommits
    (`git log` por repo), readEnvKeys (le SO os nomes: equivalente de `grep '^[A-Z_]*=' | sed 's/=.*//'`, RHS descartado;
    NENHUM objeto pode ter campo value), readMcp (~/.claude.json + ~/.cursor/mcp.json keys), readAccounts (gh auth status),
    readSupabase (config.toml project_id), readVersions (versions.lock string-eq). Cada uma retorna objeto puro.
    machine_id derivado 1x via sha256(IOPlatformUUID)[:12], cacheado; NUNCA hostname (gotcha alias 192/MacBook-Air-2).

    Tarefa 3.2: descoberta dinamica — iterar ~/dev/*/ que sejam repos git (test -d "$d/.git"); classificar produto
    vs test-dir por heuristica determinista (produto = remote origin + (em git-autosync-repos.txt OU tem .env.example/supabase/package.json);
    test-dir = sem remote ou nome casando ^(test|tmp|sandbox|fixture) -> is_test_dir:true). Nunca assumir N=5.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && printf 'FOO=bar\nSECRET_KEY=sk-abc123\n' > /tmp/fix.env && node -e 'const c=require("./source/agentd/collect");const k=c.readEnvKeys("/tmp/fix.env");process.exit(k.every(x=>!("value" in x))?0:1)'; echo "noval_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e 'const c=require("./source/agentd/collect");const k=c.readEnvKeys("/tmp/fix.env");process.exit(k.every(x=>!("value" in x))?0:1)'` exit 0 (tarefa 3.1 — nenhum objeto tem campo value).
    - Descoberta dinamica produz JSON valido com IdeiaOS como is_test_dir:false: `node source/agentd/collect.js --discover | python3 -m json.tool >/dev/null` exit 0 (tarefa 3.2).
    - `grep -c 'os.hostname' source/agentd/collect.js` == 0 (machine_id nunca por hostname).
    - `grep -q 'IOPlatformUUID' source/agentd/collect.js` exit 0 (machine_id derivado do hardware-uuid).
  </acceptance_criteria>
  <done>collect.js le so metadata (sem value), descobre produtos dinamicamente, deriva machine_id do IOPlatformUUID.</done>
</task>

<task type="auto">
  <name>Task 2 (3.3 + 3.6): agentd.js monta o snapshot v1, grava no ref, fail-silent</name>
  <files>source/agentd/agentd.js</files>
  <read_first>
    - source/agentd/collect.js (os leitores da Task 1)
    - source/lib/cockpit.sh (cockpit_write_snapshot do plano 02 — o agentd invoca-o via shell para gravar o snapshot)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 3 (SHAPE completo ideiaos-cockpit-snapshot/v1: machine_id, agentd_version, os_version, taken_epoch, daemons[], doctor{}, installed_versions{}, accounts[], projects[]{api_keys[]{var_name,present,expected,risk_tier,mtime_epoch — SEM value}}; cadencia StartInterval 900; fail-silent erro->stderr+exit 0)
    - 14.0-PATTERNS.md secao "source/agentd/collect.js" (fail-silent = hook-contract de daemon: process.exit(0) sempre)
  </read_first>
  <action>
    Tarefa 3.3: criar source/agentd/agentd.js — entry `--once` (uma coleta) que: chama os leitores de collect.js,
    monta o objeto no SHAPE EXATO ideiaos-cockpit-snapshot/v1 (com `schema`, `agentd_version` declarado, `os_version`,
    `taken_epoch`, e a hierarquia projects[].api_keys[] SEM value), serializa para JSON e grava via cockpit_write_snapshot
    (source/lib/cockpit.sh do plano 02; invocar via `bash -c '. source/lib/cockpit.sh; cockpit_write_snapshot <MID> "$JSON"'`
    ou helper equivalente). Nenhum campo value em lugar nenhum do snapshot.

    Tarefa 3.6: fail-silent — envolver a coleta em try/catch; qualquer erro (fonte ausente) -> log no stderr +
    process.exit(0). O daemon nunca derruba nada (hook-contract). Simular fonte ausente e confirmar exit 0.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && MID=$(ioreg -rd1 -c IOPlatformExpertDevice | awk -F'"' '/IOPlatformUUID/{print $4}' | shasum -a 256 | cut -c1-12) && node source/agentd/agentd.js --once && git show "cockpit:snapshots/$MID.json" | python3 -c 'import json,sys;d=json.load(sys.stdin);assert d["schema"]=="ideiaos-cockpit-snapshot/v1";assert "machine_id" in d;sys.exit(0)'; echo "snap_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Apos `node source/agentd/agentd.js --once`: `git show cockpit:snapshots/$MID.json | python3 -c '...schema==ideiaos-cockpit-snapshot/v1; machine_id presente...'` exit 0 (tarefa 3.3).
    - O snapshot nao tem campo value em api_keys: `git show cockpit:snapshots/$MID.json | grep -c '"value"'` == 0.
    - FAIL-SILENT (tarefa 3.6): com uma fonte propositalmente ausente, `node source/agentd/agentd.js --once; echo $?` == 0 (warning no stderr aceitavel).
    - `git status --porcelain` vazio apos a gravacao (A4 herdada do plano 02).
  </acceptance_criteria>
  <done>agentd.js monta o snapshot v1 (sem value), grava no ref cockpit, e fail-silent (exit 0 mesmo com fonte ausente).</done>
</task>

<task type="auto">
  <name>Task 3 (3.4 + 3.5): Gate Zero-Leak do snapshot + plist com StartInterval 900</name>
  <files>source/agentd/zeroleak-snapshot.sh, infra/launchd/com.ideiaos.cockpit.plist</files>
  <read_first>
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 3.4 (Zero-Leak: varrer o snapshot contra sk-/gho_/eyJ JWT/service_role/hex>40 -> 0 matches; falha = release bloqueado) e secao 3.5 (plist StartInterval 900, ProgramArguments apontando para o agentd, plutil -lint + grep <integer>900</integer>)
    - docs/ideiaos-console/78-test-verification-strategy.md (Zero-Leak materializa cada superficie em arquivo antes de varrer; dogfood de veneno: sk-ant-FAKE DEVE reprovar, senao o gate e teatro)
    - ~/Library/LaunchAgents/com.ideiaos.gitautosync.plist (template VERBATIM: StartInterval 900, RunAtLoad, StandardOut/ErrPath, EnvironmentVariables>PATH com /opt/homebrew/bin — Node precisa)
    - 14.0-PATTERNS.md secao "infra/launchd/com.ideiaos.cockpit.plist" (usar o gitautosync plist, NAO o refresh-ai-security que e StartCalendarInterval mensal)
  </read_first>
  <action>
    Tarefa 3.4 (GATE CRITICO — DoD #1): criar source/agentd/zeroleak-snapshot.sh que recebe um path/FD de snapshot
    e varre contra os padroes de segredo: `sk-`, `gho_`, `eyJ` (JWT), `service_role` value-shaped, hex>40.
    Exit 0 se 0 matches; exit 1 se qualquer match (release bloqueado). Header de proveniencia. INCLUIR o dogfood
    de veneno como auto-teste no SUMMARY: um fixture com `sk-ant-FAKE...` DEVE reprovar (exit 1), senao o gate e teatro.

    Tarefa 3.5: criar infra/launchd/com.ideiaos.cockpit.plist a partir do template gitautosync (Label ->
    com.ideiaos.cockpit; ProgramArguments -> node + caminho do agentd.js com --once; manter StartInterval 900,
    RunAtLoad, EnvironmentVariables>PATH com /opt/homebrew/bin; logs em /tmp/ideiaos-cockpit.{out,err}.log; incluir
    comentario <!-- cp + launchctl bootstrap/bootout -->). E o 4o LaunchAgent, irmao de envsync/gitautosync/refresh-ai-security.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && MID=$(ioreg -rd1 -c IOPlatformExpertDevice | awk -F'"' '/IOPlatformUUID/{print $4}' | shasum -a 256 | cut -c1-12) && git show "cockpit:snapshots/$MID.json" > /tmp/snap.json && bash source/agentd/zeroleak-snapshot.sh /tmp/snap.json; ZL=$?; plutil -lint infra/launchd/com.ideiaos.cockpit.plist && grep -q '<integer>900</integer>' infra/launchd/com.ideiaos.cockpit.plist; PL=$?; echo "zeroleak=$ZL plist=$PL"</automated>
  </verify>
  <acceptance_criteria>
    - ZERO-LEAK (tarefa 3.4, DoD #1): `bash source/agentd/zeroleak-snapshot.sh /tmp/snap.json` exit 0 (0 matches no snapshot real).
    - DOGFOOD DE VENENO: `printf 'sk-ant-FAKE0123456789' > /tmp/poison.json && bash source/agentd/zeroleak-snapshot.sh /tmp/poison.json; [ $? -eq 1 ]` exit 0 (o gate reprova veneno — nao e teatro).
    - PLIST (tarefa 3.5): `plutil -lint infra/launchd/com.ideiaos.cockpit.plist` exit 0 E `grep -q '<integer>900</integer>' infra/launchd/com.ideiaos.cockpit.plist` exit 0.
    - `grep -q 'com.ideiaos.cockpit' infra/launchd/com.ideiaos.cockpit.plist` exit 0 (Label correto, 4o LaunchAgent).
  </acceptance_criteria>
  <done>Zero-Leak do snapshot exit 0 + dogfood de veneno reprova; plist valido com StartInterval 900 (4o LaunchAgent).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env / disco -> snapshot JSON | o coletor le nomes de var e metadata; o RHS de = (o valor) e descartado e NUNCA entra no snapshot |
| coletor -> ref cockpit | grava por plumbing (plano 02); working tree limpo (A4) |
| agentd -> producao | read-only: nenhum verbo rotate/revoke/deploy/push (gated v14.4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.0-05-I | Information Disclosure | valor de segredo no snapshot | mitigate | readEnvKeys descarta RHS de =; gate Zero-Leak (sk-/gho_/eyJ/service_role/hex>40) exit 0; dogfood de veneno reprova fixture envenenado (DoD #1) |
| T-14.0-05-T | Tampering | working tree dos repos durante coleta | mitigate | grava so via cockpit_write_snapshot (plumbing); git status --porcelain vazio (DoD #3) |
| T-14.0-05-E | Elevation of Privilege | verbos de mutacao de producao | mitigate | grep de rotate/revoke/deploy/git push/gh pr no escopo do agentd == 0 (allowlist vazio; gated v14.4 — DoD #2) |
| T-14.0-05-D | Denial of Service | daemon derrubando o ambiente | mitigate | fail-silent: erro -> stderr + exit 0 (hook-contract de daemon) |
| T-14.0-05-SC | Tampering | deps Node do agentd | mitigate | Node 18 nativo, sem deps pesadas (token-economy); collect.js sem dependencia externa de runtime |
</threat_model>

<verification>
- collect.readEnvKeys nunca retorna value; descoberta dinamica produz JSON valido.
- snapshot v1 gravado no ref; sem campo value; A4 mantida; fail-silent exit 0.
- Zero-Leak exit 0 no real + reprova veneno; plist valido com 900.
- `grep -cE 'rotate|revoke|deploy|git push|gh pr' source/agentd/*.js source/agentd/*.sh` == 0 (read-only, DoD #2).
</verification>

<success_criteria>
- ideiaos-agentd coleta read-only (sem value), grava o snapshot no ref cockpit (A4), e fail-silent.
- Zero-Leak estrutural sobre o snapshot (gate de release) + dogfood de veneno reprovando.
- 4o LaunchAgent com.ideiaos.cockpit valido (StartInterval 900).
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.0-substrate-spine/14.0-05-SUMMARY.md` when done
</output>
