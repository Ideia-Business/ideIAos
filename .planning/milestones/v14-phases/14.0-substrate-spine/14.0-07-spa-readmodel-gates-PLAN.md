---
phase: "14.0"
plan: "14.0-07"
type: execute
wave: 3
depends_on: ["14.0-04", "14.0-06"]
files_modified:
  - apps/cockpit/src/App.tsx
  - apps/cockpit/src/components/MachineCard.tsx
  - apps/cockpit/server/read.js
  - scripts/check-cockpit.sh
  - scripts/idea-doctor.sh
autonomous: false
requirements: [R14-04, R14-CTX-A1, R14-CTX-A2, R14-CTX-A4]
must_haves:
  truths:
    - "A SPA le o read-model SQLite e renderiza >=1 card de maquina com machine_id e last_doctor (Espinha conecta substrato->UI)"
    - "check-cockpit.sh verifica agentd vivo + ref cockpit existe + snapshot local fresco (<2 ciclos), exit 0 quando saudavel"
    - "idea-doctor ganha a secao 15 (Cockpit) consumivel via idea-doctor --json"
    - "Zero-Leak=0 estrutural: scan do snapshot exit 0 E api_key sem coluna value; ambos por exit-code"
    - "Working tree limpo (A4), read-only a producao (sem rotate/revoke/deploy/push no escopo), reconstrutibilidade (A5)"
  artifacts:
    - path: "apps/cockpit/src/components/MachineCard.tsx"
      provides: "Card de maquina lendo o read-model (machine_id, last_doctor)"
      min_lines: 25
    - path: "scripts/check-cockpit.sh"
      provides: "Gate de fechamento (agentd/ref/snapshot fresco) + idea-doctor §15"
      min_lines: 30
  key_links:
    - from: "apps/cockpit/server/read.js"
      to: "~/.ideiaos/console/read-model.db"
      via: "better-sqlite3 SELECT (server local loopback)"
      pattern: "read-model.db"
    - from: "scripts/check-cockpit.sh"
      to: "refs/heads/cockpit"
      via: "git rev-parse --verify --quiet + taken_epoch < 2*900s"
      pattern: "refs/heads/cockpit"
    - from: "scripts/idea-doctor.sh"
      to: "step 15 Cockpit"
      via: "nova secao consumivel pelo --json"
      pattern: "15) Cockpit"
---

<objective>
Fechar a Espinha: a SPA (plano 04) le o read-model SQLite (plano 06) e renderiza >=1 card de maquina
(`machine_id` + `last_doctor`) — prova de que **a Espinha conecta substrato->UI**. Mais os gates de
fechamento: `check-cockpit.sh` + `idea-doctor §15` (dogfooding), suite verde por exit-code, archive do
delta `/spec`, e SOAK/re-selo (operacoes @devops).

Purpose: e a integracao (Wave 3) — junta o read-model (06) com o scaffold (04). Carrega a **prova
goal-backward da fase inteira**: federacao por ref (working tree limpo), Espinha conecta substrato->UI em
loopback, Zero-Leak estrutural, zero mutacao de producao. Um revisor verifica o objetivo da fase a partir
dos acceptance_criteria deste plano.
Output: card de maquina + `apps/cockpit/server/read.js` + `scripts/check-cockpit.sh` + `idea-doctor §15`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-CONTEXT.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-PATTERNS.md
@docs/ideiaos-console/72-phase-v14_0-buildable.md
@specs/cockpit/spec.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-04-spa-scaffold-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-06-console-ingest-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (6.3): SPA le o read-model e renderiza o card de maquina (Espinha conecta substrato->UI)</name>
  <files>apps/cockpit/src/App.tsx, apps/cockpit/src/components/MachineCard.tsx, apps/cockpit/server/read.js</files>
  <read_first>
    - apps/cockpit/src/App.tsx (o shell minimo do plano 04 — agora monta o MachineCard)
    - /Users/gustavolopespaiva/dev/nfideia/src/components/clientes/NotaGatewayHealthCard.tsx:8-12 (padrao shadcn Card/CardHeader/CardTitle/CardContent + Badge + lucide — copiar a ESTRUTURA do health-card)
    - source/console/schema.sql (colunas de machine/machine_snapshot a consultar: machine_id, last doctor)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 6.3 (pagina unica lendo o read-model via better-sqlite3 em server local OU pre-render no build; frontend-visual-loop: screenshot mostra >=1 card com criterio "card renderiza machine_id e last_doctor")
    - 14.0-PATTERNS.md secao "apps/cockpit/ SPA" (unico gate runtime/UI da fase = frontend-visual-loop; loopback 127.0.0.1, sem login)
  </read_first>
  <action>
    Criar apps/cockpit/server/read.js: server local (loopback) que abre ~/.ideiaos/console/read-model.db via
    better-sqlite3 e expoe um endpoint JSON com a lista de maquinas (machine_id + last_doctor de machine_snapshot).
    Criar apps/cockpit/src/components/MachineCard.tsx: card de saude (estrutura shadcn Card/CardHeader/CardTitle/
    CardContent + Badge de status + icone lucide, padrao do nfideia) que renderiza machine_id e last_doctor.
    App.tsx monta >=1 MachineCard com os dados do read-model. Loopback 127.0.0.1, sem login. Antes do gate visual,
    garantir o read-model populado: `node source/console/ingest.js` (plano 06).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && node source/console/ingest.js && test -s ~/.ideiaos/console/read-model.db && grep -q 'read-model.db' apps/cockpit/server/read.js; echo "exit=$?"</automated>
  </verify>
  <what-built>
    apps/cockpit/server/read.js: server local loopback abrindo ~/.ideiaos/console/read-model.db via better-sqlite3,
    endpoint /machines (machine_id + last_doctor). apps/cockpit/src/components/MachineCard.tsx: health-card shadcn
    renderizando machine_id e last_doctor. App.tsx monta >=1 MachineCard. Loopback 127.0.0.1, sem login.
  </what-built>
  <how-to-verify>
    1. Garantir read-model populado: `node source/console/ingest.js` (plano 06) + `test -s ~/.ideiaos/console/read-model.db`.
    2. Subir o server local + dev server em loopback; abrir http://127.0.0.1:<port>/.
    3. frontend-visual-loop (regime runtime/UI — criterio explicito): screenshot deve mostrar >=1 card de maquina
       com o machine_id desta maquina e o last_doctor (ok/warn/fail). Criterio NAO e "parece certo" — e
       "o card renderiza machine_id E last_doctor visiveis no screenshot".
    4. Confirmar zero valor de segredo no DOM/markup servido (Zero-Leak na superficie UI — materializa-se no gate da Task 3).
  </how-to-verify>
  <acceptance_criteria>
    - `test -s ~/.ideiaos/console/read-model.db` exit 0 (read-model populado antes da verificacao).
    - O server local responde com >=1 maquina: `curl -sf http://127.0.0.1:<read-port>/machines | grep -q machine_id` exit 0.
    - `grep -q 'read-model.db' apps/cockpit/server/read.js` exit 0 (le o read-model real).
    - frontend-visual-loop: screenshot anexado mostrando >=1 card com machine_id E last_doctor (criterio explicito, regime runtime/UI — antifragile-gates "dois regimes").
  </acceptance_criteria>
  <done>A SPA le o read-model e renderiza >=1 card de maquina (machine_id + last_doctor) em loopback; screenshot confirma o criterio explicito.</done>
  <resume-signal>Digite "approved" quando o screenshot mostrar >=1 card com machine_id e last_doctor renderizados em loopback — ou descreva o que falta.</resume-signal>
</task>

<task type="auto">
  <name>Task 2 (7.1): check-cockpit.sh + idea-doctor §15 (dogfooding)</name>
  <files>scripts/check-cockpit.sh, scripts/idea-doctor.sh</files>
  <read_first>
    - scripts/check-soak.sh:38-47,68,80,114 (contrato de exit 0=ok/1=falhou, MODE-dispatch, header com bloco de USO — espelho de estrutura)
    - scripts/check-security-freshness.sh (modos --tier/--record/--gate, ledger commitado — referencia de estilo de gate)
    - scripts/idea-doctor.sh:567-593 (onde inserir a nova secao 15 antes do bloco Resumo; usar step "15) Cockpit" + pass/warn/fail)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 7.1 (3 checks: agentd ativo? ref cockpit existe? snapshot local fresco <2 ciclos? + idea-doctor §15 consumivel via --json section id==15)
    - 14.0-PATTERNS.md secao "scripts/check-cockpit.sh" (3 checks por exit-code; build script exit 1 em falha, nao hook)
  </read_first>
  <action>
    Tarefa 7.1: criar scripts/check-cockpit.sh com header + bloco de USO. Tres checks por exit-code:
    (a) agentd vivo — `launchctl list | grep -q com.ideiaos.cockpit` (ou PID-check); (b) ref cockpit existe —
    `git rev-parse --verify --quiet refs/heads/cockpit`; (c) snapshot local fresco — taken_epoch desta maquina
    dentro de 2*900s. Build script: exit 0 quando saudavel, exit 1 quando nao (NAO hook).
    Adicionar ao scripts/idea-doctor.sh uma nova secao `step "15) Cockpit"` (antes do Resumo, linha ~567+) que
    chama os mesmos 3 checks e emite pass/warn/fail — consumivel via `idea-doctor --json` (plano 01) como section id==15.
    Re-verificar a nao-regressao ANSI (gate do plano 01) apos adicionar a §15: a saida sem-flag continua diffavel.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && bash -n scripts/check-cockpit.sh && bash scripts/check-cockpit.sh; CC=$?; bash scripts/idea-doctor.sh --json | python3 -c 'import json,sys;d=json.load(sys.stdin);assert any(s["id"]==15 for s in d["sections"]);sys.exit(0)'; ID=$?; echo "check-cockpit=$CC doctor15=$ID"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/check-cockpit.sh` exit 0.
    - `bash scripts/check-cockpit.sh; echo $?` == 0 quando saudavel (agentd ativo + ref existe + snapshot fresco) — tarefa 7.1.
    - idea-doctor §15 consumivel: `bash scripts/idea-doctor.sh --json | python3 -c 'import json,sys;d=json.load(sys.stdin);assert any(s["id"]==15 for s in d["sections"]);sys.exit(0)'` exit 0 (tarefa 7.1).
    - NAO-REGRESSAO ANSI mantida apos §15: `bash scripts/idea-doctor.sh >/dev/null; echo $?` consistente (plano 01) e `grep -q '15) Cockpit' scripts/idea-doctor.sh` exit 0.
  </acceptance_criteria>
  <done>check-cockpit.sh com 3 checks (exit 0 saudavel); idea-doctor §15 presente e consumivel via --json (id==15).</done>
</task>

<task type="auto">
  <name>Task 3 (7.2 + 7.3): Suite verde por exit-code + prova goal-backward da fase (Zero-Leak/A4/A5/read-only) + archive</name>
  <files>scripts/check-cockpit.sh</files>
  <read_first>
    - source/agentd/zeroleak-snapshot.sh (gate Zero-Leak do plano 05)
    - source/console/schema.sql (guard api_key sem value, plano 06)
    - source/console/ingest.js (reconstrutibilidade A5, plano 06)
    - source/lib/cockpit.sh (A4 working tree limpo, plano 02)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secoes 7.2 e 7.3 (suite verde por exit-code, NAO Read tool; archive specs/_archive/2026-06-20-v14-cockpit-foundation/ existe)
    - .planning/milestones/v14-cockpit-PLAN.md (Definition of Done — os 7 itens que a fase deve provar)
  </read_first>
  <action>
    Tarefa 7.2: rodar a suite completa garantindo verde por EXIT-CODE (nunca Read tool). Consolidar a prova
    goal-backward da fase nos gates abaixo (cada um e um exit-code que um revisor pode reexecutar):
    Zero-Leak=0 estrutural (scan do snapshot exit 0 + api_key sem coluna value), A4 (working tree limpo apos gravar),
    read-only a producao (grep de rotate/revoke/deploy/git push/gh pr no escopo agentd/ingest == 0), A5
    (rm db && rebuild reconstroi). idea-doctor verde (exit 0) incluindo a §15.
    Tarefa 7.3: confirmar o archive do delta /spec ja mergeado: `test -d specs/_archive/2026-06-20-v14-cockpit-foundation/`.
    Os checks de SOAK/re-selo/README/vault sao a Task 4 (operacoes @devops).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && MID=$(ioreg -rd1 -c IOPlatformExpertDevice | awk -F'"' '/IOPlatformUUID/{print $4}' | shasum -a 256 | cut -c1-12); git show "cockpit:snapshots/$MID.json" > /tmp/snap.json 2>/dev/null && bash source/agentd/zeroleak-snapshot.sh /tmp/snap.json; ZL=$?; sqlite3 /tmp/v.db < source/console/schema.sql && sqlite3 /tmp/v.db 'PRAGMA table_info(api_key)' | grep -qiv '|value|'; NV=$?; rm -f /tmp/v.db; RO=$(grep -rlE 'rotate|revoke|deploy|git push|gh pr' source/agentd source/console 2>/dev/null | wc -l | tr -d ' '); test -d specs/_archive/2026-06-20-v14-cockpit-foundation; AR=$?; echo "zeroleak=$ZL noval=$NV readonly_files=$RO archive=$AR"</automated>
  </verify>
  <acceptance_criteria>
    - ZERO-LEAK=0 (DoD #1): `bash source/agentd/zeroleak-snapshot.sh /tmp/snap.json` exit 0 E `sqlite3 /tmp/v.db 'PRAGMA table_info(api_key)' | grep -qiv '|value|'` exit 0 (snapshot sem segredo + api_key sem value).
    - READ-ONLY A PRODUCAO (DoD #2): `grep -rlE 'rotate|revoke|deploy|git push|gh pr' source/agentd source/console | wc -l` == 0 (allowlist vazio nesta fase).
    - A4 (DoD #3): `git status --porcelain` vazio apos um ciclo do agentd (re-confirma plano 02/05).
    - A5: `rm -f ~/.ideiaos/console/read-model.db && node source/console/ingest.js && test -s ~/.ideiaos/console/read-model.db` exit 0.
    - ARCHIVE (tarefa 7.3): `test -d specs/_archive/2026-06-20-v14-cockpit-foundation/` exit 0.
    - idea-doctor verde: `bash scripts/idea-doctor.sh >/dev/null; echo $?` == 0 (incl. §15).
  </acceptance_criteria>
  <done>Suite verde por exit-code; prova goal-backward consolidada (Zero-Leak=0, A4, read-only, A5); archive do delta confirmado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 (7.4): SOAK + re-selo de seguranca + README + vault (operacoes @devops)</name>
  <files>.security/review-ledger.log, .planning/soak/v14.0.log</files>
  <read_first>
    - scripts/check-soak.sh (re-gravar heartbeat; span>=1d sobre epochs gravados — learning soak-span-is-record-delta-not-wallclock)
    - scripts/check-security-freshness.sh (--record PASS @security-reviewer)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 7.4
    - MEMORY: feedback-readme-update-no-final (atualizar README do GitHub no final) + feedback-session-closing-vault (Changelog + extract-learnings)
  </read_first>
  <action>
    Fechamento da fase: re-gravar heartbeat SOAK em >=2 maquinas com span>=1d SOBRE epochs gravados (nao wall-clock);
    re-selo de seguranca (@security-reviewer sobre o diff desde o ultimo selo) via
    `check-security-freshness.sh --record PASS @security-reviewer`; atualizar o README do GitHub com os recursos novos
    da fase (idea-doctor --json, ref cockpit, agentd, ingest, SPA scaffold); atualizar o vault Obsidian (Changelog +
    extract-learnings). O commit/push do ledger e operacao @devops — prefixar `AIOX_ACTIVE_AGENT=devops` (learning
    devops-push-gate-command-scoped-agent). Em produtos Lovable iria por branch+PR (IdeiaOS pode main).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && bash scripts/check-soak.sh v14.0; echo "soak_exit=$?"; tail -1 .security/review-ledger.log | grep -q PASS && echo "reselo=PASS"</automated>
  </verify>
  <what-built>
    Heartbeat SOAK re-gravado em >=2 maquinas (span>=1d sobre epochs gravados); re-selo de seguranca PASS no ledger;
    README do GitHub atualizado com os recursos da fase; vault Obsidian (Changelog + extract-learnings). Push do ledger via @devops.
  </what-built>
  <how-to-verify>
    1. SOAK: re-gravar heartbeat e `bash scripts/check-soak.sh v14.0; echo $?` == 0 quando o span maturar
       (>=1d entre epochs GRAVADOS, em >=2 maquinas) — NAO esperar wall-clock; tem que re-gravar depois de 1 dia.
    2. Re-selo: `bash scripts/check-security-freshness.sh --record PASS @security-reviewer` e commitar o ledger.
    3. README do GitHub atualizado com os recursos da fase (feedback-readme-update-no-final).
    4. Vault Obsidian: Changelog + extract-learnings (feedback-session-closing-vault).
    O commit/push do ledger e operacao @devops: `AIOX_ACTIVE_AGENT=devops` (learning devops-push-gate-command-scoped-agent).
  </how-to-verify>
  <acceptance_criteria>
    - `bash scripts/check-soak.sh v14.0; echo $?` == 0 quando o span (delta de epochs gravados) >= 1d em >=2 maquinas.
    - `.security/review-ledger.log` tem um registro PASS recente: `tail -1 .security/review-ledger.log | grep -q PASS`.
    - README do GitHub atualizado (recursos da fase) e vault Obsidian (Changelog + learnings) registrados no SUMMARY.
    - Operacoes de push/commit do ledger feitas via `AIOX_ACTIVE_AGENT=devops` (nunca sem o prefixo).
  </acceptance_criteria>
  <done>SOAK v14.0 exit 0 (span>=1d sobre epochs gravados, >=2 maquinas); re-selo PASS no ledger; README+vault atualizados; pushes via @devops.</done>
  <resume-signal>Digite "approved" quando o SOAK v14.0 sair 0 (span>=1d sobre epochs gravados, >=2 maquinas), o re-selo PASS estiver no ledger, e README+vault atualizados — ou descreva o bloqueio.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-model -> DOM da SPA | a UI le metadata; nenhum valor de segredo no markup servido (Zero-Leak na superficie UI) |
| superficie de comando -> producao | nenhum verbo de mutacao/cross-maquina no allowlist (gated v14.4) |
| maquina local -> origin (SOAK/re-selo push) | commit/push do ledger e operacao @devops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.0-07-I | Information Disclosure | DOM/markup da SPA | mitigate | UI renderiza so metadata (machine_id/last_doctor); Zero-Leak materializa a superficie UI em arquivo e varre (DoD #1) |
| T-14.0-07-E | Elevation of Privilege | superficie de comando | mitigate | allowlist vazio nesta fase: grep de rotate/revoke/deploy/git push/gh pr no escopo == 0 (DoD #2/#5) |
| T-14.0-07-T | Tampering | working tree / read-model | mitigate | A4 (git status limpo) reconfirmado; A5 (read-model reconstruivel dos refs) |
| T-14.0-07-R | Repudiation | tag prematura sem SOAK | mitigate | SOAK span>=1d sobre epochs gravados (>=2 maquinas) + re-selo PASS antes de qualquer tag (DoD #6/#7) |
| T-14.0-07-SC | Tampering | better-sqlite3 no server local | mitigate | dep mainstream pinada; reuso do lockfile do plano 06; sem novos installs [ASSUMED]/[SUS] |
</threat_model>

<verification>
- frontend-visual-loop: >=1 card com machine_id + last_doctor (regime runtime/UI, criterio explicito).
- check-cockpit.sh exit 0 + idea-doctor §15 via --json (id==15).
- Zero-Leak=0 (scan exit 0 + api_key sem value), A4, read-only==0, A5, archive — todos por exit-code.
- SOAK v14.0 exit 0 (span>=1d sobre epochs gravados) + re-selo PASS (operacoes @devops).
</verification>

<success_criteria>
- A Espinha conecta substrato->UI: >=1 card de maquina (machine_id + last_doctor) em loopback.
- Gates de fechamento verdes por exit-code; idea-doctor verde incl. §15.
- Prova goal-backward da fase reexecutavel: federacao por ref (working tree limpo), Zero-Leak estrutural, zero mutacao de producao.
- SOAK maturado + re-selo de seguranca (gating de tag respeitado).
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.0-substrate-spine/14.0-07-SUMMARY.md` when done
</output>
