---
phase: "14.0"
plan: "14.0-06"
type: execute
wave: 2
depends_on: ["14.0-02", "14.0-05"]
files_modified:
  - source/console/schema.sql
  - source/console/ingest.js
  - source/console/machine-aliases.json
autonomous: true
requirements: [R14-03, R14-CTX-A2]
must_haves:
  truths:
    - "schema.sql cria as 8 tabelas v14.0; api_key NAO tem coluna value (guard estrutural)"
    - "ingest.js le os snapshots do ref cockpit + ledgers do disco e faz UPSERT idempotente por PK"
    - "rm read-model.db && node ingest.js reconstroi o DB integralmente dos refs (cache descartavel — A5)"
    - "Dedup de Machine por machine-aliases.json; gsd por string-equality; ator por classe determinista"
  artifacts:
    - path: "source/console/schema.sql"
      provides: "DDL das 8 tabelas v14.0; api_key sem coluna value"
      min_lines: 40
    - path: "source/console/ingest.js"
      provides: "ETL ref+ledger -> SQLite, UPSERT idempotente, dedup"
      min_lines: 60
  key_links:
    - from: "source/console/ingest.js"
      to: "refs/heads/cockpit"
      via: "cockpit_list_machines + git show cockpit:snapshots/<id>.json (object store)"
      pattern: "cockpit:snapshots"
    - from: "source/console/schema.sql"
      to: "api_key"
      via: "PK (project_slug,var_name) + risk_tier CHECK, SEM value"
      pattern: "CREATE TABLE api_key"
---

<objective>
Construir o `console-ingest` -> read-model SQLite **descartavel** (`~/.ideiaos/console/read-model.db`):
`schema.sql` (8 tabelas v14.0; `api_key` SEM coluna `value` — guard estrutural) + `ingest.js` (le N
snapshots do ref `cockpit` via object store + ledgers commitados do disco, UPSERT idempotente por PK).
Invariante A5: `rm db && node ingest.js` reconstroi tudo dos refs.

Purpose: a camada de leitura que a SPA (plano 07) consome. Consome o ref do plano 02 e os snapshots
gravados pelo agentd (plano 05) — por isso Wave 2. credential-isolation materializada no schema (DoD #1):
qualquer ALTER que adicione `value` reprova o guard. Dedup/classificacao determinista (alias-map, string-eq,
ator) — o ponto sem exit-code natural, normalizado antes do UPSERT.
Output: `source/console/{schema.sql,ingest.js,machine-aliases.json}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-CONTEXT.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-PATTERNS.md
@docs/ideiaos-console/72-phase-v14_0-buildable.md
@specs/cockpit/spec.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-02-cockpit-ref-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-05-agentd-collector-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (4.1 + 4.2): schema.sql das 8 tabelas + guard estrutural anti-value em api_key</name>
  <files>source/console/schema.sql</files>
  <read_first>
    - docs/ideiaos-console/40-data-model-telemetry-mesh.md:332-496 (DDL canonico; api_key em 374-386 — reusar verbatim o subconjunto v14.0)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 4 (8 tabelas v14.0: machine, project, api_key, mcp_connection, productivity_event, soak_heartbeat, daemon_status, machine_snapshot; api_key com PK (project_slug,var_name), risk_tier CHECK IN ('critical','sensitive','low','none'), SEM coluna value)
    - 14.0-PATTERNS.md secao "source/console/schema.sql" (excerpt do api_key sem value; guard PRAGMA table_info | grep -qiv '|value|')
  </read_first>
  <action>
    Tarefa 4.1: criar source/console/schema.sql com o DDL das 8 tabelas v14.0 (subconjunto do doc 40 secao 3,
    reusado verbatim onde aplicavel): machine, project, api_key, mcp_connection, productivity_event, soak_heartbeat,
    daemon_status, machine_snapshot. Header de proveniencia em comentario SQL. As tabelas commit_log/session/
    milestone/security_seal/version_pin ficam para v14.2+ (NAO criar nesta fase). Nota: o gate 4.5 referencia
    commit_log; como ela e v14.2+, esta fase classifica o ator no proprio ingest (atributo do snapshot) — ver Task 3.

    Tarefa 4.2: api_key com PK (project_slug, var_name), present/expected INTEGER, risk_tier TEXT CHECK IN
    ('critical','sensitive','low','none') NOT NULL, file_mtime_epoch INTEGER, committed INTEGER DEFAULT 0.
    PROPOSITALMENTE SEM coluna `value` — credential-isolation materializada (DoD #1). Qualquer ALTER que a
    adicione reprova o guard PRAGMA table_info abaixo.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && sqlite3 /tmp/t.db < source/console/schema.sql && sqlite3 /tmp/t.db '.tables' | grep -q api_key && sqlite3 /tmp/t.db 'PRAGMA table_info(api_key)' | grep -qiv '|value|'; echo "schema_exit=$?"; rm -f /tmp/t.db</automated>
  </verify>
  <acceptance_criteria>
    - `sqlite3 /tmp/t.db < source/console/schema.sql && sqlite3 /tmp/t.db '.tables' | grep -q api_key` exit 0 (tarefa 4.1 — schema aplica e api_key existe).
    - GUARD ESTRUTURAL (tarefa 4.2, DoD #1): `sqlite3 /tmp/t.db 'PRAGMA table_info(api_key)' | grep -qiv '|value|'` exit 0 (NAO ha coluna value; falha se alguem a adicionar).
    - As 8 tabelas existem: `for t in machine project api_key mcp_connection productivity_event soak_heartbeat daemon_status machine_snapshot; do sqlite3 /tmp/t.db '.tables' | grep -qw $t || exit 1; done` exit 0.
    - `sqlite3 /tmp/t.db "PRAGMA table_info(api_key)" | grep -q "risk_tier"` exit 0 (CHECK de risk_tier presente).
  </acceptance_criteria>
  <done>schema.sql cria as 8 tabelas v14.0; api_key sem coluna value (guard PRAGMA exit 0); risk_tier com CHECK.</done>
</task>

<task type="auto">
  <name>Task 2 (4.3 + 4.4): ingest.js — UPSERT idempotente + reconstrutibilidade A5</name>
  <files>source/console/ingest.js</files>
  <read_first>
    - source/lib/cockpit.sh (cockpit_list_machines + git show cockpit:snapshots/<id>.json — leitura do object store, plano 02)
    - source/console/schema.sql (as 8 tabelas alvo do UPSERT)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 4 (regra rm db && rebuild reconstroi dos refs; le ledgers commitados .planning/soak/*.log e .security/review-ledger.log direto do disco; UPSERT idempotente por PK; better-sqlite3 aceitavel)
    - scripts/check-soak.sh:50-65 (parse awk -F'|' dos ledgers — formato 6 campos pipe-delimited, referencia para o parse em Node)
    - 14.0-PATTERNS.md secao "source/console/ingest.js" (formato dos ledgers confirmado; UPSERT INSERT ... ON CONFLICT DO UPDATE)
  </read_first>
  <action>
    Tarefa 4.3: criar source/console/ingest.js (Node 18; better-sqlite3 aceitavel). Fluxo: aplica schema.sql se o
    DB nao existe; le `cockpit_list_machines` -> para cada machine_id, `git show cockpit:snapshots/<id>.json`
    (object store, NUNCA arquivo no disco); le ledgers commitados (.planning/soak/*.log, .security/review-ledger.log)
    direto do disco parseando os 6 campos pipe-delimited; faz UPSERT idempotente por PK em cada tabela
    (INSERT ... ON CONFLICT DO UPDATE). Nenhum campo value escrito (api_key nao tem a coluna).

    Tarefa 4.4: reconstrutibilidade (A5) — o read-model e cache 100% descartavel. `rm read-model.db && node ingest.js`
    reconstroi integralmente dos refs. Gate abaixo prova.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && node source/console/ingest.js && N1=$(sqlite3 ~/.ideiaos/console/read-model.db 'SELECT COUNT(*) FROM machine') && node source/console/ingest.js && N2=$(sqlite3 ~/.ideiaos/console/read-model.db 'SELECT COUNT(*) FROM machine') && [ "$N1" = "$N2" ] && rm -f ~/.ideiaos/console/read-model.db && node source/console/ingest.js && test -s ~/.ideiaos/console/read-model.db; echo "ingest_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - IDEMPOTENCIA (tarefa 4.3): rodar `node ingest.js` 2x -> `SELECT COUNT(*) FROM machine` igual nas duas (N1==N2) exit 0.
    - RECONSTRUTIBILIDADE A5 (tarefa 4.4): `rm -f ~/.ideiaos/console/read-model.db && node source/console/ingest.js && test -s ~/.ideiaos/console/read-model.db` exit 0 (DB reconstruido dos refs).
    - O ingest le do object store, nao de arquivo: `grep -q 'cockpit:snapshots' source/console/ingest.js` exit 0.
    - Nenhuma escrita de value: `grep -c "'value'" source/console/ingest.js` == 0 e `grep -c '"value"' source/console/ingest.js` == 0.
  </acceptance_criteria>
  <done>ingest.js le refs+ledgers, UPSERT idempotente (N1==N2), DB reconstruivel dos refs (A5), sem escrever value.</done>
</task>

<task type="auto">
  <name>Task 3 (4.5): Dedup de Machine + classificacao determinista de ator/gsd</name>
  <files>source/console/ingest.js, source/console/machine-aliases.json</files>
  <read_first>
    - source/console/ingest.js (o ETL da Task 2 — adicionar a normalizacao ANTES do UPSERT)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 4.5 (dedup de Machine por machine-aliases.json curado 192<->Mac-mini; gsd por string-equality NUNCA semver — learning version-reset-migration-semver-trap; ator por classe determinista: .local$/^wip: autosync -> autosync, [bot]@ -> bot, senao human)
    - 14.0-PATTERNS.md secao "source/console/ingest.js" (dedup + classificacao de ator)
    - MEMORY: learning version-reset-migration-semver-trap (1.1.0 redux > 1.36.0 pre-redux — comparar gsd por string, nunca semver)
  </read_first>
  <action>
    Tarefa 4.5: criar source/console/machine-aliases.json (mapa curado, ex.: {"192":"Mac-mini"} / aliases reais do
    substrato). No ingest, ANTES do UPSERT: (a) dedup de Machine via alias-map — entradas com aliases conhecidos
    colapsam num unico machine_id; (b) versao gsd comparada por STRING-EQUALITY (nunca parse semver — learning
    version-reset-migration-semver-trap); (c) classificacao de ator determinista — subject `^wip: autosync` ou
    autor `.local$` -> actor_class='autosync', autor contendo `[bot]@` -> 'bot', senao 'human'. Como commit_log e
    v14.2+, esta fase materializa actor_class como atributo do registro normalizado/snapshot (nao na tabela commit_log).
    O gate abaixo valida o numero de Machine deduplicado e que commits autosync nao sao classificados como human.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && test -s source/console/machine-aliases.json && python3 -c 'import json;json.load(open("source/console/machine-aliases.json"))' && node source/console/ingest.js && sqlite3 ~/.ideiaos/console/read-model.db 'SELECT COUNT(DISTINCT machine_id) FROM machine' >/dev/null; echo "dedup_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `test -s source/console/machine-aliases.json` exit 0 E e JSON valido: `python3 -c 'import json;json.load(open("source/console/machine-aliases.json"))'` exit 0.
    - Dedup aplicado: `SELECT COUNT(DISTINCT machine_id) FROM machine` retorna o N esperado apos colapsar aliases (registrar o N no SUMMARY com fixture 192/Mac-mini).
    - gsd por string-equality: `grep -nE 'semver|parseInt.*version|localeCompare.*version' source/console/ingest.js | grep -vi 'string'` retorna vazio (sem parse semver de gsd).
    - Classificacao de ator: com fixture de commit `wip: autosync`, o registro tem actor_class='autosync' (nunca 'human') — assert documentado no SUMMARY com o SELECT correspondente.
  </acceptance_criteria>
  <done>machine-aliases.json valido; dedup de Machine por alias; gsd string-eq (sem semver); ator autosync nunca classificado human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ref cockpit (object store) -> SQLite | ingest le snapshots do object store; o DB e cache descartavel, reconstruivel (A5) |
| snapshot -> api_key table | api_key sem coluna value: o schema impede materializar valor de segredo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.0-06-I | Information Disclosure | valor de segredo no read-model | mitigate | api_key SEM coluna value (guard PRAGMA table_info exit 0); ingest nunca escreve campo value (DoD #1) |
| T-14.0-06-T | Tampering | DB corrompido/divergente | mitigate | cache descartavel: rm db && rebuild reconstroi dos refs (A5); UPSERT idempotente por PK |
| T-14.0-06-R | Repudiation | falsa contagem de maquinas | mitigate | dedup determinista por alias-map curado + classificacao de ator (autosync != human) normalizada antes do UPSERT |
| T-14.0-06-SC | Tampering | better-sqlite3 (dep nativa) | mitigate | better-sqlite3 e mainstream e pinado; auditar lockfile no install (sem [ASSUMED]/[SUS]) |
</threat_model>

<verification>
- schema.sql 8 tabelas; api_key sem value (guard exit 0).
- ingest idempotente (N1==N2); A5 (rm+rebuild) exit 0; le do object store; nunca escreve value.
- machine-aliases.json valido; dedup; gsd string-eq; ator autosync != human.
</verification>

<success_criteria>
- read-model SQLite descartavel reconstruivel dos refs (A5).
- api_key sem coluna value (credential-isolation materializada).
- Dedup/classificacao determinista normalizada antes do UPSERT.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.0-substrate-spine/14.0-06-SUMMARY.md` when done
</output>
