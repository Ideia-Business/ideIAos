---
phase: "14.0"
plan: "14.0-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - source/lib/cockpit.sh
  - /Users/gustavolopespaiva/.local/bin/git-autosync
autonomous: false
requirements: [R14-02]
must_haves:
  truths:
    - "cockpit_write_snapshot grava snapshots/<machine_id>.json em refs/heads/cockpit sem tocar o working tree (git status --porcelain vazio)"
    - "A gravacao de uma maquina preserva os snapshots das demais (append sobre a arvore-alvo cockpit)"
    - "Re-gravar o mesmo machine_id substitui a entrada (nao duplica)"
    - "O autosync empurra o ref cockpit nos dois ramos (main e feature), nunca empurra main"
  artifacts:
    - path: "source/lib/cockpit.sh"
      provides: "cockpit_write_snapshot / cockpit_read_snapshot / cockpit_list_machines (git-plumbing puro)"
      min_lines: 40
    - path: "/Users/gustavolopespaiva/.local/bin/git-autosync"
      provides: "push_cockpit_ref espelhando push_planning_ref, chamado nas linhas 70 e 112"
      min_lines: 1
  key_links:
    - from: "source/lib/cockpit.sh"
      to: "refs/heads/cockpit"
      via: "git hash-object -w -> ls-tree(cockpit:snapshots)|grep -v|append -> mktree(subtree flat) -> mktree(top 040000 tree snapshots) -> commit-tree -> update-ref"
      pattern: "update-ref refs/heads/cockpit"
    - from: "/Users/gustavolopespaiva/.local/bin/git-autosync"
      to: "origin cockpit"
      via: "push_cockpit_ref (espelho de push_planning_ref) nas linhas 70 e 112"
      pattern: "push_cockpit_ref"
---

<objective>
Criar `source/lib/cockpit.sh` com o protocolo de federacao por ref: grava `snapshots/<machine_id>.json`
dentro de `refs/heads/cockpit` por git-plumbing puro (hash-object -> mktree -> commit-tree -> update-ref),
**sem nunca tocar o working tree** (invariante A4). Espelhar `push_planning_ref` como `push_cockpit_ref`
no `~/.local/bin/git-autosync` para que o autosync empurre o ref (nunca `main`).

Purpose: e o git-as-bus do Cockpit (ADR `v14-cockpit-local-first-git-as-bus`). O `git add -A` cego do
autosync (linha 90) so pega working tree; o ref escrito por plumbing nunca aparece la, entao o autosync
cego nunca o captura e a protecao pull-only de `main` permanece intacta. Consumido pelo agentd (plano 05)
e pelo ingest (plano 06).
Output: `source/lib/cockpit.sh` (3 funcoes) + `push_cockpit_ref` no autosync nos dois sites de chamada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-CONTEXT.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-PATTERNS.md
@docs/ideiaos-console/72-phase-v14_0-buildable.md
@specs/cockpit/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (2.1 + 2.2): cockpit_write_snapshot por git-plumbing + invariante A4 (working tree limpo)</name>
  <files>source/lib/cockpit.sh</files>
  <read_first>
    - source/lib/gates.sh:18-19 (padrao de double-source guard `__IDEIAOS_GATES_LOADED` — copiar para `__IDEIAOS_COCKPIT_LOADED`) e :24-32 (assert_nonempty/gate_output)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 2 (sequencia EXATA — `git mktree` e UM nivel so, entao a arvore e construida em DOIS niveis: hash-object -w --stdin -> ls-tree da subarvore snapshots/ atual (`git ls-tree refs/heads/cockpit:snapshots`) -> grep -v da entrada DESTA maquina (nome = `<MID>.json`, SEM prefixo `snapshots/`) -> append `100644 blob <BLOB>\t<MID>.json` -> mktree(SUBARVORE flat) -> mktree(TOPO: `040000 tree <SUB>\tsnapshots`) -> commit-tree (-p se ref existe, orfao se nao) -> update-ref refs/heads/cockpit). NUNCA mktree de uma entrada com `/` no nome (`snapshots/<MID>.json`) — `git mktree` falha com `fatal: ... contains slash` (exit 128).
    - 14.0-PATTERNS.md secao "source/lib/cockpit.sh" (excerpt completo de cockpit_write_snapshot/read/list + invariantes A4/preserva/idempotencia/orfao; learning git-plumbing-partial-branch-overlay-sync — NUNCA update-index/indice temp)
  </read_first>
  <action>
    Tarefa 2.1: criar source/lib/cockpit.sh com header de proveniencia (`# SOURCE: IdeiaOS v14 | kind: lib`),
    double-source guard `__IDEIAOS_COCKPIT_LOADED` (espelho de gates.sh:18-19), e sourcar gates.sh.
    Implementar `cockpit_write_snapshot MID JSON` construindo a arvore em DOIS niveis (porque `git mktree`
    e UM nivel so — pipar uma entrada com `/` no nome, ex `snapshots/<MID>.json`, falha com
    `fatal: ... contains slash`, exit 128, e o ref NUNCA e criado, cascateando para os planos 05/06/07):

    1. BLOB do json: `BLOB=$(printf '%s' "$JSON" | git hash-object -w --stdin)`.
    2. SUBARVORE `snapshots/` FLAT — o nome da entrada e APENAS `<MID>.json`, SEM o prefixo `snapshots/`.
       Ramo APPEND (ref ja existe): ler as entradas da subarvore snapshots/ atual via
       `EXISTING=$(git ls-tree refs/heads/cockpit:snapshots 2>/dev/null | grep -v $'\t'"$MID.json"'$' || true)`,
       descartar a entrada DESTA maquina, somar a nova e `mktree` o conjunto:
       `SUB=$(printf '%s\n100644 blob %s\t%s.json\n' "$EXISTING" "$BLOB" "$MID" | grep -v '^$' | git mktree)`.
       Ramo ORFAO (1a vez, ref ausente): a subarvore tem so a entrada nova —
       `SUB=$(printf '100644 blob %s\t%s.json\n' "$BLOB" "$MID" | git mktree)`.
    3. ARVORE-TOPO referenciando a subarvore sob o nome `snapshots`:
       `TOP=$(printf '040000 tree %s\tsnapshots\n' "$SUB" | git mktree)`.
    4. COMMIT (parent = tip atual do cockpit se existir, senao orfao) e mover o ref — working tree NUNCA tocado:
       `PARENT=$(git rev-parse -q --verify refs/heads/cockpit || true)`;
       `COMMIT=$(git commit-tree "$TOP" ${PARENT:+-p "$PARENT"} -m "cockpit: snapshot $MID")`;
       `git update-ref refs/heads/cockpit "$COMMIT"`.
    NUNCA usar git add/update-index nem indice temporario — a lista de entradas vem da arvore-alvo (cockpit),
    nao do indice. AMBOS os ramos (orfao e append) passam pela mesma construcao de subarvore acima.

    Tarefa 2.2: o teste do cenario "working tree limpo" e o gate A4 (abaixo) — gravar e assertar git status --porcelain vazio.
  </action>
  <verify>
    <automated>cd "$(mktemp -d)" && git init -q && git commit -q --allow-empty -m base && . /Users/gustavolopespaiva/dev/IdeiaOS/source/lib/cockpit.sh && cockpit_write_snapshot demo123456ab '{"schema":"ideiaos-cockpit-snapshot/v1","machine_id":"demo123456ab"}' && git rev-parse --verify cockpit >/dev/null && [ -z "$(git status --porcelain)" ]; echo "A4_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n source/lib/cockpit.sh` exit 0.
    - Apos `cockpit_write_snapshot` num repo /tmp: `git rev-parse --verify cockpit` exit 0 (ref criado).
    - INVARIANTE A4: `[ -z "$(git status --porcelain)" ]` exit 0 apos a gravacao (working tree intocado, tarefa 2.2).
    - `grep -c 'update-index' source/lib/cockpit.sh` == 0 (nunca toca o indice — learning git-plumbing-partial-branch-overlay-sync).
    - `grep -q '__IDEIAOS_COCKPIT_LOADED' source/lib/cockpit.sh` (double-source guard presente).
  </acceptance_criteria>
  <done>cockpit_write_snapshot grava o ref por plumbing puro; git status --porcelain vazio (A4); sem update-index.</done>
</task>

<task type="auto">
  <name>Task 2 (2.3 + 2.4 + 2.6): Preserva outras maquinas, idempotencia por MID, leitores</name>
  <files>source/lib/cockpit.sh</files>
  <read_first>
    - source/lib/cockpit.sh (o write da Task 1 — os leitores complementam-no)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 2 (`git show cockpit:snapshots/<id>.json` e `git ls-tree --name-only cockpit snapshots/`)
    - 14.0-PATTERNS.md secao "source/lib/cockpit.sh" (cockpit_read_snapshot/cockpit_list_machines + invariantes "preserva outras maquinas" e "idempotencia por MID == 1")
  </read_first>
  <action>
    Tarefa 2.6: adicionar `cockpit_read_snapshot MID` (= `git show "cockpit:snapshots/$1.json"`) e
    `cockpit_list_machines` (= `git ls-tree --name-only cockpit snapshots/`).
    Tarefas 2.3 e 2.4 sao cenarios de teste que provam as invariantes ja garantidas pela sequencia da Task 1:
    gravar MID_A depois MID_B mantem 2 entradas (preserva); re-gravar MID_A mantem 1 entrada para MID_A (substitui).
    Os gates abaixo materializam esses cenarios num repo /tmp.
  </action>
  <verify>
    <automated>cd "$(mktemp -d)" && git init -q && git commit -q --allow-empty -m base && . /Users/gustavolopespaiva/dev/IdeiaOS/source/lib/cockpit.sh && cockpit_write_snapshot aaaaaaaaaaaa '{"machine_id":"aaaaaaaaaaaa"}' && cockpit_write_snapshot bbbbbbbbbbbb '{"machine_id":"bbbbbbbbbbbb"}' && cockpit_write_snapshot aaaaaaaaaaaa '{"machine_id":"aaaaaaaaaaaa","v":2}' && N=$(cockpit_list_machines | wc -l | tr -d ' ') && DUP=$(git ls-tree cockpit snapshots/ | grep -c "aaaaaaaaaaaa.json") && [ "$N" -eq 2 ] && [ "$DUP" -eq 1 ]; echo "preserve_idem_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - PRESERVA (tarefa 2.3): apos gravar MID_A e MID_B, `git ls-tree --name-only cockpit snapshots/ | wc -l` == 2 exit 0.
    - IDEMPOTENCIA (tarefa 2.4): apos re-gravar MID_A, `git ls-tree cockpit snapshots/ | grep -c "aaaaaaaaaaaa.json"` == 1 exit 0.
    - LEITORES (tarefa 2.6): `cockpit_list_machines | wc -l` == 2 exit 0; `cockpit_read_snapshot bbbbbbbbbbbb | grep -q machine_id` exit 0.
  </acceptance_criteria>
  <done>Leitores presentes; preserva outras maquinas (2 entradas) e idempotencia por MID (1 entrada) provadas em /tmp.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (2.5): push_cockpit_ref no git-autosync + push inicial -u (operacao @devops)</name>
  <files>/Users/gustavolopespaiva/.local/bin/git-autosync</files>
  <read_first>
    - /Users/gustavolopespaiva/.local/bin/git-autosync:16-24 (push_planning_ref — espelho EXATO, trocar planning->cockpit)
    - /Users/gustavolopespaiva/.local/bin/git-autosync:70 (chamada no ramo main) e :112 (chamada no ramo feature)
    - /Users/gustavolopespaiva/.local/bin/git-autosync:90 (`git add -A` cego — prova de que so pega working tree; A4)
    - 14.0-PATTERNS.md secao "~/.local/bin/git-autosync" (excerpt de push_planning_ref + sites 70/112 + learning devops-push-gate-command-scoped-agent)
  </read_first>
  <action>
    Adicionar `push_cockpit_ref()` ao `~/.local/bin/git-autosync` (espelho literal de push_planning_ref, linhas 16-24,
    trocando todas as ocorrencias de `planning` por `cockpit`) e chama-la nas linhas 70 (ramo main) e 112 (ramo feature),
    ao lado de `push_planning_ref`. Apenas no repo IdeiaOS (hub). O push inicial `git push -u origin cockpit` cria o
    upstream `cockpit@{u}` (sem ele push_cockpit_ref e no-op offline-safe) e e operacao @devops — prefixar
    `AIOX_ACTIVE_AGENT=devops` (learning devops-push-gate-command-scoped-agent). So apos o agentd (plano 05) gravar
    ao menos um snapshot local.
  </action>
  <verify>
    <automated>grep -c 'push_cockpit_ref' /Users/gustavolopespaiva/.local/bin/git-autosync; bash -n /Users/gustavolopespaiva/.local/bin/git-autosync; echo "exit=$?"</automated>
  </verify>
  <what-built>
    push_cockpit_ref() adicionada ao ~/.local/bin/git-autosync (espelho de push_planning_ref, planning->cockpit),
    chamada nas linhas 70 e 112; push inicial -u origin cockpit via @devops cria cockpit@{u}.
  </what-built>
  <how-to-verify>
    1. `grep -c 'push_cockpit_ref' ~/.local/bin/git-autosync` deve retornar 3 (1 def + 2 chamadas).
    2. `bash -n ~/.local/bin/git-autosync` deve sair 0 (sintaxe).
    3. Push inicial (operacao EXCLUSIVA @devops; a constituicao AIOX bloqueia git push por substring):
       `AIOX_ACTIVE_AGENT=devops git push -u origin cockpit` (learning devops-push-gate-command-scoped-agent).
    4. `git rev-parse --verify --quiet cockpit@{u}` exit 0 apos o push inicial.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c 'push_cockpit_ref' /Users/gustavolopespaiva/.local/bin/git-autosync` == 3 exit 0 (tarefa 2.5).
    - `bash -n /Users/gustavolopespaiva/.local/bin/git-autosync` exit 0 (tarefa 2.5).
    - Push inicial executado via `AIOX_ACTIVE_AGENT=devops git push -u origin cockpit` (nunca sem o prefixo).
  </acceptance_criteria>
  <done>push_cockpit_ref presente 3x (1 def + 2 chamadas), bash -n passa, cockpit@{u} criado pelo push inicial -u via @devops.</done>
  <resume-signal>Digite "approved" quando push_cockpit_ref estiver presente 3x, bash -n passar, e o push inicial -u (via AIOX_ACTIVE_AGENT=devops) tiver criado cockpit@{u} — ou descreva o bloqueio.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> ref cockpit | snapshot escrito por plumbing nunca cruza o working tree; o autosync cego (git add -A) nunca o captura |
| maquina local -> origin (push) | push do ref cockpit e operacao @devops; nunca empurra main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.0-02-T | Tampering | working tree dos repos | mitigate | invariante A4: git status --porcelain vazio apos gravar (plumbing puro, sem update-index) |
| T-14.0-02-E | Elevation of Privilege | git push origin cockpit | mitigate | push e operacao @devops via AIOX_ACTIVE_AGENT=devops; main nunca empurrado (autosync so pull em main) |
| T-14.0-02-D | Denial of Service | ref cockpit corrompendo main | mitigate | ref dedicado orfao/append; update-ref so move refs/heads/cockpit, nunca main/master |
| T-14.0-02-SC | Tampering | nenhum install de pacote | accept | plano sem npm/pip/cargo; sem superficie de supply-chain |
</threat_model>

<verification>
- A4 (working tree limpo apos gravar) exit 0 em repo /tmp.
- Preserva (2 entradas) e idempotencia (1 entrada por MID) exit 0.
- `grep -c push_cockpit_ref` == 3 e `bash -n` exit 0 no git-autosync.
</verification>

<success_criteria>
- source/lib/cockpit.sh com write/read/list por git-plumbing puro; A4 garantida.
- push_cockpit_ref presente 3x no autosync, sintaxe valida; push inicial -u via @devops criou cockpit@{u}.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.0-substrate-spine/14.0-02-SUMMARY.md` when done
</output>
