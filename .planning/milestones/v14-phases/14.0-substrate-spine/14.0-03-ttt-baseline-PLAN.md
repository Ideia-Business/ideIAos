---
phase: "14.0"
plan: "14.0-03"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ttt-baseline.sh
  - scripts/ttt-median.sh
autonomous: true
requirements: [R14-06]
must_haves:
  truths:
    - "ttt-baseline.sh cronometra uma jornada (J1/J4/J2) via terminal e anexa ao TSV ttt-baseline.tsv"
    - "ttt-median.sh imprime a mediana por jornada (J1/J4/J2) em bash puro, sem deps"
    - "O harness aceita o modo bridge (--mode=bridge --dry-run) como estrutura para a v14.1"
  artifacts:
    - path: "scripts/ttt-baseline.sh"
      provides: "Cronometragem terminal de J1/J4/J2 + append ao TSV"
      min_lines: 30
    - path: "scripts/ttt-median.sh"
      provides: "Mediana por jornada (sort + linha do meio, bash puro)"
      min_lines: 20
  key_links:
    - from: "scripts/ttt-baseline.sh"
      to: "~/.ideiaos/console/ttt-baseline.tsv"
      via: "printf append (jornada modo segundos epoch separados por TAB)"
      pattern: "ttt-baseline.tsv"
    - from: "scripts/ttt-median.sh"
      to: "~/.ideiaos/console/ttt-baseline.tsv"
      via: "leitura + sort + mediana por jornada"
      pattern: "J[124]"
---

<objective>
Entregar o harness de medicao de Time-to-Truth: `ttt-baseline.sh` cronometra (terminal) as 3 jornadas
J1/J4/J2 e anexa a um TSV; `ttt-median.sh` computa a mediana por jornada em bash puro. Este e o baseline
medido antes da Bridge (satisfaz A1 da spec) — a v14.1 vai bater este numero, nao um "2-15min assumido".

Purpose: torna o ganho de Time-to-Truth verificavel, nao assumido (DoD #6). v14.0 entrega so o baseline
terminal + a estrutura reaproveitavel para o modo bridge (a execucao pos-Bridge e criterio da v14.1).
Plano independente — nao depende de nenhum outro grupo (doc 72: Grupo 5 independe de tudo).
Output: `scripts/ttt-baseline.sh` + `scripts/ttt-median.sh`; TSV de baseline com N>=5 por jornada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-CONTEXT.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-PATTERNS.md
@docs/ideiaos-console/72-phase-v14_0-buildable.md
@specs/cockpit/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (5.1 + 5.4): ttt-baseline.sh cronometra J1/J4/J2 e aceita modo bridge</name>
  <files>scripts/ttt-baseline.sh</files>
  <read_first>
    - scripts/check-soak.sh:1-36 (header SOURCE + set -uo pipefail + ROOT + helpers ANSI ok/warn/err/info — copiar o estilo)
    - scripts/check-soak.sh:104 (padrao de append printf TAB-delimited para ledger — espelho do append ao TSV)
    - source/lib/gates.sh:24-32 (assert_nonempty/gate_output para gatear o TSV — SOURCEAR via forma GUARDED, ver <action>: IDEIAOS_DIR esta UNSET no runtime e `set -u` aborta com source cru)
    - .claude/rules/ideiaos-common-antifragile-gates.md secao "Inline Fallback" (forma guarded canonica + fallback inline de assert_nonempty)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 5 (3 jornadas J1/J4/J2; TSV jornada-modo=terminal-segundos-epoch; aceitar --mode=bridge --dry-run)
    - 14.0-PATTERNS.md secao "scripts/ttt-baseline.sh + ttt-median.sh"
  </read_first>
  <action>
    Tarefa 5.1: criar scripts/ttt-baseline.sh com header de proveniencia + set -uo pipefail. Aceitar
    `ttt-baseline.sh J1|J4|J2` como primeiro arg. Cronometrar com `date +%s.%N` antes e depois de UMA execucao
    manual da jornada via terminal (J1 = "frota saudavel?"; J4 = "chave X existe + idade, NUNCA valor"; J2 =
    "pronto p/ tag? soak >=2 maquinas, span>=1d sobre epochs gravados — learning soak-span-is-record-delta-not-wallclock").
    Anexar a `~/.ideiaos/console/ttt-baseline.tsv` no formato de 4 campos separados por TAB
    (jornada, modo, segundos, epoch) — criar o diretorio com mkdir -p. Como build script: gate o TSV com
    assert_nonempty e exit 1 em falha.

    IMPORTANTE (gates.sh sob `set -u`): `IDEIAOS_DIR` NAO esta definido no runtime do executor, entao um
    `. "$IDEIAOS_DIR/source/lib/gates.sh"` cru aborta com `unbound variable` ANTES de o TSV ser escrito.
    Use a forma GUARDED canonica (antifragile-gates.md) com fallback inline, nunca o snippet cru:
        [ -n "${IDEIAOS_DIR:-}" ] && [ -f "$IDEIAOS_DIR/source/lib/gates.sh" ] && . "$IDEIAOS_DIR/source/lib/gates.sh" 2>/dev/null || true
        type assert_nonempty >/dev/null 2>&1 || assert_nonempty(){ test -s "${1:-}"; }

    Tarefa 5.4: aceitar `--mode=bridge --dry-run` (estrutura para v14.1) — quando presente, registra modo=bridge
    sem exigir que a Bridge exista; sai 0. Nao implementar a medicao pos-Bridge (e v14.1).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && bash scripts/ttt-baseline.sh J1 && test -s ~/.ideiaos/console/ttt-baseline.tsv && bash scripts/ttt-baseline.sh J1 --mode=bridge --dry-run; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/ttt-baseline.sh` exit 0.
    - `bash scripts/ttt-baseline.sh J1 && test -s ~/.ideiaos/console/ttt-baseline.tsv` exit 0 (tarefa 5.1 — TSV criado e nao-vazio).
    - A ultima linha tem 4 campos TAB: `tail -1 ~/.ideiaos/console/ttt-baseline.tsv | awk -F'\t' 'NF==4{exit 0} {exit 1}'` exit 0.
    - `bash scripts/ttt-baseline.sh J1 --mode=bridge --dry-run; echo $?` == 0 (tarefa 5.4 — aceita o modo, nao exige Bridge).
  </acceptance_criteria>
  <done>ttt-baseline.sh cronometra J1/J4/J2, anexa 4 campos TAB ao TSV, aceita --mode=bridge --dry-run.</done>
</task>

<task type="auto">
  <name>Task 2 (5.2 + 5.3): coletar N>=5 por jornada + ttt-median.sh (mediana bash puro)</name>
  <files>scripts/ttt-median.sh</files>
  <read_first>
    - scripts/ttt-baseline.sh (formato do TSV produzido na Task 1)
    - scripts/check-soak.sh:50-65 (padrao awk -F de parse de ledger — referencia conceitual)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secoes 5.2 e 5.3 (N>=5 por jornada; mediana por sort + linha do meio em bash puro; saida iniciando por J1/J4/J2 + numero)
    - 14.0-PATTERNS.md secao "scripts/ttt-baseline.sh + ttt-median.sh"
  </read_first>
  <action>
    Tarefa 5.2: coletar N>=5 medicoes por jornada (J1/J4/J2). Como a execucao real e manual e cronometrada,
    para o gate o executor roda o baseline >=5 vezes por jornada (medicoes reais; aceitavel repetir a mesma
    jornada para popular o baseline). Registrar no SUMMARY que sao medicoes terminais reais, nao sinteticas.

    Tarefa 5.3: criar scripts/ttt-median.sh — le o TSV, agrupa por jornada, ordena os segundos de cada grupo
    (sort -n) e imprime a mediana (linha do meio para N impar; escolha determinista documentada para N par),
    em bash puro (sem jq/python). Saida: 3 linhas comecando por J1, J4, J2 seguidas da mediana (TAB ou espaco).
    Registra a baseline que a v14.1 vai bater (meta <10s pos-Bridge).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && for j in J1 J4 J2; do for i in 1 2 3 4 5; do bash scripts/ttt-baseline.sh $j >/dev/null; done; done; bash scripts/ttt-median.sh | grep -cE '^J[124][[:space:]]+[0-9.]+'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/ttt-median.sh` exit 0.
    - N>=5 por jornada (tarefa 5.2): `for j in J1 J4 J2; do [ "$(awk -F'\t' -v J=$j '$1==J' ~/.ideiaos/console/ttt-baseline.tsv | wc -l | tr -d ' ')" -ge 5 ] || exit 1; done` exit 0.
    - `bash scripts/ttt-median.sh | grep -cE '^J[124][[:space:]]+[0-9.]+'` == 3 (tarefa 5.3 — 3 linhas de mediana).
    - `grep -nE '(^|[^a-z])(jq|python3?)([^a-z]|$)' scripts/ttt-median.sh | grep -v '^[[:space:]]*#'` retorna vazio (mediana em bash puro).
  </acceptance_criteria>
  <done>N>=5 por jornada no TSV; ttt-median.sh imprime 3 medianas (J1/J4/J2) em bash puro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jornada J4 -> TSV | a medicao da idade de uma chave (J4) registra so segundos/epoch da cronometragem, nunca o valor da chave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.0-03-I | Information Disclosure | jornada J4 (idade de chave) | mitigate | J4 mede tempo de resposta; o conteudo medido e present/risk_tier/age, NUNCA valor; o TSV grava so segundos/epoch |
| T-14.0-03-R | Repudiation | baseline assumido vs medido | mitigate | baseline e cronometrado (date +%s.%N) e gravado em TSV — numero real, nao "2-15min assumido" (DoD #6) |
| T-14.0-03-SC | Tampering | nenhum install de pacote | accept | plano sem npm/pip/cargo; sem superficie de supply-chain |
</threat_model>

<verification>
- TSV criado com 4 campos TAB; modo bridge --dry-run aceito (exit 0).
- N>=5 por jornada; ttt-median.sh imprime 3 medianas em bash puro.
</verification>

<success_criteria>
- ttt-baseline.sh cronometra e anexa ao TSV; aceita o modo bridge como estrutura para v14.1.
- ttt-median.sh computa a mediana por jornada (J1/J4/J2) sem deps.
- Baseline terminal N>=5 registrado — satisfaz A1 da spec.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.0-substrate-spine/14.0-03-SUMMARY.md` when done
</output>
