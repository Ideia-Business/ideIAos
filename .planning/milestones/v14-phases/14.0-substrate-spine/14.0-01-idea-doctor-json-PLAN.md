---
phase: "14.0"
plan: "14.0-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/idea-doctor.sh
autonomous: true
requirements: [R14-01]
must_haves:
  truths:
    - "idea-doctor.sh --json emite JSON ideiaos-doctor/v1 com >=14 seções e um summary"
    - "Sem a flag --json, a saída humana ANSI é byte-idêntica ao baseline (não-regressão)"
    - "O exit-code sem a flag é preservado (0 se FAIL==0, senão 1) e idêntico ao summary.exit do JSON"
  artifacts:
    - path: "scripts/idea-doctor.sh"
      provides: "Flag --json com emissão paralela e fallback ANSI provado intocado"
      min_lines: 600
  key_links:
    - from: "scripts/idea-doctor.sh"
      to: "step()/pass()/warn()/fail()/info()"
      via: "decoração dos 5 emitters (echo ANSI + push em arrays bash 3.2)"
      pattern: "JSON_MODE"
    - from: "scripts/idea-doctor.sh"
      to: "ideiaos-doctor/v1"
      via: "sink final json_escape (sem jq/python no runtime)"
      pattern: "ideiaos-doctor/v1"
---

<objective>
Adicionar a flag `--json` ao `scripts/idea-doctor.sh` (14 seções → JSON estruturado `ideiaos-doctor/v1`),
**provando antes** que o fallback ANSI continua byte-idêntico ao de hoje. A flag é a fundação de
consumo do agentd (plano 05) e do gate `idea-doctor §15` (plano 07).

Inclui a Preparação de fase (sanity 0.1-0.4 do doc 72) como critérios de aceitação verificáveis na
primeira task, pois esta é a entrada da Wave 1.

Purpose: o coletor `ideiaos-agentd` consome `idea-doctor --json` para popular `MachineSnapshot.doctor`;
nenhum consumidor pode depender do JSON enquanto a saída humana (script vivo de 593 linhas, 14 seções)
não estiver provada intocada — este é o maior risco da fase (não-regressão ANSI).
Output: `scripts/idea-doctor.sh` com `--json`, schema `ideiaos-doctor/v1`, fallback ANSI provado por diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-CONTEXT.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-PATTERNS.md
@docs/ideiaos-console/72-phase-v14_0-buildable.md
@specs/cockpit/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (0.x + 1.1): Preparacao de fase + buffer de acumulacao nos 5 emitters</name>
  <files>scripts/idea-doctor.sh</files>
  <read_first>
    - scripts/idea-doctor.sh:26-32 (os 5 emitters pass/warn/fail/info/step + contadores PASS/WARN/FAIL — CONFIRMADO no disco)
    - scripts/idea-doctor.sh:582-593 (bloco "Resumo" + exit 1 na linha 589 / exit 0 na 593)
    - 14.0-PATTERNS.md secao "scripts/idea-doctor.sh" (excerpts dos emitters + formato de step e o parse `^([0-9]+)\)[[:space:]]*(.*)$`; secoes numeradas nas linhas 49,60,70,80,123,169,180,389,440,491,504,528,544,567)
    - specs/cockpit/spec.md (os 9 requisitos — colar o checklist no SUMMARY como sanity 0.1)
  </read_first>
  <action>
    Sanity de fase (doc 72 secao 0, registrar no SUMMARY como evidencia, nao codigo):
    0.1 confirmar escopo lendo specs/cockpit/spec.md (9 req); 0.2 `bash scripts/idea-doctor.sh >/dev/null; echo $?` == 0;
    0.3 derivar machine_id via `ioreg -rd1 -c IOPlatformExpertDevice | awk -F'"' '/IOPlatformUUID/{print $4}' | shasum -a 256 | cut -c1-12` (12 hex, gate `[ -n "$MID" ]`);
    0.4 grep por rotate/revoke/deploy/git push/gh pr no escopo planejado deste plano == 0 (allowlist vazio).

    Tarefa 1.1: DECORAR (nao reescrever) os 5 emitters. Capturar o baseline ANSI E o exit-code ANTES de qualquer edicao:
    `bash scripts/idea-doctor.sh > /tmp/ansi_baseline.txt 2>/dev/null; echo $? > /tmp/ansi_baseline_exit`. Declarar arrays bash 3.2 paralelos no topo
    (SEC_ID SEC_TITLE SEC_STATUS ITEM_SEC ITEM_LEVEL ITEM_MSG) e variaveis de contadores por secao.
    Em step(): alem do echo atual, fazer `[[ "$1" =~ ^([0-9]+)\)[[:space:]]*(.*)$ ]]` e empilhar
    id/titulo (a secao "Resumo", sem numero, NAO empilha — alimenta o summary). Em pass/warn/fail/info:
    alem do echo, empilhar {level,msg} associado a secao corrente. Os echo ANSI permanecem inline e
    identicos. Nao tocar a logica de PASS/WARN/FAIL nem o exit (linhas 589/593) nesta task.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && bash -n scripts/idea-doctor.sh && bash scripts/idea-doctor.sh >/dev/null 2>&1; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/idea-doctor.sh` exit 0 (sintaxe integra apos decorar emitters).
    - `bash scripts/idea-doctor.sh >/dev/null; echo $?` == exit-code do baseline (sanity 0.2 preservado).
    - `MID=$(ioreg -rd1 -c IOPlatformExpertDevice | awk -F'"' '/IOPlatformUUID/{print $4}' | shasum -a 256 | cut -c1-12); [ ${#MID} -eq 12 ]` exit 0 (sanity 0.3).
    - `grep -nE 'rotate|revoke|deploy|git push|gh pr' scripts/idea-doctor.sh | grep -v '^[[:space:]]*#' | wc -l` == 0 nas linhas que ESTE plano adiciona (sanity 0.4).
    - `test -s /tmp/ansi_baseline.txt && test -s /tmp/ansi_baseline_exit` exit 0 (baseline de texto E de exit-code gravados antes de qualquer edicao).
  </acceptance_criteria>
  <done>Os 5 emitters empilham em arrays paralelos alem de imprimir; baseline ANSI capturado; sanity 0.1-0.4 registrado no SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2 (1.2 + 1.3): Parsing de --json + sink final + JSON parseavel</name>
  <files>scripts/idea-doctor.sh</files>
  <read_first>
    - scripts/idea-doctor.sh:582-593 (onde inserir o sink final, antes/no lugar do exit conforme JSON_MODE)
    - 14.0-PATTERNS.md secao "scripts/idea-doctor.sh" (regra: JSON_MODE=1 suprime ANSI redirecionando echo->/dev/null; sink monta JSON por concatenacao com json_escape() local — SEM jq/python no runtime)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secao 1 (schema exato `ideiaos-doctor/v1` com sections[]{id,titulo,status,counts,itens[]} + summary{ok,warn,fail,exit})
  </read_first>
  <action>
    Tarefa 1.2: adicionar parsing de --json no inicio do script (JSON_MODE=1 quando presente). Quando
    JSON_MODE=1, suprimir o ANSI (redirecionar os echo dos emitters para /dev/null — guard dentro de
    cada emitter ou redirecionamento global do bloco de secoes). Implementar json_escape() local (escapa so
    `\` e `"` nas msg; bash 3.2, sem jq/python). Implementar o sink final: monta o JSON `ideiaos-doctor/v1`
    por concatenacao de strings dos arrays — schema, generated_epoch, repo, sections[]
    (id, titulo, status = pior item: FAIL se algum fail, senao WARN se algum warn, senao OK; counts; itens[]),
    e summary{ok,warn,fail,exit}. summary.exit = 0 se summary.fail==0 senao 1 — identico ao exit ANSI.

    Tarefa 1.3: garantir JSON parseavel. python3 e validador-de-TESTE apenas (nunca dependencia de runtime do script).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && bash scripts/idea-doctor.sh --json | python3 -c 'import json,sys; d=json.load(sys.stdin); assert d["schema"]=="ideiaos-doctor/v1"; assert len(d["sections"])>=14; assert "summary" in d; sys.exit(0)'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/idea-doctor.sh --json | head -c1` == `{` (tarefa 1.2 — primeiro byte e abre-chave).
    - `bash scripts/idea-doctor.sh --json | python3 -c 'import json,sys; d=json.load(sys.stdin); assert d["schema"]=="ideiaos-doctor/v1"; assert len(d["sections"])>=14; sys.exit(0)'` exit 0 (tarefa 1.3).
    - `grep -nE '(^|[^a-z])(jq|python3?)([^a-z]|$)' scripts/idea-doctor.sh | grep -v '^[[:space:]]*#'` retorna vazio — o script NAO invoca jq/python no runtime (validacao so nos testes).
  </acceptance_criteria>
  <done>--json emite JSON `ideiaos-doctor/v1` valido e parseavel com >=14 sections + summary, sem jq/python no runtime.</done>
</task>

<task type="auto">
  <name>Task 3 (1.4 + 1.5): Gate central de fallback ANSI + concordancia JSON-ANSI</name>
  <files>scripts/idea-doctor.sh</files>
  <read_first>
    - /tmp/ansi_baseline.txt (capturado na Task 1 ANTES das edicoes)
    - scripts/idea-doctor.sh:583-584 (linha "OK: N WARN: N FAIL: N" do resumo ANSI que o summary do JSON deve bater)
    - 14.0-PATTERNS.md secao "scripts/idea-doctor.sh" (gate 1.4: diff ANSI-stripped baseline-vs-after exit 0 — criterio C3 do BLUEPRINT)
    - docs/ideiaos-console/72-phase-v14_0-buildable.md secoes 1.4 e 1.5
  </read_first>
  <action>
    Tarefa 1.4 (GATE CENTRAL — maior risco da fase): comparar a saida humana atual contra o baseline da Task 1,
    sem ANSI-codes. Capturar `bash scripts/idea-doctor.sh > /tmp/ansi_after.txt` e diffar os dois ANSI-stripped.
    Se o diff falhar, ajustar a decoracao ate a saida sem-flag ficar identica byte-a-byte (texto stripped).
    Tambem confirmar que o exit-code do modo sem-flag e o mesmo do baseline.

    Tarefa 1.5: concordancia — o summary.ok do JSON deve bater com o numero da linha "OK: N" do ANSI.
    Sem mudar a logica de contadores; e uma verificacao de que a mesma fonte (PASS/WARN/FAIL) alimenta os dois caminhos.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && bash scripts/idea-doctor.sh > /tmp/ansi_after.txt 2>/dev/null; diff <(sed 's/\x1b\[[0-9;]*m//g' /tmp/ansi_baseline.txt) <(sed 's/\x1b\[[0-9;]*m//g' /tmp/ansi_after.txt); echo "diff_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `diff <(sed 's/\x1b\[[0-9;]*m//g' /tmp/ansi_baseline.txt) <(sed 's/\x1b\[[0-9;]*m//g' /tmp/ansi_after.txt)` exit 0 (tarefa 1.4 — texto sem ANSI identico).
    - `[ "$(bash scripts/idea-doctor.sh >/dev/null 2>&1; echo $?)" = "$(cat /tmp/ansi_baseline_exit)" ]` exit 0 — exit-code sem-flag identico ao baseline GRAVADO na Task 1 (sem fallback auto-anulavel; tarefa 1.4).
    - `test "$(bash scripts/idea-doctor.sh --json | python3 -c 'import json,sys;print(json.load(sys.stdin)["summary"]["ok"])')" = "$(bash scripts/idea-doctor.sh | grep -oE 'OK:[^ ]* [0-9]+' | grep -oE '[0-9]+')"` exit 0 (tarefa 1.5 — concordancia JSON-ANSI).
  </acceptance_criteria>
  <done>Fallback ANSI provado byte-identico (diff exit 0); exit-code preservado; summary.ok do JSON == OK do ANSI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| substrato local -> stdout JSON | idea-doctor le estado do disco e emite JSON consumido pelo agentd; nenhum valor de segredo deve entrar no JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.0-01-T | Tampering | saida ANSI humana do idea-doctor | mitigate | gate 1.4: diff ANSI-stripped baseline-vs-after exit 0 antes de qualquer consumidor depender do JSON |
| T-14.0-01-I | Information Disclosure | itens[].msg do JSON | mitigate | json_escape escapa so estrutura; as msg ja sao metadata (nomes de skill/path), nunca valores de segredo (Zero-Leak verificado no plano 05) |
| T-14.0-01-SC | Tampering | nenhum install de pacote neste plano | accept | plano sem npm/pip/cargo; sem superficie de supply-chain |
</threat_model>

<verification>
- `bash -n scripts/idea-doctor.sh` exit 0.
- `bash scripts/idea-doctor.sh --json | python3 -c '...schema/sections>=14...'` exit 0.
- Gate de nao-regressao ANSI (diff stripped) exit 0 — criterio C3 do BLUEPRINT, maior risco da fase.
</verification>

<success_criteria>
- Flag `--json` funcional emitindo `ideiaos-doctor/v1` com >=14 sections + summary parseavel.
- Fallback ANSI provado byte-identico ao baseline (diff exit 0) e exit-code preservado.
- summary.ok do JSON == numero OK da linha ANSI (concordancia).
- Sanity 0.1-0.4 da fase registrado no SUMMARY.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.0-substrate-spine/14.0-01-SUMMARY.md` when done
</output>
