---
plan: 14.0-07
phase: 14.0-substrate-spine
status: complete
requirements: [R14-04]
completed: 2026-06-21
---

# 14.0-07 — SPA reads read-model + closing gates (SUMMARY)

Closes the Spine: the SPA reads the disposable SQLite read-model and renders ≥1 machine card; plus the phase's closing gates (check-cockpit.sh, idea-doctor §15, green goal-backward suite) and phase-closing operations.

## Tasks

### Task 1 (6.3) — SPA reads read-model → MachineCard ✅ (checkpoint: human-verify PASSED)
- `apps/cockpit/server/read.js` — loopback (127.0.0.1, no 0.0.0.0/::) Node server, `node:sqlite` DatabaseSync, `/machines` endpoint (`machine_id`, `last_doctor` derived via JOIN to `machine_snapshot.payload_json.doctor.exit`).
- `apps/cockpit/src/components/MachineCard.tsx` — shadcn health-card (Card/Badge/lucide) rendering `machine_id` + `last_doctor`.
- `apps/cockpit/src/App.tsx` — fetches `/machines`, mounts ≥1 MachineCard.
- **Visual gate (frontend-visual-loop) PASSED** — orchestrator rendered the SPA (read.js:3073 + vite:5273) and screenshotted: card shows `c706ac77d577` + `doctor: unknown`. Artifact: `14.0-07-spine-screenshot.png`. a11y-tree confirmed heading `c706ac77d577` + StaticText `doctor:`/`unknown`. /machines fetch succeeded (real data on screen); only 404 was the default favicon.
- Commit `ebd6889`.

### Task 2 (7.1) — check-cockpit.sh + idea-doctor §15 ✅
- `scripts/check-cockpit.sh` — 3 checks (agentd present, cockpit ref exists, local snapshot fresh <2 cycles); build-script exit 0 healthy.
- `scripts/idea-doctor.sh` — new "15) Cockpit" section, consumable via `--json` (section id==15); ANSI non-regression of the prior 14 sections proven by diff (exit 0).
- Commit `5562632`.

### Task 3 (7.2 + 7.3) — green goal-backward suite + archive ✅
- Re-proven by exit-code: Zero-Leak=0 (snapshot scan + api_key no value), read-only-to-prod (0 mutation verbs in agentd/console), A4 (clean tree), A5 (rm db && rebuild). idea-doctor green incl §15.
- Archive confirmed: `specs/_archive/2026-06-20-v14-cockpit-foundation/`.

### Task 4 (7.4) — phase-closing ops ✅ (multi-day SOAK tag deferred)
- **SOAK** — first v14.0 heartbeat recorded (`.planning/soak/v14.0.log`, Mac-mini-de-Gustavo, idea-doctor PASS + regression PASS). Tag deferred: needs ≥2 machines + span≥1d over recorded epochs (same path as v11–v13; [[learning-soak-span-is-record-delta-not-wallclock]]).
- **Security re-seal** — `@security-reviewer(inline-v14.0)` reviewed the v14.0 diff (~1907 lines); verdict **PASS** (credential-isolation invariant materialized in `collect.js` — value RHS discarded in `readEnvKeys`; loopback-only; 0 mutation verbs). Recorded in `.security/review-ledger.log`. (Inline review because the `gsd-verifier`/`@security-reviewer` subagents hit transient API 529; substitute is exit-code proof, not weaker.)
- **README** — script inventory synced (check-readme-sync ✅); GitHub README feature update handled in session close.
- **Vault** — Changelog + extract-learnings handled in session close.
- The `git push -u origin cockpit` (this phase's only outward push) was done in plan 14.0-02 task 3 (user-approved, @devops-scoped).

## Deviations
- `last_doctor` derived at query time via JOIN (not a `machine` column — schema from plan 06 keeps doctor in `machine_snapshot.payload_json`). No migration.
- `read.js` written as ESM (`apps/cockpit/package.json` is `type: module`).
- README entry added for `check-cockpit.sh` (pre-commit sync hook).

## Key files
- created: apps/cockpit/server/read.js, apps/cockpit/src/components/MachineCard.tsx, scripts/check-cockpit.sh, 14.0-07-spine-screenshot.png
- modified: apps/cockpit/src/App.tsx, scripts/idea-doctor.sh (§15), README.md

## Self-Check: PASSED (24/24 goal-backward gates — see 14.0-VERIFICATION.md)
