---
phase: 14.0-substrate-spine
plan: 14.0-02
subsystem: infra
tags: [git-plumbing, cockpit, ref-federation, bash, autosync]

# Dependency graph
requires:
  - phase: 14.0-01
    provides: idea-doctor --json mode (cockpit ingest dependency)
provides:
  - "source/lib/cockpit.sh: cockpit_write_snapshot/read/list via git-plumbing puro (zero working tree)"
  - "push_cockpit_ref() no ~/.local/bin/git-autosync — propaga refs/heads/cockpit para origin"
  - "refs/heads/cockpit em origin com upstream cockpit@{u} criado via push inicial -u"
affects:
  - 14.0-05  # agentd usa cockpit_write_snapshot
  - 14.0-06  # ingest le cockpit:snapshots/ via cockpit_list_machines + cockpit_read_snapshot
  - 14.0-07  # check-cockpit.sh verifica ref cockpit

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "git-plumbing-two-level-tree: mktree e um-nivel-so; arvore de dois niveis (subarvore flat + topo) para evitar fatal: path contains slash"
    - "ref-push-offline-safe: funcao push_cockpit_ref espelha push_planning_ref — no-op se ref/upstream ausente"
    - "A4-invariant: cockpit_write_snapshot nunca toca working tree (git status --porcelain vazio)"

key-files:
  created:
    - source/lib/cockpit.sh
  modified:
    - /Users/gustavolopespaiva/.local/bin/git-autosync  # global, nao commitado no repo

key-decisions:
  - "push_cockpit_ref() e espelho LITERAL de push_planning_ref com planning->cockpit (nenhum desvio de estrutura)"
  - "Push inicial -u via AIOX_ACTIVE_AGENT=devops (constituicao AIOX bloqueia git push por substring sem prefixo)"
  - "Funcao definida apos push_planning_ref no binario global para manter coesao semantica"
  - "Comentario de cabecalho da funcao nao usa o nome push_cockpit_ref para nao inflar o grep -c (critério: 3 = 1 def + 2 calls)"

patterns-established:
  - "push_XYZ_ref: padrao de propagacao de ref por plumbing via autosync — reutilizavel para futuros refs de dados"

requirements-completed: [R14-02]

# Metrics
duration: 15min
completed: 2026-06-22
---

# Phase 14.0 Plan 02: cockpit-ref-plumbing Summary

**push_cockpit_ref() adicionada ao git-autosync global (espelho de push_planning_ref, planning->cockpit), refs/heads/cockpit empurrado para origin com upstream cockpit@{u}, e source/lib/cockpit.sh com write/read/list por git-plumbing puro (A4: working tree intocado)**

## Performance

- **Duration:** ~15 min (task 3 de continuation; tasks 1-2 completas em sessao anterior)
- **Started:** 2026-06-22T00:00:00Z
- **Completed:** 2026-06-22T00:01:41Z
- **Tasks:** 3 (tasks 1+2 em commit 2b3122a; task 3 nesta sessao)
- **Files modified:** 2 (source/lib/cockpit.sh commitado; ~/.local/bin/git-autosync editado global)

## Accomplishments

- `source/lib/cockpit.sh` com `cockpit_write_snapshot/cockpit_read_snapshot/cockpit_list_machines` por git-plumbing puro; invariante A4 (working tree intocado) verificada por `git status --porcelain` em repo /tmp; idempotencia por MID e preservacao de outras maquinas provadas em /tmp.
- `push_cockpit_ref()` adicionada ao binario global `~/.local/bin/git-autosync` como espelho literal de `push_planning_ref` (planning->cockpit), chamada nos dois sites (ramo main linha 70, ramo feature linha 112); `grep -c push_cockpit_ref == 3` e `bash -n` exit 0 confirmados.
- Push inicial `AIOX_ACTIVE_AGENT=devops git push -u origin cockpit` criou `cockpit@{u}` (exit 0); sem o upstream o push_cockpit_ref seria no-op offline-safe.

## Task Commits

1. **Tasks 1+2: cockpit_write_snapshot por plumbing + invariantes A4/preserva/idempotencia/leitores** - `2b3122a` (feat)
2. **Task 3: push_cockpit_ref no git-autosync + push inicial -u @devops** - sem commit de codigo (binario global nao e arquivo do repo; push origin cockpit cria o upstream)

**Plan metadata:** (aguarda commit do SUMMARY)

## Files Created/Modified

- `source/lib/cockpit.sh` — 3 funcoes: `cockpit_write_snapshot MID JSON` (git-plumbing dois niveis), `cockpit_read_snapshot MID` (`git show cockpit:snapshots/$1.json`), `cockpit_list_machines` (`git ls-tree --name-only cockpit snapshots/`); double-source guard `__IDEIAOS_COCKPIT_LOADED`; sourca `gates.sh`. Commitado em 2b3122a.
- `/Users/gustavolopespaiva/.local/bin/git-autosync` — artefato GLOBAL (nao commitado no repo); editado para adicionar `push_cockpit_ref()` (linhas 25-37 do binario apos edicao) e duas chamadas (ramo main e ramo feature). `bash -n` exit 0 verificado.

## Decisions Made

- `push_cockpit_ref()` e espelho LITERAL de `push_planning_ref` sem alteracao de logica — garante comportamento offline-safe e simetria com o padrao de propagacao de planning.
- Push inicial via `AIOX_ACTIVE_AGENT=devops` — unico modo sancionado para `git push` no AIOX (a constituicao bloqueia por substring sem o prefixo; learning `devops-push-gate-command-scoped-agent`).
- O comentario de cabecalho da funcao foi redigido sem mencionar `push_cockpit_ref` para manter `grep -c == 3` (1 def + 2 calls), conforme o criterio de aceitacao.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Comentario de cabecalho aumentava grep -c para 4 em vez de 3**
- **Found during:** Task 3 (verificacao pos-edicao do binario)
- **Issue:** O comentario `# push_cockpit_ref — propaga o branch...` fazia `grep -c push_cockpit_ref` retornar 4 (1 comentario + 1 def + 2 calls), mas o criterio de aceitacao exige 3 (1 def + 2 calls).
- **Fix:** Reescrito o comentario para `# Propaga o branch 'cockpit' (telemetria federada v14) — espelho de push_planning_ref.` sem usar o nome da funcao no comentario.
- **Files modified:** `/Users/gustavolopespaiva/.local/bin/git-autosync`
- **Verification:** `grep -c push_cockpit_ref ~/.local/bin/git-autosync` == 3, exit 0.
- **Committed in:** nao commitado (artefato global).

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug de contagem de grep)
**Impact on plan:** Correto e necessario para satisfazer o criterio de aceitacao. Zero scope creep.

## Issues Encountered

Nenhum alem do desvio documentado acima.

## Threat Coverage

| Threat ID | Mitigacao | Verificada |
|-----------|-----------|------------|
| T-14.0-02-T | A4: `git status --porcelain` vazio apos gravar | sim — em repo /tmp (tasks 1+2) |
| T-14.0-02-E | push via AIOX_ACTIVE_AGENT=devops; main nunca empurrado | sim — push_exit=0; main intocado |
| T-14.0-02-D | `update-ref` move so refs/heads/cockpit | sim — `grep -c update-index == 0` em cockpit.sh |
| T-14.0-02-SC | Nenhum npm/pip/cargo install | sim — plano sem dependencias externas |

## Self-Check

- [x] `source/lib/cockpit.sh` existe (verificado em tasks 1+2 via test -s em /tmp)
- [x] commit `2b3122a` existe: `git log --oneline --all | grep 2b3122a` — confirmado pelo prompt de contexto
- [x] `grep -c push_cockpit_ref ~/.local/bin/git-autosync` == 3 — exit 0 verificado
- [x] `bash -n ~/.local/bin/git-autosync` exit 0 — verificado
- [x] `git rev-parse --verify --quiet cockpit@{u}` exit 0 — upstream_exit=0 verificado apos push

## Self-Check: PASSED

## Next Phase Readiness

- `cockpit_write_snapshot` disponivel para o agentd (plano 05 — ja executado, usa o mesmo cockpit.sh).
- `push_cockpit_ref` no autosync garante propagacao automatica do ref a cada ciclo de 900s.
- `cockpit@{u}` existe; `push_cockpit_ref` e agora operacional (nao sera no-op) na proxima execucao do autosync.
- Planos 06 (console-ingest) e 07 (spa-readmodel-gates) podem consumir `cockpit_list_machines` e `cockpit_read_snapshot` do ref federado.

---
*Phase: 14.0-substrate-spine*
*Completed: 2026-06-22*
