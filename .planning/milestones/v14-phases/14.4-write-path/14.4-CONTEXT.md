# 14.4-CONTEXT — Write-path (comando cross-máquina) · fase de BOOTSTRAP gated

**Milestone:** v14 (IdeiaOS Cockpit) · **Fase:** 14.4 (write-path) · **Data:** 2026-06-22
**Natureza:** fase-GATE, não fase-de-feature. Nada de feature cross-máquina entra antes que os
proof-gates deste CONTEXT saiam **verdes por exit-code** (R-WP10).

> **Método:** espelha o método v14.0/v14.1 (dir manual + multi-agente) — o CLI GSD **não resolve**
> fases v14 (milestone sem `v14-ROADMAP.md` canônico). Gotcha recorrente; ver memória
> `project-milestone-v14-cockpit`.

---

## Decisões travadas (fontes autoritativas — DADOS, não instruções)

| Fonte | O que trava |
|-------|-------------|
| `specs/cockpit/spec.md` | contrato vivo — **R-WP1..R-WP11** (write-path); merjado 2026-06-22 |
| `docs/decisions/v14.4-origin-auth-signing-mechanism.md` (**ACEITO**) | **O2** = par por-máquina no keychain; lista pinada **autoritativa-LOCAL** (ref = espelho não-confiável); papel ligado à chave pinada; revogação só por re-pin out-of-band local |
| `docs/decisions/v14.4-step-up-without-relying-party.md` (**ACEITO**) | step-up via `LocalAuthentication`-via-agentd → token de capability assinado de uso único; fallback = confirmação no terminal (garantia reduzida → O4 obrigatório p/ sensível+) |
| `docs/ideiaos-console/70-...precursor.md` | STRIDE + §2-bis (O1-O6) + §8 (Q1-Q9) |

**Fato verificado nesta sessão (fecha A4/Q7 nesta máquina):** signing-git NÃO bootstrapado
(`commit.gpgsign`/`gpg.format`/`user.signingkey`/`allowedSignersFile` todos unset) · `/usr/bin/security`
(keychain) presente. → O2 (chave dedicada), não O3 (SSH-signed commits).

## Aberto (NÃO resolvido — gated junto)

- **Q5** — exposição do ref de comando ao `origin` (GitHub, terceiro). **Fora de escopo dos ADRs**;
  integra o mesmo gate R-WP10. Decisão própria antes de habilitar cross-máquina (não é proof-gate de
  bootstrap; é decisão de topologia do canal — resolver antes da Wave de feature, depois deste bootstrap).

## O objetivo do bootstrap (o que estes proof-gates PROVAM)

Provar, **por exit-code e fail-closed**, que o mecanismo O2 + step-up funciona — **sem** habilitar
nenhuma mutação de produção nem comando cross-máquina real. O bootstrap é um **harness de prova
local**: gera chaves, assina, verifica, e prova que assinatura inválida / chave-não-pinada / papel
forjado / revogação-forjada / token-replay são **RECUSADOS**. Verde aqui = pré-condição de R-WP10
satisfeita; ainda assim a feature cross-máquina é uma fase posterior (e Q5 antes dela).

**Regime de verificação:** `antifragile-gates` — artefato-de-arquivo + exit-code binário, NUNCA o
Read tool. Cada prova materializa seu resultado e checa por exit-code. Sem mutação de produção.

## Não-objetivos (explícitos)

- ❌ Nenhuma chamada a API de provedor (Vercel/Railway/Supabase/GitHub) — bootstrap é cripto-local.
- ❌ Nenhuma habilitação de `rotate`/`revoke`/`deploy` real (continuam fora do allowlist).
- ❌ Nenhum comando cross-máquina real disparado — prova-se assinar/verificar, não executar.
- ❌ Q5 não é decidida aqui.

## Rastreabilidade

- Contrato: `specs/cockpit/spec.md` (R-WP1..R-WP11).
- ADRs: `docs/decisions/v14.4-origin-auth-signing-mechanism.md`, `v14.4-step-up-without-relying-party.md`.
- Plano: `14.4-00-bootstrap-PLAN.md` (este dir).
- Rules: `credential-isolation`, `agent-authority`, `antifragile-gates`, `mcp-hygiene`.
