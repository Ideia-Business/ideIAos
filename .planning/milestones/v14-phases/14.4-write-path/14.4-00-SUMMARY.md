# 14.4-00-SUMMARY — Bootstrap proof-gates (PARCIAL: B0/B1/B2 DONE · B3/B4 PENDENTE)

**Plano:** `14.4-00-bootstrap-PLAN.md` · **Data:** 2026-06-22 · **Status:** 🟡 PARCIAL (Wave 1 + B2 autônomo executados; B3/B4 pendentes de ator humano / harness agregado)

## Executado (verde por exit-code — 19/19 asserções)

| Gate | O que provou | Exit-code |
|------|--------------|-----------|
| **B0** | par por-máquina (`ssh-keygen -Y`); **privada nunca na saída** (value-compare contra o material real, não header); prova-por-construção (sign não materializa a privada); lista pinada **autoritativa-LOCAL** (espelho-do-ref bogus NÃO surfaced); entrada `{machine_id\|fingerprint\|role}` | 9/9 ✓ |
| **B1** | assinar→verificar **cross-identity** (peerA pinado, verifica como peerB); fail-closed: **payload-tamper**=cód3 (veneno canônico, imune ao quirk de armadura), **não-pinada**=cód4, **papel-forjado**=cód5 (papel vem do PIN, R-WP2), **só-sha256**=cód6, **downgrade**=cód3 (sha256 correto não salva sig ruim) | 6/6 ✓ |
| **B2** (bônus, autônomo) | re-pin local autoritativo; **revogação FORJADA assinada-por-peer-pinado RECUSADA** (pin de peerA permanece — o CRÍTICO da revisão adversarial fechado); caminho-quente exercido (alerta); adição-de-pin via ref recusada | 4/4 ✓ |

## Artefatos criados

- `source/agentd/pinned-keys.sh` — lista pinada autoritativa-local (add/list/role-of/is-pinned/allowed-signers-for/revoke-local/process-ref-revocation/process-ref-addition).
- `source/agentd/sign-payload.sh` — assina payload via `ssh-keygen -Y sign` (privada por path/agent, nunca materializada).
- `source/agentd/verify-payload.sh` — verifica contra o PIN local; papel do PIN (R-WP2); exit-codes específicos 0/3/4/5/6.

## Pendente (ator humano / harness)

- **B3 — step-up:** exige **Touch ID** (`LocalAuthentication`-via-agentd) ou o fallback de terminal exercido por exit-code. **`autonomous:false`** — precisa do operador.
- **B4 — gate agregado anti-teatro:** `scripts/test-writepath-bootstrap.sh` com manifesto de casos + exit-code/REASON específico + canário + gate negativo (sem API de provedor) + nonce durável cross-processo. Roda após B3.

## Verificação

19/19 asserções por exit-code (fixtures efêmeras em `/tmp`, descartadas). ZERO chamada a API de provedor; ZERO comando cross-máquina real; ZERO mutação de produção. Os scripts são cripto-local puro (`ssh-keygen -Y`).

## Aberto registrado (não over-read do verde)

- Cerimônia **N=2 real** (enrollment out-of-band num 2º host físico) e decisão **Q5** (ref ao `origin`) ficam para a fase de feature. O verde mono-máquina aqui satisfaz a pré-condição R-WP10 só no regime mono-máquina.
- **R-WP10 segue FECHADO** — falta B3+B4 (e a cerimônia N=2) antes de qualquer feature cross-máquina.
