# 14.4-00-SUMMARY — Bootstrap proof-gates (B0–B4 · F0a autônomo CONCLUÍDO)

**Plano:** `14.4-00-bootstrap-PLAN.md` · **Data:** 2026-06-23 · **Status:** 🟢 **F0a DONE** (B0–B4 provados por
exit-code; **47/47** no gate agregado). Resíduo = **F0b** (ação humana gated) + cerimônia N=2 + Q5.

## O que F0a entregou (autônomo, ZERO segredo / ZERO produção / ZERO chamada a provedor)

| Gate | O que prova (exit-code) |
|------|-------------------------|
| **B0** | par por-máquina (`ssh-keygen -Y`); privada **nunca** na saída (value-compare); lista pinada autoritativa-local; entrada `{mid\|fp\|role}` |
| **B1** | assinar→verificar cross-identity; fail-closed: tamper=3 · não-pinada=4 · papel-forjado=5 · só-sha256=6 · downgrade=3 |
| **B2** | re-pin local; **revogação-forjada-via-ref RECUSADA** (pin permanece); adição-via-ref recusada |
| **B3** | comprovante Ed25519/WebCrypto verificado contra pubkey **PINADA ANTES do binding** (sig→3 · não-pinada→4 · binding A≠B→7 · expirado→8 · booleano→2); **subject = aprovador-do-OTP** (não o command-file); fail-closed sem comprovante→5; cliente fail-closed→5; replay cross-processo→10; CSPRNG; signed-not-boolean; sem login-por-senha; CORS loopback; rate-limit por-email; remember-device tiering; Touch ID atalho não-bloqueante |
| **B4** | token O2 fail-closed: **sig-de-máquina inválida/não-pinada/sem-sig RECUSA (3/4/6)** · expirado→8 · binding-divergente→7 · **nonce-replay cross-processo→10**; gate-negativo (sem egress) + canário + manifesto |

**Artefatos** (`source/agentd/`): `stepup-verify-comprovante.mjs` (verify Ed25519, autenticidade-antes-de-binding) ·
`stepup-token.sh` (produtor O2, atômico, subject-do-comprovante, jti-charset) · `stepup-verify-token.sh` (verify
O2 fail-closed) · `stepup-otp-client.sh` (seam de transporte, código por STDIN, fail-closed) · `stepup-pin-backend.sh`
(pin local da pubkey do backend) · `stepup-tier-policy.sh` · `stepup-touchid.sh`. Backend scaffold (`stepup/`):
4 edge functions (CSPRNG, comprovante assinado, OTP **só como digest salgado** no DB, loopback CORS) + `schema.sql`
(deny-all RLS) + `README.md` (F0b). Gate: `scripts/test-writepath-bootstrap.sh` (47 casos). Test signer:
`tests/writepath/lib/fake-stepup-backend.mjs` + `stub-transport.sh`.

## Verificação adversarial (round 1) — achou e FECHOU um CRÍTICO

Workflow de refutação 4-lentes (`wf_f6d3f10e-e2f`) + 2ª lente focada. **Verde 34/34 inicial NÃO bastou** — a lente
confused-deputy achou um **CRÍTICO real** que o gate não pegava (blind-spot estrutural):

- 🔴 **CRÍTICO — `stepup-verify-token.sh` aceitava QUALQUER token forjado (exit 0)**: o trap bash `if ! cmd; then rc=$?`
  faz `$?` ser **sempre 0** após o `!`-negado → `exit 0` em toda falha de assinatura. **FIX:** capturar o rc REAL antes
  de ramificar + **3 casos B4 negativos** (sig-forjada→3 / não-pinada→4 / sem-sig→6) que fecham o blind-spot. Mutação
  M4 prova que o gate agora pega a regressão.
- 🟠 **HIGH — subject não-bound**: token.subject vinha do command-file do chamador, não do aprovador. **FIX:** subject =
  `comprovante.sub` (provado pelo OTP) + caso B3 (mutação M5 prova).
- 🟠 **HIGH — OTP nunca provado leak-free + S-07/S-08 só em prosa**: **FIX:** código OTP por **STDIN** (nunca em arquivo;
  `shred` é no-op em macOS/APFS) + casos value-compare/CORS/rate-limit.
- 🟡 **MED** — token não-assinado escrito antes de assinar (→ publicação atômica); OTP em texto plano no DB (→ **digest
  salgado** `sha256(salt:code)`); replay do comprovante agora cross-processo; tiering/TouchID/schema com casos;
  gate-negativo ampliado (egress). 🔵 **LOW** — jti charset; umask 077.

**Anti-teatro:** o gate de 47 casos é **mutação-testado** (5/5 sabotagens → vermelho, verde após restore).

## Resíduo F0b (NÃO discharge pelo verde de F0a — registrado)

- **S-04 / deploy real**: provisionar o projeto Supabase DEDICADO `ideiaos-cockpit-stepup`, aplicar `schema.sql`,
  setar `STEPUP_SIGNING_KEY`/`KID`, deployar as 4 edge functions, enrollment **out-of-band** da pubkey
  (`stepup-pin-backend.sh add`), e fiar o transporte real (`STEPUP_TRANSPORT`). Ver `source/agentd/stepup/README.md`.
- **Cerimônia N=2 real** (enrollment num 2º host físico) + decisão **Q5** (ref ao origin) — pré-condição da feature
  cross-máquina. O verde **mono-máquina** aqui satisfaz R-WP10 só no regime mono-máquina.
- **R-WP10 segue FECHADO** até F0b + N=2.

## Verificação

47/47 asserções por exit-code (fixtures efêmeras em `/tmp`, descartadas; stores isolados por env — operador intocado).
ZERO chamada a provedor; ZERO comando cross-máquina; ZERO mutação de produção. `bash -n`/`node --check` limpos.
