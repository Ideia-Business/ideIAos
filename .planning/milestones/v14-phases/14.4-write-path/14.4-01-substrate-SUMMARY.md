# 14.4-01-SUMMARY — Substrato LOCAL do write-path (B5–B8 · cripto-local CONCLUÍDO)

**Data:** 2026-06-24 · **Status:** 🟢 **B5–B8 DONE** (provados por exit-code; gate agregado
`scripts/test-writepath-substrate.sh` verde + cada lib com teste standalone manifesto+canário+mutação).
**Pré-condição:** Q5 ratificado (ADR `v14.4-command-ref-origin-exposure.md` → **ACEITO 2026-06-24**, com 2
reconciliações de segurança aplicadas antes do flip). **R-WP10 segue FECHADO** — isto é substrato LOCAL,
não a feature cross-máquina (seal + N=2 + push ao origin permanecem gated no owner).

## O que B5–B8 entregou (autônomo, ZERO segredo / ZERO produção / ZERO provedor)

| Gate | Lib | O que prova (exit-code) | R-WP |
|------|-----|-------------------------|------|
| **B5** | `source/agentd/cmd-ref.sh` | ref OPACO `refs/ideiaos/cmd` por plumbing puro (`hash-object -w`+`update-ref`), **ISOLADO do working tree** — após `put`, `git status --porcelain` vazio + `add -A` não captura + `rev-parse` resolve mas `ls-files` não vê; round-trip byte-a-byte; get/list sem ref → 3 `no-cmd-ref`; repo inválido → 4. **Não cifra** (transporta blob já-opaco; o seal é gated). | R-WP6 |
| **B6** | `source/agentd/ledger.sh` | hash-chained append-only LOCAL + **âncora-de-cauda** (`HEAD-file` 0600 = `contagem\|sha256(última-linha)`) + **append atômico** (lock-por-dir `mkdir` + O_APPEND). verify=3 para: editar interior (prev_hash), editar/substituir/truncar/append-forjar a CAUDA (HEAD), bad-field. | R-WP9 |
| **B7** | `source/agentd/ack.sh` | ACK idempotente DURÁVEL cross-processo (1-arquivo-por-hash + noclobber) + high-water mark; reentrega do mesmo `input_hash` = efeito único; is-applied inédito → 3; bad-hash (traversal) → 2; store RO → 4 fail-closed. | R-WP8 |
| **B8** | `source/agentd/rate-limit.sh` | throttle determinístico por (ref+subject+bucket) com `IDEIAOS_RL_NOW` injetável; N≤MAX → 0, N+1 → 3 `rate-limited`; janela reseta; contadores independentes; store RO → 4 fail-closed; **nega-nunca-concede** (não autoriza sozinho — verify-payload continua obrigatório). | R-WP12 |
| **SEAL** | `source/agentd/{seal,unseal}.mjs` + `pinned-keys.sh` (B0-bis) | **sealed-box X25519 NATIVO do Node** (`node:crypto`: eph X25519 + `diffieHellman` + `hkdf` domínio-separado `ideiaos-cmd-seal/v1` + AES-256-GCM). `assina(P)→sela(P‖sig)` (seal-then-sign recusado por construção — `seal.mjs` rejeita sig vazia); destinatário **DENTRO do ciphertext** (sem alvo/role/fingerprint em claro). **`age` dispensado** (native-first, zero dep). **B0-bis**: 5º campo `enc_pubkey` no pin (retrocompat). `test-seal.sh` 7/7 + canário + mutação + **verificação adversarial PASS** (0 achados). | R-WP6 #1/#4 |

**Gate agregado:** `scripts/test-writepath-substrate.sh` (irmão do `test-writepath-bootstrap.sh` B0–B4) —
roda os **5** standalone (B5-B8 + SEAL) + **meta-canário** (detecta sub-gate quebrado) + **gate-negativo**
(zero egress/provedor nas 4 libs `.sh` **e** nos `seal/unseal.mjs`). Bootstrap B0–B4 **não regrediu** (47/47).

## Como foi construído + verificação adversarial (achou e FECHOU 1 CRITICAL + 1 HIGH)

1. **Re-review do Q5** (`wf_ceb14fd4-9d3`, 8 agentes, 4 lentes + refutação) → `RATIFY_WITH_EDITS`: topologia
   sólida; 2 achados reconciliados ANTES da ratificação (canonical `payload_hash` alinhado ao código real +
   `target_machine` pinado cross-máquina; primitiva de selo `age`/X25519 nomeada + B0-bis registrado).
2. **Build paralelo** das 4 libs (`wf_ecedc8e0-983`, 4 `gsd-executor`, arquivos isolados).
3. **Verificação adversarial** (`wf_35d229e3-d24`, 3 lentes + refutação) → **NEEDS_FIX**: 2 reais no `ledger.sh`,
   ambos confirmados por exit-code:
   - 🔴 **CRITICAL — verify CEGO NA CAUDA:** a última entrada não tinha "seguinte" que a checasse → editar/
     substituir/truncar/append-no-fim davam `verify=0`. **FIX:** âncora-de-cauda (HEAD-file `contagem|sha`).
   - 🟠 **HIGH — append não-atômico (read-modify-write de arquivo inteiro):** sob concorrência perdia entradas
     e quebrava a cadeia. **FIX:** lock-por-diretório (`mkdir` atômico, nativo — macOS sem flock) + O_APPEND.
4. **Re-verificação focada do fix** (`wf_51de2527-d3b`, refutação) → **PASS**: 4 ataques de cauda → verify=3;
   30 appends paralelos × 3 → 30 linhas/verify=0; zero bug NOVO do fix sobreviveu. `test-ledger.sh` 13/13 +
   mutação-interior + mutação-cauda (prova que a âncora HEAD.sha é load-bearing).

**Limite honesto declarado no header do `ledger.sh` (`debt:`):** cadeia+âncora NÃO-assinadas detectam
adulteração acidental/parcial e trazem a cauda à PARIDADE com o interior; tamper-evidência contra um
adversário com WRITE-ACCESS coordenado (reescreve cadeia+HEAD juntos, ou apaga o ledger) exige **assinar o
HEAD** com a chave de máquina (sign-payload.sh/B0) — **diferido para a Wave de wiring** ledger↔daemon (hoje
LATENTE, sem call-site de produção). Os fixes desta lib DEVEM preceder esse wiring.

## Resíduo (gated — NÃO discharge pelo verde local)

- ~~**Seal**~~ ✅ **CONSTRUÍDO 2026-06-24** (sealed-box X25519 nativo do Node — `age` dispensado) + **B0-bis**
  ✅ (`enc_pubkey` no pin). Pré-condições #1 (sem metadado em claro) e #4 (`assina→sela`) do ADR Q5 = VERDE.
- **`push_cmd_ref`** (4ª entrada de allowlist daemon-only, espelha `_push_state_ref`) ao `origin`: gated no
  **2º host real** (pré-condição #2/#3 do ADR Q5 — push de ref selado + bundle órfão/squash/high-water só
  fazem sentido com um peer que dá fetch; adicionar a função dormente agora seria dead code).
- **F0b** (backend Supabase dedicado) + **cerimônia N=2** em 2º host físico.
- **HEAD assinado** do ledger (não-repúdio forte) — Wave de wiring.
- **R-WP10 FECHADO** até seal + N=2 + prova end-to-end cross-máquina.

## Verificação

`bash scripts/test-writepath-substrate.sh` = exit 0 (4/4 + meta-canário + gate-negativo);
`bash tests/writepath/test-ledger.sh` = exit 0 (13/13 + 2 mutações);
`bash scripts/test-writepath-bootstrap.sh` = exit 0 (47/47, não-regressão);
`refs/ideiaos/cmd` AUSENTE no repo vivo (isolamento do cmd-ref). `bash -n` limpo nas 4 libs.
