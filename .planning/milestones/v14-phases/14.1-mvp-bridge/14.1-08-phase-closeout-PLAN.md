---
phase: "14.1"
plan: "14.1-08"
type: execute
wave: 4
depends_on: ["14.1-01", "14.1-02", "14.1-03", "14.1-04", "14.1-05", "14.1-06", "14.1-07"]
files_modified:
  - scripts/idea-doctor.sh
  - .security/review-ledger.log
  - .planning/soak/v14.1.log
autonomous: false
requirements: [R14-05, R14-06]
must_haves:
  truths:
    - "A suite A1-A12 inteira sai verde por EXIT-CODE (A7/A10 com o screenshot regime-R de criterio explicito): A1 baseline N>=5; A2 mediana Bridge <10s; A3 Zero-Leak=0; A4 working tree limpo; A5 read-model reconstruivel; A6 Trust-Rate 100% vs disco; A7 ⌘K 3 verbos inline; A8 mutacao-de-producao ausente; A9 idea-doctor §15; A10 WCAG; A11 closeout; A12 Flight Recorder"
    - "idea-doctor §15 e estendido para consumir o estado REAL do console (frescor do read-model, nao so o ref) SEM regredir a saida ANSI das 14 secoes antigas (diff stripped exit 0) — A9"
    - "Cobertura >=3/8 JTBD respondida na Bridge sem cair pro terminal (J1 frota, J4 chave, J2 tag)"
    - "Fechamento: SOAK v14.1 (>=2 maquinas, span>=1d sobre epochs gravados) + re-selo de seguranca PASS no ledger + README do GitHub atualizado + vault Obsidian (Changelog + extract-learnings)"
  artifacts:
    - path: "scripts/idea-doctor.sh"
      provides: "§15 estendida ao read-model real, sem regressao ANSI"
      contains: "15) Cockpit"
  key_links:
    - from: "scripts/idea-doctor.sh §15"
      to: "~/.ideiaos/console/read-model.db"
      via: "frescor do read-model (alem do ref), consumivel via --json id==15"
      pattern: "read-model"
    - from: "scripts/check-soak.sh v14.1"
      to: ".planning/soak/v14.1.log"
      via: ">=2 maquinas + span>=1d sobre epochs gravados"
      pattern: "v14.1"
---

<objective>
Fechar a fase v14.1: rodar a suite A1-A12 inteira verde por EXIT-CODE (A7/A10 com screenshot regime-R de criterio
explicito), estender o idea-doctor §15 para consumir o estado REAL do console (frescor do read-model, alem do ref)
SEM regredir a saida ANSI das 14 secoes antigas (A9), e o closeout padrao (A11): SOAK v14.1 + re-selo de seguranca +
README do GitHub + vault Obsidian.

Purpose: e Wave 4 final (depende de TODOS os planos — consolida a prova goal-backward da fase). Um revisor reexecuta a
suite A1-A12 a partir deste plano. A9 = a §15 ja shipada (14.0-07) ESTENDIDA ao read-model real, com a nao-regressao ANSI
ancorada em "━━━ Resumo ━━━" (W2 de 14.0). Cobertura: >=3/8 JTBD na Bridge (J1/J4/J2). Closeout: SOAK por delta de epochs
gravados (nao wall-clock), re-selo PASS, README + vault (memorias feedback-readme-update-no-final + feedback-session-closing-vault).
Output: idea-doctor §15 estendida + ledger SOAK/re-selo + README/vault atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-CONTEXT.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-RESEARCH.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-PATTERNS.md
@specs/cockpit/spec.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-07-spa-readmodel-gates-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: idea-doctor §15 estendida ao read-model real, sem regressao ANSI (A9)</name>
  <files>scripts/idea-doctor.sh</files>
  <read_first>
    - scripts/idea-doctor.sh (a §15 ja existe em lines ~688-732: `step "15) Cockpit (v14.0)"` com 3 checks agentd/ref/snapshot-fresco; o bloco "━━━ Resumo ━━━" esta na line ~743; ESTENDER a §15 sem mexer nas 14 secoes anteriores nem reescrever o que ja existe alem do necessario)
    - 14.1-PATTERNS.md secao "idea-doctor §15" (verificar nao-regride a saida ANSI; estende para consumir o read-model real — frescor do read-model, nao so o ref; bump label v14.0->v14.1; NUNCA reverter a mission-control — learning ambiguous-drift-warning-induces-agent-revert)
    - source/console/ingest.js + source/console/schema.sql (machine_snapshot.taken_epoch = frescor do read-model a checar)
    - 14.0-07-spa-readmodel-gates-PLAN.md Task 2 (o padrao de prova de nao-regressao ANSI: capturar /tmp/ansi_pre antes, diff stripped ate "━━━ Resumo ━━━" exit 0; so a §15 e a contagem do Resumo mudam)
  </read_first>
  <action>
    Capturar `bash scripts/idea-doctor.sh > /tmp/ansi_pre151.txt 2>/dev/null` ANTES de tocar a §15. Estender a `step "15) Cockpit"`
    para tambem checar o FRESCOR DO READ-MODEL (`~/.ideiaos/console/read-model.db`): o `taken_epoch` do snapshot mais recente desta
    maquina dentro de 2*900s (alem dos 3 checks ja existentes de agentd/ref/snapshot). Bump do label da secao para v14.1 (NUNCA
    reintroduzir mission-control). Provar a NAO-REGRESSAO ANSI: as 14 secoes antigas (tudo ate "━━━ Resumo ━━━") byte-identicas;
    so a §15 e a linha de contagem do Resumo mudam legitimamente. Manter consumivel via `--json` como section id==15.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/idea-doctor.sh` exit 0; `grep -q '15) Cockpit' scripts/idea-doctor.sh` exit 0.
    - §15 consome o read-model: `grep -Eq 'read-model|read-model\.db|taken_epoch' scripts/idea-doctor.sh` exit 0.
    - NAO-REGRESSAO ANSI (A9, ancorada no Resumo): `diff <(sed 's/\x1b\[[0-9;]*m//g' /tmp/ansi_pre151.txt | sed '/━━━ Resumo ━━━/,$d') <(bash scripts/idea-doctor.sh | sed 's/\x1b\[[0-9;]*m//g' | sed '/━━━ Resumo ━━━/,$d')` exit 0 (14 secoes byte-identicas).
    - §15 consumivel via --json: `bash scripts/idea-doctor.sh --json | python3 -c 'import json,sys;d=json.load(sys.stdin);assert any(s["id"]==15 for s in d["sections"]);sys.exit(0)'` exit 0.
    - SEM mission-control: `! grep -q 'mission-control\|missioncontrol' scripts/idea-doctor.sh` exit 0.
  </acceptance_criteria>
  <done>idea-doctor §15 estende para o frescor do read-model real (alem do ref), consumivel via --json id==15, SEM regredir as 14 secoes ANSI antigas; sem mission-control.</done>
</task>

<task type="auto">
  <name>Task 2: Suite A1-A12 verde por exit-code + cobertura >=3/8 JTBD (prova goal-backward da fase)</name>
  <files>scripts/idea-doctor.sh</files>
  <read_first>
    - 14.1-RESEARCH.md "Validation Architecture > Phase Requirements -> Test Map" (a tabela A1-A12 com o comando exit-code de CADA criterio — usar verbatim)
    - 14.1-CONTEXT.md secao "Specific Ideas" (A1-A12 + cobertura >=3/8 JTBD J1/J4/J2) e "must_haves_source"
    - todos os SUMMARYs dos planos 14.1-01..07 (os artefatos que cada A-criterio reexecuta: /verify A6, ⌘K A7, enum A8, test:zeroleak A3, test:recorder A12, ttt-median --mode=bridge A2)
    - scripts/check-cockpit.sh + source/agentd/zeroleak-snapshot.sh + scripts/autosync-pause.sh (autosync ja PAUSADO; rodar os git-tests em /tmp sandbox)
  </read_first>
  <action>
    Rodar a suite A1-A12 inteira garantindo verde por EXIT-CODE (nunca Read tool), consolidando a prova goal-backward da fase.
    Cada criterio e um exit-code reexecutavel (comandos da Validation Architecture): A1 baseline N>=5/jornada; A2
    `ttt-median.sh --mode=bridge | awk '$2>=10{f=1} END{exit f}'`; A3 `npm run test:zeroleak`; A4 working tree limpo apos collect
    (sandbox); A5 rm db && ingest && test -s db; A6 Trust-Rate via /verify == git show; A7 ⌘K 3 verbos inline (do 14.1-04); A8
    `grep -E 'rotate|deploy|revoke|git push|gh pr'` no enum == 0; A9 idea-doctor §15 (Task 1); A10 WCAG (screenshot regime-R de
    14.1-02/03/05); A11 este closeout (Task 3); A12 `npm run test:recorder`. Cobertura: provar >=3/8 JTBD respondidas na Bridge sem
    cair pro terminal (J1 frota via /fleet, J4 chave via /vault metadata-only, J2 tag via soak). Git-tests em /tmp sandbox com
    autosync PAUSADO (verify-guards-in-sandbox-not-live-repo).
  </action>
  <acceptance_criteria>
    - A3: `cd apps/cockpit && npm run test:zeroleak; echo $?` == 0.
    - A12: `cd apps/cockpit && npm run test:recorder; echo $?` == 0.
    - A2: `bash scripts/ttt-median.sh --mode=bridge | awk '$2>=10{f=1} END{exit f}'; echo $?` == 0.
    - A8 (mutacao-de-producao ausente): `grep -E 'rotate|deploy|revoke|git push|gh pr' apps/cockpit/server/read.js apps/cockpit/src/components/CommandPalette.tsx | grep -vE '403|gate|forever|OUT|//|comentario' | wc -l | tr -d ' '` == `0`.
    - A4 + A5: `git status --porcelain` vazio apos um ciclo de collect (sandbox) E `rm -f ~/.ideiaos/console/read-model.db && node source/console/ingest.js && test -s ~/.ideiaos/console/read-model.db` exit 0.
    - A6 (Trust-Rate vs disco): a resposta de `/verify` para uma celula bate com `git show cockpit:...` recomputado (verified:true).
    - A9: `bash scripts/idea-doctor.sh >/dev/null; echo $?` == 0 (verde, incl. §15).
    - COBERTURA >=3/8 JTBD: J1 via `curl -sf http://127.0.0.1:3073/fleet | grep -q machine_id`, J4 via `curl -sf http://127.0.0.1:3073/vault | grep -q var_name`, J2 via `bash scripts/check-soak.sh v14.1 --status` — os 3 respondidos pela Bridge (exit 0 cada).
  </acceptance_criteria>
  <done>Suite A1-A12 verde por exit-code (A7/A10 com screenshot regime-R); >=3/8 JTBD respondidas pela Bridge (J1/J4/J2); prova goal-backward da fase reexecutavel.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Closeout (A11) — SOAK v14.1 + re-selo de seguranca + README + vault</name>
  <files>.security/review-ledger.log, .planning/soak/v14.1.log</files>
  <read_first>
    - scripts/check-soak.sh (re-gravar heartbeat; span>=1d sobre epochs GRAVADOS, nao wall-clock — learning soak-span-is-record-delta-not-wallclock; >=2 maquinas)
    - scripts/check-security-freshness.sh (--record PASS @security-reviewer; ledger commitado .security/review-ledger.log)
    - 14.0-07-spa-readmodel-gates-PLAN.md Task 4 (o padrao exato de closeout: SOAK + re-selo + README + vault; push via AIOX_ACTIVE_AGENT=devops)
    - MEMORY: feedback-readme-update-no-final (atualizar README do GitHub com os recursos novos da fase) + feedback-session-closing-vault (Changelog + extract-learnings) + learning devops-push-gate-command-scoped-agent (push/commit do ledger via AIOX_ACTIVE_AGENT=devops) + feedback-lovable-projects-branch-commit (IdeiaOS pode main)
  </read_first>
  <action>
    Fechamento da fase v14.1: (a) re-gravar heartbeat SOAK em >=2 maquinas com span>=1d SOBRE epochs gravados (nao wall-clock) —
    `check-soak.sh v14.1`; (b) re-selo de seguranca (@security-reviewer sobre o diff desde o ultimo selo, dado que v14.1 introduz a
    superficie de COMANDO — STRIDE/OWASP-LLM) via `check-security-freshness.sh --record PASS @security-reviewer`; (c) atualizar o README
    do GitHub com os recursos novos da fase (Overview/Frota/Cofre, ⌘K allowlist, Flight Recorder v0, gate Zero-Leak, harness TtT Bridge);
    (d) atualizar o vault Obsidian (Changelog + extract-learnings). O commit/push do ledger e operacao @devops — prefixar
    `AIOX_ACTIVE_AGENT=devops` (IdeiaOS pode main; Lovable iria por branch+PR). NAO criar a tag v14.1 aqui (a tag espera o SOAK maturar
    e e operacao @devops separada).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS && bash scripts/check-soak.sh v14.1; echo "soak_exit=$?"; tail -1 .security/review-ledger.log | grep -q PASS && echo "reselo=PASS"</automated>
  </verify>
  <what-built>
    Heartbeat SOAK v14.1 re-gravado (>=2 maquinas, span>=1d sobre epochs gravados); re-selo de seguranca PASS no ledger (cobre a
    nova superficie de comando); README do GitHub atualizado com os recursos da fase; vault Obsidian (Changelog + learnings). Push via @devops.
  </what-built>
  <how-to-verify>
    1. SOAK: re-gravar heartbeat e `bash scripts/check-soak.sh v14.1; echo $?` == 0 quando o span maturar (>=1d entre epochs GRAVADOS,
       >=2 maquinas) — NAO esperar wall-clock; tem que re-gravar depois de 1 dia.
    2. Re-selo: `bash scripts/check-security-freshness.sh --record PASS @security-reviewer` e commitar o ledger (cobre o diff da fase, incl. POST /command).
    3. README do GitHub atualizado com os recursos da fase (feedback-readme-update-no-final).
    4. Vault Obsidian: Changelog + extract-learnings (feedback-session-closing-vault).
    O commit/push do ledger e @devops: `AIOX_ACTIVE_AGENT=devops` (devops-push-gate-command-scoped-agent).
  </how-to-verify>
  <acceptance_criteria>
    - `bash scripts/check-soak.sh v14.1; echo $?` == 0 quando o span (delta de epochs gravados) >= 1d em >=2 maquinas.
    - `.security/review-ledger.log` tem um registro PASS recente: `tail -1 .security/review-ledger.log | grep -q PASS`.
    - README do GitHub atualizado (recursos da fase) e vault Obsidian (Changelog + learnings) registrados no SUMMARY.
    - Operacoes de push/commit do ledger feitas via `AIOX_ACTIVE_AGENT=devops` (nunca sem o prefixo).
  </acceptance_criteria>
  <done>SOAK v14.1 exit 0 (span>=1d sobre epochs gravados, >=2 maquinas); re-selo PASS no ledger (cobre a superficie de comando); README+vault atualizados; pushes via @devops. Tag v14.1 fica para depois (SOAK maturar).</done>
  <resume-signal>Digite "approved" quando o SOAK v14.1 sair 0 (span>=1d sobre epochs gravados, >=2 maquinas), o re-selo PASS estiver no ledger, e README+vault atualizados — ou descreva o bloqueio.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| superficie de comando (nova nesta fase) -> re-selo | o re-selo @security-reviewer cobre o diff que introduz POST /command (STRIDE/OWASP-LLM) |
| maquina local -> origin (SOAK/re-selo push) | commit/push do ledger e operacao @devops (AIOX_ACTIVE_AGENT=devops) |
| tag prematura -> release | SOAK span>=1d sobre epochs gravados + re-selo PASS antes de qualquer tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-08-R | Repudiation | tag sem SOAK/re-selo | mitigate | SOAK v14.1 span>=1d sobre epochs gravados (>=2 maquinas) + re-selo PASS antes de tag (DoD #6/#7) |
| T-14.1-08-T | Tampering | regressao ANSI silenciosa na §15 | mitigate | diff stripped ate "━━━ Resumo ━━━" exit 0 (14 secoes byte-identicas); A9 (W2 de 14.0) |
| T-14.1-08-E | Elevation of Privilege | push do ledger fora de @devops | mitigate | AIOX_ACTIVE_AGENT=devops (devops-push-gate-command-scoped-agent); nunca push direto |
| T-14.1-08-I | Information Disclosure | regressao de Zero-Leak no fechamento | mitigate | A3 (test:zeroleak) reexecutado na suite; re-selo de seguranca cobre o diff da fase |
</threat_model>

<verification>
- idea-doctor §15 estendida ao read-model, sem regressao ANSI (diff stripped exit 0), consumivel via --json id==15.
- Suite A1-A12 verde por exit-code (A7/A10 screenshot regime-R); >=3/8 JTBD na Bridge.
- SOAK v14.1 exit 0 (span>=1d sobre epochs gravados) + re-selo PASS + README + vault (pushes @devops).
</verification>

<success_criteria>
- A1-A12 inteira verde por exit-code; cobertura >=3/8 JTBD na Bridge.
- §15 consome o estado real do console sem regredir ANSI.
- Closeout padrao: SOAK maturado + re-selo PASS + README + vault; tag deferida ao SOAK.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-08-SUMMARY.md` when done
</output>
