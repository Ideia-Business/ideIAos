---
phase: "14.1"
plan: "14.1-04"
type: execute
wave: 3
depends_on: ["14.1-01", "14.1-02"]
files_modified:
  - apps/cockpit/server/read.js
  - apps/cockpit/src/components/CommandPalette.tsx
  - apps/cockpit/src/components/ui/command.tsx
  - apps/cockpit/package.json
autonomous: false
requirements: [R14-05]
must_haves:
  truths:
    - "read.js ganha POST /command com um ENUM TIPADO FECHADO de verbos (B1-B6), default-deny: verbo fora do enum => 403; nenhum exec(input do usuario); spawnSync(file, argsArray) sem shell"
    - "Os 6 verbos sao LOCAIS+REVERSIVEIS+NAO-PRODUCAO+NAO-@devops: B1 pausar autosync, B2 retomar autosync, B3 re-selar seguranca, B4 forcar sync, B5 kickstart daemon (envsync/refresh-ai-security/cockpit), B6 rodar idea-doctor"
    - "Verbos destrutivos-mas-reversiveis (B1, B3) exigem armar-antes-de-disparar: o server REJEITA sem confirmed:true (defesa server-side, nao so gesto de UI)"
    - "O canal POST /command e AUTENTICADO server-side (Origin+Host fail-closed + token efemero por-boot no header X-Cockpit-Token); CORS NAO e defesa server-side (uma aba local maliciosa via CSRF/DNS-rebinding ainda entregaria o POST). Fecha o boundary browser->exec intra-maquina que 14.1-04 introduz — NAO e antecipacao de v14.4"
    - "Forever-OUT garantido por exit-code: grep de rotate|deploy|revoke|git push|gh pr no enum == 0 (A8); B5 valida o nome do daemon contra um lookup-map fechado {envsync,refresh-ai-security,cockpit} — NUNCA interpola body.daemon na arg launchctl; $UID vem de process.getuid()"
  artifacts:
    - path: "apps/cockpit/src/components/CommandPalette.tsx"
      provides: "⌘K overlay (cmdk), allowlist de verbos, resultado inline, armar-antes-de-disparar, envia X-Cockpit-Token"
      min_lines: 40
    - path: "apps/cockpit/server/read.js"
      provides: "POST /command enum tipado default-deny + Origin+Host+token + body-parsing hardened + spawnSync no-shell + confirmed:true"
      contains: "/command"
  key_links:
    - from: "apps/cockpit/src/components/CommandPalette.tsx"
      to: "POST http://127.0.0.1:3073/command"
      via: "fetch POST {verb, confirmed} + header X-Cockpit-Token (loopback)"
      pattern: "/command"
    - from: "apps/cockpit/server/read.js (handleCommand)"
      to: "CLI sancionado (autosync-pause.sh, launchctl, idea-doctor, check-security-freshness.sh)"
      via: "spawnSync(file, argsArray) — sem shell, enum fechado, apos auth Origin+Host+token"
      pattern: "spawnSync"
---

<objective>
Entregar o Command Palette ⌘K (R14-05, spec "Comando local reversivel com allowlist fixo"): o overlay `cmdk`
(portado do nfideia) + um `POST /command` no `read.js` com ENUM TIPADO FECHADO de 6 verbos (B1-B6), default-deny,
`spawnSync(file, argsArray)` sem shell, armar-antes-de-disparar server-side (`confirmed:true`) para os destrutivos-
mas-reversiveis (B1, B3), E autenticacao server-side do canal (Origin+Host fail-closed + token efemero por-boot).
Este e o plano security-sensitive da fase: a superficie de COMANDO que executa `spawnSync` na maquina do operador.

Purpose: e Wave 3 (depende de 14.1-01 read.js + 14.1-02 seam de montagem do ⌘K em App.tsx). Este plano edita read.js
(propriedade de 14.1-01, wave anterior — sem conflito) e cria CommandPalette.tsx + ui/command.tsx; nao edita App.tsx
(o seam ja monta <CommandPalette/>). cmdk@^1.1.1 e a UNICA dep nova sancionada (ja usada no nfideia).
Output: read.js (POST /command enum + auth + body-parsing hardened) + CommandPalette.tsx + ui/command.tsx + cmdk em package.json.

**Autenticacao do canal browser->exec (FIX S-01):** CORS NAO e defesa server-side — uma aba local maliciosa (CSRF, DNS-rebinding)
ainda entrega o POST, e `confirmed:true` NAO e segredo. O canal e fechado por (a) validacao server-side de Origin AND Host
fail-closed (403), (b) token efemero por-boot injetado na HTML same-origin e exigido em `X-Cockpit-Token`. Isso fecha o
boundary browser->exec INTRA-MAQUINA que 14.1-04 introduz; NAO e antecipacao de v14.4 (que e cross-maquina + producao).

**Forever-OUT (A8, doc 77 §B.2):** `git push`/`gh pr` (@devops — no maximo GERA o comando), MCP add/remove (@devops),
`rotate`/`revoke`/`deploy` (v14.4-gated), cross-machine (v14.4), revelar/copiar valor de segredo (NUNCA — credential-isolation),
exec arbitrario no agentd (NUNCA — Excessive Agency), revoke em massa (NUNCA — DoS estrutural). B5 usa `cockpit`,
NUNCA `missioncontrol` (doc 77 B5 mostra o nome stale — corrigir).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-CONTEXT.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-RESEARCH.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-PATTERNS.md
@specs/cockpit/spec.md
@docs/ideiaos-console/77-atalaia-alerts-command-allowlist.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-01-data-access-foundation-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read.js POST /command — auth (Origin+Host+token) + body-parsing hardened + enum tipado fechado default-deny + spawnSync no-shell + confirmed:true</name>
  <files>apps/cockpit/server/read.js</files>
  <read_first>
    - apps/cockpit/server/read.js (apos 14.1-01 — adicionar handleCommand ao roteador http.createServer ao lado dos GET; reusar o header CORS http://127.0.0.1:5273 + loopback HOST + finally; precisa ler o body do POST. O server gera o token efemero no boot e injeta na HTML que serve em :5273 same-origin)
    - 14.1-RESEARCH.md "Architecture Patterns > Pattern 2" (o shape recomendado do enum: VERBS = {pause_autosync:{cmd:[...],arm:true}, resume_autosync:..., reseal_security:{...,arm:true}, force_sync:..., kickstart_daemon:{cmd:null,arm:false}, run_doctor:...}; const v=VERBS[body.verb]; if(!v) 403; spawnSync(v.cmd[0], v.cmd.slice(1)) — NUNCA execSync(string com input)) + "Security Domain > V5" + "Open Questions > Q4"
    - docs/ideiaos-console/77-atalaia-alerts-command-allowlist.md (B1-B6 exatos + forever-OUT B.2; ATENCAO: B5 mostra `missioncontrol` STALE — usar `cockpit`)
    - scripts/autosync-pause.sh (B1 `on`, B2 `off`); scripts/check-security-freshness.sh (B3 --record PASS @security-reviewer); scripts/idea-doctor.sh (B6 read-only)
    - 14.1-CONTEXT.md secao "Command Palette ⌘K allowlist" (4 criterios binarios; armar-antes-de-disparar B1/B3; forever-OUT) e spec cenarios "acao fora do allowlist" / "acao reversivel confirmada" / "operacao exclusiva solicitada"
    - .claude/rules/agent-authority.md (git push/gh pr/MCP = @devops EXCLUSIVO — Cockpit no maximo GERA o comando)
    - .claude/rules/ideiaos-common-credential-isolation.md (o token efemero por-boot e loopback-only, nao duravel — consistente com credential-isolation; nunca commitado, nunca no contexto do LLM)
  </read_first>
  <action>
    Adicionar `handleCommand` em `POST /command` ao read.js, NESTA ORDEM (fail-closed):

    (1) AUTENTICACAO DO CANAL (FIX S-01) — ANTES de qualquer parsing/exec. Validar server-side Origin AND Host, fail-closed => 403:
        rejeitar a menos que `req.headers.origin === 'http://127.0.0.1:5273'` AND `req.headers.host` casar com `127.0.0.1:3073`
        ou `localhost:3073` (o check de Host e o que neutraliza DNS-rebinding — uma aba que resolve um hostname atacante para
        127.0.0.1 vem com Host != esperado). Gerar no BOOT do server um token efemero por-boot (ex.: `crypto.randomBytes`),
        injeta-lo na HTML que o server serve em :5273 (same-origin), e exigi-lo em `X-Cockpit-Token` em TODO POST /command;
        ausente/invalido => 403. Uma aba cross-origin NAO consegue ler o token (SOP). Sem segredo DURAVEL, loopback-only
        (credential-isolation).

    (2) BODY-PARSING HARDENED (FIX S-02): exigir `Content-Type: application/json` (senao 415/400); cap de tamanho do body <= 4KB
        — abortar acima com 413; `try/catch` ao redor de `JSON.parse` => 400 em malformado.

    (3) ENUM FECHADO `VERBS` (objeto const) com exatamente 6 chaves: `pause_autosync` (B1, cmd
        `['scripts/autosync-pause.sh','on','via Cockpit']`, arm:true), `resume_autosync` (B2, `['scripts/autosync-pause.sh','off']`,
        arm:false), `reseal_security` (B3, `['scripts/check-security-freshness.sh','--record','PASS','@security-reviewer']`, arm:true),
        `force_sync` (B4, `['launchctl','kickstart','-k','gui/'+process.getuid()+'/com.ideiaos.gitautosync']`, arm:false),
        `kickstart_daemon` (B5, ver (5) abaixo, arm:false), `run_doctor` (B6, `['bash','scripts/idea-doctor.sh']`, arm:false).
        DEFAULT-DENY: `const v=VERBS[body.verb]; if(!v) => 403` com mensagem referenciando o gate v14.4.

    (4) ARMAR-ANTES-DE-DISPARAR server-side: se `v.arm` e `body.confirmed !== true` => 412/400 (rejeita; defesa server-side,
        nao so gesto de UI — Open Q4).

    (5) B5 DAEMON COMO CHAVE DE LOOKUP-MAP (FIX S-04): `body.daemon` e usado SOMENTE como chave num map fixo
        `{envsync:'com.ideiaos.envsync','refresh-ai-security':'com.ideiaos.refresh-ai-security',cockpit:'com.ideiaos.cockpit'}`,
        NUNCA string-interpolado na arg `gui/$UID/com.ideiaos.<daemon>` do launchctl; `$UID` vem de `process.getuid()`, NUNCA do body.
        Chave fora do map => 403.

    (6) EXECUCAO: SO com `spawnSync(v.cmd[0], v.cmd.slice(1), {cwd: ROOT})` — SEM shell, SEM interpolacao de input.

    (7) STDOUT VIA ZERO-LEAK (FIX S-03): o stdout do spawnSync retornado inline a UI DEVE ser varrido pelo detector Zero-Leak
        (source/agentd/zeroleak-snapshot.sh) ANTES de `res.end` — OU materializado como uma superficie S7 que o gate 14.1-06 varre.
        Nunca retornar stdout cru sem passar pelo detector. Retornar {verb, exitCode, stdout-trunc(ja-scaneado)} inline.

    NENHUM verbo de mutacao de producao/cross-maquina/@devops no enum.
  </action>
  <acceptance_criteria>
    - `node -c apps/cockpit/server/read.js` exit 0; rota registrada: `grep -q "'/command'" apps/cockpit/server/read.js` exit 0.
    - AUTH ORIGIN+HOST fail-closed (FIX S-01, exit-code): com o server no ar, `curl -s -H 'Origin: http://evil.example' -X POST http://127.0.0.1:3099/command -d '{"verb":"run_doctor"}' -w '%{http_code}' -o /dev/null` == `403` (Origin invalido rejeitado); E um POST com Origin valido mas SEM `X-Cockpit-Token` valido => `403` (`curl -s -H 'Origin: http://127.0.0.1:5273' -X POST http://127.0.0.1:3099/command -H 'Content-Type: application/json' -d '{"verb":"run_doctor"}' -w '%{http_code}' -o /dev/null` == `403`). Fonte: `grep -Eq "X-Cockpit-Token|x-cockpit-token" apps/cockpit/server/read.js` exit 0 E `grep -Eq "headers\.origin|headers\['origin'\]" apps/cockpit/server/read.js` exit 0 E `grep -Eq "headers\.host|headers\['host'\]" apps/cockpit/server/read.js` exit 0 (Origin AND Host validados).
    - BODY-PARSING HARDENED (FIX S-02, exit-code): com Origin+token validos, JSON malformado => `400` (`... -d '{bad json'` -w '%{http_code}'` == `400`); body > 4KB => rejeitado (`413`); fonte: `grep -Eq '4096|4 *\* *1024|4KB|413' apps/cockpit/server/read.js` exit 0 E `grep -q 'JSON.parse' apps/cockpit/server/read.js` (em try/catch).
    - DEFAULT-DENY (spec "acao fora do allowlist"): com Origin+token validos, `... -d '{"verb":"deploy_prod"}' -w '%{http_code}'` == `403`.
    - ARMAR-ANTES-DE-DISPARAR server-side (spec "acao reversivel confirmada"): com Origin+token validos, `-d '{"verb":"pause_autosync"}'` != `200` (rejeitado sem confirmed) E com `-d '{"verb":"pause_autosync","confirmed":true}'` executa (200).
    - SPAWNSYNC NO-SHELL (Excessive Agency): `grep -q 'spawnSync' apps/cockpit/server/read.js` exit 0 E `! grep -En "execSync\([\`\"'].*\\\$\\{|exec\(.*body\." apps/cockpit/server/read.js` exit 0 (sem exec de string com input).
    - B5 DAEMON COMO CHAVE DE MAP, SEM INTERPOLACAO (FIX S-04, exit-code): `! grep -En "com\.ideiaos\.[\`\"']?\s*\+\s*body\.daemon|gui/.*\\\$\\{?body\.daemon|launchctl.*body\.daemon" apps/cockpit/server/read.js` exit 0 (nenhuma interpolacao de body.daemon na arg launchctl) E `grep -Eq "process\.getuid\(\)" apps/cockpit/server/read.js` exit 0 ($UID vem de getuid, nao do body) E `grep -Eq "envsync.*refresh-ai-security.*cockpit|com\.ideiaos\.cockpit" apps/cockpit/server/read.js` exit 0 (map fechado).
    - STDOUT VIA ZERO-LEAK (FIX S-03): o stdout inline passa pelo detector antes do res.end — `grep -Eq 'zeroleak|zero-leak|scan.*stdout|S7' apps/cockpit/server/read.js` exit 0 (stdout do /command e varrido/materializado para a varredura S6/S7 de 14.1-06).
    - FOREVER-OUT (A8, exit-code): `grep -E 'rotate|deploy|revoke|git push|gh pr' apps/cockpit/server/read.js | grep -v '403\|gate\|forever\|OUT\|comentario\|//' | wc -l | tr -d ' '` == `0` (nenhum verbo de mutacao no enum executavel) E `! grep -q 'missioncontrol' apps/cockpit/server/read.js` exit 0 (B5 usa cockpit).
  </acceptance_criteria>
  <done>POST /command autenticado (Origin AND Host fail-closed 403 + token efemero X-Cockpit-Token), body-parsing hardened (Content-Type + 4KB cap + JSON.parse try/catch), enum fechado de 6 verbos default-deny (403), armar-antes-de-disparar server-side (B1/B3 confirmed:true), B5 daemon como chave de lookup-map sem interpolacao ($UID de getuid), stdout via Zero-Leak, spawnSync no-shell, forever-OUT por exit-code (cockpit, nunca missioncontrol).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CommandPalette.tsx (cmdk) + ui/command.tsx — ⌘K, allowlist, resultado inline, envia X-Cockpit-Token</name>
  <files>apps/cockpit/src/components/CommandPalette.tsx, apps/cockpit/src/components/ui/command.tsx, apps/cockpit/package.json</files>
  <read_first>
    - /Users/gustavolopespaiva/dev/nfideia/src/components/ui/command.tsx (PORTAR este arquivo — a primitiva shadcn cmdk: CommandDialog/CommandInput/CommandList/CommandEmpty/CommandGroup/CommandItem; ajustar imports @/ e remover acoplamento de dominio nfideia)
    - /Users/gustavolopespaiva/dev/nfideia/src/components/CommandPalette.tsx (lines ~27-88: ⌘K listener metaKey/ctrlKey + setOpen toggle; allowlist-como-array; CommandDialog/CommandGroup/CommandItem onSelect — COPIAR a SHAPE; mas nfideia NAVEGA (navigate(href)) e o cockpit EXECUTA verbos: trocar o PAGES nav array por uma allowlist de verbos que faz POST /command)
    - apps/cockpit/src/App.tsx (apos 14.1-02 — o seam ja monta <CommandPalette/>; nao editar App.tsx; o token efemero ja esta na HTML same-origin servida em :5273)
    - apps/cockpit/server/read.js (apos Task 1 — os 6 verbos, o contrato {verb, confirmed}, e o header X-Cockpit-Token exigido)
    - apps/cockpit/package.json (adicionar cmdk@^1.1.1 — a UNICA dep nova sancionada; nfideia usa ^1.1.1)
    - 14.1-PATTERNS.md secao "Command Palette" (analog EXATO; tabela B1-B6; armar-antes-de-disparar B1/B3; A8) + "Notes" item 3 (cmdk + ui/command.tsx sao as unicas adicoes front-end)
  </read_first>
  <action>
    Portar `ui/command.tsx` do nfideia (primitiva cmdk shadcn; ajustar imports `@/`). Adicionar `cmdk@^1.1.1` ao package.json
    (UNICA dep nova). Criar `CommandPalette.tsx`: listener ⌘K (metaKey||ctrlKey + 'k' => toggle open), `CommandDialog` com
    `CommandInput`/`CommandList`/`CommandEmpty`/`CommandGroup`/`CommandItem`. Substituir o nav-array do nfideia por uma ALLOWLIST de
    6 verbos (B1-B6, labels PT-BR da tabela); `onSelect` faz `POST /command {verb}` ao read.js loopback, lendo o token efemero
    injetado same-origin na HTML e enviando-o no header `X-Cockpit-Token` (FIX S-01) + `Content-Type: application/json`, e mostra o
    resultado INLINE (exitCode/stdout). Para B1 e B3 (destrutivos-mas-reversiveis): armar-antes-de-disparar — um 2o passo de
    confirmacao na UI que so entao envia `confirmed:true` (o server ja exige; defesa em profundidade). Operacoes @devops (push/PR/MCP)
    NAO estao no palette; se um dia pertinentes, no maximo GERA o comando (nunca executa — agent-authority). cmdk so; sem outra dep nova.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS/apps/cockpit && grep -q '"cmdk"' package.json && npm install >/dev/null 2>&1 && npm run build && grep -q '/command' src/components/CommandPalette.tsx && grep -Eiq 'X-Cockpit-Token|x-cockpit-token' src/components/CommandPalette.tsx && ! grep -Eq 'rotate|deploy|revoke|git push|gh pr' src/components/CommandPalette.tsx; echo "exit=$?"</automated>
  </verify>
  <what-built>
    CommandPalette.tsx: ⌘K overlay cmdk com allowlist de 6 verbos (B1-B6) fazendo POST /command (com header X-Cockpit-Token) e
    resultado inline; B1/B3 com armar-antes-de-disparar na UI. ui/command.tsx portado do nfideia. cmdk@^1.1.1 (unica dep nova).
  </what-built>
  <how-to-verify>
    1. read.js + dev no ar (`node apps/cockpit/server/read.js &`; `cd apps/cockpit && npm run dev`); abrir http://127.0.0.1:5273/.
    2. Apertar ⌘K (ou Ctrl+K): o overlay abre com os 6 verbos.
    3. frontend-visual-loop (criterio explicito): screenshot mostra o palette aberto com os 6 verbos B1-B6 e NENHUM verbo de
       rotate/deploy/revoke/push/PR/MCP. Criterio NAO e "parece certo".
    4. A7 — rodar 3 verbos LOCAIS inline: executar B6 (idea-doctor, read-only), B2 (retomar autosync), e B1 (pausar autosync, que
       exige confirmar) e confirmar que o resultado (exitCode/stdout) aparece INLINE no palette. B1 deve exigir a confirmacao antes.
       (O POST so executa porque a UI envia o X-Cockpit-Token same-origin; uma aba externa receberia 403.)
  </how-to-verify>
  <acceptance_criteria>
    - cmdk e a unica dep nova: `grep -q '"cmdk"' apps/cockpit/package.json` exit 0 E o diff de dependencies adiciona SO cmdk (nenhuma outra dep nova).
    - `cd apps/cockpit && npm run build` exit 0.
    - Faz POST /command com token: `grep -q '/command' apps/cockpit/src/components/CommandPalette.tsx` exit 0 E `grep -Eiq 'X-Cockpit-Token|x-cockpit-token' apps/cockpit/src/components/CommandPalette.tsx` exit 0 (envia o token efemero same-origin).
    - FOREVER-OUT no palette (A8): `! grep -Eiq 'rotate|deploy|revoke|git push|gh pr|mcp.*add|mcp.*remove' apps/cockpit/src/components/CommandPalette.tsx` exit 0.
    - ARMAR-ANTES-DE-DISPARAR na UI para B1/B3: `grep -Eiq 'confirm|armar|confirmed' apps/cockpit/src/components/CommandPalette.tsx` exit 0.
    - frontend-visual-loop (A7): screenshot do palette aberto com os 6 verbos + evidencia de >=3 verbos locais executados inline (B6/B2/B1 com B1 exigindo confirmacao), criterio explicito.
  </acceptance_criteria>
  <done>⌘K (cmdk) abre com os 6 verbos B1-B6, faz POST /command com o header X-Cockpit-Token e resultado inline, B1/B3 armam-antes-de-disparar; forever-OUT no palette; cmdk unica dep nova; build verde + screenshot (>=3 verbos inline).</done>
  <resume-signal>Digite "approved" quando o ⌘K abrir com os 6 verbos (sem nenhum verbo de mutacao/cross-maquina) e >=3 verbos locais rodarem inline (B1 exigindo confirmacao) — ou descreva o que falta.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (nao-confiavel, incl. aba local maliciosa) -> POST /command | so 127.0.0.1; AUTENTICADO server-side (Origin+Host fail-closed + token efemero por-boot X-Cockpit-Token) ANTES de qualquer exec; verbo validado contra enum fechado |
| enum de verbo -> CLI sancionado (own machine) | spawnSync no-shell, args constantes; B5 daemon como chave de lookup-map fechado (sem interpolacao); $UID de getuid |
| stdout do comando -> UI (loopback) | varrido pelo detector Zero-Leak / materializado como S7 antes do res.end (14.1-06 varre /command) |
| superficie de comando -> producao / @devops / cross-maquina | forever-OUT por construcao; gate exit-code prova ausencia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-04-S | Spoofing | aba local maliciosa (CSRF/DNS-rebinding) entrega POST /command | mitigate | AUTENTICACAO server-side: Origin AND Host validados fail-closed => 403 (Host neutraliza DNS-rebinding); token efemero por-boot exigido em X-Cockpit-Token, ilegivel cross-origin (SOP). CORS NAO e a defesa. Fecha o boundary browser->exec intra-maquina de 14.1-04 (FIX S-01) |
| T-14.1-04-S2 | Spoofing | verbo do body do POST | mitigate | verbo validado contra enum fechado; default-deny 403 para qualquer verbo desconhecido (V5 input validation) |
| T-14.1-04-T | Tampering | bypass de armar-antes-de-disparar | mitigate | server exige `confirmed:true` para verbos arm:true; um curl unico (mesmo autenticado) sem confirmed e rejeitado (defesa server-side, nao so UI — Open Q4) |
| T-14.1-04-T2 | Tampering | body malformado/oversized | mitigate | Content-Type application/json exigido; cap 4KB (413 acima); JSON.parse em try/catch => 400 (FIX S-02) |
| T-14.1-04-I | Information Disclosure | stdout do comando ecoa segredo na UI | mitigate | stdout inline varrido pelo detector Zero-Leak / materializado S7 antes do res.end; 14.1-06 inclui /command em S6 (FIX S-03, output-as-surface OWASP LLM02) |
| T-14.1-04-E | Elevation of Privilege | exec arbitrario via ⌘K | mitigate | spawnSync(file, argsArray) sem shell; args constantes; B5 daemon como chave de lookup-map fechado (sem interpolacao de body.daemon), $UID de getuid; gate proibe execSync(string com input) (OWASP LLM06 Excessive Agency — FIX S-04) |
| T-14.1-04-E2 | Elevation of Privilege | @devops/producao/cross-maquina via UI | mitigate | rotate/revoke/deploy/git push/gh pr/MCP forever-OUT; A8 grep==0 no enum E no palette; cross-maquina gated v14.4 (agent-authority) |
| T-14.1-04-D | Denial of Service | revoke em massa | mitigate | revoke (e mass-revoke) forever-OUT por construcao — nenhuma assinatura o torna atomicamente seguro (doc 77 B.2) |
</threat_model>

<verification>
- node -c read.js; build verde; cmdk unica dep nova.
- AUTH (FIX S-01): Origin invalido => 403; sem X-Cockpit-Token valido => 403; Origin AND Host validados na fonte.
- body-parsing hardened (FIX S-02): malformado => 400; >4KB => 413; Content-Type exigido.
- default-deny (403 p/ verbo fora do enum); armar-antes-de-disparar server-side (rejeita sem confirmed); spawnSync no-shell.
- B5 daemon como chave de map sem interpolacao (FIX S-04); stdout via Zero-Leak (FIX S-03).
- A8 forever-OUT por exit-code no enum E no palette; B5 usa cockpit (nunca missioncontrol).
- frontend-visual-loop (A7): palette com 6 verbos + >=3 rodando inline (B1 confirmando), enviando X-Cockpit-Token.
</verification>

<success_criteria>
- ⌘K executa so os 6 verbos locais-reversiveis (B1-B6), default-deny para o resto.
- Canal autenticado server-side (Origin+Host+token efemero); body-parsing hardened; stdout via Zero-Leak.
- Armar-antes-de-disparar server-side (B1/B3) + spawnSync no-shell + enum fechado + B5 lookup-map.
- Forever-OUT (producao/cross-maquina/@devops) provado por exit-code; A7/A8 verdes.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-04-SUMMARY.md` when done
</output>
</content>
