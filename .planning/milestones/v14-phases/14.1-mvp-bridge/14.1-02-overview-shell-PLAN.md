---
phase: "14.1"
plan: "14.1-02"
type: execute
wave: 2
depends_on: ["14.1-01", "14.1-05"]
files_modified:
  - apps/cockpit/src/App.tsx
  - apps/cockpit/src/pages/Overview.tsx
  - apps/cockpit/src/components/SystemPulse.tsx
  - apps/cockpit/src/components/StatCard.tsx
autonomous: false
requirements: [R14-05]
must_haves:
  truths:
    - "App.tsx vira um shell com nav fixa para 3 telas (Overview/Frota/Cofre) + um ponto-de-montagem estavel do ⌘K, registrando TODAS as rotas (Frota/Cofre sao preenchidas em 14.1-03; ⌘K em 14.1-04) — seam unico em App.tsx para os planos da Wave 3 nao colidirem"
    - "Overview renderiza o bento grid (System Pulse hero local-vivo + cards Frota/Seguranca/Releases-SOAK lean + Atencao-Agora) + a banda Flight Recorder (FlightRecorder de 14.1-05)"
    - "System Pulse anima SOMENTE no heartbeat local (file-watch ~1-5s); nos remotos mostram idade honesta (ultimo sinal ha Xh), nunca fluxo continuo simulado; critico => estado vermelho/arritmico + LABEL textual (cor nunca e o unico sinal)"
    - "O card Releases-SOAK e lean (PRONTO PARA TAG + v## n/2 ✓ span x/1d), sem countdown decorativo (doc 71 — cede o slot ao Flight Recorder)"
  artifacts:
    - path: "apps/cockpit/src/pages/Overview.tsx"
      provides: "Bento grid + banda Flight Recorder"
      min_lines: 40
    - path: "apps/cockpit/src/components/SystemPulse.tsx"
      provides: "Hero local-vivo (file-watch), frescor honesto, color-not-sole-signal"
      min_lines: 25
  key_links:
    - from: "apps/cockpit/src/pages/Overview.tsx"
      to: "http://127.0.0.1:3073/overview"
      via: "fetch em useEffect (padrao App.tsx existente)"
      pattern: "/overview"
    - from: "apps/cockpit/src/pages/Overview.tsx"
      to: "apps/cockpit/src/components/FlightRecorder.tsx"
      via: "import e montagem da banda"
      pattern: "FlightRecorder"
---

<objective>
Transformar `App.tsx` no shell de navegacao das 3 telas + montar a Overview (bento grid + banda Flight Recorder).
Este plano define o SEAM de integracao: registra TODAS as rotas (Overview/Frota/Cofre) e o ponto-de-montagem do
⌘K em App.tsx, para que 14.1-03 (Frota/Cofre) e 14.1-04 (⌘K) adicionem SO seus arquivos novos na Wave 3 sem editar
App.tsx (evita conflito de file-ownership na mesma wave).

Purpose: e Wave 2 (depende de 14.1-01 endpoints + 14.1-05 FlightRecorder). Entrega a primeira tela completa de valor
e o frescor honesto (spec). System Pulse anima SO no heartbeat local (file-watch); remoto mostra idade honesta; critico
=> vermelho/arritmico + LABEL textual (cor nunca o unico sinal — WCAG). Releases-SOAK e lean (sem countdown — cede o
slot ao Flight Recorder, doc 71).
Output: App.tsx (shell+nav+seam) + Overview.tsx + SystemPulse.tsx + StatCard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-CONTEXT.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-RESEARCH.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-PATTERNS.md
@specs/cockpit/spec.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-01-data-access-foundation-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-05-flight-recorder-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx shell + nav + seam de rotas (Overview/Frota/Cofre + ponto-de-montagem ⌘K)</name>
  <files>apps/cockpit/src/App.tsx, apps/cockpit/src/components/StatCard.tsx</files>
  <read_first>
    - apps/cockpit/src/App.tsx (o arquivo a modificar — hoje e single-screen: API_BASE loopback http://127.0.0.1:3073, useEffect fetch /machines, triade loading/error/empty, header black-gold. Manter o API_BASE e a triade; trocar o conteudo unico por um switch de tela)
    - /Users/gustavolopespaiva/dev/nfideia/src/components/KPICard.tsx (lines ~16-44: shape title/value/subtitle/icon/trend para os stat tiles — ADAPTAR; DROPAR o import framer-motion, usar card sem motion)
    - apps/cockpit/src/components/MachineCard.tsx (padrao shadcn Card + Badge + lucide + color-not-sole-signal — o estilo black-gold do cockpit)
    - 14.1-PATTERNS.md secao "Screen pages" (data fetch pattern canonico do App.tsx; AppLayout/AppSidebar do nfideia sao REFERENCE-only, muito acoplados a auth/router/trial — construir o shell thin sobre o header existente) + secao "Notes for the planner" item 4
    - docs/ideiaos-console/50-ux-experience.md (sidebar: icones colapsados + barra ativa ouro 2px; bento grid; System Pulse hero)
  </read_first>
  <action>
    Reescrever `App.tsx` como shell: manter `API_BASE = http://127.0.0.1:${READ_PORT}` e a triade loading/error/empty.
    Adicionar um estado de tela (`'overview'|'frota'|'cofre'`) e uma nav thin (sidebar/topbar — icones lucide, barra ativa
    ouro 2px per 50-ux) que troca entre as 3 telas SEM router lib (estado local; sem dep nova). Registrar AS TRES rotas
    agora (Overview montada neste plano; Frota e Cofre apontam para `pages/Frota.tsx` e `pages/CofreEspelho.tsx` que
    14.1-03 cria — usar import lazy/dynamic ou um placeholder de import resolvido por 14.1-03). Deixar um ponto-de-montagem
    ESTAVEL para o ⌘K (ex.: `<CommandPalette/>` de `@/components/CommandPalette` que 14.1-04 cria) — este e o seam: 03 e 04
    nao editam App.tsx, so adicionam seus arquivos. Criar `StatCard.tsx` (tile title/value/subtitle/icon, sem framer) para
    os bento stats. Loopback 127.0.0.1, sem login.
  </action>
  <acceptance_criteria>
    - `cd apps/cockpit && npm run build` exit 0 (TODAS as rotas resolvem — usar lazy import ou stub de modulo que 14.1-03/04 preenchem; se stub, ele deve existir para o build passar).
    - SEAM registrado: `grep -Eq "Frota|frota" apps/cockpit/src/App.tsx && grep -Eq "Cofre|cofre" apps/cockpit/src/App.tsx && grep -q "Overview" apps/cockpit/src/App.tsx` exit 0 (3 rotas) E `grep -q "CommandPalette" apps/cockpit/src/App.tsx` exit 0 (ponto-de-montagem ⌘K).
    - SEM ROUTER LIB / SEM DEP NOVA: `! grep -Eq "react-router|@tanstack/router" apps/cockpit/package.json` exit 0 (nav por estado local).
    - API_BASE loopback preservado: `grep -q "127.0.0.1" apps/cockpit/src/App.tsx` exit 0.
  </acceptance_criteria>
  <done>App.tsx e shell com nav das 3 telas + seam estavel do ⌘K; build verde; sem router lib/dep nova; loopback preservado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Overview (bento + System Pulse local-vivo + banda Flight Recorder)</name>
  <files>apps/cockpit/src/pages/Overview.tsx, apps/cockpit/src/components/SystemPulse.tsx</files>
  <read_first>
    - apps/cockpit/src/App.tsx (apos Task 1 — Overview e montada na rota 'overview')
    - apps/cockpit/src/components/FlightRecorder.tsx (de 14.1-05 — a banda a montar na Overview)
    - apps/cockpit/server/read.js (de 14.1-01 — GET /overview retorna machines/projects/checks + saude-por-produto n/a onde idea-doctor nao roda)
    - apps/cockpit/src/components/StatCard.tsx (de Task 1) + MachineCard.tsx (estilo)
    - apps/cockpit/src/index.css (tokens --status-success/--status-warning de 14.1-01; usar oklch(var(--status-*)))
    - docs/ideiaos-console/50-ux-experience.md (System Pulse hero; bento grid; pulse-warn ~line 142; WCAG ~116-131) e docs/ideiaos-console/76-pulso-productivity-formulas.md (KPIs ininflaveis para o preview do Pulso)
    - 14.1-CONTEXT.md secao "Honest freshness" e "Per-product health" (Pulse anima so local; remoto idade honesta; idea-doctor n/a em Lovable; monousuario => "aguardando segundo ator")
    - spec "Frescor honesto" + "Saude por produto com sub-sinal honesto"
  </read_first>
  <action>
    `SystemPulse.tsx`: hero de heartbeat que anima SOMENTE no sinal local (file-watch / fetch curto ~1-5s do snapshot da
    propria maquina). Nos remotos NAO animam: mostram idade honesta "ultimo sinal ha Xh" derivada de `last_seen_epoch`
    (de /fleet). Estado critico => vermelho/arritmico COM label textual (ex.: "CRITICO") — cor NUNCA e o unico sinal (WCAG).
    `pulse-warn` gated por `prefers-reduced-motion`.
    `Overview.tsx`: fetch `GET /overview` (padrao useEffect/cancelled do App.tsx) e monta o bento grid: SystemPulse hero +
    StatCards (MAQUINAS/PROJETOS/CHECKS OK) + card Frota (resumo) + card Seguranca + card Releases-SOAK LEAN
    ("PRONTO PARA TAG" + `v## n/2 ✓ span x/1d`, SEM countdown decorativo — doc 71) + card Atencao-Agora. Abaixo, montar a
    BANDA Flight Recorder (`<FlightRecorder/>` de 14.1-05). Saude-por-produto: onde idea-doctor nao roda (Lovable) renderiza
    `n/a` (nunca nota fabricada); monousuario => label "aguardando segundo ator", nunca card-fantasma.
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS/apps/cockpit && npm run build && grep -q 'FlightRecorder' src/pages/Overview.tsx && grep -q "/overview" src/pages/Overview.tsx && grep -q 'prefers-reduced-motion' src/components/SystemPulse.tsx; echo "exit=$?"</automated>
  </verify>
  <what-built>
    Overview.tsx: bento grid (System Pulse hero local-vivo + StatCards + cards Frota/Seguranca/Releases-SOAK lean + Atencao-Agora)
    + banda Flight Recorder. SystemPulse.tsx: heartbeat anima so no local, remoto mostra idade honesta, critico vermelho/arritmico
    + label textual, motion gated.
  </what-built>
  <how-to-verify>
    1. Garantir read-model + read.js no ar: `node source/console/ingest.js`; `node apps/cockpit/server/read.js &`; `cd apps/cockpit && npm run dev`.
    2. Abrir http://127.0.0.1:5273/ na rota Overview.
    3. frontend-visual-loop (regime runtime/UI — criterio explicito): screenshot mostra o bento grid COM (a) System Pulse hero,
       (b) >=1 StatCard com valor, (c) o card Releases-SOAK SEM countdown (lean: "PRONTO PARA TAG"/"v## n/2 span"), (d) a banda
       Flight Recorder com a step-line de dois niveis. Criterio NAO e "parece certo".
    4. Frescor honesto: confirmar que um no remoto mostra "ultimo sinal ha Xh" (texto), nao animacao continua.
    5. WCAG: estado critico tem label textual alem da cor.
  </how-to-verify>
  <acceptance_criteria>
    - `cd apps/cockpit && npm run build` exit 0.
    - Overview consome /overview e monta a banda: `grep -q "/overview" apps/cockpit/src/pages/Overview.tsx && grep -q "FlightRecorder" apps/cockpit/src/pages/Overview.tsx` exit 0.
    - FRESCOR HONESTO (spec): `grep -Eq 'last_seen|ha .*h|ultimo sinal|idade' apps/cockpit/src/components/SystemPulse.tsx` exit 0 (idade honesta para remoto) E `grep -q 'prefers-reduced-motion' apps/cockpit/src/components/SystemPulse.tsx` exit 0.
    - COR NAO E O UNICO SINAL (A10): `grep -Eq 'CRITICO|critico|label|aria-label' apps/cockpit/src/components/SystemPulse.tsx` exit 0 (estado tem texto, nao so cor).
    - RELEASES-SOAK LEAN (sem countdown — doc 71): `! grep -Eiq 'countdown|contagem regressiva' apps/cockpit/src/pages/Overview.tsx` exit 0.
    - frontend-visual-loop: screenshot anexado (bento + Pulse + StatCard + Releases-SOAK lean + banda Flight Recorder), criterio explicito.
  </acceptance_criteria>
  <done>Overview renderiza o bento (Pulse local-vivo + stats + Releases-SOAK lean) + banda Flight Recorder; frescor honesto + WCAG por exit-code + screenshot.</done>
  <resume-signal>Digite "approved" quando o screenshot mostrar o bento grid com System Pulse, >=1 StatCard, Releases-SOAK lean (sem countdown) e a banda Flight Recorder — ou descreva o que falta.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /overview /fleet -> Overview DOM | metadata renderizada; nenhum valor de segredo no markup (Zero-Leak superficie S4/S5) |
| heartbeat local vs remoto | so o local anima; remoto = idade honesta (nao simular fluxo sobre lote) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-02-I | Information Disclosure | DOM/state da Overview | mitigate | so metadata renderizada; o gate Zero-Leak (14.1-06) varre S4/S5 sobre esta tela |
| T-14.1-02-R | Repudiation | frescor simulado | mitigate | Pulse anima so no heartbeat local; remoto mostra idade honesta (spec "Frescor honesto") — gate exit-code prova idade textual |
| T-14.1-02-I2 | Information Disclosure | saude-por-produto fabricada | accept->mitigate | idea-doctor n/a em Lovable renderiza `n/a`, nunca nota inventada (spec "Saude por produto") |
</threat_model>

<verification>
- npm run build verde; Overview consome /overview + monta FlightRecorder.
- Frescor honesto (idade textual remota + motion gated); cor nao e o unico sinal; Releases-SOAK sem countdown.
- frontend-visual-loop: screenshot do bento + banda Flight Recorder.
</verification>

<success_criteria>
- App.tsx e shell com nav das 3 telas + seam estavel do ⌘K (03/04 nao editam App.tsx).
- Overview completa (bento + Pulse local-vivo + Releases-SOAK lean + banda Flight Recorder).
- Frescor honesto e WCAG honrados por exit-code + screenshot.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-02-SUMMARY.md` when done
</output>
