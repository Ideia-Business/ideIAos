---
phase: 14.1-mvp-bridge
plan: 14.1-01
subsystem: api
tags: [node:sqlite, loopback, cockpit, credential-isolation, oklch, git-show]

# Dependency graph
requires:
  - phase: 14.0-cockpit
    provides: "read.js loopback server (node:sqlite, 127.0.0.1:3073), schema.sql (api_key sem coluna value), cockpit.sh (ref git-plumbing), read-model.db (ingest.js), index.css (tema black-gold OKLCH)"
provides:
  - "GET /overview — contagens agregadas (máquinas, projetos, checks doctor) para o bento"
  - "GET /fleet — por-máquina: JOIN snapshot mais recente + daemon_status + installed_versions + last_seen_epoch cru (frescor honesto)"
  - "GET /vault — matriz var×project METADATA-ONLY (sem campo value; credential-isolation estrutural)"
  - "GET /verify?cell=<machine_id> — recompute-from-disk via git show cockpit:snapshots/<MID>.json no instante da pergunta (A6 Trust-Rate)"
  - "Tokens CSS --status-success / --status-warning OKLCH (50-ux) — pré-requisito visual de Frota/Cofre/Flight Recorder/Pulse"
affects: [14.1-02, 14.1-03, 14.1-04, Overview, Frota, CofreEspelho, FlightRecorder]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Loopback-bind + CORS fixo (127.0.0.1:5273) + try/finally db.close — replicado em cada novo handler"
    - "Recompute-from-disk com argv-array (execFileSync) e allowlist de machine_id (^[0-9a-f]{12}$) — sem interpolação de input em shell"
    - "SELECT de colunas explícitas (nunca SELECT *) na superfície /vault — credential-isolation materializada"
    - "Tokens de estado OKLCH bare 'lightness chroma hue' fora do hue de marca 75 (Ouro != estado)"

key-files:
  created:
    - .planning/milestones/v14-phases/14.1-mvp-bridge/14.1-01-SUMMARY.md
  modified:
    - apps/cockpit/server/read.js
    - apps/cockpit/src/index.css

key-decisions:
  - "node:sqlite (DatabaseSync) reusado — ZERO dep nova; NÃO better-sqlite3 (reconciliação #1, sobrepõe CONTEXT §Stack stale)"
  - "/verify compara taken_epoch (disco vs read-model) como prova de frescor verificável; <MID> validado por regex ANTES de virar argv"
  - "execFileSync com argv-array (não execSync com string de shell) — nenhum input do usuário transita por shell"
  - "Status tokens em hues 150 (verde) e 85 (âmbar), nunca o hue de marca 75 — ouro reservado a frame/seleção"

patterns-established:
  - "Recompute-from-disk endpoint: query param → validação por allowlist → execFileSync argv-array → comparação com o servido → {verified, recomputed_at_epoch}"
  - "Metadata-only read: colunas explícitas no SELECT, gate exit-code provando ausência de campo `value` no body servido"

requirements-completed: [R14-05, R14-06]

# Metrics
duration: 2min
completed: 2026-06-22
---

# Phase 14.1 Plan 14.1-01: Data Access Foundation Summary

**read.js loopback estendido com /overview, /fleet, /vault (node:sqlite, metadata-only) e /verify (recompute-from-disk via git show, argv-array validado), mais tokens de estado OKLCH --status-success/--status-warning no index.css.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-22T11:40:25Z
- **Completed:** 2026-06-22T11:42:44Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Quatro endpoints de leitura novos no server loopback já shipado, todos reusando `openDb()` (node:sqlite `DatabaseSync`), o CORS fixo `127.0.0.1:5273` e o `finally { db.close() }` do `handleMachines` — sem dep nova, supply-chain nula.
- `/vault` é metadata-only por construção: SELECT de colunas explícitas (`project_slug, var_name, present, expected, risk_tier, file_mtime_epoch, committed`) — o schema não tem coluna `value` e o payload servido nunca introduz um campo `value` (provado por exit-code).
- `/verify?cell=<MID>` recomputa do disco no instante da pergunta (`git show cockpit:snapshots/<MID>.json`), comparando `taken_epoch` disco vs read-model, com `recomputed_at_epoch` fresco — nunca de cache (A6 Trust-Rate). `<MID>` validado contra `^[0-9a-f]{12}$` ANTES de virar argumento, e o git é invocado por `execFileSync` com argv em array (sem shell), de modo que input do usuário nunca é interpolado numa string de shell.
- `index.css` ganhou `--status-success` (OKLCH `0.80 0.18 150`) e `--status-warning` (`0.83 0.16 85`) — verde/âmbar do 50-ux, fora do hue de marca 75 (ouro permanece reservado a frame/seleção).

## Task Commits

Cada task foi commitada atomicamente:

1. **Task 1: read.js /overview /fleet /vault (leitura, loopback, node:sqlite)** — `7673478` (feat)
2. **Task 2: /verify (recompute-from-disk, A6 Trust-Rate) + tokens de status no index.css** — `73e6b2b` (feat)

_Nota: autosync está PAUSADO nesta máquina; nenhum push/PR foi feito (operações @devops-exclusivas). Os commits são locais na branch `work`._

## Files Created/Modified
- `apps/cockpit/server/read.js` — +4 handlers (`handleOverview`, `handleFleet`, `handleVault`, `handleVerify`) e respectivas rotas no `http.createServer`; import de `node:child_process` (`execFileSync`); constante `JSON_CORS` e `MID_RE` (allowlist machine_id). Bind `127.0.0.1` (HOST) intacto.
- `apps/cockpit/src/index.css` — tokens `--status-success` / `--status-warning` OKLCH no bloco `:root @layer base`, sem redefinir `--brand`/`--primary`.

## Decisions Made
- **node:sqlite reusado, NÃO better-sqlite3:** o substrato v14.0 já escolheu o built-in (`read.js:20`); o PLAN e o PATTERNS sobrepõem o CONTEXT §Stack ("better-sqlite3") como stale. Zero `npm install`.
- **/verify por comparação de `taken_epoch`:** o sinal verificável mais barato e determinístico do snapshot é o `taken_epoch`; comparar disco (`git show`) vs servido (read-model) prova frescor sem reler payload inteiro.
- **`execFileSync` + argv-array + allowlist regex:** defesa-em-profundidade contra Excessive Agency — mesmo que a rota recebesse lixo, o regex barra antes do git, e a ausência de shell elimina injeção por construção.
- **Hues 150/85 para status:** honram o 50-ux ("Ouro != estado") sem tocar o hue de marca 75.

## Deviations from Plan

None - plan executed exactly as written.

(Notas de implementação dentro do escopo, não desvios: `JSON_CORS`/`MID_RE` são constantes auxiliares introduzidas para DRY/segurança; `handleVerify` parseia o query param via `new URL(req.url, 'http://127.0.0.1')` — host constante, nunca de input. Nenhuma funcionalidade fora do `<action>` foi adicionada.)

## Issues Encountered
None. A `read-model.db` já existia; rodei `node source/console/ingest.js` (reconstrói: 1 máquina, 7 projetos, 121 api_keys) antes dos testes de endpoint, conforme o critério de aceitação.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Fundação de acesso a dados pronta: 14.1-02/03/04 (Overview/Frota/Cofre/⌘K) podem fetchar `/overview`, `/fleet`, `/vault`, `/verify` do loopback e usar os tokens `--status-*`.
- Bind 127.0.0.1 contido; `/vault` metadata-only; `/verify` recompute-from-disk — superfície Zero-Leak S2/S3/S6 centralizada num único server auditável.
- Fora de escopo deste plano (continuam para planos seguintes): `POST /command` (⌘K verb dispatch), `/recorder` (Flight Recorder data), camada de entropia do Zero-Leak (Open Question A2), `test:zeroleak`/`test:recorder` gates.

## Acceptance Criteria — exit-code evidence

Task 1:
- `node -c apps/cockpit/server/read.js` → exit 0
- `curl -sf /overview | grep -Eq '"machines"|"projects"'` → exit 0 (`{"machines":1,"projects":7,...}`)
- `curl -sf /fleet | grep -q machine_id` → exit 0
- `curl -sf /vault | grep -Eq 'var_name|risk_tier'` → exit 0
- `! curl -sf /vault | grep -qiw value` (metadata-only) → exit 0
- bind negativa `! grep -Eq "0\.0\.0\.0|host:...'::|listen(..,'::"` → exit 0; positiva `grep -Eq "127\.0\.0\.1|listen(..,HOST"` → exit 0
- `grep -q "'/fleet'" && grep -q "'/overview'" && grep -q "'/vault'"` → exit 0

Task 2:
- `node -c apps/cockpit/server/read.js` → exit 0
- `grep -q "git show" && grep -q "'/verify'"` → exit 0
- `! grep -En "exec(Sync)?\([\`\"'].*\$\{"` (sem interpolação em shell) → exit 0
- `grep -q -- '--status-success' && grep -q -- '--status-warning'` → exit 0
- `! grep -E -- '--status-(success|warning):[^;]*75'` (ouro != estado) → exit 0
- `cd apps/cockpit && npm run build` → exit 0 (`✓ built in ~1s`)

Verificação funcional ao vivo (não-AC): `/verify?cell=c706ac77d577` → `{"verified":true,"served_epoch":1782084258,"disk_epoch":1782084258,...}`; `/verify?cell=foo;rm -rf` → HTTP 400 (allowlist rejeita antes do git).

## Self-Check: PASSED

- FOUND: apps/cockpit/server/read.js
- FOUND: apps/cockpit/src/index.css
- FOUND: .planning/milestones/v14-phases/14.1-mvp-bridge/14.1-01-SUMMARY.md
- FOUND commit: 7673478 (Task 1)
- FOUND commit: 73e6b2b (Task 2)

---
*Phase: 14.1-mvp-bridge*
*Completed: 2026-06-22*
