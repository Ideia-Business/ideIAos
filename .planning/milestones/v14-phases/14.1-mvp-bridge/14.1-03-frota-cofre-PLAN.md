---
phase: "14.1"
plan: "14.1-03"
type: execute
wave: 3
depends_on: ["14.1-01", "14.1-02"]
files_modified:
  - apps/cockpit/src/pages/Frota.tsx
  - apps/cockpit/src/pages/CofreEspelho.tsx
autonomous: false
requirements: [R14-05]
must_haves:
  truths:
    - "Frota renderiza um card por maquina + tabela densa de heartbeat + version-drift por STRING-EQUALITY (nunca semver)"
    - "Cofre-Espelho renderiza a matriz var x project METADATA-ONLY: nenhum controle de UI le/copia/escreve/rotaciona um valor; banner de doutrina; empty-state celebrado"
    - "Cofre exibe nome/presenca/idade/risco de cada credencial e NUNCA o valor (nem parcial, nem mascarado a partir do valor real) — spec 'render de uma credencial conhecida'"
    - "Frota mostra nos remotos com idade honesta (ultimo sinal ha Xh), nunca fluxo simulado"
  artifacts:
    - path: "apps/cockpit/src/pages/Frota.tsx"
      provides: "Card por maquina + heartbeat table + version-drift string-equality"
      min_lines: 40
    - path: "apps/cockpit/src/pages/CofreEspelho.tsx"
      provides: "Matriz var x project metadata-only + banner + empty-state"
      min_lines: 40
  key_links:
    - from: "apps/cockpit/src/pages/Frota.tsx"
      to: "http://127.0.0.1:3073/fleet"
      via: "fetch em useEffect"
      pattern: "/fleet"
    - from: "apps/cockpit/src/pages/CofreEspelho.tsx"
      to: "http://127.0.0.1:3073/vault"
      via: "fetch em useEffect (metadata-only, sem value)"
      pattern: "/vault"
---

<objective>
Entregar as duas telas de pilar restantes da Wave 3: Frota (card por maquina + tabela densa de heartbeat +
version-drift por string-equality) e Cofre-Espelho (matriz var x project METADATA-ONLY, banner de doutrina,
empty-state celebrado). Ambas consomem os endpoints de 14.1-01 (`/fleet`, `/vault`) e sao montadas nas rotas que
14.1-02 ja registrou em App.tsx — este plano cria SO os arquivos `pages/Frota.tsx` e `pages/CofreEspelho.tsx`
(nao edita App.tsx; file-ownership limpo na Wave 3).

Purpose: e Wave 3 (depende de 14.1-01 endpoints + 14.1-02 shell). O Cofre carrega a prova VISUAL do credential-isolation
(spec "Isolamento absoluto" / "render de uma credencial conhecida"): nenhum controle de UI le/copia/revela um valor;
renderiza so {var_name, present, expected, risk_tier, idade}. Frota: version-drift por STRING-EQUALITY, nunca semver
(learning version-reset-migration-semver-trap); remoto com idade honesta.
Output: Frota.tsx + CofreEspelho.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-CONTEXT.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-RESEARCH.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-PATTERNS.md
@specs/cockpit/spec.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-01-data-access-foundation-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-02-overview-shell-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frota (card por maquina + heartbeat table + version-drift string-equality)</name>
  <files>apps/cockpit/src/pages/Frota.tsx</files>
  <read_first>
    - apps/cockpit/src/App.tsx (apos 14.1-02 — a rota 'frota' importa pages/Frota.tsx; usar o mesmo padrao de fetch/triade)
    - apps/cockpit/src/components/MachineCard.tsx (o card de saude shadcn ja shipado: Card/CardHeader/CardTitle/CardContent + Badge + lucide + color-not-sole-signal — REUSAR/estender; doctorVariant + DoctorIcon)
    - apps/cockpit/server/read.js (GET /fleet de 14.1-01: {machine_id, canonical_name, os_version, agentd_version, last_seen_epoch, last_doctor, daemons[], installed_versions})
    - source/agentd/collect.js (readVersions ~line 262: installed_versions e objeto { [key]: version_string } — comparar por igualdade de string)
    - 14.1-PATTERNS.md secao "Screen pages" (Frota version-drift string-equality NUNCA semver; data fetch pattern; AppLayout/AppSidebar do nfideia = REFERENCE-only)
    - 14.1-CONTEXT.md secao "Honest freshness" (remoto: ultimo sinal ha Xh)
  </read_first>
  <action>
    Criar `pages/Frota.tsx`: fetch `GET /fleet` (padrao useEffect/cancelled do App.tsx). Renderizar um MachineCard (reuso) por
    maquina + uma TABELA DENSA de heartbeat (machine_id, canonical_name, os_version, agentd_version, last_doctor, ultimo sinal).
    Frescor honesto: remoto mostra "ultimo sinal ha Xh" de `last_seen_epoch` (texto), nunca animacao. VERSION-DRIFT por
    STRING-EQUALITY: comparar `installed_versions[key]` entre maquinas por `===` de string (drift = strings diferentes),
    NUNCA parse semver (1.1.0 redux > 1.36.0 pre-redux invertido — learning version-reset-migration-semver-trap); drift => badge
    amber `oklch(var(--status-warning))` + LABEL textual "drift" (cor nunca o unico sinal). Assimetria entre maquinas
    (agentd_version/os_version divergente) => amber, nao quebra.
  </action>
  <acceptance_criteria>
    - `cd apps/cockpit && npm run build` exit 0.
    - Consome /fleet: `grep -q "/fleet" apps/cockpit/src/pages/Frota.tsx` exit 0.
    - VERSION-DRIFT STRING-EQUALITY (nao semver): `! grep -Eiq 'semver|localeCompare.*numeric|parseInt.*version|\.split\("\."\)' apps/cockpit/src/pages/Frota.tsx` exit 0 (nenhuma comparacao semver).
    - FRESCOR HONESTO: `grep -Eq 'last_seen|ha .*h|ultimo sinal' apps/cockpit/src/pages/Frota.tsx` exit 0 (idade textual para remoto).
    - COR NAO E O UNICO SINAL: `grep -Eq 'drift|label|Badge' apps/cockpit/src/pages/Frota.tsx` exit 0 (drift tem texto, nao so cor).
  </acceptance_criteria>
  <done>Frota renderiza card por maquina + heartbeat table + version-drift por string-equality (nunca semver); remoto com idade honesta; build verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cofre-Espelho (matriz var x project metadata-only, banner, empty-state)</name>
  <files>apps/cockpit/src/pages/CofreEspelho.tsx</files>
  <read_first>
    - apps/cockpit/src/App.tsx (apos 14.1-02 — a rota 'cofre' importa pages/CofreEspelho.tsx)
    - apps/cockpit/server/read.js (GET /vault de 14.1-01: {project_slug, var_name, present, expected, risk_tier, file_mtime_epoch, committed} — SEM campo value)
    - source/console/schema.sql (api_key NAO tem coluna value — credential-isolation materializada estruturalmente; risk_tier CHECK critical/sensitive/low/none)
    - source/agentd/collect.js (readEnvKeys ~line 160-186: emite {var_name, present, mtime_epoch} — o RHS de `=` e descartado; NENHUM value)
    - apps/cockpit/src/components/MachineCard.tsx + Badge variants (estilo black-gold; risk_tier => variant de Badge)
    - 14.1-PATTERNS.md secao "Cofre-Espelho specific guard" (matriz renderiza so metadata; nenhum controle le/copia/revela valor; Zero-Leak S4/S5 GATEia)
    - spec "Isolamento absoluto de credenciais (Zero-Leak)" + cenario "render de uma credencial conhecida" (mostra nome/presenca/idade/classe-de-risco e NUNCA o valor — nem parcial, nem mascarado a partir do valor real)
    - docs/ideiaos-console/50-ux-experience.md (banner de doutrina; empty-state celebrado)
  </read_first>
  <action>
    Criar `pages/CofreEspelho.tsx`: fetch `GET /vault` e renderizar a MATRIZ var x project METADATA-ONLY — colunas
    {var_name, presenca (present/expected), risk_tier (Badge por classe), idade (de file_mtime_epoch), committed}.
    NENHUM controle de UI le/copia/escreve/rotaciona/revela um valor — sem botao "copiar"/"revelar"/"mostrar", sem campo de
    valor mascarado derivado de valor real (NUNCA — spec). Banner de doutrina no topo (ex.: "Espelho de metadados — o valor de
    um segredo jamais transita por aqui"). Empty-state CELEBRADO (quando nao ha chave faltando/exposta — estado bom, nao vazio
    triste). risk_tier critico => destaque com LABEL textual, nao so cor (WCAG).
    NOTA AO GATE (FIX 6): a prova estrutural Zero-Leak (S3 api_key sem coluna value + S4/S5 de 14.1-06) e a garantia real; o
    grep de fonte abaixo e ESTREITO (verbos que REVELAM valor) — NAO proibir `.value` generico (ex.: `e.target.value` legitimo
    de input/handler React nao deve fazer o gate falhar).
  </action>
  <verify>
    <automated>cd /Users/gustavolopespaiva/dev/IdeiaOS/apps/cockpit && npm run build && grep -q "/vault" src/pages/CofreEspelho.tsx && ! grep -Eiq 'reveal|copiar|mostrar.?valor|navigator\.clipboard|secret.?value' src/pages/CofreEspelho.tsx; echo "exit=$?"</automated>
  </verify>
  <what-built>
    CofreEspelho.tsx: matriz var x project metadata-only (var_name, presenca, risk_tier, idade, committed), banner de doutrina,
    empty-state celebrado. Nenhum controle le/copia/revela um valor de segredo.
  </what-built>
  <how-to-verify>
    1. read-model + read.js no ar (`node source/console/ingest.js`; `node apps/cockpit/server/read.js &`; `cd apps/cockpit && npm run dev`).
    2. Abrir http://127.0.0.1:5273/ na rota Cofre-Espelho.
    3. frontend-visual-loop (criterio explicito): screenshot mostra a matriz var x project SO com metadata (nome/presenca/risco/idade),
       o banner de doutrina, e ZERO botao de copiar/revelar/mostrar-valor. Criterio NAO e "parece certo" — e "nenhuma celula exibe
       um valor de segredo e nenhum controle permite revela-lo".
    4. Inspecionar o DOM/Network (devtools): confirmar que nenhum valor de segredo aparece no markup nem no body de /vault
       (a prova exit-code disso e o gate Zero-Leak S5/S6 de 14.1-06).
  </how-to-verify>
  <acceptance_criteria>
    - `cd apps/cockpit && npm run build` exit 0.
    - Consome /vault: `grep -q "/vault" apps/cockpit/src/pages/CofreEspelho.tsx` exit 0.
    - METADATA-ONLY / SEM CONTROLE QUE REVELA VALOR (credential-isolation, exit-code de fonte — FIX 6, grep ESTREITO em verbos que revelam valor; NAO no `.value` generico): `! grep -Eiq 'reveal|copiar|mostrar.?valor|navigator\.clipboard|secret.?value' apps/cockpit/src/pages/CofreEspelho.tsx` exit 0 (nenhum controle de leitura/copia/revelacao de valor; a garantia REAL e a prova estrutural Zero-Leak S3/S4/S5 de 14.1-06).
    - Banner de doutrina + empty-state: `grep -Eiq 'metadados|metadata|espelho|doutrina' apps/cockpit/src/pages/CofreEspelho.tsx` exit 0.
    - risk_tier com LABEL textual (WCAG): `grep -Eq 'risk_tier|critical|sensitive|Badge' apps/cockpit/src/pages/CofreEspelho.tsx` exit 0.
    - frontend-visual-loop: screenshot anexado (matriz metadata-only + banner + zero botao de revelar/copiar), criterio explicito.
  </acceptance_criteria>
  <done>Cofre-Espelho renderiza a matriz metadata-only (sem nenhum controle que revela valor), banner de doutrina, empty-state celebrado; build verde + screenshot.</done>
  <resume-signal>Digite "approved" quando o screenshot mostrar a matriz var x project SO com metadata (sem valor, sem botao revelar/copiar) e o banner de doutrina — ou descreva o que falta.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /vault -> Cofre DOM/state | so metadata; nenhum valor de segredo no markup ou na resposta (Zero-Leak S4/S5/S6) |
| /fleet -> Frota | metadata de versao/heartbeat; drift por string, nunca semver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-03-I | Information Disclosure | Cofre matriz/controles | mitigate | matriz metadata-only; gate exit-code (estreito, verbos que revelam valor) proibe controle de copiar/revelar valor; a garantia REAL e a prova estrutural Zero-Leak (14.1-06) varrendo S3/S4/S5 desta tela (credential-isolation) |
| T-14.1-03-I2 | Information Disclosure | DOM da Frota | mitigate | so metadata de versao/heartbeat; nenhum segredo na superficie |
| T-14.1-03-T | Tampering | version-drift semver | mitigate | comparacao por string-equality; gate proibe semver (version-reset-migration-semver-trap) — 1.1.0 redux nao "perde" pra 1.36.0 |
</threat_model>

<verification>
- npm run build verde; Frota consome /fleet; Cofre consome /vault.
- Cofre metadata-only (sem controle que revela valor: reveal/copiar/mostrar-valor/clipboard/secret-value); banner + risk_tier textual.
- Frota version-drift por string (sem semver); remoto idade honesta.
- frontend-visual-loop: screenshots de Frota e Cofre (criterios explicitos).
</verification>

<success_criteria>
- Frota: card por maquina + heartbeat table + version-drift string-equality; remoto idade honesta.
- Cofre: matriz metadata-only sem nenhum controle que revela valor; banner de doutrina; empty-state celebrado.
- Ambas verdes por exit-code de fonte + screenshot.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-03-SUMMARY.md` when done
</output>
</content>
