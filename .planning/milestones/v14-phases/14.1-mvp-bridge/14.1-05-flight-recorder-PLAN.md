---
phase: "14.1"
plan: "14.1-05"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/cockpit/scripts/build-flight-recorder.mjs
  - apps/cockpit/src/flight-recorder.json
  - apps/cockpit/src/components/FlightRecorder.tsx
  - scripts/test-recorder.sh
  - apps/cockpit/package.json
autonomous: true
requirements: [R14-05]
must_haves:
  truths:
    - "A fita do Flight Recorder e derivada do git LOCAL (git log -- versions.lock + git show <H>:versions.lock | grep '^gsd='), NUNCA de um array literal — 13 commits reais, nao os 4 tidy do doc 71"
    - "A classificacao de ator e string-match determinista (subject ^wip: autosync OU author *.local => autosync, senao human) — reusa a logica de ingest.js:classifyActor, nao re-inventa"
    - "test:recorder re-deriva a fita do git e compara SET-to-SET com o render (flight-recorder.json) -> exit 0 se identico, exit 1 se divergiu; E >=1 no de reversao amber deve existir (A12)"
    - "Onde git nao tem linha gsd=, o no e <absent> — nunca inventado (LAW vs INTERPRETED)"
  artifacts:
    - path: "apps/cockpit/src/components/FlightRecorder.tsx"
      provides: "Fita SVG two-level step-line (1.36.0 topo / 1.1.0 base), color-by-state, hover tooltip mono"
      min_lines: 40
    - path: "scripts/test-recorder.sh"
      provides: "Gate A12 SET-to-SET git<->render + >=1 no amber"
      min_lines: 25
    - path: "apps/cockpit/src/flight-recorder.json"
      provides: "Fita derivada do git no build (snapshot imutavel)"
      contains: "gsd"
  key_links:
    - from: "apps/cockpit/scripts/build-flight-recorder.mjs"
      to: "versions.lock (git history)"
      via: "git log + git show <H>:versions.lock | grep '^gsd='"
      pattern: "versions.lock"
    - from: "scripts/test-recorder.sh"
      to: "apps/cockpit/src/flight-recorder.json"
      via: "re-derive do git, diff SET-to-SET, assert >=1 reversal"
      pattern: "flight-recorder.json"
---

<objective>
Construir o Flight Recorder v0 — o "naco de wow estrutural" (R14-05, doc 71): UMA reconstrucao read-only do
flip-flop real do pin `gsd` em `versions.lock`, derivada do git LOCAL do IdeiaOS (nao precisa nem do ref `cockpit`).
Fita SVG de dois niveis (`1.36.0` topo / `1.1.0` base) color-by-state, com o gate de invariante `test:recorder`
(A12): re-deriva a fita do git e compara SET-to-SET com o render -> exit 0 se identico, exit 1 se a UI divergiu,
e exige >=1 no de reversao amber (o flip-flop real existe).

Purpose: e Wave 1, totalmente independente das telas e do read-model — le o git diretamente. Carrega o
diferencial estrutural da fase. A fonte de dados ja FOI confirmada viva no repo (13 commits, pin atual `gsd=1.1.0`).
Output: build-step que deriva `flight-recorder.json` do git + componente `FlightRecorder.tsx` + gate `test-recorder.sh`.

**Reconciliacao que SOBREPÕE os design-docs (#2):** o flip-flop NAO sao os 4 nos tidy do doc 71 — sao 13 commits
reais. Renderizar do `git log -- versions.lock` + `git show <H>:versions.lock | grep '^gsd='` em build/run time,
NUNCA de um array hard-coded. Reusar a classificacao de ator de `ingest.js:classifyActor` (um check subject-only
ingenuo classifica errado o commit de 06-11, cujo subject NAO e `wip: autosync` mas o author e `*.local`).
`<absent>` onde git nao tem `gsd=`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-CONTEXT.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-RESEARCH.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-PATTERNS.md
@docs/ideiaos-console/71-mvp-wow-resolution.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: build-flight-recorder.mjs deriva flight-recorder.json do git (LAW, nunca array literal)</name>
  <files>apps/cockpit/scripts/build-flight-recorder.mjs, apps/cockpit/src/flight-recorder.json, apps/cockpit/package.json</files>
  <read_first>
    - source/console/ingest.js (a funcao classifyActor lines ~72-79: subject.startsWith('wip: autosync')=>autosync; author.endsWith('.local')=>autosync; '[bot]@'=>bot; senao human — REUSAR esta logica verbatim, nao re-inventar)
    - source/agentd/collect.js (readCommits: parse de `git log --format='%H|%cI|%s'` por split('|') — o padrao de leitura de git)
    - 14.1-RESEARCH.md "Code Examples > RQ3" (o reader exato, RAN nesta sessao: git log --reverse --format='%H|%cI|%s|%ae' -- versions.lock; por commit git show <H>:versions.lock | grep -m1 '^gsd=' | cut -d= -f2; <absent> se vazio) + "Pitfall 2" (a tabela dos 13 commits REAIS verificados; a subtileza do 06-11 .local e do 06-12 wip-autosync-mas-human)
    - 14.1-PATTERNS.md secao "Flight Recorder v0 > Source data" e "Git-read pattern" (host do sufixo (MacBook-Air-2)/(Mac-mini-de-Gustavo); le o git LOCAL, NAO o ref cockpit)
    - apps/cockpit/package.json (adicionar o npm script de prebuild e test:recorder)
  </read_first>
  <action>
    Criar `apps/cockpit/scripts/build-flight-recorder.mjs` (Node ESM, sem dep): executa
    `git log --reverse --format='%H|%cI|%s|%ae' -- versions.lock` no repo IdeiaOS e, por commit, `git show <H>:versions.lock`
    e extrai a 1a linha `^gsd=` (valor ou `<absent>` se ausente — NUNCA inventar). Classifica o ator com a MESMA regra de
    `ingest.js:classifyActor` (subject `^wip: autosync` OU author `*.local` => autosync; senao human) — copiar a logica, nao
    aproximar. Deriva o host do sufixo `(MacBook-Air-2)`/`(Mac-mini-de-Gustavo)` no subject quando presente. Marca um no como
    REVERSAL quando o ator e autosync E o pin mudou em relacao ao commit anterior (o daemon re-pinando — o flip-flop real).
    Escreve `apps/cockpit/src/flight-recorder.json` com o array de nos {hash8, iso, gsd, actor, host, subject, reversal}.
    Em package.json: adicionar script `"build:recorder": "node scripts/build-flight-recorder.mjs"` e encadea-lo no `build`
    (prebuild) para a fita ser regenerada do git a cada build. NENHUM array literal de pins no codigo — a fita vem do git.
  </action>
  <acceptance_criteria>
    - `cd apps/cockpit && node scripts/build-flight-recorder.mjs && test -s src/flight-recorder.json` exit 0.
    - A fita tem os 13 nos reais (nao 4): `node -e "const a=require('./apps/cockpit/src/flight-recorder.json');process.exit(a.length>=13?0:1)"` exit 0.
    - PROIBIDO ARRAY LITERAL DE PINS (reconciliacao #2): `! grep -E "\['?1\.36\.0'?,\s*'?1\.1\.0'?" apps/cockpit/scripts/build-flight-recorder.mjs apps/cockpit/src/components/FlightRecorder.tsx 2>/dev/null` exit 0 (nenhum flip-flop hard-coded).
    - CLASSIFICADOR REUSADO: `grep -q "wip: autosync" apps/cockpit/scripts/build-flight-recorder.mjs && grep -q "\.local" apps/cockpit/scripts/build-flight-recorder.mjs` exit 0 (ambas as regras de classifyActor, nao so subject).
    - >=1 no amber/reversal derivado: `node -e "const a=require('./apps/cockpit/src/flight-recorder.json');process.exit(a.some(n=>n.reversal)?0:1)"` exit 0.
    - `<absent>` honrado (nunca inventa): a chave gsd e string e em commits sem pin vale exatamente `<absent>` (grep no JSON nao acha gsd `null`): `! grep -q '"gsd": *null' apps/cockpit/src/flight-recorder.json` exit 0.
  </acceptance_criteria>
  <done>flight-recorder.json derivado do git (13 nos reais, classificador reusado, >=1 reversal, <absent> honrado); sem array literal de pins; encadeado no build.</done>
</task>

<task type="auto">
  <name>Task 2: FlightRecorder.tsx (SVG step-line color-by-state) + test-recorder.sh (gate A12 SET-to-SET)</name>
  <files>apps/cockpit/src/components/FlightRecorder.tsx, scripts/test-recorder.sh, apps/cockpit/package.json</files>
  <read_first>
    - apps/cockpit/src/flight-recorder.json (a fita gerada na Task 1 — fonte do render)
    - .aiox-core/scripts/diagnostics/health-dashboard/src/components/HealthScore.jsx (lines ~7-12, 34-77: SVG hand-rolled com geometria computada + cor INJETADA por status, nunca decoracao — a TECNICA a adaptar para a step-line)
    - apps/cockpit/src/index.css (tokens --status-success/--status-warning adicionados em 14.1-01; usar via oklch(var(--status-warning)) etc.; ouro reservado a frame/selecao)
    - scripts/check-cockpit.sh (contrato de gate exit 0=ok/1=falhou; estilo de header + bloco USO) e source/agentd/zeroleak-snapshot.sh (estilo de scan/exit)
    - 14.1-PATTERNS.md secao "Flight Recorder v0 > UI analog / LAW vs INTERPRETED" (no a (x_i, y_top|y_bottom) por pin; amber no no de reversao; verde discreto no fim estavel; hover tooltip mono "06-08 . gsd=1.36.0 . daemon (Mac-mini) . wip: autosync"; entrada draw-left-to-right ~600ms atras de prefers-reduced-motion; narrativa = annotation rotulada, nunca asserida como fato)
    - 14.1-RESEARCH.md "Validation Architecture" linha A12 (gate re-deriva do git == render; assert >=1 reversal; rodar em /tmp sandbox com autosync pausado)
  </read_first>
  <action>
    (a) `FlightRecorder.tsx`: componente que importa `@/flight-recorder.json` e desenha uma fita SVG de dois niveis
        (linha `1.36.0` no topo, `1.1.0` na base), uma `<circle>`/`<line>` por no, x computado por indice. Cor INJETADA por
        estado (HealthScore technique): `oklch(var(--status-warning))` num no de reversao (`reversal:true`); verde discreto
        `oklch(var(--status-success))` no `✓` do fim estavel; OURO so em frame/selecao (nunca "tudo certo"). COR NUNCA E O UNICO
        SINAL: cada no carrega tambem o texto do pin + icone (WCAG — A10). Hover -> tooltip mono `MM-DD . gsd=VAL . ATOR (HOST) . "SUBJECT"`.
        Click -> painel lateral. Entrada draw-left-to-right ~600ms gated por `@media (prefers-reduced-motion: reduce)`. A narrativa
        ("daemon reverteu human") aparece como annotation ROTULADA (ex.: tag "interpretacao"), nunca como fato asserido.
    (b) `scripts/test-recorder.sh` (gate A12): re-deriva a fita do git (mesmo reader da Task 1) num /tmp sandbox com autosync
        PAUSADO (autosync ja PAUSADO nesta maquina — verify-guards-in-sandbox-not-live-repo), compara SET-to-SET (conjunto de
        {hash8,gsd} ordenado) contra `flight-recorder.json` -> exit 0 se identico, exit 1 se divergiu. E exige >=1 no com
        `reversal:true` (>=1 no amber). Build script: exit 1 em falha (nao hook). Em package.json: `"test:recorder": "bash ../../scripts/test-recorder.sh"` (ou caminho absoluto resolvido a partir do root) que regenera a fita antes de comparar.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/test-recorder.sh` exit 0 e o gate roda: `cd /Users/gustavolopespaiva/dev/IdeiaOS && bash scripts/test-recorder.sh; echo $?` == 0 (SET-to-SET git==render + >=1 reversal).
    - GATE PEGA DIVERGENCIA (anti-teatro): mutar 1 no do flight-recorder.json num /tmp copy e rodar o gate apontado pro copy DEVE dar exit 1 (`a fita divergiu da fonte git`).
    - >=1 NO AMBER (A12): o gate falha (exit 1) se nenhum no tiver reversal:true — comprovar que a assercao existe: `grep -Eq 'reversal|amber|status-warning' scripts/test-recorder.sh` exit 0.
    - COR NAO E O UNICO SINAL (A10, exit-code de fonte): `grep -Eq 'gsd|subject|pin' apps/cockpit/src/components/FlightRecorder.tsx` exit 0 (texto do pin renderizado alem da cor) E `grep -q 'prefers-reduced-motion' apps/cockpit/src/components/FlightRecorder.tsx` exit 0 (motion gated).
    - Build do SPA com a fita: `cd apps/cockpit && npm run build` exit 0.
    - frontend-visual-loop (regime runtime/UI, criterio explicito): screenshot da fita montada mostra a step-line de dois niveis com >=1 no amber visivel e o texto do pin em cada no (criterio NAO e "parece certo" — e "a fita mostra >=1 no amber E o valor do pin textual"). Anexar no SUMMARY.
  </acceptance_criteria>
  <done>FlightRecorder.tsx desenha a fita SVG color-by-state (texto+icone alem de cor, motion gated); test-recorder.sh prova SET-to-SET git==render + >=1 amber, e pega divergencia; build verde + screenshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history (subjects de commit) -> classificador | subject e DADO; so string-MATCH (^wip: autosync), nunca interpretado como instrucao |
| fita renderizada -> afirmacao de fato | pin/ordem/reversao = LAW (git show); narrativa = annotation rotulada, nunca fato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-05-T | Tampering | fita hard-coded divergindo do git | mitigate | test:recorder re-deriva do git e compara SET-to-SET; gate exit 1 se divergiu; gate proibe array literal de pins |
| T-14.1-05-I | Information Disclosure | tooltip/painel da fita | accept | so metadata de commit (hash/iso/subject/pin); subject de commit nao carrega segredo (escopo versions.lock); nenhuma superficie de valor |
| T-14.1-05-T2 | Tampering | commit subject como instrucao (injection) | mitigate | classificador faz string-MATCH literal, nunca executa/interpreta o subject (anti-injection; Copiloto e v14.3) |
</threat_model>

<verification>
- build-flight-recorder.mjs deriva 13 nos do git (sem array literal; classificador reusado; <absent> honrado; >=1 reversal).
- test-recorder.sh: SET-to-SET git==render exit 0; pega divergencia (exit 1) num copy mutado; exige >=1 amber.
- FlightRecorder.tsx: cor+texto+icone, motion gated; build verde; screenshot da fita com no amber.
</verification>

<success_criteria>
- Fita derivada do git LOCAL (13 nos reais), nunca de array; classificador determinista reusado; <absent> honrado.
- A12 verde por exit-code: SET-to-SET git==render + >=1 no amber; gate pega divergencia.
- UI color-by-state com texto+icone (WCAG), motion gated; narrativa rotulada (nunca fato).
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-05-SUMMARY.md` when done
</output>
