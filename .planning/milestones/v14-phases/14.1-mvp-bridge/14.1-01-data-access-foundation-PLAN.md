---
phase: "14.1"
plan: "14.1-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/cockpit/server/read.js
  - apps/cockpit/src/index.css
autonomous: true
requirements: [R14-05, R14-06]
must_haves:
  truths:
    - "read.js expoe GET /overview, GET /fleet, GET /vault e GET /verify em loopback 127.0.0.1, lendo o read-model via node:sqlite (sem better-sqlite3, sem nova dep)"
    - "GET /vault retorna a matriz var x project SEM nenhuma coluna/campo `value` (credential-isolation estrutural)"
    - "GET /verify recomputa do disco no instante da pergunta (git show cockpit:...) e nao de cache (A6 Trust-Rate)"
    - "index.css define --status-success e --status-warning OKLCH (50-ux), nao definidos no scaffold ate agora; ouro permanece reservado a frame/selecao"
  artifacts:
    - path: "apps/cockpit/server/read.js"
      provides: "Endpoints de leitura /overview /fleet /vault /verify, loopback, node:sqlite"
      contains: "/fleet"
    - path: "apps/cockpit/src/index.css"
      provides: "Tokens de status --status-success / --status-warning"
      contains: "--status-success"
  key_links:
    - from: "apps/cockpit/server/read.js"
      to: "~/.ideiaos/console/read-model.db"
      via: "node:sqlite DatabaseSync SELECT (loopback)"
      pattern: "read-model.db"
    - from: "apps/cockpit/server/read.js (/verify)"
      to: "git show cockpit:snapshots/<MID>.json"
      via: "recompute-from-disk no instante da pergunta (nao cache)"
      pattern: "git show"
---

<objective>
Estabelecer a fundacao de acesso a dados das telas v14.1: estender o server loopback ja shipado
(`apps/cockpit/server/read.js`, `node:sqlite`, bind `127.0.0.1:3073`) com os endpoints que Overview/Frota/
Cofre consomem (`/overview`, `/fleet`, `/vault`) e o endpoint de verdade-contra-o-disco (`/verify`, A6
Trust-Rate). E adicionar ao `apps/cockpit/src/index.css` os tokens de status `--status-success`/
`--status-warning` (50-ux) que ainda NAO existem no scaffold — pre-requisito visual de Frota/Cofre/Flight
Recorder/Pulse.

Purpose: e Wave 1 — fundacao da qual 02/03/04 dependem. Centraliza o acesso a SQLite e a superficie loopback
num unico lugar auditavel (Zero-Leak surface S2/S3/S6), reusa o `node:sqlite` built-in (zero dep, supply-chain
nula — reconciliacao #1: NAO `better-sqlite3`).
Output: `read.js` estendido (4 endpoints novos) + tokens de status no `index.css`.

**Reconciliacao que SOBREPÕE os design-docs (verificada no codigo shipado):** o acesso a dados e o server
loopback `read.js` com `node:sqlite` (`DatabaseSync`), NAO `better-sqlite3`. v14.1 ESTENDE este server; nao
adiciona server novo nem dep nova. (CONTEXT §Stack diz "better-sqlite3" — esta errado; o codigo usa `node:sqlite`.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-CONTEXT.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-RESEARCH.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-PATTERNS.md
@specs/cockpit/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender read.js com /overview, /fleet, /vault (leitura, loopback, node:sqlite)</name>
  <files>apps/cockpit/server/read.js</files>
  <read_first>
    - apps/cockpit/server/read.js (o arquivo a modificar — replicar o contrato existente: openDb via node:sqlite DatabaseSync; handleMachines; loopback HOST='127.0.0.1'; CORS 'Access-Control-Allow-Origin: http://127.0.0.1:5273'; try/finally db.close; router em http.createServer)
    - source/console/schema.sql (colunas reais a consultar: tabela `machine` [machine_id, canonical_name, os_version, agentd_version, last_seen_epoch]; `machine_snapshot` [taken_epoch, payload_json]; `daemon_status` [label, pid, status_code, last_seen_epoch]; `project` [project_slug, supabase_project_id]; `api_key` [project_slug, var_name, present, expected, risk_tier, file_mtime_epoch, committed] — SEM coluna value)
    - 14.1-PATTERNS.md secao "App shell App.tsx + read.js" e "Shared Patterns > Loopback-bind + CORS" (contrato loopback-bind + CORS + finally db.close exato)
    - 14.1-RESEARCH.md "Code Examples > RQ1" (shape do router estendido) + "Architectural Responsibility Map" (read.js = unico lugar que abre o DB)
  </read_first>
  <action>
    Adicionar tres handlers de leitura ao `read.js`, cada um reusando `openDb()` (node:sqlite `DatabaseSync`),
    o header CORS `Access-Control-Allow-Origin: http://127.0.0.1:5273`, e o `finally { db.close() }` exatos do
    `handleMachines` existente. Registra-los no roteador `http.createServer` ao lado de `GET /machines` e `GET /health`.
    (a) `handleOverview` em GET /overview: contagens agregadas para o bento — total de maquinas (`SELECT COUNT(*) FROM machine`),
        total de projetos (`SELECT COUNT(*) FROM project`), e o estado doctor derivado (reusar a funcao `doctorFromPayload`
        ja existente sobre o snapshot mais recente por maquina). Saude-por-produto: onde idea-doctor nao roda (Lovable),
        o sub-sinal vem `n/a` (spec "Saude por produto" — nunca nota fabricada).
    (b) `handleFleet` em GET /fleet: por-maquina, JOIN `machine` + snapshot mais recente (mesmo padrao MAX(taken_epoch)
        do handleMachines) + `daemon_status`, retornando {machine_id, canonical_name, os_version, agentd_version,
        last_seen_epoch, last_doctor, daemons[], installed_versions} — version-drift por STRING-EQUALITY, nunca semver
        (learning version-reset-migration-semver-trap). Frescor honesto: incluir `last_seen_epoch` cru para a UI calcular
        "ultimo sinal ha Xh" (spec "Frescor honesto").
    (c) `handleVault` em GET /vault: matriz var x project a partir de `api_key`, retornando SOMENTE
        {project_slug, var_name, present, expected, risk_tier, file_mtime_epoch, committed} — METADATA-ONLY.
        O schema NAO tem coluna `value`; o SELECT NUNCA pode introduzir um campo `value` (credential-isolation).
    BIND LOOPBACK preservado (M-01): nao alterar `HOST='127.0.0.1'`; nenhum bind-all.
  </action>
  <acceptance_criteria>
    - `node -c apps/cockpit/server/read.js` exit 0 (sintaxe valida ESM).
    - Subindo o server (`READ_PORT=3099 node apps/cockpit/server/read.js &`), apos `node source/console/ingest.js`:
      `curl -sf http://127.0.0.1:3099/overview | grep -Eq '"machines"|"projects"'` exit 0;
      `curl -sf http://127.0.0.1:3099/fleet | grep -q machine_id` exit 0;
      `curl -sf http://127.0.0.1:3099/vault | grep -Eq 'var_name|risk_tier'` exit 0.
    - VAULT METADATA-ONLY (credential-isolation, exit-code): `curl -sf http://127.0.0.1:3099/vault | grep -qiw value` exit != 0 (NENHUM campo `value` no payload) — gate: `! curl -sf http://127.0.0.1:3099/vault | grep -qiw value`.
    - BIND LOOPBACK (M-01): `grep -Eq "host:[[:space:]]*['\"]127\.0\.0\.1|listen\([0-9A-Za-z_]+,[[:space:]]*HOST" apps/cockpit/server/read.js` exit 0 E `! grep -Eq "0\.0\.0\.0|host:[[:space:]]*['\"]::" apps/cockpit/server/read.js`.
    - `grep -q "'/fleet'" apps/cockpit/server/read.js && grep -q "'/overview'" apps/cockpit/server/read.js && grep -q "'/vault'" apps/cockpit/server/read.js` exit 0 (rotas registradas).
  </acceptance_criteria>
  <done>read.js serve /overview, /fleet, /vault em loopback; /vault e metadata-only (sem `value`); bind 127.0.0.1 preservado; node:sqlite reusado (sem dep nova).</done>
</task>

<task type="auto">
  <name>Task 2: /verify (recompute-from-disk, A6 Trust-Rate) + tokens de status no index.css</name>
  <files>apps/cockpit/server/read.js, apps/cockpit/src/index.css</files>
  <read_first>
    - apps/cockpit/server/read.js (apos a Task 1; adicionar handleVerify ao roteador)
    - source/lib/cockpit.sh (helpers de ref git-plumbing; como o snapshot vive em snapshots/<machine_id>.json no ref cockpit — `git show cockpit:snapshots/<MID>.json`)
    - apps/cockpit/src/index.css (o arquivo a modificar — bloco :root @layer base com tokens OKLCH "lightness chroma hue"; --brand-hue:75; --status-* AINDA NAO existem)
    - docs/ideiaos-console/50-ux-experience.md (linhas ~116-131: --status-success #4ADE80 oklch(0.80 0.18 150), --status-warning #FBBF24 oklch(0.83 0.16 85); "Ouro != estado" — ouro reservado a frame/selecao)
    - 14.1-PATTERNS.md secao "Flight Recorder v0 > Token grammar" e "Shared Patterns > Black-gold OKLCH" (planner ADICIONA --status-success/--status-warning; nao definidos no scaffold)
    - 14.1-RESEARCH.md "Open Questions > Q1/Q4" + spec "Verdade verificavel contra o disco" (verify recomputa do disco no instante, expoe "verificado ha Xs")
  </read_first>
  <action>
    (a) read.js: adicionar `handleVerify` em GET /verify?cell=... que RECOMPUTA do disco no instante da pergunta:
        para a celula consultada, executar `git show cockpit:snapshots/<machine_id>.json` (via child_process,
        argumentos constantes/validados — NUNCA interpolar input do usuario num shell) e comparar com o valor servido
        pelo read-model, retornando {verified:true|false, recomputed_at_epoch, source:"git-show-cockpit"}. NAO ler de cache —
        recomputar do disco e o requisito (A6, spec "Verdade verificavel"). A UI mostrara "verificado ha Xs" a partir de
        `recomputed_at_epoch`. Manter CORS + loopback + finally.
    (b) index.css: adicionar ao bloco `:root` os tokens `--status-success` e `--status-warning` no MESMO formato OKLCH
        "lightness chroma hue" dos tokens existentes (ex.: `--status-success: 0.80 0.18 150;` e `--status-warning: 0.83 0.16 85;`,
        valores do 50-ux). NAO redefinir `--brand`/`--primary` (ouro continua reservado a marca/frame/selecao — 50-ux "Ouro != estado").
  </action>
  <acceptance_criteria>
    - `node -c apps/cockpit/server/read.js` exit 0.
    - `/verify` recomputa do disco (nao cache): `grep -q "git show" apps/cockpit/server/read.js && grep -q "'/verify'" apps/cockpit/server/read.js` exit 0; e o handler usa argumentos constantes/validados (sem interpolacao de input em string de shell): `! grep -En "exec(Sync)?\([\`\"'].*\\\$\\{" apps/cockpit/server/read.js` exit 0.
    - TOKENS DE STATUS: `grep -q -- '--status-success' apps/cockpit/src/index.css && grep -q -- '--status-warning' apps/cockpit/src/index.css` exit 0.
    - OURO != ESTADO (50-ux): nenhum dos novos tokens reaproveita o hue de marca 75: `! grep -E -- '--status-(success|warning):[^;]*75' apps/cockpit/src/index.css` exit 0.
    - Build do SPA nao quebra: `cd apps/cockpit && npm run build` exit 0.
  </acceptance_criteria>
  <done>/verify recomputa do disco (git show) com timestamp; index.css ganha --status-success/--status-warning OKLCH (ouro intacto); build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-model.db -> read.js -> rede loopback | metadata atravessa; nenhum valor de segredo pode entrar no payload |
| browser (nao-confiavel) -> read.js | so 127.0.0.1 alcanca; CORS fixo a 127.0.0.1:5273 |
| /verify -> git (disco) | recompute do disco com argumentos constantes; nenhum input do usuario vira shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-01-I | Information Disclosure | GET /vault payload | mitigate | matriz metadata-only; gate exit-code prova ausencia de campo `value` no body servido (credential-isolation) |
| T-14.1-01-I2 | Information Disclosure | bind do server | mitigate | bind explicito 127.0.0.1 preservado; gate forbids 0.0.0.0/:: (M-01) — treasure-map contido ao loopback |
| T-14.1-01-T | Tampering | /verify recompute | mitigate | git show com argumentos constantes/validados; gate proibe interpolacao de input em string de shell (Excessive Agency) |
| T-14.1-01-SC | Tampering | node:sqlite no server | mitigate | modulo built-in do Node (sem npm install, sem better-sqlite3) — superficie de supply-chain nula (reconciliacao #1) |
</threat_model>

<verification>
- node -c read.js exit 0; build do SPA verde.
- curl loopback dos 4 endpoints; /vault sem campo `value`; bind 127.0.0.1 (sem 0.0.0.0/::).
- /verify recomputa do disco (git show, argumentos constantes); tokens --status-* presentes (ouro intacto).
</verification>

<success_criteria>
- read.js serve /overview /fleet /vault /verify em loopback via node:sqlite (sem dep nova).
- /vault metadata-only; /verify recompute-from-disk; bind 127.0.0.1 contido.
- index.css com tokens de status OKLCH (50-ux), ouro reservado.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-01-SUMMARY.md` when done
</output>
