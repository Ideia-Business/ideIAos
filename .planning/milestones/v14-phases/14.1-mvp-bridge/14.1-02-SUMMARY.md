---
phase: 14.1-mvp-bridge
plan: 14.1-02
subsystem: ui
tags: [react, vite, typescript, cockpit, bento, system-pulse, flight-recorder, wcag, seam]

# Dependency graph
requires:
  - phase: 14.1-01
    provides: read.js endpoints GET /overview e GET /fleet (loopback 127.0.0.1:3073)
  - phase: 14.1-05
    provides: FlightRecorder.tsx (banda SVG derivada do git, flight-recorder.json)
provides:
  - "App.tsx como shell de navegação das 3 telas (Overview/Frota/Cofre) por estado local — sem router lib"
  - "SEAM único em App.tsx: 3 rotas registradas + ponto-de-montagem estável do ⌘K (<CommandPalette/>)"
  - "3 stubs de seam para a Wave 3 preencher: pages/Frota.tsx, pages/CofreEspelho.tsx, components/CommandPalette.tsx"
  - "Overview.tsx: bento grid (System Pulse hero + StatCards + cards Frota/Segurança/Releases-SOAK lean/Atenção-Agora) + banda Flight Recorder"
  - "SystemPulse.tsx: heartbeat anima só no sinal local; remoto = idade honesta; crítico vermelho/arrítmico + label textual; motion gated"
  - "StatCard.tsx: tile title/value/subtitle/icon zero-motion para o bento"
affects: [14.1-03 (Frota/Cofre preenchem stubs), 14.1-04 (⌘K preenche CommandPalette), 14.1-06 (Zero-Leak varre S4/S5 sobre esta tela)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Nav por estado local (useState<Screen>) — sem router lib, sem dep nova"
    - "Seam de file-ownership: App.tsx registra rotas + ponto-de-montagem; planos da mesma wave só preenchem stubs, não editam App.tsx"
    - "Frescor honesto: animação só no heartbeat local fresco; remoto mostra idade textual (humanAge) de last_seen_epoch"
    - "Cor nunca é o único sinal (WCAG/A10): label textual + ícone + aria-label além da cor; pulse gated por prefers-reduced-motion"

key-files:
  created:
    - apps/cockpit/src/pages/Overview.tsx
    - apps/cockpit/src/components/SystemPulse.tsx
    - apps/cockpit/src/components/StatCard.tsx
    - apps/cockpit/src/pages/Frota.tsx (stub)
    - apps/cockpit/src/pages/CofreEspelho.tsx (stub)
    - apps/cockpit/src/components/CommandPalette.tsx (stub)
  modified:
    - apps/cockpit/src/App.tsx

key-decisions:
  - "App.tsx deixa de fazer fetch /machines+MachineCards; o data-fetch de overview migra para Overview.tsx (apiBase passado como prop). MachineCard.tsx fica intocado (Frota o reusa em 14.1-03)."
  - "SystemPulse poll ~3s de GET /fleet; nó LOCAL = o de maior last_seen_epoch; só ele anima e só se idade ≤300s (FRESH_LOCAL_S). Remotos nunca animam."
  - "Releases-SOAK lean: ≥2 máquinas ⇒ 'PRONTO PARA TAG' + 'n/2 ✓ span —/1d'; <2 ⇒ 'aguardando segundo ator'. Span de SOAK real não disponível neste plano (placeholder '—/1d'), nunca um relógio decorativo."
  - "Saúde-por-produto: checks.unknown (idea-doctor n/a, ex.: Lovable) renderiza 'n/a' e nunca soma como falha."

patterns-established:
  - "Stub de seam: componente mínimo com header SOURCE explicando qual plano da Wave 3 preenche o corpo; compila sob TS strict (sem var não-usada)"
  - "humanAge(ageSeconds): idade honesta legível ('último sinal há Xh'), nunca inventa frescor"

requirements-completed: [R14-05]

# Metrics
duration: ~25min
completed: 2026-06-22
---

# Phase 14.1 Plan 02: Overview & App Shell Summary

**App.tsx vira o shell de navegação das 3 telas (estado local, sem router) e o SEAM da Wave 3; Overview entrega o bento grid com System Pulse local-vivo (frescor honesto + WCAG) e a banda Flight Recorder.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 2/2 (Task 2 era checkpoint:human-verify — verificação visual deferida ao orquestrador, ver abaixo)
- **Files created:** 6 (3 deliverables + 3 stubs de seam)
- **Files modified:** 1 (App.tsx)

## Accomplishments

### Task 1 — App.tsx shell + nav + SEAM
- Reescrita de `App.tsx`: header black-gold preservado, `API_BASE` loopback `http://127.0.0.1:${READ_PORT}` preservado.
- Nav thin (sidebar 16px, ícones lucide, barra ativa ouro 2px) com `useState<Screen>` — **sem router lib, sem dep nova**.
- **SEAM**: as 3 rotas (`overview`/`frota`/`cofre`) registradas agora; Overview montada, Frota/Cofre apontam para stubs. `<CommandPalette/>` montado UMA VEZ fora do switch (ponto-de-montagem estável do ⌘K).
- `StatCard.tsx` criado (title/value/subtitle/icon, zero-motion, sem framer).

### Task 2 — Overview + SystemPulse
- `SystemPulse.tsx`: poll ~3s de `/fleet`; o nó local (maior `last_seen_epoch`) anima só se fresco (≤300s); crítico ⇒ ícone arrítmico **+ label textual "CRÍTICO" + aria-label**. Remotos mostram idade honesta `humanAge()` ("último sinal há Xh") e nunca animam. `pulse` gated por `@media (prefers-reduced-motion: reduce)`.
- `Overview.tsx`: fetch `GET /overview`; bento = System Pulse hero + StatCards (Máquinas/Projetos/Checks OK) + cards Frota/Segurança/Releases-SOAK **lean** (sem relógio decorativo — doc 71)/Atenção-Agora + banda `<FlightRecorder/>`.
- Saúde-por-produto honesta: `checks.unknown` ⇒ "n/a", nunca nota fabricada; monousuário (<2 máquinas) ⇒ "aguardando segundo ator", nunca card-fantasma.

## Stubs criados (o SEAM da Wave 3)

| Stub | Preenchido por | Conteúdo atual |
|------|----------------|----------------|
| `apps/cockpit/src/pages/Frota.tsx` | 14.1-03 | placeholder "Frota — em construção (14.1-03)" |
| `apps/cockpit/src/pages/CofreEspelho.tsx` | 14.1-03 | placeholder "Cofre-Espelho — em construção (14.1-03)" |
| `apps/cockpit/src/components/CommandPalette.tsx` | 14.1-04 | retorna `null` (sem listener ainda) |

14.1-03 e 14.1-04 **só substituem o corpo destes arquivos** — não editam App.tsx. Isso evita colisão de file-ownership na Wave 3.

## Acceptance criteria — exit-code (antifragile-gates)

| # | Critério | Comando | exit |
|---|----------|---------|------|
| Build | npm run build (todas as rotas resolvem via stubs) | `cd apps/cockpit && npm run build` | **0** |
| T1.AC2a | 3 rotas em App.tsx | `grep -Eq "Frota\|frota" && "Cofre\|cofre" && "Overview"` | **0** |
| T1.AC2b | ponto-de-montagem ⌘K | `grep -q "CommandPalette" App.tsx` | **0** |
| T1.AC3 | sem router lib/dep nova | `! grep -Eq "react-router\|@tanstack/router" package.json` | **0** |
| T1.AC4 | loopback preservado | `grep -q "127.0.0.1" App.tsx` | **0** |
| T2.AC2 | /overview + FlightRecorder | `grep -q "/overview" && "FlightRecorder" Overview.tsx` | **0** |
| T2.AC3a | frescor honesto (idade remota) | `grep -Eq 'last_seen\|último sinal\|idade' SystemPulse.tsx` | **0** |
| T2.AC3b | prefers-reduced-motion | `grep -q 'prefers-reduced-motion' SystemPulse.tsx` | **0** |
| T2.AC4 | cor não é o único sinal | `grep -Eq 'CRÍTICO\|label\|aria-label' SystemPulse.tsx` | **0** |
| T2.AC5 | Releases-SOAK lean (sem countdown) | `! grep -Eiq 'countdown\|contagem regressiva' Overview.tsx` | **0** |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Gate] Comentários tripavam o gate "sem countdown" (T2.AC5)**
- **Found during:** Task 2, sweep de gates.
- **Issue:** O gate `! grep -Eiq 'countdown|contagem regressiva' Overview.tsx` falhava (exit 1) porque meus PRÓPRIOS comentários explicativos diziam "SEM countdown" / "sem contagem regressiva". O card SOAK é genuinamente lean (não há relógio), mas o literal nos comentários disparava o grep cego.
- **Fix:** Reescrevi os 4 comentários para usar "relógio" no lugar dos literais proibidos, preservando a intenção. O gate passa (exit 0) e o card continua lean.
- **Files modified:** apps/cockpit/src/pages/Overview.tsx
- **Commit:** (incluído no commit do Task 2)

### Override de checkpoint (instrução direta do orquestrador)

**Task 2 era `checkpoint:human-verify` (gate="blocking")** com resume-signal "approved" via screenshot. O orquestrador instruiu explicitamente a NÃO parar: o executor não tem ferramentas de browser/screenshot, e o `frontend-visual-loop` consolidado roda no orquestrador APÓS a Wave 3 (quando Frota/Cofre/⌘K existirem). Por isso: implementei o Task 2 por completo, satisfiz TODOS os acceptance_criteria de exit-code, e construí os componentes screenshot-ready.

**Screenshot deferido ao orquestrador (passe visual pós-W3).** O critério `frontend-visual-loop` (regime runtime/UI — render+screenshot, não exit-code) NÃO foi executado neste plano por ausência de instrumento; é a única verificação pendente, e por desenho do orquestrador.

## Known Stubs

Os 3 stubs acima são **intencionais** (o seam) e serão preenchidos por 14.1-03 (Frota/Cofre) e 14.1-04 (⌘K) na Wave 3. Não bloqueiam o objetivo deste plano: a Overview está completa e o build é verde com as rotas resolvendo. O placeholder "—/1d" no span do card Releases-SOAK é honesto (o span de SOAK real não é exposto por /overview neste plano), nunca um valor fabricado.

## Threat surface scan

Nenhuma superfície de segurança nova além do `<threat_model>` do plano. SystemPulse e Overview renderizam SÓ metadata de `/overview` e `/fleet` (machine_id, contagens, last_seen_epoch, last_doctor) — nenhum valor de segredo no markup/state (Zero-Leak S4/S5 será varrido por 14.1-06). Nenhum endpoint novo, nenhuma escrita.
