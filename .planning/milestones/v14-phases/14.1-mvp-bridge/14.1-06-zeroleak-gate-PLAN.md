---
phase: "14.1"
plan: "14.1-06"
type: execute
wave: 4
depends_on: ["14.1-01", "14.1-02", "14.1-03", "14.1-04"]
files_modified:
  - source/agentd/zeroleak-snapshot.sh
  - scripts/test-zeroleak.sh
  - apps/cockpit/package.json
autonomous: true
requirements: [R14-06]
must_haves:
  truths:
    - "test:zeroleak varre 7 superficies (S1 ref snapshot, S2 read-model .dump, S3 schema PRAGMA, S4 estado React serializado, S5 DOM renderizado, S6 corpos de rede loopback, S7 logs), cada uma MATERIALIZADA em arquivo antes de varrer (exit-code, nunca Read tool)"
    - "Prova estrutural positiva bate o scan negativo: S3 assert que api_key NAO tem coluna value (PRAGMA table_info | grep -qiw value => FAIL)"
    - "Detector v14.1 = (a) regex de chave literal (reusa os 5 padroes shipados) + (b) entropia de Shannon >=4.0 com allowlist por NOME/SHAPE (machine_id sha256[:12], SHAs de commit, supabase_project_id publico, input_hash, hashes de audit-log) — nunca por valor"
    - "Dogfood-veneno DUPLO (anti-teatro): um sk-ant-FAKEKEY (camada regex) E um token novo de alta-entropia non-sk- (camada entropia) plantados em /tmp DEVEM fazer o gate sair !=0 — senao o gate e teatro"
  artifacts:
    - path: "scripts/test-zeroleak.sh"
      provides: "Gate 7-superficies (materializa cada uma em arquivo) + dogfood-veneno duplo"
      min_lines: 40
    - path: "source/agentd/zeroleak-snapshot.sh"
      provides: "Detector estendido: regex + camada de entropia com allowlist por nome/shape"
      contains: "entropia"
  key_links:
    - from: "scripts/test-zeroleak.sh"
      to: "7 superficies materializadas (/tmp)"
      via: "git show / sqlite3 .dump / PRAGMA / vite build+curl / logs => scan por exit-code"
      pattern: "table_info"
    - from: "scripts/test-zeroleak.sh"
      to: "dogfood-veneno (sk-ant-FAKEKEY + token alta-entropia)"
      via: "planta em /tmp, exige exit !=0"
      pattern: "FAKEKEY"
---

<objective>
Cravar o gate Zero-Leak (R14-06, A3) — o P0 de release: `npm run test:zeroleak` varre 7 superficies (S1-S7), cada
uma MATERIALIZADA em arquivo antes do scan por exit-code (nunca o Read tool), com prova estrutural positiva (S3:
api_key sem coluna value) + detector de DUAS camadas (regex literal + entropia de Shannon >=4.0 com allowlist por
nome/shape) + dogfood-veneno DUPLO (sk-ant-FAKEKEY que a regex pega E um token novo de alta-entropia que so a entropia
pega). Sai 0 em estado limpo; exit 1 em qualquer valor de segredo em qualquer superficie.

Purpose: e Wave 4 (depende de 01/02/03/04 — varre o UI renderizado: S4 estado React, S5 DOM, S6 corpos de rede das telas
Cofre/Frota/Overview e do /command). Carrega a prova A3 da fase. Estende o `zeroleak-snapshot.sh` shipado (regex-only,
1 superficie S1) para 7 superficies + a camada de entropia.

**Reconciliacao que SOBREPÕE os design-docs (#3):** o scanner v14.0 e regex-only (header A-01). v14.1 ADICIONA a camada
de entropia + allowlist por nome/shape — design-faithful (doc 78 §1.2), NAO invencao; NAO adiar. Mantem o dogfood-veneno
obrigatorio (planted sk-ant-FAKEKEY DEVE sair !=0) e adiciona um 2o veneno de alta-entropia non-sk- para exercer de fato
a camada de entropia (o veneno sk- sozinho so prova a regex). Surfaced para o usuario: Open Q A2 do RESEARCH — a leitura
adotada e que R14-06 ("P0 release gate") + CONTEXT §Zero-Leak + planning_context #3 exigem a entropia AGORA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-CONTEXT.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-RESEARCH.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-PATTERNS.md
@specs/cockpit/spec.md
@docs/ideiaos-console/78-test-verification-strategy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender zeroleak-snapshot.sh com a camada de entropia + allowlist por nome/shape</name>
  <files>source/agentd/zeroleak-snapshot.sh</files>
  <read_first>
    - source/agentd/zeroleak-snapshot.sh (o arquivo a modificar — scan_pattern + 5 regex shipados [sk-, gho_, JWT eyJ...\.eyJ, "role":"service_role", hex41+]; contrato exit 0=limpo/1=detectado/2=arg; header A-01 admite REGEX-only sem entropia/allowlist — esta tarefa adiciona a camada b)
    - docs/ideiaos-console/78-test-verification-strategy.md §1.2 (detector 2-camadas: (a) regex literal + (b) entropia Shannon >=4.0 com allowlist por NOME/SHAPE)
    - 14.1-PATTERNS.md secao "Zero-Leak gate > Detector v14.1 = (a) literal regex + (b) Shannon entropy" (allowlist: machine_id=12-hex, SHAs de commit, supabase_project_id publico, input_hash, audit hashes — por NOME/SHAPE, nunca por valor)
    - 14.1-RESEARCH.md "Pitfall 3" (o landmine: regex-only nao pega chave nova de alta-entropia; precisa de um 2o veneno non-sk-)
    - source/console/schema.sql (campos legitimos que NAO podem dar falso-positivo: machine_id 12-hex, supabase_project_id ~20 base32, input_hash) e source/agentd/collect.js (machine_id sha256[:12])
  </read_first>
  <action>
    Adicionar ao `zeroleak-snapshot.sh` uma 2a camada de deteccao apos os 5 regex: para cada token candidato (sequencia de
    >=20 chars [A-Za-z0-9_+/=.-]), computar a entropia de Shannon (bash/awk puro, sem dep) e flagar se >=4.0 — A MENOS que o
    token esteja na ALLOWLIST por NOME/SHAPE: machine_id (12-hex), SHAs de commit (40-hex em contexto de hash), supabase_project_id
    (base32-ish ~20 publico), input_hash, hashes de audit-log. A allowlist e por NOME do campo/SHAPE estrutural — NUNCA por valor
    (nunca listar um valor real). Manter o contrato de exit (0 limpo / 1 detectado / 2 arg) e os 5 regex existentes. Atualizar o
    header (remover/atualizar a NOTA A-01 "regex-only" — agora ha entropia + allowlist; bump label v14.1, NUNCA reintroduzir
    mission-control). Falso-positivo zero nos campos legitimos do SHAPE v1 (machine_id/supabase_project_id/input_hash).
  </action>
  <acceptance_criteria>
    - `bash -n source/agentd/zeroleak-snapshot.sh` exit 0.
    - CAMADA DE ENTROPIA presente: `grep -Eiq 'entropia|entropy|shannon' source/agentd/zeroleak-snapshot.sh` exit 0 E `grep -Eq '4\.0|>=4|4,0' source/agentd/zeroleak-snapshot.sh` exit 0 (limiar 4.0).
    - ALLOWLIST POR NOME/SHAPE (nunca valor): `grep -Eiq 'allowlist|machine_id|input_hash|supabase_project_id' source/agentd/zeroleak-snapshot.sh` exit 0.
    - VENENO DE ALTA-ENTROPIA pego (camada b — anti-teatro): `printf '{"tok":"Xq9Zk2Lm7Pw4Rt8Vn3Bc6Dh1Fj5Gs0Ay"}' > /tmp/zl_entropy.json && bash source/agentd/zeroleak-snapshot.sh /tmp/zl_entropy.json; echo $?` == 1 (token novo non-sk- de alta entropia bloqueado).
    - SEM FALSO-POSITIVO em campo legitimo: um snapshot so com machine_id 12-hex + supabase_project_id + input_hash NAO dispara: usar um snapshot real (`MID=$(ioreg -rd1 -c IOPlatformExpertDevice | awk -F'"' '/IOPlatformUUID/{print $4}' | shasum -a 256 | cut -c1-12); git show "cockpit:snapshots/$MID.json" > /tmp/zl_real.json && bash source/agentd/zeroleak-snapshot.sh /tmp/zl_real.json; echo $?`) == 0.
    - REGEX shipada preservada (sk- ainda pega): `printf '{"k":"sk-ant-0123456789abcdef0123"}' > /tmp/zl_sk.json && bash source/agentd/zeroleak-snapshot.sh /tmp/zl_sk.json; echo $?` == 1.
    - SEM mission-control no header atualizado: `! grep -q 'mission-control\|missioncontrol' source/agentd/zeroleak-snapshot.sh` exit 0.
  </acceptance_criteria>
  <done>zeroleak-snapshot.sh ganha a camada de entropia Shannon >=4.0 + allowlist por nome/shape; pega veneno de alta-entropia, zero falso-positivo nos campos legitimos, regex shipada preservada.</done>
</task>

<task type="auto">
  <name>Task 2: test-zeroleak.sh — 7 superficies materializadas + dogfood-veneno duplo (npm run test:zeroleak, A3)</name>
  <files>scripts/test-zeroleak.sh, apps/cockpit/package.json</files>
  <read_first>
    - source/agentd/zeroleak-snapshot.sh (apos Task 1 — o detector 2-camadas que test-zeroleak invoca por superficie)
    - source/console/schema.sql (S3: api_key sem coluna value — PRAGMA table_info(api_key) | cut -d'|' -f2 | grep -qiw value => FAIL)
    - apps/cockpit/server/read.js (S6: corpos de /overview /fleet /vault /command; S4/S5: o build do SPA serve o DOM/estado)
    - docs/ideiaos-console/78-test-verification-strategy.md §1.1 (as 7 superficies S1-S7 e como materializar cada uma em arquivo ANTES de varrer; dogfood-veneno obrigatorio "sk-ant-FAKE..." DEVE reprovar senao e teatro)
    - 14.1-PATTERNS.md secao "Zero-Leak gate" (tabela das 7 superficies + materializar por: S1 git show cockpit:snapshots; S2 sqlite3 .dump; S3 PRAGMA; S4 estado serializado; S5 DOM render; S6 corpos loopback; S7 logs; prova estrutural positiva bate scan negativo) + "Shared Patterns > Antifragile gate exit-code"
    - 14.1-RESEARCH.md "Open Questions > Q3" (materializacao de S4/S5/S6 para CI: preferir `vite build` -> servir dist/ em loopback -> curl o HTML+bundle para arquivos -> scan; chrome-devtools MCP so para o screenshot regime-R, nao para o gate exit-code)
    - .claude/rules/ideiaos-common-antifragile-gates.md (build script: exit 1 em falha; materializar em arquivo, test -s, nunca Read tool)
  </read_first>
  <action>
    Criar `scripts/test-zeroleak.sh` (build script: exit 1 em falha): materializar CADA uma das 7 superficies em um arquivo /tmp
    e varrer com o detector da Task 1 (ou o PRAGMA estrutural em S3), por exit-code:
    S1 ref snapshot: `git show cockpit:snapshots/<MID>.json > /tmp/s1` => scan;
    S2 read-model dump: `sqlite3 ~/.ideiaos/console/read-model.db .dump > /tmp/s2` => scan;
    S3 schema (PROVA ESTRUTURAL POSITIVA, bate scan negativo): `sqlite3 db 'PRAGMA table_info(api_key)' | cut -d'|' -f2 | grep -qiw value` => se achar `value`, FAIL;
    S4 estado React serializado: serializar o estado/SSR JSON para /tmp/s4 => scan;
    S5 DOM renderizado: `cd apps/cockpit && npm run build` e servir `dist/` em loopback, `curl` o HTML+bundle para /tmp/s5 => scan (regime exit-code; chrome-devtools so para o screenshot regime-R);
    S6 corpos de rede loopback: `curl` /overview /fleet /vault para /tmp/s6 => scan;
    S7 logs: stdout/err do read.js/agentd para /tmp/s7 => scan.
    DOGFOOD-VENENO DUPLO (anti-teatro, obrigatorio): plantar (1) `sk-ant-FAKEKEY...` (camada regex) e (2) um token novo de
    alta-entropia non-sk- (camada entropia) num /tmp snapshot e provar que o gate sai !=0 com cada um. Em package.json:
    `"test:zeroleak": "bash ../../scripts/test-zeroleak.sh"` (ou caminho absoluto a partir do root). Limpar os arquivos /tmp ao fim.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/test-zeroleak.sh` exit 0.
    - A3 (estado limpo): `cd apps/cockpit && npm run test:zeroleak; echo $?` == 0 (7 superficies limpas).
    - 7 SUPERFICIES materializadas: `grep -Eq 'table_info\(api_key\)' scripts/test-zeroleak.sh && grep -q '.dump' scripts/test-zeroleak.sh && grep -q 'git show' scripts/test-zeroleak.sh && grep -Eq 'dist|curl.*127\.0\.0\.1' scripts/test-zeroleak.sh` exit 0 (S1/S2/S3/S5/S6 presentes).
    - PROVA ESTRUTURAL S3 (positiva bate negativa): `grep -Eq "table_info\(api_key\).*value|grep -qiw value" scripts/test-zeroleak.sh` exit 0 (assert ausencia da coluna value).
    - DOGFOOD-VENENO regex: `printf '{"poison":"sk-ant-FAKEKEY0123456789abcdef"}' > /tmp/zl_poison_sk.json && bash source/agentd/zeroleak-snapshot.sh /tmp/zl_poison_sk.json; echo $?` == 1.
    - DOGFOOD-VENENO entropia (2o veneno, exerce a camada b): o gate referencia/planta um token de alta-entropia non-sk- e exige exit !=0 — `grep -Eiq 'veneno|poison|FAKEKEY|entrop' scripts/test-zeroleak.sh` exit 0 E o token de entropia da Task 1 sai 1.
    - MATERIALIZA-ANTES-DE-VARRER (antifragile-gates, nunca Read tool): cada superficie vira arquivo (`test -s`) antes do scan — `grep -Eq 'test -s|> */tmp/s[1-7]' scripts/test-zeroleak.sh` exit 0.
  </acceptance_criteria>
  <done>test:zeroleak varre 7 superficies materializadas em arquivo (S1-S7) + prova estrutural S3 (sem coluna value) + dogfood-veneno DUPLO (sk- regex + alta-entropia); A3 exit 0 em estado limpo, exit 1 com veneno.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| qualquer superficie (S1-S7) -> release | um valor de segredo em qualquer S reprova o build (P0, exit-code) |
| campo legitimo (machine_id/hash) -> detector | allowlist por nome/shape, nunca por valor — zero falso-positivo |
| superficie runtime (DOM/state/rede) -> arquivo | materializada em arquivo antes do scan (regime-2 vira regime-1 exit-code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-06-I | Information Disclosure | as 7 superficies | mitigate | scan por exit-code de cada superficie materializada; prova estrutural positiva S3 (api_key sem value) bate o scan negativo (credential-isolation, DoD #1) |
| T-14.1-06-T | Tampering | gate-teatro (passa com veneno) | mitigate | dogfood-veneno DUPLO obrigatorio (sk- + alta-entropia); o gate DEVE sair !=0 com cada veneno, senao e teatro (dogfood-review-tool-catches-own-defect) |
| T-14.1-06-T2 | Tampering | bind 0.0.0.0 expoe S5/S6 | mitigate | a materializacao de S5/S6 serve dist/ em loopback; o bind 127.0.0.1 ja gateado em 14.1-01 (M-01) |
| T-14.1-06-SC | Tampering | sqlite3/bash do gate | mitigate | sqlite3 do sistema + bash/awk puro (entropia sem dep); zero npm install novo no gate |
</threat_model>

<verification>
- bash -n; npm run test:zeroleak exit 0 (limpo); 7 superficies materializadas (S1-S7); prova estrutural S3.
- camada de entropia Shannon >=4.0 + allowlist por nome/shape; zero falso-positivo em campo legitimo.
- dogfood-veneno DUPLO (sk- + alta-entropia) => exit 1; sem mission-control.
</verification>

<success_criteria>
- A3 verde por exit-code: 7 superficies limpas, prova estrutural positiva (api_key sem value).
- Detector 2-camadas (regex + entropia com allowlist por nome/shape), design-faithful (doc 78).
- Dogfood-veneno DUPLO prova que o gate nao e teatro.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-06-SUMMARY.md` when done
</output>
