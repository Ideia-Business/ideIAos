---
phase: "14.1"
plan: "14.1-07"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ttt-baseline.sh
  - scripts/ttt-median.sh
autonomous: true
requirements: [R14-06]
must_haves:
  truths:
    - "ttt-baseline.sh --mode=bridge grava MEDICOES REAIS (modo `bridge`, segundos reais), nao mais a linha placeholder; mesmo formato TSV <jornada>\\t<modo>\\t<segundos>\\t<epoch>"
    - "ttt-median.sh --mode=bridge seleciona SOMENTE as linhas $2==bridge e imprime a mediana por jornada"
    - "O gate A2 `ttt-median.sh --mode=bridge | awk '$2>=10{f=1} END{exit f}'` exit 0 quando todas as jornadas (J1/J4/J2) tem mediana Bridge < 10s"
    - "As 3 jornadas medidas: J1 frota-saudavel, J4 chave-existe-e-idade (NUNCA valor), J2 pronto-pra-tag (soak_satisfied = >=2 maquinas + span>=1d sobre epochs gravados, nao wall-clock)"
  artifacts:
    - path: "scripts/ttt-baseline.sh"
      provides: "Modo bridge com medicao real (substitui placeholder)"
      contains: "bridge"
    - path: "scripts/ttt-median.sh"
      provides: "Flag --mode=bridge selecionando linhas bridge"
      contains: "--mode=bridge"
  key_links:
    - from: "scripts/ttt-median.sh --mode=bridge"
      to: "~/.ideiaos/console/ttt-baseline.tsv"
      via: "filtra $2==bridge, mediana por jornada, gate awk <10s"
      pattern: "bridge"
---

<objective>
Fechar a medicao Time-to-Truth do lado Bridge (R14-06, A2): estender `ttt-baseline.sh` para gravar MEDICOES
REAIS no modo `bridge` (hoje so grava `bridge-placeholder`), e `ttt-median.sh` para aceitar `--mode=bridge`
selecionando essas linhas, provando a mediana < 10s vs o baseline terminal (N>=5 ja gravado em 14.0-03).
Trust-Rate = verify-against-disk-now (consome o `/verify` de 14.1-01) — alvo 100%.

Purpose: e Wave 1, independente da UI (scripts bash puros). O baseline terminal ja existe (14.0-03); este plano
mede o lado Bridge e crava o gate A2. As 3 jornadas: J1 frota-saudavel, J4 chave-existe-e-idade (NUNCA o valor),
J2 pronto-pra-tag (`soak_satisfied` = >=2 maquinas + span>=1d sobre EPOCHS GRAVADOS, nao wall-clock —
learning soak-span-is-record-delta-not-wallclock).
Output: `ttt-baseline.sh` (modo bridge real) + `ttt-median.sh` (--mode=bridge).

**Reconciliacao que SOBREPÕE os design-docs (#4):** hoje `ttt-median.sh` NAO tem flag `--mode`; ele filtra OUT
`bridge-placeholder`/`bridge-dry-run` (o oposto de selecionar linhas bridge). E `ttt-baseline.sh --mode=bridge`
so escreve um `bridge-placeholder`. v14.1 grava o `bridge` REAL e seleciona-o.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v14-cockpit-PLAN.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-CONTEXT.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-RESEARCH.md
@.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-PATTERNS.md
@docs/ideiaos-console/78-test-verification-strategy.md
@.planning/milestones/v14-phases/14.0-substrate-spine/14.0-03-ttt-baseline-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ttt-baseline.sh --mode=bridge grava medicao REAL (substitui o placeholder)</name>
  <files>scripts/ttt-baseline.sh</files>
  <read_first>
    - scripts/ttt-baseline.sh (o arquivo a modificar — lines ~61-77 sao o bloco `--mode=bridge` que hoje grava `bridge-placeholder` e sai 0; lines ~95-157 sao a cronometragem real do modo terminal e o append TSV; manter o formato `<jornada>\t<modo>\t<segundos>\t<epoch>`)
    - scripts/ttt-median.sh (lines ~52-81: median_for_journey; ele EXCLUI `bridge-placeholder`/`bridge-dry-run` — entender porque o modo bridge precisa gravar literalmente `bridge`, nao um sufixo)
    - 14.1-RESEARCH.md "Pitfall 4" (o landmine exato: --mode=bridge nao existe; placeholder em vez de medicao real) + "RQ5"
    - 14.1-PATTERNS.md secao "TtT harness (Bridge side)" (3 jornadas J1/J4/J2; soak_satisfied = >=2 maq + span>=1d sobre epochs gravados; Trust-Rate via /verify)
    - source/console/schema.sql (soak_heartbeat, daemon_status, api_key — o que cada jornada Bridge consulta no read-model)
  </read_first>
  <action>
    Substituir o bloco placeholder de `--mode=bridge` por uma MEDICAO REAL: cronometrar a jornada respondida via a
    Bridge (o read-model/endpoints loopback de 14.1-01, nao o terminal) com `date +%s%N` antes/depois e gravar
    `printf '%s\t%s\t%s\t%s\n' "$JORNADA" "bridge" "$SECONDS_ELAPSED" "$NOW_EPOCH"` (modo literal `bridge`, NAO
    `bridge-placeholder`). As 3 jornadas: J1 (frota saudavel — query /fleet + daemon_status), J4 (chave X existe +
    idade NUNCA valor — query /vault, so metadata), J2 (pronto pra tag — `soak_satisfied`: >=2 maquinas distintas com
    PASS + span>=1d entre EPOCHS GRAVADOS no ledger soak, nao wall-clock; learning soak-span-is-record-delta-not-wallclock).
    Manter `--dry-run` (estrutura, exit 0) e o modo terminal intactos. Nao-interativo (pipe) grava medicao imediata como
    hoje, mas com modo `bridge`. NUNCA expor valor de chave em J4 (credential-isolation).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/ttt-baseline.sh` exit 0.
    - Grava modo literal `bridge` (nao placeholder): `printf 'J1\n' | bash scripts/ttt-baseline.sh J1 --mode=bridge >/dev/null; tail -1 ~/.ideiaos/console/ttt-baseline.tsv | awk -F'\t' '$2=="bridge"{exit 0} {exit 1}'` exit 0.
    - `--dry-run` ainda sai 0 sem gravar bridge real: `bash scripts/ttt-baseline.sh J1 --mode=bridge --dry-run; echo $?` == 0.
    - J4 NUNCA grava/loga valor de chave (credential-isolation): `! grep -Eq 'sk-[A-Za-z0-9]{16,}|eyJ[A-Za-z0-9_-]{20,}\.eyJ' scripts/ttt-baseline.sh` exit 0 (nenhum valor de segredo na fonte do harness).
    - Formato TSV preservado (4 colunas tab): `tail -1 ~/.ideiaos/console/ttt-baseline.tsv | awk -F'\t' 'NF==4{exit 0} {exit 1}'` exit 0.
  </acceptance_criteria>
  <done>ttt-baseline.sh --mode=bridge grava medicao real (modo `bridge`); --dry-run intacto; J4 metadata-only; formato TSV preservado.</done>
</task>

<task type="auto">
  <name>Task 2: ttt-median.sh --mode=bridge seleciona linhas bridge + gate A2 (<10s)</name>
  <files>scripts/ttt-median.sh</files>
  <read_first>
    - scripts/ttt-median.sh (o arquivo a modificar — lines ~38-46 parse do arg posicional TSV; lines ~52-81 median_for_journey que hoje EXCLUI bridge-placeholder/dry-run; lines ~83-87 loop J1/J4/J2 imprimindo `<jornada>\t<mediana>`)
    - scripts/ttt-baseline.sh (apos Task 1 — confirma que as linhas bridge tem `$2=="bridge"`)
    - 14.1-RESEARCH.md "Validation Architecture" linha A2 (gate exato: `ttt-median.sh --mode=bridge | awk '$2>=10{f=1} END{exit f}'`)
    - 14.1-PATTERNS.md secao "TtT harness > Gate (A2)" (o awk de gate; saida 2 colunas jornada/mediana)
  </read_first>
  <action>
    Adicionar parse do flag `--mode=bridge` (e `--mode=terminal`, default terminal para nao regredir o baseline).
    Quando `--mode=bridge`, `median_for_journey` filtra SOMENTE linhas `$2=="bridge"` (em vez de excluir placeholders);
    quando terminal/default, manter o comportamento atual (exclui placeholders bridge — nao regride 14.0-03). Saida
    permanece 2 colunas `<jornada>\t<mediana>` por jornada J1/J4/J2, para o gate `awk '$2>=10{f=1} END{exit f}'` funcionar.
    Manter o arg posicional opcional de TSV customizado e o gate de TSV ausente/vazio (exit 1).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/ttt-median.sh` exit 0.
    - Flag presente: `grep -q -- '--mode=bridge' scripts/ttt-median.sh` exit 0.
    - SELECIONA bridge (nao exclui): com >=1 linha `bridge` < 10s por jornada gravada, `bash scripts/ttt-median.sh --mode=bridge` imprime 3 linhas e `bash scripts/ttt-median.sh --mode=bridge | awk -F'\t' '$2!="N/A"{c++} END{exit (c>=1)?0:1}'` exit 0.
    - GATE A2 (<10s): com medicoes bridge < 10s, `bash scripts/ttt-median.sh --mode=bridge | awk '$2>=10{f=1} END{exit f}'` exit 0.
    - NAO REGRIDE TERMINAL: `bash scripts/ttt-median.sh` (sem flag) ainda imprime as medianas terminais (baseline 14.0-03 intacto) — `bash scripts/ttt-median.sh | wc -l | tr -d ' '` == 3.
  </acceptance_criteria>
  <done>ttt-median.sh --mode=bridge seleciona linhas bridge e o gate A2 (<10s) sai 0 com medicoes bridge sub-10s; modo terminal intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jornada J4 -> medicao | mede so metadata (presenca/idade da chave); valor de chave NUNCA entra no harness/TSV/log |
| ledger soak -> jornada J2 | soak_satisfied derivado de epochs GRAVADOS (delta), nunca wall-clock (anti-fraude de maturacao) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-07-I | Information Disclosure | jornada J4 (chave) | mitigate | mede presenca/idade, NUNCA valor; gate exit-code prova ausencia de regex de segredo na fonte do harness (credential-isolation) |
| T-14.1-07-R | Repudiation | J2 soak maturacao | mitigate | span>=1d sobre epochs GRAVADOS (delta), nao wall-clock (soak-span-is-record-delta-not-wallclock) — nao da pra "esperar" maturar |
| T-14.1-07-T | Tampering | gate A2 tautologico | mitigate | gate seleciona linhas `bridge` reais (nao placeholder) e compara <10s; medicao real, nao stub |
</threat_model>

<verification>
- ttt-baseline.sh --mode=bridge grava `bridge` real (nao placeholder); J4 metadata-only; TSV 4-col; --dry-run intacto.
- ttt-median.sh --mode=bridge seleciona linhas bridge; gate A2 (<10s) exit 0; modo terminal nao regride (3 linhas).
</verification>

<success_criteria>
- Lado Bridge medido com numeros reais (3 jornadas), gate A2 (<10s) verde por exit-code.
- J4 mede metadata, nunca valor; J2 soak por delta de epochs gravados.
- Baseline terminal (14.0-03) preservado.
</success_criteria>

<output>
Create `.planning/milestones/v14-phases/14.1-mvp-bridge/14.1-07-SUMMARY.md` when done
</output>
