---
phase: 23-antifragile-gates
plan: 23-01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/lib/gates.sh
  - source/hooks/memory-export.sh
  - source/hooks/observe-session-end.sh
  - scripts/build-adapters.sh
  - source/rules/common/antifragile-gates.md
  - README.md
autonomous: true
requirements: [R6-01]
must_haves:
  truths:
    - "source/lib/gates.sh exists, is idempotent to double-source, and exports assert_nonempty / gate_output / require_file"
    - "memory-export.sh calls at least one gate function to verify its TMP_LIST output before committing"
    - "build-adapters.sh calls at least one gate function to verify each target output directory/file after copy"
    - "observe-session-end.sh calls at least one gate function to verify the append to observations.jsonl"
    - "antifragile-gates.md rule is present in source/rules/common/ with SOURCE header and zero HTML comments"
    - "All gate functions use binary test -s / exit codes only — no Read-tool inspection"
    - "Hooks remain fail-silent (exit 0 on any gate failure) per IdeiaOS hook contract"
    - "README.md updated to reference source/lib/gates.sh"
  artifacts:
    - path: "source/lib/gates.sh"
      provides: "Reusable gate helper (assert_nonempty, gate_output, require_file)"
      contains: "__IDEIAOS_GATES_LOADED"
    - path: "source/rules/common/antifragile-gates.md"
      provides: "Rule doc for antifragile gate pattern"
      contains: "SOURCE: IdeiaOS"
  key_links:
    - from: "source/hooks/memory-export.sh"
      to: "source/lib/gates.sh"
      via: "source \"$IDEIAOS_DIR/source/lib/gates.sh\""
      pattern: "gate_output|assert_nonempty|require_file"
    - from: "scripts/build-adapters.sh"
      to: "source/lib/gates.sh"
      via: "source"
      pattern: "gate_output|require_file"
---

<objective>
Absorb the OpenSquad anti-hallucination bash-gate pattern (R6-01). Create a reusable
shell helper at source/lib/gates.sh with three functions — assert_nonempty, gate_output,
require_file — that validate step I/O via binary `test -s` / exit codes only (never
trusting LLM Read). Apply the helper at ≥3 real points in existing hooks and build
scripts. Document the pattern as a rule. Update README to reference the new lib file.

Purpose: Prevent hallucinated outputs from silently propagating through hook and build
pipelines. Every step that produces a file artifact must be verified before the next step
consumes it. Principle sourced from OpenSquad runner.pipeline.md: "Do NOT rely on reading
the file with the Read tool to verify output. Use ONLY the bash test -s command — its
output is binary and cannot be hallucinated."

Output:
- source/lib/gates.sh (new)
- source/rules/common/antifragile-gates.md (new)
- source/hooks/memory-export.sh (modified — gate applied at TMP_LIST check)
- source/hooks/observe-session-end.sh (modified — gate applied at observations.jsonl append)
- scripts/build-adapters.sh (modified — gate applied at copy output check)
- README.md (modified — lib section added/updated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gustavolopespaiva/dev/IdeiaOS/.planning/REQUIREMENTS.md
@/Users/gustavolopespaiva/dev/IdeiaOS/source/hooks/memory-export.sh
@/Users/gustavolopespaiva/dev/IdeiaOS/source/hooks/observe-session-end.sh
@/Users/gustavolopespaiva/dev/IdeiaOS/scripts/build-adapters.sh
@/Users/gustavolopespaiva/dev/IdeiaOS/source/rules/common/token-economy.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create source/lib/gates.sh helper</name>
  <files>source/lib/gates.sh</files>
  <action>
Create source/lib/gates.sh from scratch. The file must be bash 3.2 compatible (no
[[ ]], no bash arrays with += syntax in guard checks, use [ ] test). No external
dependencies — no jq, no python3. Double-source guard using a named sentinel variable.

Header (first two lines, exact format):
  #!/usr/bin/env bash
  # SOURCE: IdeiaOS v2 | kind: lib | targets: claude,cursor

Immediately after the header, add the double-source guard:
  [ -n "${__IDEIAOS_GATES_LOADED:-}" ] && return 0
  __IDEIAOS_GATES_LOADED=1

Then implement the three exported functions. All three follow the same contract:
- ONLY use binary `test -s` or exit code of shell builtins — never cat/read the file
- Caller decides what to do on failure (hook callers: exit 0; build callers: exit 1)
- Return 0 on pass, non-zero on fail (do not echo to stdout in normal path)
- Optional second argument is a human label used in the stderr message on fail

Function 1 — assert_nonempty PATH [LABEL]
  Checks that PATH exists and is a non-empty regular file (test -s).
  On fail: print to stderr "gates: LABEL not found or empty: PATH" ; return 1
  On pass: return 0

Function 2 — gate_output PATH [LABEL]
  Alias / synonym for assert_nonempty intended for "output of a step" semantics.
  Implementation: call assert_nonempty "$@"

Function 3 — require_file PATH [LABEL]
  Checks that PATH exists as a regular file (test -f, not test -s — it may be empty
  for directories we just mkdir'd, but the common use is non-empty files, so use
  test -s for consistency with the others and to catch zero-byte ghost files).
  Implementation: call assert_nonempty "$@"

After the three functions, add a one-line comment:
  # End of gates.sh — sourced once per shell session via __IDEIAOS_GATES_LOADED guard
  </action>
  <verify>
    <automated>
bash -c '
set -euo pipefail
F="/Users/gustavolopespaiva/dev/IdeiaOS/source/lib/gates.sh"
# 1. File exists and non-empty
test -s "$F" || { echo "FAIL: gates.sh missing or empty"; exit 1; }
# 2. Double-source guard present
grep -q "__IDEIAOS_GATES_LOADED" "$F" || { echo "FAIL: no guard"; exit 1; }
# 3. Three functions declared
grep -q "assert_nonempty" "$F" || { echo "FAIL: no assert_nonempty"; exit 1; }
grep -q "gate_output"     "$F" || { echo "FAIL: no gate_output";     exit 1; }
grep -q "require_file"    "$F" || { echo "FAIL: no require_file";     exit 1; }
# 4. Smoke: source and call on a real file vs missing file
. "$F"
tmpf="$(mktemp)"
printf "hello" > "$tmpf"
assert_nonempty "$tmpf" "smoke-test" || { echo "FAIL: assert_nonempty returned non-0 on real file"; rm -f "$tmpf"; exit 1; }
rm -f "$tmpf"
assert_nonempty "/nonexistent/path/xyz123" "should-fail" 2>/dev/null && { echo "FAIL: assert_nonempty returned 0 on missing"; exit 1; } || true
# 5. Double-source is idempotent (second source must not error or redefine)
. "$F"
echo "PASS: gates.sh OK"
'
    </automated>
  </verify>
  <done>
source/lib/gates.sh exists with double-source guard, three functions (assert_nonempty,
gate_output, require_file), bash 3.2 compat, no jq/python3 deps, SOURCE header.
Smoke test passes: returns 0 on existing non-empty file, non-0 on missing file, idempotent
on double-source.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply gates at 3 real points + write rule doc</name>
  <files>
    source/hooks/memory-export.sh,
    source/hooks/observe-session-end.sh,
    scripts/build-adapters.sh,
    source/rules/common/antifragile-gates.md
  </files>
  <action>
Apply gates.sh at exactly three real validation points across the existing files.
All edits must preserve the fail-silent (exit 0) contract of hooks, and the fail-loud
(exit 1) contract of build-adapters.sh. No other logic changes — surgical insertions only.

--- Point 1: memory-export.sh (gate on TMP_LIST before committing) ---

Find the existing check:
  [ "$CHANGED" -eq 0 ] && exit 0

Immediately BEFORE that line, add the gates source and a gate call. The IDEIAOS_DIR
for hooks is not directly available, so derive it from the hook's own path using
"$(cd "$(dirname "$0")/../.." && pwd)" — but hooks are installed to ~/.claude/hooks/,
so the source path is not reachable at runtime from the installed copy. Use an env
var fallback instead: source the gates lib only if IDEIAOS_GATES is set, otherwise
define a minimal inline fallback function so the hook remains self-contained.

Concrete insertion (after the `trap cleanup_tmp EXIT` line, before the for loop):

  # Antifragile gate helper (R6-01): load from source or define inline fallback.
  # Never trusts Read tool output — binary test -s only.
  if [ -n "${IDEIAOS_DIR:-}" ] && [ -f "$IDEIAOS_DIR/source/lib/gates.sh" ]; then
    # shellcheck source=/dev/null
    . "$IDEIAOS_DIR/source/lib/gates.sh"
  else
    gate_output() { test -s "${1:-}" 2>/dev/null; }
  fi

Then, immediately before the `[ "$CHANGED" -eq 0 ] && exit 0` line, add:

  # Gate: verify TMP_LIST is a real file before plumbing commit (not a hallucination).
  # Fail-silent per hook contract: gate failure → treat as no changes, exit 0.
  if [ "$CHANGED" -gt 0 ] && ! gate_output "$TMP_LIST" "memory-export/TMP_LIST"; then
    exit 0
  fi

--- Point 2: observe-session-end.sh (gate on observations.jsonl after append) ---

Find the line:
  printf '%s\n' "$REC" >> "$OBS_DIR/observations.jsonl" 2>/dev/null || true

Replace that line with:

  printf '%s\n' "$REC" >> "$OBS_DIR/observations.jsonl" 2>/dev/null || true
  # Gate R6-01: verify append actually produced a non-empty file (binary check only).
  # Inline gate — hooks are self-contained; no IDEIAOS_DIR dependency at install time.
  test -s "$OBS_DIR/observations.jsonl" 2>/dev/null || {
    printf '[observe-session-end] WARNING: observations.jsonl missing or empty after append\n' >&2
  }

(This is a warn-only gate because the hook must exit 0 regardless; the warning goes to
stderr so it appears in hook logs without blocking the session.)

--- Point 3: build-adapters.sh (gate on hook copy output after build_claude) ---

Inside the build_claude() function, after the hooks copy loop that ends with:
  echo "  hook: $fname → $CLAUDE_HOOKS_DIR/"

Add, still inside the loop body (before the `done`):

    # Gate R6-01: verify the copied hook file landed and is non-empty.
    if [ -z "${DRY_RUN:-}" ] || [ "$DRY_RUN" = "false" ]; then
      # shellcheck source=/dev/null
      [ -n "${IDEIAOS_DIR:-}" ] && [ -f "$IDEIAOS_DIR/source/lib/gates.sh" ] \
        && . "$IDEIAOS_DIR/source/lib/gates.sh" 2>/dev/null || true
      type gate_output >/dev/null 2>&1 || gate_output() { test -s "${1:-}" 2>/dev/null; }
      gate_output "$CLAUDE_HOOKS_DIR/$fname" "build-claude/hook/$fname" \
        || { echo "ERROR: hook copy failed or produced empty file: $fname" >&2; exit 1; }
    fi

The gate is skipped in dry-run mode (consistent with the existing run() wrapper pattern).
In live mode, a missing/empty hook after cp is a hard error (exit 1) since build scripts
must fail loudly.

--- Rule doc: source/rules/common/antifragile-gates.md ---

Create this file with the following content exactly (use the same style as token-economy.md
— prose under ## headings, SOURCE comment at top, no HTML comments in body):

  <!--SOURCE: IdeiaOS v2 | kind: rule | targets: claude,cursor-->
  # Antifragile Gates — Bash I/O Validation

  ## Principle

  Never trust the Read tool to verify that a file was written. The Read tool is an LLM
  operation and can hallucinate content or silently succeed on a missing file. Use only
  binary bash exit codes: `test -s PATH` returns 0 if the file exists and is non-empty,
  non-zero otherwise. That exit code cannot be hallucinated.

  Source: OpenSquad runner.pipeline.md — "Do NOT rely on reading the file with the Read
  tool to verify output. Use ONLY the bash test -s command — its output is binary and
  cannot be hallucinated."

  ## The Helper: source/lib/gates.sh

  All IdeiaOS hooks and build scripts that produce file artifacts must gate their outputs
  using source/lib/gates.sh. Source it once per script:

      . "$IDEIAOS_DIR/source/lib/gates.sh"

  Three functions (all bash 3.2 compat, no-jq, no python3):

  - `assert_nonempty PATH [LABEL]` — fails if PATH is missing or zero bytes
  - `gate_output PATH [LABEL]` — synonym for assert_nonempty (output-validation semantics)
  - `require_file PATH [LABEL]` — synonym for assert_nonempty (dependency-check semantics)

  Double-source guard: `__IDEIAOS_GATES_LOADED` prevents repeated function redefinition.

  ## Hook Contract vs Build Contract

  Hooks (Stop / UserPromptSubmit triggers) MUST exit 0 on any failure — a crashing hook
  blocks the IDE session. When a gate fails inside a hook, log a warning to stderr and
  exit 0. Never exit non-zero from a hook.

  Build scripts (scripts/*.sh) MUST exit 1 on gate failure. A silent bad build is worse
  than a loud failure.

  ## When to Use

  Apply a gate after every step that is expected to write a file artifact and whose output
  will be consumed by a subsequent step. Minimum 3 gate points per script that handles
  file I/O pipelines.

  ## Inline Fallback

  When a hook is installed to ~/.claude/hooks/ and IDEIAOS_DIR is not available at
  runtime, define the minimal inline fallback before using any gate:

      type gate_output >/dev/null 2>&1 \
        || gate_output() { test -s "${1:-}" 2>/dev/null; }
  </action>
  <verify>
    <automated>
bash -c '
set -euo pipefail
REPO="/Users/gustavolopespaiva/dev/IdeiaOS"

# Point 1: memory-export.sh references gate_output
grep -q "gate_output" "$REPO/source/hooks/memory-export.sh" \
  || { echo "FAIL: memory-export.sh missing gate_output"; exit 1; }

# Point 2: observe-session-end.sh has test -s on observations.jsonl
grep -q "test -s.*observations.jsonl" "$REPO/source/hooks/observe-session-end.sh" \
  || { echo "FAIL: observe-session-end.sh missing gate"; exit 1; }

# Point 3: build-adapters.sh references gate_output
grep -q "gate_output" "$REPO/scripts/build-adapters.sh" \
  || { echo "FAIL: build-adapters.sh missing gate_output"; exit 1; }

# Rule doc exists with SOURCE header and no bare HTML comment in body
test -s "$REPO/source/rules/common/antifragile-gates.md" \
  || { echo "FAIL: rule doc missing"; exit 1; }
grep -q "SOURCE: IdeiaOS" "$REPO/source/rules/common/antifragile-gates.md" \
  || { echo "FAIL: rule doc missing SOURCE header"; exit 1; }
# hooks must still exit 0 — verify no bare exit 1 added to hooks
grep -n "^exit 1" "$REPO/source/hooks/memory-export.sh" \
  && { echo "FAIL: memory-export.sh has bare exit 1 (breaks hook contract)"; exit 1; } || true
grep -n "^exit 1" "$REPO/source/hooks/observe-session-end.sh" \
  && { echo "FAIL: observe-session-end.sh has bare exit 1 (breaks hook contract)"; exit 1; } || true

echo "PASS: all 3 gate points verified"
'
    </automated>
  </verify>
  <done>
gate_output/gate pattern present in memory-export.sh, test -s gate present in
observe-session-end.sh, gate_output present in build-adapters.sh, rule doc at
source/rules/common/antifragile-gates.md with SOURCE header. Hook contract preserved
(no bare exit 1 in hook files). Build script fails loudly on bad copy.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README.md to reference source/lib/</name>
  <files>README.md</files>
  <action>
Read the current README.md. Find the section that describes the source/ directory layout
(search for "source/" or "hooks/" in the structure listing). Add "lib/" to the directory
tree under source/ if it is not already present, with a one-line description:

  lib/          — Shared shell helpers (gates.sh: antifragile I/O validation)

If there is no source/ directory tree in the README, append a minimal "## Source layout"
section at the end of the file (before any trailing newlines) with this content:

  ## Source layout (source/)

  | Path | Purpose |
  |------|---------|
  | hooks/ | Claude Code Stop / event hooks |
  | agents/ | Agent .md definitions |
  | rules/ | Cursor + Claude rules |
  | lib/ | Shared shell helpers (gates.sh: antifragile I/O validation) |

Do not touch any other section of the README. Preserve all existing content exactly.
The change must be minimal — one line or one table row.
  </action>
  <verify>
    <automated>
bash -c '
grep -q "lib/" /Users/gustavolopespaiva/dev/IdeiaOS/README.md \
  && echo "PASS: README.md references lib/" \
  || { echo "FAIL: README.md missing lib/ reference"; exit 1; }
grep -q "gates" /Users/gustavolopespaiva/dev/IdeiaOS/README.md \
  && echo "PASS: README.md mentions gates" \
  || { echo "FAIL: README.md missing gates mention"; exit 1; }
'
    </automated>
  </verify>
  <done>
README.md updated with a reference to source/lib/ and gates.sh. All other content
preserved verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook stdin → bash | JSON Stop payload from Claude Code — already sandboxed; gates.sh adds no new surface |
| gates.sh sourcing | Sourced via absolute path or IDEIAOS_DIR; path traversal impossible in normal install |
| Inline fallback | Defined with `type gate_output >/dev/null 2>&1 ||` guard — cannot override existing function |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | gates.sh sourcing path | mitigate | Use absolute IDEIAOS_DIR; fallback to inline definition if not available — attacker cannot inject a fake gates.sh via PATH since we source by absolute path |
| T-23-02 | Denial of Service | hook gate failure | accept | Hooks are fail-silent (exit 0 on gate fail) per IdeiaOS contract — gate failure warns, never blocks session |
| T-23-03 | Information Disclosure | stderr gate messages | accept | Gate error messages contain only file paths (no secrets); memory-export secret-scan gate runs before any gate function sees content |
| T-23-SC | Tampering | no new npm/pip/cargo installs in this plan | accept | Pure bash, no package manager operations; slopcheck N/A |
</threat_model>

<verification>
R6-01 Acceptance Criteria Mapping:

| Criterion | Gate | Verification |
|-----------|------|-------------|
| ≥1 reusable helper (assert_nonempty / gate_output) | Task 1 smoke test | bash smoke sources gates.sh, calls assert_nonempty on real vs missing file |
| Applied at ≥3 real points | Task 2 automated check | grep confirms gate_output in memory-export.sh + build-adapters.sh; test -s gate in observe-session-end.sh |
| Documented in rule | Task 2 automated check | test -s on rule doc + SOURCE header grep |
| Hooks remain fail-silent (exit 0) | Task 2 automated check | grep -n "^exit 1" must return empty for both hook files |
| Bash 3.2 compat | Task 1 (design): no [[ ]], no += in guards | Manual review of gates.sh |
| No-jq | Task 1 (design): only test builtin | grep -v jq gates.sh |
| SOURCE header format | Task 1 header, Task 2 rule doc | grep "SOURCE: IdeiaOS" |
| README sync | Task 3 | grep lib/ README.md |
</verification>

<success_criteria>
- source/lib/gates.sh present, sources cleanly, double-source idempotent, smoke test PASS
- gate_output applied in memory-export.sh (TMP_LIST validation before plumbing commit)
- test -s gate applied in observe-session-end.sh (observations.jsonl after append)
- gate_output applied in build-adapters.sh (hook copy output in build_claude)
- source/rules/common/antifragile-gates.md present with SOURCE: IdeiaOS header, no bare HTML comment in body
- README.md references source/lib/ and gates
- Both hooks still contain only exit 0 at top-level (grep -n "^exit 1" returns empty)
- R6-01 acceptance criteria (≥1 helper, ≥3 points, rule doc) fully met
</success_criteria>

<output>
Create `.planning/phases/23-antifragile-gates/23-01-SUMMARY.md` when done.
</output>
