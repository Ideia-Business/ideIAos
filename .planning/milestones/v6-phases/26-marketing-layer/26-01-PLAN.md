---
phase: 26-marketing-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - security/quarantine/26-marketing/
  - source/rules/marketing/
requirements: [R6-07]
autonomous: false
must_haves:
  truths:
    - "As 22 best-practices do OpenSquad existem em source/rules/marketing/ como conhecimento de conteúdo por formato"
    - "Cada best-practice promovida carrega atribuição MIT no formato # SOURCE (markdown, nunca <!--)"
    - "scan-absorbed.sh roda contra a quarentena e retorna exit 0 (PASS) antes de qualquer promoção"
  artifacts:
    - path: "security/quarantine/26-marketing/"
      provides: "Staging area versionada com >=20 best-practices de terceiros antes do scan"
      contains: "copywriting.md"
    - path: "source/rules/marketing/"
      provides: "22 rules de marketing promovidas (8 disciplinas + 14 plataformas)"
      contains: "copywriting.md"
    - path: "source/rules/marketing/README.md"
      provides: "Indice das 22 rules + mapa formato->arquivo que os content agents consomem"
      min_lines: 20
  key_links:
    - from: "security/quarantine/26-marketing/"
      to: "scan-absorbed.sh"
      via: "bash security/scan-absorbed.sh security/quarantine/26-marketing/"
      pattern: "RESULTADO: (LIMPO|APROVADO)"
    - from: "source/rules/marketing/*.md"
      to: "atribuicao MIT"
      via: "header # SOURCE: OpenSquad MIT renatoasse/opensquad"
      pattern: "# SOURCE: OpenSquad MIT"
---

<objective>
Absorver as 22 best-practices de conteudo do OpenSquad (8 disciplinas + 14 plataformas) para `source/rules/marketing/` via o pipeline de quarentena obrigatorio (`scan-absorbed.sh`), com atribuicao MIT em cada arquivo. Estas rules sao a base de conhecimento de conteudo que os content agents (Plano 02) e o orquestrador `/marketing` (Plano 03) consomem por formato — o equivalente IdeiaOS ao diretorio `_opensquad/core/best-practices/`.

Cumpre R6-07 e a decisao travada do PROJECT.md ("NENHUM conteudo de terceiros e absorvido sem passar por scan-absorbed.sh"). Sem este corpo de conhecimento, os agents de marketing seriam personas genericas — exatamente o anti-pattern que R6-03 combate.

Output: `security/quarantine/26-marketing/` (staging), `source/rules/marketing/*.md` (22 rules promovidas), `source/rules/marketing/README.md` (indice formato->arquivo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Pipeline de quarentena (obrigatorio — decisao travada)
@security/README.md
@security/scan-absorbed.sh

# Fonte de terceiros (OpenSquad MIT, clonado em /tmp/_cmp_opensquad)
# Diretorio fonte: /tmp/_cmp_opensquad/_opensquad/core/best-practices/
# Catalogo: /tmp/_cmp_opensquad/_opensquad/core/best-practices/_catalog.yaml

# Modelo de header de atribuicao ja em uso no repo (formato markdown, NAO <!--)
@source/agents/security-reviewer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copiar as 22 best-practices para quarentena e rodar scan-absorbed.sh</name>
  <files>security/quarantine/26-marketing/ (22 arquivos .md + _catalog.yaml de referencia)</files>
  <action>
Copiar TODAS as 22 best-practices de `/tmp/_cmp_opensquad/_opensquad/core/best-practices/*.md` (incluindo `_catalog.yaml` como referencia, que NAO conta como rule) para `security/quarantine/26-marketing/`. O diretorio de quarentena ja existe (criado por este plano); usar `cp` via Bash para o diretorio existente e aceitavel.

As 22 sao: 8 disciplinas — copywriting, researching, review, image-design, social-networks-publishing, strategist, technical-writing, data-analysis; 14 plataformas — instagram-feed, instagram-reels, instagram-stories, linkedin-post, linkedin-article, twitter-post, twitter-thread, youtube-script, youtube-shorts, email-newsletter, email-sales, blog-post, blog-seo, whatsapp-broadcast.

Copiar EXATAMENTE como estao (sem editar) — a quarentena e staging de terceiros sem modificacao, conforme security/README.md. NAO adicionar headers nesta etapa (a adicao de # SOURCE acontece so na promocao, Task 3, para que o scan veja o conteudo original).

Rodar o scan: `bash security/scan-absorbed.sh security/quarantine/26-marketing/`. Capturar exit code e saida. As 22 ja foram pre-verificadas pelo planner como limpas (sem `<!--`, sem `<script`/`data:`/`base64`, sem comandos suspeitos), entao o esperado e exit 0 com RESULTADO LIMPO ou APROVADO COM RESSALVA (AgentShield offline -> WARN, que nao bloqueia).
  </action>
  <verify>
    <automated>test -d security/quarantine/26-marketing && [ "$(ls -1 security/quarantine/26-marketing/*.md 2>/dev/null | grep -vc _catalog)" -ge 20 ] && bash security/scan-absorbed.sh security/quarantine/26-marketing/; echo "scan_exit=$?"</automated>
  </verify>
  <done>>=20 (na pratica 22) best-practices em security/quarantine/26-marketing/; scan-absorbed.sh retorna exit 0 (PASS). Se exit 1: BLOQUEADO — inspecionar o finding, nao promover (vai para o checkpoint da Task 2).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Revisar veredito do scan + atribuicao MIT antes de promover</name>
  <what-built>
22 best-practices de terceiros (OpenSquad MIT) copiadas para `security/quarantine/26-marketing/` e submetidas ao `scan-absorbed.sh`. O scan retornou seu veredito (esperado: LIMPO ou APROVADO COM RESSALVA).
  </what-built>
  <how-to-verify>
1. Ler a saida do scan da Task 1. Confirmar `RESULTADO: LIMPO` ou `RESULTADO: APROVADO COM RESSALVA`. Se `BLOQUEADO`, NAO prosseguir — inspecionar o arquivo apontado.
2. Se houver `WARN: comandos suspeitos`, abrir os arquivos apontados e confirmar que `curl`/`wget`/`ssh` aparecem apenas em prosa legitima, nunca como comando executavel. (Pre-analise do planner: nenhum dos 22 contem esses padroes — WARN deve vir so do AgentShield offline.)
3. Confirmar atribuicao MIT: fonte `renatoasse/opensquad`, licenca MIT (ver README.md do OpenSquad). A promocao (Task 3) deve carregar `# SOURCE: OpenSquad MIT renatoasse/opensquad | adapted: IdeiaOS v6`.
  </how-to-verify>
  <resume-signal>Digite "aprovado" para liberar a promocao das 22 rules, ou descreva o finding a investigar.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Promover as 22 best-practices para source/rules/marketing/ com header # SOURCE</name>
  <files>source/rules/marketing/*.md (22 arquivos), source/rules/marketing/README.md</files>
  <action>
Para CADA um dos 22 arquivos aprovados na quarentena, criar a versao promovida em `source/rules/marketing/{nome}.md` com o Write tool. A promocao e manual (nunca auto-promovida — security/README.md), preservando o corpo e a frontmatter YAML original (`id`, `name`, `whenToUse`, `version`), e inserindo o header de atribuicao.

POSICAO DO HEADER (CRITICO — nunca usar `<!--`): os best-practices tem frontmatter YAML (abrem com `---`). Inserir o header como a PRIMEIRA linha do corpo markdown, imediatamente apos o `---` de fechamento da frontmatter, antes do `# {Titulo}`. Formato EXATO:

# SOURCE: OpenSquad MIT renatoasse/opensquad | adapted: IdeiaOS v6

Isso satisfaz o scan-absorbed.sh (markdown `#`, nao `<!--` que dispararia FAIL no check 2) e a regra frontmatter-first (frontmatter YAML permanece como primeiro bloco). NAO converter para `<!--SOURCE-->` (formato proibido aqui pela missao e pelo scan).

Adaptacao IdeiaOS: NAO reescrever nem traduzir o corpo (escopo = absorcao fiel ao upstream; o corpo das best-practices e conhecimento de dominio consumido pelos agents, nao texto user-facing — traduzir seria scope creep e perderia fidelidade). Manter exatamente o conteudo aprovado no scan.

Apos promover os 22, criar `source/rules/marketing/README.md` com:
- Header `# SOURCE: OpenSquad MIT renatoasse/opensquad | adapted: IdeiaOS v6` na primeira linha.
- Explicacao de 2-3 linhas: base de conhecimento de conteudo da Camada de Marketing, consumida por formato pelos content agents (source/agents/mkt-*) e pelo orquestrador /marketing.
- Tabela `Formato -> Arquivo -> Quando usar` cobrindo as 8 disciplinas e 14 plataformas (derivar o `whenToUse` de cada entry do _catalog.yaml). Esta tabela e o indice que o injetor de formato do /marketing (Plano 03) usa — equivalente ao `_catalog.yaml` do OpenSquad, em idioma IdeiaOS.
- Nota de atribuicao MIT explicita ao final.
  </action>
  <verify>
    <automated>N=$(ls -1 source/rules/marketing/*.md 2>/dev/null | grep -vc README); HDR=$(grep -L "^# SOURCE: OpenSquad MIT" source/rules/marketing/*.md 2>/dev/null | grep -v README); BAD=$(grep -l '<!--' source/rules/marketing/*.md 2>/dev/null); echo "rules=$N (esperado >=22)"; echo "sem_header=${HDR:-nenhum}"; echo "com_comentario_html=${BAD:-nenhum}"; test "$N" -ge 22 && test -z "$HDR" && test -z "$BAD" && test -f source/rules/marketing/README.md && echo PROMOTE_OK</automated>
  </verify>
  <done>22 rules em source/rules/marketing/ (8 disciplinas + 14 plataformas); cada uma com header `# SOURCE: OpenSquad MIT` apos a frontmatter; zero arquivos com `<!--`; README.md com tabela formato->arquivo presente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenSquad (terceiro) -> source/ | Conteudo de terceiros (MIT) cruza para o codigo-fonte do IdeiaOS. Superficie de prompt injection (estudo ToxicSkills: 36% das skills publicas). |
| quarantine/ -> source/rules/marketing/ | Promocao manual apos scan PASS. Ponto onde conteudo malicioso entraria se a quarentena fosse pulada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | 22 best-practices de terceiros | mitigate | Quarentena obrigatoria + scan-absorbed.sh (Task 1) com gate binario exit-code; promocao so apos exit 0. |
| T-26-02 | Tampering | Prompt injection oculto (unicode invisivel) | mitigate | scan check 1 (Python3) bloqueia U+200B/202A-E/FEFF. FAIL automatico. |
| T-26-03 | Elevation | Payload HTML/JS inline (`<!--`, `<script`, base64) | mitigate | scan check 2 bloqueia. Reforco: Task 3 proibe `<!--` no header (usa `# SOURCE`); verify falha se houver `<!--`. |
| T-26-04 | Repudiation | Atribuicao de licenca ausente | mitigate | Header `# SOURCE: OpenSquad MIT renatoasse/opensquad` obrigatorio em todo arquivo promovido (Task 3 + checkpoint Task 2). |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Este plano nao instala pacotes (apenas copia/promove .md). Sem superficie de supply-chain. |
</threat_model>

<verification>
| Criterio | Como verificar | Req |
|----------|----------------|-----|
| >=20 best-practices em quarentena | `ls security/quarantine/26-marketing/*.md \| grep -vc _catalog` >= 20 | R6-07 |
| scan PASS | `bash security/scan-absorbed.sh security/quarantine/26-marketing/; echo $?` == 0 | R6-07 |
| 22 rules promovidas | `ls source/rules/marketing/*.md \| grep -vc README` == 22 | R6-07 |
| Atribuicao MIT em todas | `grep -L "^# SOURCE: OpenSquad MIT" source/rules/marketing/*.md \| grep -v README` vazio | R6-07 |
| Zero `<!--` | `grep -l '<!--' source/rules/marketing/*.md` vazio | R6-07 |
| Indice formato->arquivo | `test -f source/rules/marketing/README.md` | R6-07 |
</verification>

<success_criteria>
- 22 best-practices (8 disciplinas + 14 plataformas) absorvidas via quarentena -> scan PASS -> promovidas para source/rules/marketing/.
- Cada arquivo promovido tem header `# SOURCE: OpenSquad MIT renatoasse/opensquad | adapted: IdeiaOS v6` apos a frontmatter; zero `<!--`.
- README.md com tabela formato->arquivo serve de indice para o injetor de formato do /marketing (Plano 03).
- R6-07 criterio cumprido: ">=20 em quarentena -> scan PASS -> promovidas; header de atribuicao MIT; zero `<!--`".
</success_criteria>

<output>
Create `.planning/phases/26-marketing-layer/26-01-SUMMARY.md` when done.
</output>
