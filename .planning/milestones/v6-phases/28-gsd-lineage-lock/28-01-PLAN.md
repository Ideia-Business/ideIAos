---
phase: 28-gsd-lineage-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - versions.lock
  - scripts/check-versions-lock.sh
  - scripts/idea-doctor.sh
  - STATE.md
  - docs/CONTINUATION_HANDOFF.md
autonomous: true
requirements:
  - R6-11

must_haves:
  truths:
    - "versions.lock contém nota expandida identificando redis vs Pi, open-gsd vs gsd-build, e que 1.x redux != 3.x Pi"
    - "check-versions-lock.sh rejeita qualquer pin fora de 1.1.0 da linha redux com mensagem que nomeia a linhagem exata"
    - "idea-doctor.sh emite aviso direcional quando pin != 1.1.0 e menciona @opengsd/get-shit-done-redux"
    - "STATE.md e handoff registram linhagem definitiva como verdade documentada do projeto"
    - "nenhuma versao instalada foi alterada — blindagem e so documental/guard"
  artifacts:
    - path: "versions.lock"
      provides: "nota de linhagem expandida + pin gsd=1.1.0 intocado"
      contains: "get-shit-done-redux"
    - path: "scripts/check-versions-lock.sh"
      provides: "guard anti-revert de linhagem errada"
      contains: "redux"
    - path: "scripts/idea-doctor.sh"
      provides: "aviso direcional de versao vs linhagem"
      contains: "@opengsd/get-shit-done-redux"
  key_links:
    - from: "scripts/check-versions-lock.sh"
      to: "versions.lock"
      via: "leitura do pin gsd= em pre-commit"
      pattern: "gsd=1\\.1\\.0"
    - from: "scripts/idea-doctor.sh"
      to: "versions.lock"
      via: "read_lock gsd na secao 5"
      pattern: "get-shit-done-redux"
---

<objective>
Blindar definitivamente a linhagem do GSD: documentar no versions.lock que
@opengsd/get-shit-done-redux 1.1.0 (linha redux, versionamento recomeçou em 1.x)
é o pin canônico — completamente distinto de gsd-pi 3.0.0 (produto diferente),
de gsd-build (org diferente) e das versões pré-redux 1.36–1.42 (linha morta).

Reforçar check-versions-lock.sh e idea-doctor.sh para alertar de forma
direcional quando o pin sair de 1.1.0 da linha redux, nomeando a distinção
redux/Pi explicitamente na mensagem de erro.

Registrar a linhagem como verdade canônica em STATE.md e handoff.

Nada de mudança de versão instalada. Apenas documentação e guards.
Fecha R6-11 (causa-raiz do pin revertido 3x).

Purpose: Evitar a quarta reversão do pin eliminando toda ambiguidade sobre
qual pacote, org e linha de versão é o GSD legítimo do IdeiaOS.
Output: versions.lock com nota expandida, check-versions-lock.sh e
idea-doctor.sh com mensagens direcionais nomeando a linhagem, STATE + handoff
atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gustavolopespaiva/dev/IdeiaOS/.planning/REQUIREMENTS.md
@/Users/gustavolopespaiva/dev/IdeiaOS/versions.lock
@/Users/gustavolopespaiva/dev/IdeiaOS/scripts/check-versions-lock.sh
@/Users/gustavolopespaiva/dev/IdeiaOS/scripts/idea-doctor.sh
@/Users/gustavolopespaiva/dev/IdeiaOS/scripts/update-upstream.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir nota de linhagem no versions.lock</name>
  <files>versions.lock</files>
  <action>
    Reescreva o bloco de comentário da chave gsd= em versions.lock para
    documentar definitivamente a linhagem com três distinções explícitas.
    O bloco ATUAL tem 7 linhas de comentario; expanda para ~14 linhas.

    Conteudo obrigatorio na nova nota (prosa inline, sem markdown, sem <!--):

      1. LINHAGEM ATIVA: @opengsd/get-shit-done-redux v1.x (org: opengsd;
         plugin Claude Code; versionamento RECOMEÇOU em 1.x em 2025).
         Este é o pacote instalado em ~/.claude/get-shit-done/. Pin atual: 1.1.0.

      2. DISTINÇÃO redux vs Pi: gsd-pi é um produto DIFERENTE (v3.x,
         org diferente). Nunca confunda "GSD 3.x" com o redux — sao linhas
         incompativeis.

      3. DISTINÇÃO opengsd vs gsd-build: a org "open-gsd" no npm/GitHub
         hospeda o redux. A org "gsd-build" e outro ecossistema, nao usado
         aqui. Pacotes de gsd-build nao sao interoperaveis com o redux do
         IdeiaOS.

      4. POR QUE 1.1.0 > 1.36.0: o versionamento pré-redux (get-shit-done-cc,
         versoes 1.36–1.42) RECOMEÇOU em 1.x no redux. Logo 1.1.0 (redux)
         e mais novo que 1.36.0 (pré-redux). Esse fato causou o pin ser
         revertido 3x em 2026-06 (commits c7fc184, 3724ee9 e um agente de IA).

      5. Reiterar: unico escritor = scripts/update-upstream.sh --bump.
         NUNCA edite gsd= na mao. Pre-commit (check-versions-lock.sh) bloqueia.

    Constraints do arquivo:
    - bash 3.2: nao usar $'' syntax; apenas caracteres ASCII basicos no shebang/cabecalho
    - Cabecalho do arquivo: ja tem "# SOURCE: IdeiaOS v2" implicito no formato
    - Nunca usar <!-- em nenhum arquivo deste projeto
    - Formato do arquivo: chave=valor (um por linha); comentarios com #
    - NÃO altere o valor gsd=1.1.0 nem qualquer outro par chave=valor
    - NÃO adicione aspas aos valores
    - Mantenha a linha "# NÃO edite esta linha na mão." logo antes de gsd=1.1.0
    - updated= ao final deve permanecer como esta (nao e hoje; e a data do ultimo bump real)

    Leia o versions.lock atual inteiro antes de editar para nao perder nenhuma
    chave existente.
  </action>
  <verify>
    <automated>
      grep -c "get-shit-done-redux" /Users/gustavolopespaiva/dev/IdeiaOS/versions.lock
    </automated>
    <automated>
      grep -c "gsd-pi" /Users/gustavolopespaiva/dev/IdeiaOS/versions.lock
    </automated>
    <automated>
      grep -m1 "^gsd=" /Users/gustavolopespaiva/dev/IdeiaOS/versions.lock
    </automated>
  </verify>
  <done>
    versions.lock contem "get-shit-done-redux" e "gsd-pi" na nota expandida,
    o valor gsd=1.1.0 permanece inalterado, e nenhuma linha de chave=valor foi
    removida ou adicionada.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reforcar mensagens direcionais em check-versions-lock.sh e idea-doctor.sh</name>
  <files>scripts/check-versions-lock.sh, scripts/idea-doctor.sh</files>
  <action>
    PARTE A — check-versions-lock.sh (pre-commit guard):

    O script ja tem anti-legado (bloqueia 1.3x/1.4x) e anti-edicao-manual.
    Adicione um terceiro check: anti-Pi-drift. Se o pin gsd= for >= 2.0.0 ou
    >= 3.0.0, emitir erro explicando que gsd-pi (3.x) e um produto diferente,
    nao o redux que o IdeiaOS usa.

    Implementacao: adicione uma funcao is_gsd_pi() com case pattern "2.*|3.*|[4-9]*"
    retornando 0 (verdadeiro). Apos o bloco anti-legado (linha ~71) e antes do
    bloco --staged, insira:

      if is_gsd_pi "$NEW_GSD"; then
        echo + linha vermelha: "gsd=$NEW_GSD parece ser gsd-pi (linha 3.x/2.x)."
        echo + explicacao: "O IdeiaOS usa @opengsd/get-shit-done-redux (linha 1.x,
              org opengsd). gsd-pi e um produto diferente de org diferente."
        echo + acao: "Verifique ~/.claude/get-shit-done/VERSION e rode --bump somente
              em maquina com redux instalado."
        exit 1
      fi

    Atualize o cabecalho (bloco VALIDAÇÕES) para documentar os 3 checks:
      1. Anti-legado pré-redux (1.30–1.99)
      2. Anti-Pi-drift (2.x / 3.x = produto diferente)
      3. Anti-edicao-manual (mudanca no staged deve refletir instalacao)

    Mantenha toda logica existente intacta. Bash 3.2 (sem arrays associativos,
    sem $''). Sem <!--.

    PARTE B — idea-doctor.sh (health check):

    Secao 5 (Versões vs lock) ja tem is_legacy_gsd() e mensagem direcional.
    Adicione ao ramo de drift (bloco else na linha ~128) uma mensagem adicional
    quando o instalado for >= 2.0.0:

      elif is_gsd_pi "$GI"; then
        warn "GSD INSTALADO parece ser gsd-PI ($GI) — produto diferente do redux."
        warn "O IdeiaOS usa @opengsd/get-shit-done-redux (1.x, org opengsd)."
        warn "Remova o gsd-pi e instale o redux pelo marketplace do Claude Code."

    Adicione a funcao is_gsd_pi() logo apos is_legacy_gsd() na secao 5 (mesma
    logica do check-versions-lock.sh):

      is_gsd_pi() { case "$1" in 2.*|3.*|[4-9]*) return 0;; esac; return 1; }

    Tambem: na mensagem de warn do ramo is_legacy_gsd "$GSD_PIN" (pin legado),
    adicione ao final da linha:
      "(@opengsd/get-shit-done-redux, nao gsd-pi)"
    para tornar o aviso inequivoco.

    Constraints: bash 3.2, sem <!--, sem aspas nos valores do lock,
    # SOURCE: IdeiaOS v2 nao precisa aparecer nos scripts (ja esta no header
    geral do repo, nao nos scripts individuais).

    Leia cada script completo antes de editar para nao quebrar nenhuma logica
    existente.
  </action>
  <verify>
    <automated>
      bash /Users/gustavolopespaiva/dev/IdeiaOS/scripts/check-versions-lock.sh 2>&1
    </automated>
    <automated>
      grep -c "get-shit-done-redux" /Users/gustavolopespaiva/dev/IdeiaOS/scripts/check-versions-lock.sh
    </automated>
    <automated>
      grep -c "get-shit-done-redux" /Users/gustavolopespaiva/dev/IdeiaOS/scripts/idea-doctor.sh
    </automated>
    <automated>
      grep -c "is_gsd_pi" /Users/gustavolopespaiva/dev/IdeiaOS/scripts/check-versions-lock.sh
    </automated>
    <automated>
      grep -c "is_gsd_pi" /Users/gustavolopespaiva/dev/IdeiaOS/scripts/idea-doctor.sh
    </automated>
  </verify>
  <done>
    check-versions-lock.sh: sai com 0 e mensagem "pin gsd=1.1.0 valido" no
    working tree atual. Contem is_gsd_pi() e pelo menos 1 ocorrencia de
    "get-shit-done-redux". idea-doctor.sh: contem is_gsd_pi() e pelo menos
    1 ocorrencia de "@opengsd/get-shit-done-redux".
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar STATE.md e handoff com linhagem definitiva</name>
  <files>STATE.md, docs/CONTINUATION_HANDOFF.md</files>
  <action>
    STATE.md — adicione ou atualize a secao de decisoes tecnicas do projeto com
    um bloco "GSD — Linhagem Definitiva" registrando (em prosa concisa, ~6-8
    linhas, sem markdown aninhado):

      - Pacote: @opengsd/get-shit-done-redux, org opengsd, pin 1.1.0
      - Versionamento recomeçou em 1.x (redux mais novo que pré-redux 1.36+)
      - gsd-pi (3.x) e produto diferente; gsd-build e org diferente
      - Pin revertido 3x em 2026-06 (commits c7fc184, 3724ee9, agente IA)
      - Guardas: check-versions-lock.sh (pre-commit) bloqueia legado e Pi-drift
      - Unico escritor do pin: scripts/update-upstream.sh --bump

    Se STATE.md nao tiver secao de decisoes, crie "## Decisoes Tecnicas Canonicas"
    antes do fechamento do arquivo. Se ja houver secao relevante, insira o bloco
    la (nao duplique).

    CONTINUATION_HANDOFF.md — adicione no topo (apos o cabecalho de data/titulo,
    antes do primeiro ## de conteudo) um aviso destacado:

      ## Linhagem GSD — VERDADE CANONICA

      GSD neste projeto = @opengsd/get-shit-done-redux 1.1.0 (org opengsd).
      NAO e gsd-pi (3.x) nem pacote da org gsd-build.
      Pin revertido 3x — ver versions.lock (nota expandida) e check-versions-lock.sh.
      Proibido editar gsd= no versions.lock manualmente.

    Constraints: sem <!--, sem emojis novos alem dos ja presentes no arquivo,
    bash 3.2 nao aplicavel aqui (arquivos Markdown). Leia os dois arquivos
    completos antes de editar.
  </action>
  <verify>
    <automated>
      grep -c "get-shit-done-redux" /Users/gustavolopespaiva/dev/IdeiaOS/STATE.md
    </automated>
    <automated>
      grep -c "get-shit-done-redux" /Users/gustavolopespaiva/dev/IdeiaOS/docs/CONTINUATION_HANDOFF.md
    </automated>
  </verify>
  <done>
    STATE.md contem "get-shit-done-redux" e "gsd-pi" na secao de decisoes.
    docs/CONTINUATION_HANDOFF.md contem aviso de linhagem no topo com
    "@opengsd/get-shit-done-redux".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| editor humano/agente IA -> versions.lock | edicao manual nao-sancionada do pin |
| maquina stale -> --bump | maquina com instalacao pré-redux rodando --bump e revertendo pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigacao |
|-----------|----------|-----------|-------------|-----------|
| T-28-01 | Tampering | versions.lock gsd= | mitigate | check-versions-lock.sh bloqueia no pre-commit; is_gsd_pi() adicionada neste plano |
| T-28-02 | Tampering | --bump em maquina pré-redux | mitigate | update-upstream.sh ja recusa is_legacy_gsd; Task 2 adiciona recusa para is_gsd_pi |
| T-28-03 | Information Disclosure | nota de linhagem insuficiente | mitigate | Task 1 expande a nota com 5 pontos distintos; Task 3 replica em STATE + handoff |
| T-28-SC | Tampering | npm/pip installs | accept | plano nao instala pacotes; somente edicao de arquivos locais |
</threat_model>

<verification>
## Verificacao de Cobertura — R6-11

| Criterio R6-11 | Tarefa | Gate |
|----------------|--------|------|
| nota de linhagem no versions.lock | Task 1 | grep "get-shit-done-redux" versions.lock >= 1 |
| doctor avisa se pin sair de 1.1.0 da linha redux | Task 2 | grep "is_gsd_pi" idea-doctor.sh >= 1 |
| nenhuma confusao redux/Pi possivel | Tasks 1+2+3 | grep "gsd-pi" nos 3 arquivos-alvo |
| linhagem definitiva em STATE e handoff | Task 3 | grep "get-shit-done-redux" STATE.md + handoff >= 1 |

## Verificacao Final de Consistencia

Apos as 3 tasks, execute:

  bash scripts/check-versions-lock.sh

Esperado: saida "pin gsd=1.1.0 valido", exit 0.

Confirmar que gsd=1.1.0 permanece em versions.lock (nenhuma task deve alterar
o valor, apenas os comentarios acima da linha).
</verification>

<success_criteria>
- versions.lock: nota de linhagem expandida com 5 pontos (redux vs Pi, opengsd vs
  gsd-build, por que 1.1.0 > 1.36.0, unico escritor, pre-commit guard) — sem
  alterar o par gsd=1.1.0
- check-versions-lock.sh: funcao is_gsd_pi() ativa, mensagem de erro nomeia
  "@opengsd/get-shit-done-redux", exit 0 no working tree atual
- idea-doctor.sh: secao 5 emite aviso direcional que menciona
  "@opengsd/get-shit-done-redux" quando instalado != pin ou pin == Pi-drift
- STATE.md: bloco "GSD — Linhagem Definitiva" registrado como decisao canonica
- docs/CONTINUATION_HANDOFF.md: aviso de linhagem no topo do arquivo
- Nenhuma versao instalada foi alterada
- R6-11 fechado
</success_criteria>

<output>
Crie `.planning/phases/28-gsd-lineage-lock/28-01-SUMMARY.md` quando concluido.
</output>
