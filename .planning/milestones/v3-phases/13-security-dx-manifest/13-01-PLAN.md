---
phase: "13-security-dx-manifest"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/idea-doctor.sh
autonomous: true
requirements:
  - R3-14
  - R3-15
must_haves:
  truths:
    - "idea-doctor reporta WARN quando qualquer das 6 deny rules está ausente do settings.json e sugere install-global-patches.sh (per R3-14)"
    - "idea-doctor reporta WARN quando ideiaos-update.sh nunca foi executado nesta máquina (per R3-14 gap residual)"
    - "idea-doctor reporta WARN quando ~/.ideiaos/contexts/dev.md, review.md ou research.md estão ausentes (per R3-15)"
    - "idea-doctor reporta WARN quando as funções claude-dev/review/research não estão no shell profile (per R3-15)"
  artifacts:
    - path: "scripts/idea-doctor.sh"
      provides: "checks de deny rules (Seção 7a existente corrigida) + checks de contexts (nova Seção 8)"
      contains: "ideiaos/contexts"
  key_links:
    - from: "scripts/idea-doctor.sh"
      to: "~/.ideiaos/contexts/"
      via: "test -f check"
      pattern: "ideiaos/contexts"
    - from: "scripts/idea-doctor.sh"
      to: "~/.claude/settings.json"
      via: "python3 json parse"
      pattern: "deny.*nc"
---

<objective>
Adicionar dois blocos de verificação ao idea-doctor.sh: (1) corrigir Seção 7a para reportar WARN
(não FAIL) quando deny rules estão ausentes — alinhando com R3-14 — e adicionar check se
ideiaos-update.sh já rodou nesta máquina; (2) nova Seção 8 que audita a presença dos contexts
(~/.ideiaos/contexts/) e das funções de shell claude-dev/review/research, por R3-15.

Purpose: Fechar os gaps G-10 e G-11 que deixam máquinas sem deny rules e contexts sem diagnóstico
automático. Com ideiaos-update.sh agora existindo, o gap residual de G-10 é detectar se o script
nunca foi executado.

Output: scripts/idea-doctor.sh atualizado com Seção 7a revisada e Seção 8 nova.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@scripts/idea-doctor.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revisar Seção 7a do idea-doctor — deny rules como WARN + check de run marker</name>
  <files>scripts/idea-doctor.sh</files>
  <action>
Editar a Seção 7a (linhas ~154-165) de scripts/idea-doctor.sh com as seguintes mudanças:

1. Trocar `fail` por `warn` nos checks de deny rules ausentes (per R3-14: "reporta WARN ou ERROR").
   A mensagem de remediação deve mencionar AMBOS os caminhos: "rode: bash scripts/install-global-patches.sh" OU "bash scripts/ideiaos-update.sh".
   Manter `fail "settings.json não encontrado"` — ausência do arquivo é FAIL (ambiente crítico quebrado).

2. Após o loop das 6 deny rules, adicionar check de run marker: o ideiaos-update.sh grava
   "ok Registro de hooks verificado" no terminal mas não deixa marker persistente. O check
   deve testar se o arquivo ~/.claude/settings.json tem a statusline IdeiaOS configurada
   (campo `statusLine.command` contendo `ideiaos-statusline`) — isso é proxy confiável de
   "ideiaos-update já rodou pelo menos uma vez". Se ausente: `warn "ideiaos-update.sh nunca rodou
   nesta máquina (statusline ausente) — rode: bash scripts/ideiaos-update.sh"`.

   Usar python3 inline (sem jq — convenção do projeto):
   ```
   python3 -c "import json,sys; d=json.load(open('$SETTINGS')); sys.exit(0 if 'ideiaos-statusline' in str(d.get('statusLine',{}).get('command','')) else 1)" 2>/dev/null
   ```

3. NÃO usar heredoc `<!-- -->`. NÃO usar jq. Convenção: `# SOURCE: IdeiaOS v2` já presente no
   header do arquivo — não duplicar.
  </action>
  <verify>
    <automated>bash scripts/idea-doctor.sh 2>&1 | grep -E "deny:|ideiaos-update|settings.json" | head -20</automated>
  </verify>
  <done>
    Seção 7a usa warn (não fail) para deny rules ausentes; mensagem cita install-global-patches.sh
    e ideiaos-update.sh; check de statusline proxy presente após o loop das 6 regras.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar Seção 8 — contexts e funções de shell</name>
  <files>scripts/idea-doctor.sh</files>
  <action>
Adicionar Seção 8 ao idea-doctor.sh imediatamente antes do bloco `# ── Resumo ──` (linha ~193).

Estrutura da seção:

```
# ── 8) Contexts e shell aliases ──────────────────────────────────────────────
step "8) Contexts e funções de shell"
CONTEXTS_DIR="$HOME/.ideiaos/contexts"
```

Checks a implementar (todos `warn`, nunca `fail` — contexts são opcionais mas recomendados):

a) Presença dos 3 arquivos de context:
   Para cada um de dev.md, review.md, research.md dentro de $CONTEXTS_DIR:
   - Se ausente: `warn "context ~/.ideiaos/contexts/<nome>.md ausente — rode: bash scripts/ideiaos-update.sh"`
   - Se presente: `pass "context <nome>.md"`

b) Funções de shell presentes no profile:
   Detectar profile como já faz a Seção 4 do ideiaos-update.sh:
   `case "${SHELL:-/bin/bash}" in */zsh) PROFILE="$HOME/.zshrc" ;; *) PROFILE="$HOME/.bashrc" ;; esac`
   Checar se "claude-review()" aparece no profile:
   - Se ausente: `warn "funções claude-dev/review/research ausentes em $PROFILE — rode: bash scripts/ideiaos-update.sh"`
   - Se presente: `pass "funções claude-dev/review/research no $PROFILE"`

c) Statusline IdeiaOS no settings.json (reusa lógica da Task 1 — extrai para variável local para não duplicar python3):
   - Se ausente: `warn "statusline IdeiaOS ausente em settings.json — rode: bash scripts/ideiaos-update.sh"`
   - Se presente: `pass "statusline IdeiaOS configurada"`

Convenções: fail-silent em subshells (usar `2>/dev/null`). Sem jq. Sem heredoc comentário HTML.
  </action>
  <verify>
    <automated>bash scripts/idea-doctor.sh 2>&1 | grep -A 15 "8) Contexts"</automated>
  </verify>
  <done>
    Seção 8 presente na saída do idea-doctor; reporta WARN para cada context ausente com instrução
    de correção; reporta WARN para funções de shell ausentes; exit code do script não muda (WARN
    não falha, conforme header do arquivo).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → shell | idea-doctor lê settings.json e profile — leitura pura, sem escrita |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | python3 inline lendo settings.json | accept | script é read-only; não expõe conteúdo — só testa presença de campo |
| T-13-02 | Tampering | grep em ~/.zshrc/.bashrc | accept | grep -q, sem escrita; falha silenciosa em perfil ausente |
</threat_model>

<verification>
bash scripts/idea-doctor.sh 2>&1 | grep -c "8) Contexts" # deve retornar >= 1
bash -n scripts/idea-doctor.sh                            # syntax check
</verification>

<success_criteria>
1. `bash -n scripts/idea-doctor.sh` sai com exit 0 (sem erro de sintaxe).
2. `bash scripts/idea-doctor.sh` exibe seção "8) Contexts e funções de shell".
3. Em máquina sem contexts: pelo menos 1 WARN mencionando ~/.ideiaos/contexts/.
4. Em máquina com deny rules ausentes: WARN (não FAIL) para cada regra faltando.
</success_criteria>

<output>
Create `.planning/phases/13-security-dx-manifest/13-01-SUMMARY.md` when done
</output>
