---
phase: 11-instinct-loop-automation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - source/skills/instinct-analyze/SKILL.md
autonomous: true
requirements:
  - R3-08
  - R3-10

must_haves:
  truths:
    - "instinct-analyze/SKILL.md documenta o trigger automatico sem mencionar destilacao manual como pendencia (R3-10)"
    - "instinct-analyze atualiza sentinela .last-analyzed-<proj> ao concluir com sucesso (R3-08 — completude do loop)"
  artifacts:
    - path: "source/skills/instinct-analyze/SKILL.md"
      provides: "SKILL.md com secao Trigger automatico e instrucao de atualizar sentinela"
      contains: "Trigger automatico"
  key_links:
    - from: "source/skills/instinct-analyze/SKILL.md"
      to: "~/.ideiaos/instincts/.last-analyzed-<proj>"
      via: "Passo 9 — atualizar sentinela apos analise bem-sucedida"
      pattern: "last-analyzed"
---

<objective>
Atualizar source/skills/instinct-analyze/SKILL.md para: (1) documentar o trigger automatico via Stop hook, (2) adicionar Passo 9 que atualiza o sentinela .last-analyzed apos analise bem-sucedida, (3) remover qualquer mencao a "scheduler ausente" ou destilacao manual como pendencia.

Purpose: O loop so fecha se instinct-analyze sinaliza sua conclusao atualizando o sentinela. Sem isso, o gate em observe-session-end.sh nunca avancaria (ou dispararia em toda sessao). Tambem necessario para R3-10: SKILL.md deve descrever o trigger automatico, nao mencionar o gap como pendente.

Output: SKILL.md atualizado com secao "Trigger automatico" + Passo 9 + texto de gap removido
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@source/skills/instinct-analyze/SKILL.md
@source/hooks/observe-session-end.sh
@docs/instincts/instincts-layout.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar instinct-analyze/SKILL.md — trigger automatico + sentinela + remover gap</name>
  <files>source/skills/instinct-analyze/SKILL.md</files>
  <action>
Modificar source/skills/instinct-analyze/SKILL.md com tres mudancas cirurgicas:

**Mudanca 1 — Secao "Quando rodar": adicionar trigger automatico**

Na secao existente "## Quando rodar", adicionar como primeiro bullet (antes dos existentes):

```
- **Automaticamente via Stop hook** — `observe-session-end.sh` dispara `/instinct-analyze` como agente haiku em background quando `observations.jsonl` tem entradas mais recentes que `~/.ideiaos/instincts/.last-analyzed-<projeto>`. Nao requer acao do usuario.
```

Remover ou substituir qualquer texto que indique que a destilacao e manual ou que ha um "gap de scheduler". Buscar por: "gap", "scheduler", "manual" no contexto de trigger — e remover/reformular.

**Mudanca 2 — Adicionar Passo 9: Atualizar sentinela apos analise bem-sucedida**

Apos o Passo 8 existente (Validacao de privacidade), adicionar:

```markdown
### Passo 9 — Registrar conclusao (sentinela)

Ao concluir o processamento com sucesso (pelo menos 1 instinct criado ou reforçado, ou validacao de que nao ha novos padroes), atualizar o sentinela de ultima analise:

```bash
SENTINELA="$HOME/.ideiaos/instincts/.last-analyzed-${PROJETO_SLUG}"
/usr/bin/python3 -c "
import datetime
open('${SENTINELA}', 'w').write(datetime.datetime.now().isoformat(timespec='seconds'))
" 2>/dev/null || true
```

Se a analise falhar ou nao houver observacoes suficientes para destilacao, NAO atualizar o sentinela — o gate em observe-session-end.sh re-disparara na proxima sessao (comportamento correto: retry automatico).
```

(Nota: o bloco de codigo acima usa backticks triplos para o bloco bash — manter a formatacao correta no SKILL.md como markdown valido.)

**Mudanca 3 — Secao de relacoes e anti-padroes**

Em "## Anti-padroes", adicionar:
- `Nao atualizar o sentinela .last-analyzed apos analise bem-sucedida — causa re-analise desnecessaria a cada sessao.`

Em "## Relacoes", atualizar para incluir:
- `**Aciona:** ~/.ideiaos/instincts/.last-analyzed-<projeto> (sentinela de timestamp)`
- `**Acionado por:** observe-session-end.sh (Stop hook) via claude -p haiku background`

Manter `# SOURCE: IdeiaOS v2` no cabeçalho. Sem `<!--`. Idioma: portugues brasileiro. Nao alterar Passos 1-8 exceto onde necessario para remover referencias ao gap de scheduler.
  </action>
  <verify>
    <automated>grep -c "Trigger automatico\|trigger automatico" source/skills/instinct-analyze/SKILL.md && grep -c "last-analyzed" source/skills/instinct-analyze/SKILL.md && grep -c "Passo 9" source/skills/instinct-analyze/SKILL.md && grep -c "SOURCE: IdeiaOS v2" source/skills/instinct-analyze/SKILL.md</automated>
  </verify>
  <done>SKILL.md contem: "Trigger automatico" (ou variacao), "last-analyzed", "Passo 9", "SOURCE: IdeiaOS v2"; nao contem texto indicando destilacao manual como pendencia ou gap de scheduler aberto; nao contem "<!--".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| instinct-analyze (haiku) → sentinela | Agente escreve arquivo local do usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | sentinela .last-analyzed sobrescrito por analise falha | mitigate | Passo 9 instrui: so atualizar sentinela em conclusao bem-sucedida; falha = nao atualiza = retry automatico |
| T-11-06 | Information Disclosure | sentinela contem apenas timestamp ISO | accept | nenhum dado sensivel no sentinela; apenas datetime string |
| T-11-SC | Tampering | nenhum npm/pip/cargo nesta fase | accept | sem instalacao de pacotes |
</threat_model>

<verification>
Acceptance criteria mapeados para R3-10 (e completude do R3-08):

| Criterio | Como verificar |
|----------|---------------|
| R3-10: SKILL.md contem secao "Trigger automatico" | grep -i "trigger automatico" source/skills/instinct-analyze/SKILL.md |
| R3-10: sem mencao a destilacao manual como pendencia | grep -i "scheduler\|gap.*scheduler\|manual.*pendencia" source/skills/instinct-analyze/SKILL.md deve retornar 0 resultados |
| R3-08 completude: Passo 9 atualiza sentinela | grep "Passo 9" source/skills/instinct-analyze/SKILL.md && grep "last-analyzed" source/skills/instinct-analyze/SKILL.md |
| Nenhum jq | grep "jq" source/skills/instinct-analyze/SKILL.md deve retornar 0 |
| SOURCE: IdeiaOS v2 presente | grep "SOURCE: IdeiaOS v2" source/skills/instinct-analyze/SKILL.md |
</verification>

<success_criteria>
- source/skills/instinct-analyze/SKILL.md: secao "Trigger automatico" no "Quando rodar", Passo 9 com instrucao de sentinela, anti-padrao de nao atualizar sentinela adicionado, relacoes atualizadas
- Nenhuma referencia a gap de scheduler ou destilacao manual como pendencia
- Loop completo documentado: observe-session-end.sh spawna haiku → instinct-analyze roda → Passo 9 atualiza sentinela → proximo session_end gate passa/falha corretamente
</success_criteria>

<output>
Create .planning/phases/11-instinct-loop-automation/11-02-SUMMARY.md when done
</output>
