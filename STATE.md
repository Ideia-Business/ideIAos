# Estado do projeto — ideIAos

**Atualizado:** 2026-06-22 · **Branch:** `work` → `main` · **🏷️ tags `v11.0`+`v12.0`+`v13.0` SHIPPED** — SOAK queue VAZIA. v11.0 (`1ba01c8`) 06-20; **v12.0 + v13.0 (`57daf9c`) fechados manualmente na Mac mini 2026-06-21 18:31** porque as tasks agendadas NÃO completaram (v12 disparou 06-20 22:45 mas ABORTOU antes de taggear; v13 estava overdue/nunca disparou — exigiam app aberto na Mac mini). Re-record fechou span ≥1d (2 máquinas reais já satisfeitas); gates idea-doctor+regressão PASS; ambas as tasks one-shot agora `enabled:false`. · **Sessão 2026-06-20 — v13 Security Freshness Gate:** núcleo W1-W4 (`8779d88`) + **camada de surfacing por produto** (opção C — hook `post-commit` advisory, `SECFRESH_ROOT` 1-engine, `.git/info/exclude` local-only, husky-aware; sandbox 14/14) (`a6ab59d`) + **propagação aos 4 produtos** (hook+ledger-baseline local, tier=ok, **zero footprint versionado**) + SOAK heartbeat (1 máquina/0d). Tag `v13.0` aguarda 2ª máquina + span ≥1d. ADR Aceito. _Histórico abaixo (Sessão 2026-06-19):_ validação de resíduos **5/5 COMPLETA** — itens 1–4 fechados (verify-deploy `IN_SYNC` · ENV-04 · Mac mini · nfideia spec PR [nfideia#40](https://github.com/Ideia-Business/nfideia/pull/40)); **item 5 (2 stashes) RESOLVIDO:** nfideia `stash@{0}` + ideiapartner autostash dropados (noise regenerável); ideiapartner `stash@{1}` (type-safety pass, 20 arq — 16 casts puros + 4 mudanças schema-coupled NÃO-verificáveis) **arquivado em patch git-excluded** (`~/dev/ideiapartner/.stash-archive/`, 24 KB) e dropado — não aplicado (repo Lovable, stale 3 sem, conflito em 1 arq). **PR ideIAos [#4](https://github.com/Ideia-Business/ideIAos/pull/4) MERGED** (2026-06-19, merge commit `7e4809f` — `main` consolidado v6→v10; `origin/main..origin/work=0`). · **Versão ideIAos:** v9 shipped (tag `v9.0`); v10 Lovable MCP — Fase A (v1 read-only) SHIPPED; **Fase B (sandbox) CONCLUÍDA 2026-06-18 — veredito 🔴 BLOQUEAR `publish` via MCP** (A1-lag + A2 inmensuráveis no sandbox: o MCP não expõe/gerencia gitsync GitHub). Contenção `deny=19` enforçada mid-session; a "integridade 5 alvos" da auditoria `wf_4fec3ed7-fc0` era POINT-IN-TIME e REGREDIU p/ 2/5 — **RE-REMEDIADA p/ 5/5 PERSISTIDO em 2026-06-18** (`wf_247740a6`, ver §Sessão 2026-06-18). Fases C/D PARQUEADAS-GATED. Janela fechada (assert deny=19). Fork descartável (`1d0652c4`) **deletado** pelo usuário (get_project=404). **v10 FECHADO em escopo PARCIAL** (read-only SHIPPED; write-path BLOQUEADO; sem tag) — ver `.planning/v10-MILESTONE-AUDIT.md`

## Snapshot

| Área | Status |
|------|--------|
| **v14 IdeiaOS Cockpit** | 🟢 **v14.0 EXECUTADO/COMPLETO 2026-06-21 (no-tag — SOAK maturando)** — 7/7 planos DONE, **verificação goal-backward 24/24 gates por exit-code** (`14.0-VERIFICATION.md` status=passed). Substrato federável + Espinha nascida: `idea-doctor --json` (§0-§15) · ref `cockpit` (git-plumbing, A4-clean, **pushed `cockpit@{u}=30edb3e`**) · `ideiaos-agentd` read-only (Zero-Leak=0, snapshot metadata-only no ref) · `console-ingest`→SQLite descartável (api_key sem `value`, A5) · TtT harness · SPA black-gold (loopback) renderiza card `c706ac77d577`+`doctor` (screenshot anexado). SOAK v14.0 1 máquina/0d (tag aguarda ≥2 máq + span≥1d, igual v11-13); security re-selo PASS. **v14.1 (MVP Bridge) EXECUTADO/code-complete + CLOSEOUT 2026-06-22** — 8/8 planos via `gsd-executor` (sequencial, gate por wave), suite **A1–A12 9/9 por exit-code** (cada plano re-verificado pelo orquestrador). Entregue: `read.js` (`/overview /fleet /vault /verify /command-token` + `POST /command`) · 3 telas Overview/Frota/Cofre · ⌘K (cmdk, allowlist B1–B6 default-deny) · Flight Recorder v0 (13 nós/3 reversões) · Zero-Leak 7-superfícies+veneno triplo · TtT Bridge · §15 read-model. **Closeout A7/A10 via frontend-visual-loop (Chrome DevTools MCP):** screenshots Overview/Frota/Cofre + verbos inline (`run_doctor→exit 0`; `pause_autosync` arm→Confirmar?→exit 0). **🔴 Bug material achado no visual-loop (não pelo curl):** `POST /command` falhava no browser (`net::ERR_FAILED`) — faltava preflight CORS (`OPTIONS /command`→404); curl mascarava. **Fix S-05** (preflight gated por `isTrustedOrigin`, 204 confiável/403 senão, auth do POST intacta) → re-revisão `@security-reviewer` focada **PASS, 0 findings**; re-selo **PASS**. README + vault atualizados. **SOAK v14.1 1 máq/0d → tag DEFERIDA** (≥2 máq + span≥1d). Lição [[learning-curl-masks-cors-preflight-verify-browser]]. **v14.4 origin-auth: contrato `/spec` MERJADO 2026-06-22** (`specs/cockpit/spec.md` 9→20 reqs; spec-analyze 0 HARD) + **ADRs Q1-Q3 ACEITOS** (`docs/decisions/v14.4-*` — O2/lista-pinada-local; CRÍTICO de revogação forjada corrigido) + **plano de bootstrap** (`14.4-write-path/14.4-00-bootstrap-PLAN.md`, 5 proof-gates fail-closed, 2 BLOCKERS+2 CRITICALs adversariais integrados); R-WP10 segue FECHADO até bootstrap-por-exit-code; commits `c119afb`/`4bb25ba`/`b23ac7e`). _CLI GSD não resolve fases v14 (milestone sem `v14-ROADMAP.md` padrão; usa `v14-cockpit-PLAN.md`, igual v11–v13) → planejamento espelha o método v14.0 (dir manual + multi-agente), não o fluxo CLI._ |
| **Milestones v2.0–v9** | ✅ Todos shipped (tags v2.0 … v9.0) |
| **v13 Security Freshness Gate** | ✅ **SHIPPED — tag `v13.0` 2026-06-21** (`57daf9c`). Núcleo W1-W4 (`8779d88`) + surfacing opção C (`a6ab59d`) + propagação 4 produtos (zero tracked churn). idea-doctor §14 verde. SOAK 2 máquinas + span ≥1d fechado via re-record manual na Mac mini (task agendada `close-soak-v13-tag-tomorrow` estava overdue → executei os passos dela). `SECFRESH_GATE_ENABLED` segue 0 (decisão pós-1º-ciclo, à parte) |
| **v10 Lovable MCP — Fase A** | ✅ v1 read-only SHIPPED (2026-06-18): `/lovable-mcp` + helper + harness-deny 19 tools + rule. Rollout: deny=19 em **5/5** (4 produtos + IdeiaOS) — **PERSISTIDO** após regressão 2/5 remediada 2026-06-18 (nfideia/cfoai commit na `work`; ideiapartner `settings.local.json`); ✅ toggles de painel feitos; ✅ `/lovable-mcp verify-deploy` validado e2e contra nfideia real → `IN_SYNC` (2026-06-19). **Fase A 100% entregue e validada** |
| **v10 Lovable MCP — Fase B** | ✅ CONCLUÍDA 2026-06-18 — **veredito 🔴 BLOQUEAR `publish` via MCP**. Read-only: A1-namespace=ACOPLADO + A3=PASS. Escrita (fork `1d0652c4`, janela aberta+fechada): **muro de viabilidade** — MCP não expõe/gerencia gitsync GitHub (sem connector github, `get_project` sem repo, `add_connector` negado), logo A1-lag + A2 **inmensuráveis no sandbox** → indeterminado vota BLOQUEAR. Pior-caso do A2 refutado (git pushes entram no Cloud via `developer_update`). Contenção `deny=19` **enforçada mid-session** (comprovado). Fases C/D gateadas. Janela fechada (assert deny=19). Ver `B-01-SUMMARY.md` |
| **tool-output-compressor** | ✅ SHIPPED (2026-06-21) — capability nativa CLI-First: skill `source/skills/tool-output-compressor/` + lib stdlib + spec viva `specs/tool-output-compressor/spec.md` (7 req) + tests gate verde (log 97,8%) + plugin + **Deia routing**. Padrão minerado de **headroom** (Apache-2.0); **dep NÃO adotada**. Commits `05d7149→9f3d02a→f50c022` |
| **v9 Camada de Alinhamento** | ✅ SHIPPED (tag `v9.0`) — `/grelha`, glossário `CONTEXT.md`, `ubiquitous-language`, Passo 1.5, `/aprofundar` |
| **v8 Camada de Disciplina** | ✅ `/doubt`, `operating-discipline`, `/context-engineering`, R8-09 (rules Claude×Cursor) |
| **v7 Resiliência + Spec** | ✅ Piloto `/spec` nfideia, drift-guard, branch `spec/multi-tenancy-pilot` |
| **v6 Marketing + GSD** | ✅ `/marketing`, antifragile gates, `/spec` delta-spec brownfield |
| **v5 Memória entre IDEs** | ✅ import/export hooks, branch `planning`, 3 suites verdes |
| **Branches** | ✅ `main` = `work` · `planning` — alinhados e pushed (ver `git log`; hashes voláteis não fixados aqui) |
| **idea-doctor** | ✅ 74 OK · 1 WARN · 0 FAIL (2026-06-20, §14 frescor=ok). _Hist:_ 65 OK · 0 WARN · 0 FAIL (2026-06-18) — FAIL anterior era **falso-positivo** num dummy de fixture de teste (`sk-abcdEFGH…`, do `test-memory-export.sh`); `plausible_sk()` endurecido p/ rejeitar corridas sequenciais/dicionário. IdeiaOS repo limpo |
| **README sync** | ✅ 122/122 (2026-06-21, +/tool-output-compressor) |
| **Deploy máquinas** | ✅ MacBook-Air-2 · ✅ Mac mini git-synced (autosync ativo 06-18/06-19); `ideiaos-update.sh` aceito como baixo-risco (rodar quando for usar o mini) |
| Próximo passo | Ver `docs/CONTINUATION_HANDOFF.md` § Próximo passo |

## Sessão 2026-06-22 (tarde) — v14.4 write-path: /spec MERJADO + ADRs Q1-Q3 ACEITOS + plano de bootstrap verificado

Pedido: "faça tudo que pode ser feito" enquanto o SOAK das tags v14.0/v14.1 amadurece (gate temporal ≥1d, 2ª máquina = MacBook gravando o heartbeat). Janela usada para o único 🔴 aberto que é design sem código: o **contrato `/spec` de segurança do write-path v14.4** — o gate que a capability `cockpit` já exigia (requisito vivo "Comando cross-máquina e mutação de produção gated").

- **Autorado** `specs/_changes/v14.4-write-path-security/` (proposta + `delta/cockpit.md` + tasks) consumindo o precursor `docs/ideiaos-console/70-security-v14_4-threat-model-precursor.md` (§8 Q1-Q9) + doc 77 §B.2. **11 requisitos R-WP1..R-WP11 / 29 cenários**, todos rastreáveis (zero invenção, Art. IV). Define **COMPORTAMENTO** (SHALL/DEVE), não mecanismo — O2 assinatura-por-máquina / O3 SSH-signing / O4 out-of-band ficam **decisão do operador**. Q1-Q3 (CRÍTICAS, estruturalmente ABERTAS) encodadas como **pré-condições bloqueantes** em R-WP10 — **merge ≠ resolução**.
- **Gate determinístico** `spec-validate.sh` = exit 0. **Painel adversarial 3 lentes** (`wf_7cb55bba`: security-STRIDE / no-invention / contract-quality) = **SOUND · 0 CRITICAL · 0 fabricação de resolução**. 9 achados integrados: **1 HIGH** = loophole "resolvidas neste contrato" no R-WP10 (merge do gate vivo poderia ler-se como resolução de Q1-Q3) → reescrito p/ merge NUNCA ser resolução + cenário explícito; **2 MED** = STRIDE-I metadado-ao-`origin`/Q5 (cláusula+cenário em R-WP4) + STRIDE-D flood (novo **R-WP11** rate-limit); **6 LOW**. Re-validado exit 0.
- **RATIFICADO (merge):** o operador autorizou ("faça isso tudo") → `spec-merge.sh` aplicou os 11 reqs em `specs/cockpit/spec.md` (**9→20 reqs**), arquivado em `specs/_archive/2026-06-22-v14.4-write-path-security/`; `spec-analyze` (gate da fonte pós-merge) = **0 defeitos HARD**. Commits `c119afb` (contrato) + `4bb25ba` (merge+ADRs), pushados (via `AIOX_ACTIVE_AGENT=devops`).
- **ADRs Q1-Q3 PROPOSTOS** (`docs/decisions/v14.4-origin-auth-signing-mechanism.md` [Q1+Q2] + `v14.4-step-up-without-relying-party.md` [Q3]). Fato verificado (fecha A4/Q7 nesta máquina): **signing-git NÃO bootstrapado** → recomenda **O2** (par por-máquina no keychain) sobre O3. Verificação adversarial 2 lentes (security-soundness + honesty) = **SOUND-WITH-FIXES**; **CRÍTICO** pego (host comprometido assina a revogação da chave BOA via ref → DoS no write-path) → fix estrutural: **lista pinada autoritativa-LOCAL** (ref = espelho não-confiável), papel ligado à chave pinada (R-WP2), revogação só por re-pin out-of-band. Status PROPOSTO — **aguardam aceitação do operador + bootstrap-verificado-por-exit-code** (R-WP10 segue FECHADO; Q5 marcada aberta/fora-de-escopo).
- idea-doctor **76 OK / 3 WARN / 0 FAIL** (WARN = read-model cockpit stale, cosmético). Autosync pausado durante a cirurgia e religado ao fim.
- **ADRs Q1-Q3 ACEITOS** (operador "vamos em frente"; aceitar NÃO abre o gate — bootstrap-por-exit-code ainda obrigatório; reversível) + **plano de bootstrap escrito e adversarialmente verificado** (`.planning/milestones/v14-phases/14.4-write-path/14.4-00-bootstrap-PLAN.md` — fase-GATE, 5 proof-gates B0-B4 fail-closed por exit-code, ZERO mutação de produção). 2 lentes (security-reviewer + gsd-plan-checker, este PROBOU o host) pegaram **2 BLOCKERS** (openssl do host é LibreSSL sem Ed25519 → `ssh-keygen -Y`; `ssh-keygen -Y` ignora byte pós-footer → veneno = payload-tamper) **+ 2 CRITICALs** (anti-teatro do gate agregado era asserção → agora estrutural c/ manifesto+REASON+canário; revogação-forjada agora assinada-por-peer-pinado, não strawman) — todos integrados. Commit `b23ac7e`.
- **Próximo:** **executar o bootstrap** (espelhando v14.0; `autonomous:false` — exige Touch ID + re-pin out-of-band) → 5 proof-gates verdes = pré-condição R-WP10 satisfeita (mono-máquina). Registrado como aberto: cerimônia N=2 real + Q5 ficam p/ a fase de feature. Tags v14.0/v14.1: SOAK **≥2 máq ✓** / span≥1d ✗ (v14.0 re-record após hoje ~21:58, v14.1 após amanhã ~11:00 → `git tag`).

## Sessão 2026-06-22 — v14.1 MVP Bridge EXECUTADO/code-complete + CLOSEOUT (`/gsd-execute-phase 14.1`)

Retomada pós-`/compact`. Execução dos **8 planos** em **modo SEQUENCIAL com gate por wave** (escolha do usuário) via `gsd-executor`; cada plano **re-verificado pelo orquestrador** por exit-code (não pela alegação do subagente). **8/8 DONE · suite A1–A12 = 9/9 por exit-code.** Autosync PAUSADO o tempo todo (religar só ao fim, pós-push).

**Closeout (Task 3 / A11):**
1. **Visual A7/A10** via `frontend-visual-loop` (Chrome DevTools MCP): subiu read.js (:3073) + Vite (:5273), screenshotou Overview/Frota/Cofre e rodou verbos **inline** na ⌘K (`run_doctor → exit 0 (clean)` + stdout do doctor; `pause_autosync` **arm → Confirmar? → exit 0**). WCAG por critério explícito (contraste gold/near-black; cor nunca sozinha — ícone+texto+pill; nav teclável; alt-text na fita). Screenshots em `.visual-shots/` (git-excluded).
2. **🔴 Bug material achado NO visual-loop (não pelo exit-code/curl):** o `POST /command` falhava no browser (`net::ERR_FAILED`) — faltava handler de **preflight CORS** (`OPTIONS /command` caía no 404). curl não faz preflight → o canal passava verde no exit-code e estava **quebrado no browser**. **Fix S-05** (`read.js`, +25 linhas aditivas): preflight gated por `isTrustedOrigin` (204 só p/ origem confiável; 403 sem `Allow-*` caso contrário; `Allow-Origin` exato + `Vary`, nunca `*`; auth do POST byte-idêntica). Re-verificado: matriz curl (run_doctor 200 · git_push 403 · no-token 403 · bad-origin 403 · arm 412 · OPTIONS 204) sem regressão + browser agora renderiza inline. Learning [[learning-curl-masks-cors-preflight-verify-browser]].
3. **Re-selo de segurança:** `@security-reviewer` no diff da fase = **PASS** (9 travas do `POST /command` confirmadas; A8 production-mutation ausente; /verify injection-safe; /vault metadata-only). Re-revisão **focada** do delta S-05 = **PASS, 0 findings**. Ledger `.security/review-ledger.log` re-selado.
4. **README** — nova seção "Cockpit — console web local-first v14.1" (3 telas, ⌘K B1–B6, Flight Recorder, Zero-Leak, TtT, segurança do canal). **Vault Obsidian** — Changelog + Learning. **Memória nativa** — learning CORS-preflight + v14 milestone atualizado.
5. **SOAK v14.1** 1º heartbeat (1 máq/0d) → **tag DEFERIDA** (≥2 máq + span≥1d, igual v11–v14.0).

**Follow-ups de debt — RESOLVIDOS na mesma sessão (commit `b960daf`):** (a) stdout inline da ⌘K mostrava códigos ANSI literais (`[0;36m`) → helper `stripAnsi` no render, verificado no browser (stdout do `run_doctor` limpo); (c) idea-doctor §15 dava WARN espúrio porque derivava o MID por `awk | shasum` (hasheava o `\n` do `awk print`) → `131fd55c…` ≠ `c706ac77…` canônico do `collect.js`; fix captura o UUID + `printf '%s' | shasum` → §15 resolve o MID correto, idea-doctor exit 0 (76 OK/0 FAIL). (b) `/fleet` "agressivo" **reavaliado: NÃO é bug** — poll de 3s do heartbeat local na Overview (~20 req/min), design legítimo; não alterado. Re-selo de segurança no HEAD pós-polish (`b57d1a2`, delta não-security) = PASS.

---

## Sessão 2026-06-21 (noite) — v14.1 MVP Bridge PLANEJADO (`/gsd-plan-phase v14.1`)

Sessão de **planejamento** (multi-agente), zero código de produto. Pedido: `/gsd-plan-phase v14.1`. **Decisões do usuário (AskUserQuestion):** espelhar o método v14.0 (dir manual + multi-agente, sem editar o roadmap canônico) · **pesquisa-alvo antes** de planejar.

**Achado estrutural (surfaced):** o CLI GSD **não resolve** a fase 14.1 (`roadmap.get-phase 14.1` → `found:false`) — o milestone v14 usa o layout `v14-cockpit-PLAN.md` (igual v11–v13), nunca registrado no `.planning/ROADMAP.md` canônico. Mesma causa-raiz que quebrou o `phase.complete` do 14.0. Por isso o planejamento espelhou o v14.0 (fora do CLI).

**Fluxo executado (autosync PAUSADO o tempo todo — pause-file agora funciona, [[learning-autosync-pause-file-guard-not-deployed]] FIXED):**
1. **CONTEXT** `14.1-CONTEXT.md` autorado das fontes travadas (spec viva `cockpit` + milestone PLAN R14-05/06 + docs 02/50/71/76/77/78/79 + ADR). **Naming travado:** capability/ref = **`cockpit`** (docs 02/71 usam o codinome morto `mission-control` — corrigido).
2. **Pesquisa-alvo** (`gsd-phase-researcher`) + **pattern-map** (`gsd-pattern-mapper`) em paralelo → `14.1-RESEARCH.md` (54KB) + `14.1-PATTERNS.md`. **4 landmines** achadas no código v14.0 real: (a) `node:sqlite` (NÃO `better-sqlite3` — CONTEXT corrigido); (b) Flight Recorder = 13 commits reais via `ingest.js:classifyActor` (não os 4 do doc 71); (c) Zero-Leak shipped é regex-only, doc 78 manda regex+entropia; (d) `test:zeroleak`/`test:recorder`/`ttt-median --mode=bridge` ainda não existem (Wave-0).
3. **Planner** (`gsd-planner`, opus) → 8 `PLAN.md` (17 tasks/4 waves), slices verticais MVP + os 2 gates R14-06. Ambos R14-05/06 cobertos; A3/A8/A12 são gates duros por exit-code.
4. **Verificação 3 lentes (paralelo):** `gsd-plan-checker` = **CONCERNS** (0 bloqueador de goal; Nyquist + 4 warnings); `security-reviewer` (opus) = **SOUND-WITH-FIXES** (1 HIGH **S-01**: canal `POST /command` autenticava origem só por CORS → CSRF/DNS-rebinding driva `spawnSync`; 4 menores); `antifragile-gates` auditor = **0 VIOLAÇÕES** (52 critérios: 44 exit-code, 8 regime-R legítimos; A3/A8/A12 testados empiricamente, anti-teatro).
5. **9 fixes aplicados** (fresh planner, contidos a 5 planos + novo `14.1-VALIDATION.md`; 02/05/07 intactos): **S-01** auth server-side (Origin+Host fail-closed + token efêmero `X-Cockpit-Token`) · body-cap/parse · stdout do `/command` pelo Zero-Leak + `/command` na superfície S6 · daemon como chave de lookup · veneno runtime (S4/S5/S6) · grep do Cofre estreitado · A6 com assert curl · regex de bind endurecido · Nyquist `VALIDATION.md`. Todos os 8 planos `valid:true` no validador SDK.

**Commits limpos (`work`, autosync pausado):** `3972a88` (CONTEXT) → `2c4cb11` (research+patterns+fix) → `5c89a2c` (8 planos) → `3a69f67` (9 fixes + VALIDATION). **Próximo: `/gsd-execute-phase 14.1`** com contexto fresco (rodar `14.1-08` Task 2 após `/compact` — é a task mais pesada).

## Sessão 2026-06-21 (noite) — v14.0 Substrato + Espinha EXECUTADO (`/gsd-execute-phase 14.0`)

Execução **sequencial** (escolha do usuário — fase auto-modificante: o OS edita o próprio autosync e empurra ref novo) dos 7 planos, um a um, na ordem de dependência. Todos via subagentes `gsd-executor` (sonnet), sem worktrees.

1. **Incidente autosync (achado + remediado):** o `~/.local/state/git-autosync.pause` existia (19:02) mas o daemon o IGNOROU — capturou um write do executor como `a16907a wip: autosync` e **empurrou `work` p/ origin** (19:56). Causa: o binário deployado `~/.local/bin/git-autosync` **não tem o guard de pause-file** (existe só na intenção do `autosync-pause.sh`). Fix: **`launchctl bootout`** do `com.ideiaos.gitautosync` (hard-stop real) → build isolado. **RESTAURAR ao fim** (`launchctl bootstrap gui/501 …`). Learning novo [[learning-autosync-pause-file-guard-not-deployed]].
2. **Planos 1-7 (commits limpos `feat()`/`docs()`):** `14.0-01` idea-doctor `--json` (ANSI byte-idêntico, 14 seções+§15) · `14.0-02` `cockpit.sh` git-plumbing (A4/preserve/idempotência em /tmp) + **task 3 (cross-wave, user-aprovada): `push_cockpit_ref` no autosync + `git push -u origin cockpit`** via `AIOX_ACTIVE_AGENT=devops` → `cockpit@{u}=30edb3e` · `14.0-03` TtT baseline (N≥5/jornada) · `14.0-04` SPA scaffold (Vite7 build exit 0, loopback) · `14.0-05` agentd (snapshot real no ref, Zero-Leak dogfood sk-/JWT reprovam, plist 4º LaunchAgent **NÃO bootstrapped** por design) · `14.0-06` console-ingest (read-model SQLite, api_key sem `value`, A5 rebuild, 7 projetos/121 keys/0 value) · `14.0-07` SPA lê read-model + §15 + suite verde + **frontend-visual-loop PASSED** (screenshot: card `c706ac77d577`/`doctor:unknown`).
3. **Verificação:** `gsd-verifier` bateu em 529 (API overload 2×) → fiz a verificação goal-backward **inline por exit-code** (mais robusta, não NL): **24/24 PASS** → `14.0-VERIFICATION.md` status=passed.
4. **Fechamento:** SOAK heartbeat v14.0 gravado (idea-doctor+regressão PASS, 1 máq/0d) · security re-selo **PASS** (`@security-reviewer(inline-v14.0)`, credential-isolation materializada em `collect.js`) · README script-sync ✅. Tag v14.0 **deferida** (≥2 máq + span≥1d). `phase.complete` CLI falhou (milestone sem `v14-ROADMAP.md`) → completude marcada manualmente.

**Próximo:** v14.1 (MVP Bridge read-only).

### Follow-up FECHADO na mesma sessão — guard de pause do autosync DEPLOYADO
Investigação multi-agente (`wf_1ece3f22`) achou que o guard de pause **já existia nas fontes** (`setup-dev-machine.sh` heredoc + `scripts/ideiaos-update.sh` step 2d patcher) mas o binário deployado estava em **drift de 2 vias**: tinha `push_cockpit_ref` (meu edit ad-hoc do v14.0, nunca propagado ao heredoc) e **faltava** os 2 guards (pause-file + conflict-marker). Fix simétrico: (a) **patcher idempotente** (`ideiaos-update.sh` step 2d) grafou os 2 guards no binário vivo **preservando** `push_cockpit_ref`; (b) adicionei `push_cockpit_ref` ao heredoc do `setup-dev-machine.sh`. **Convergência provada:** heredoc body ≡ binário vivo (diff de código vazio) → os 2 caminhos de deploy não divergem mais. **Teste determinístico PASSOU:** pause-file global E por-repo fazem o binário logar `pausado (pause-file) — pulado` e sair 0 sem commitar/pushar. Agora `autosync-pause.sh on` funciona de verdade (não precisa mais de `launchctl bootout`). Memória [[learning-autosync-pause-file-guard-not-deployed]] atualizada p/ FIXED.

## Sessão 2026-06-21 (noite, Mac mini) — fechamento manual SOAK v12.0 + v13.0 (tasks agendadas não completaram)

Pedido: testar/verificar o comportamento de uma task agendada para hoje. Diagnóstico: as **duas tasks one-shot de fechamento de SOAK não completaram** o tag:

- **`close-soak-v12-tag-tonight`** (devida 06-20 22:45 local) — `lastRunAt` confirma que **disparou**, mas **abortou antes de taggear** (o ledger v12 não tinha re-record de 22:45 → bailou num gate inicial: `--record` ou pull não-ff naquele instante). `v12.0` ficou sem existir.
- **`close-soak-v13-tag-tomorrow`** (devida hoje 06-21 17:50 local) — **nunca disparou** (`lastRunAt` ausente; overdue). Essas tasks exigem o app Claude Code **aberto na Mac mini** no gatilho.

O **gate em si estava correto** (testado read-only): ambos recusavam taggear com span 0d. As 2 janelas ≥1d já tinham aberto e eu estava na Mac mini (máquina já contada no ledger) → re-record fecha só o **span de tempo**, sem fraudar o requisito de ≥2 atores reais distintos (já satisfeito) — alinhado a [[learning-automate-the-reminder-not-the-integrity-stamp]] e [[learning-soak-span-is-record-delta-not-wallclock]].

**Executado (com confirmação do usuário):** autosync pausado → `pull --ff-only` (0/0) → `check-soak --record` v12 + v13 (idea-doctor PASS + regressão PASS, span→1d cada) → 2 commits de ledger + push (`3100e45→57daf9c`) → verify exit 0 nos dois → `git tag v12.0 v13.0` + push (`AIOX_ACTIVE_AGENT=devops`) → autosync religado. **Ambas as tasks one-shot agora `enabled:false`** (sem risco de re-fire). SOAK queue VAZIA.

**Desbloqueio:** com o **v13 taggeado**, a execução do **v14.0 (Cockpit)** deixa de estar gated — ver § Próximo passo no handoff.

## Sessão 2026-06-21 (tarde) — Headroom eval + tool-output-compressor SHIPPED + HF cookbook (em curso)

Sessão de **análise + entrega**, isolada da sessão v14 (paths distintos). Pedido: avaliar `chopratejas/headroom` (camada de compressão de contexto) e ver aproveitamento.

1. **Headroom → NÃO adotar** (dep/proxy/MCP). Empírico (instalado 0.26.0; medido local: log 99,7%, JSON 38–58%, código 0% no core, prosa/usuário 0% por design) + workflow 62-agentes (5 dimensões, verificação adversarial). Razões: proxy colide com `mcp-hygiene` (`ANTHROPIC_BASE_URL`), ~$0 em subscription, inaplicável a Lovable-hosted (cfoai/nfideia) e Deno-edge (ideiapartner/lapidai). Memória `headroom-eval-2026-06`; relatório `/tmp/headroom-analysis/`.
2. **tool-output-compressor → SHIPPED** (o padrão minerado, invertido p/ CLI-First): skill + `lib/toc_compress.py` (stdlib) + `lib/toc.sh` (fail-open) + spec viva (7 req via `/spec` propose→merge→archive) + `tests/tool-output-compressor-test.sh` (ALL PASS) + registro `modules.json`/`build-plugins.sh`/`plugin-membership.md` (drift OK) + deploy global `~/.claude/skills/` + **matriz da Deia**. Commits `05d7149`, `9f3d02a`, `f50c022` (+ `ddf6bca` autosync). Opção (b) hook auto-compressão por limiar = **PARQUEADA** não-bloqueante.
3. **HF cookbook (`huggingface/cookbook`) — CONCLUÍDA: NÃO absorver** (dep/milestone). 6 especialistas + verificação adversarial 8/8. Recurso de praticante-ML vs nossa stack Deno/hosted-API → técnica≠código. **3 wins ADAPT-no-produto** (extração-com-âncora→nfideia; PII-scrubber→cfoai/nfideia; DDL-subset-text2SQL→cfoai) + disciplina de judge; **RAG gated em retrieval** (zero pgvector nos produtos). Guia: `docs/guides/hf-cookbook-patterns.md`; memória `hf-cookbook-eval-2026-06`.

**Learning:** autosync atropelou 1 commit (`ddf6bca`); memória `autosync-races-ai-git-surgery` corrigida (pausar via `scripts/autosync-pause.sh`; morde até em trabalho single-repo multi-step).

## Sessão 2026-06-21 — v14 IdeiaOS Cockpit (plano completo, PROPOSTO/zero-código)

Sessão de **design + planejamento** (multi-agente). Pedido: transformar instalação/ativação/gestão dos projetos numa página web de visão CTO/Tech-Lead = **IdeiaOS Cockpit** (console local-first sobre o substrato auto-telemetrado). **Nenhum código de produto** — só o pacote de planejamento, todo commitado/pushado em `work`.

**Decisões do usuário (AskUserQuestion):** nome **IdeiaOS Cockpit**; **formalizar via /spec + GSD antes de código**; comando cross-máquina **aprovado p/ v14.4 gated** por threat-model; brand-hue ouro.

**Entregue (`docs/ideiaos-console/` 20 docs + `specs/cockpit/` + ADR + `.planning/milestones/v14-cockpit-PLAN.md`):**
1. **Blueprint multi-agente** (13 agentes/5 fases) — o crítico adversarial pegou contradição fatal ("piggyback no SOAK `--record`" é manual → quebraria o heartbeat vivo); 6 contradições + 6 buracos de segurança fechados.
2. **Contrato /spec vivo** `specs/cockpit/spec.md` (9 req SHALL/DEVE) — validado+merged+arquivado; **1º uso de `specs/` no próprio IdeiaOS**.
3. **ADR** `docs/decisions/v14-cockpit-local-first-git-as-bus.md` (Aceito): local-first, git-as-bus por ref `cockpit`, Zero-Leak=0, teto de poder gated.
4. **Apuração 100% ("verify, don't assume"):** doc 73 validou o substrato **na própria Mac mini** (corrigiu `192`→MacBook-Air-2; Constelação = **7 projetos** reais — Jarvis 469 sessões; **nenhum segredo crítico git-tracked**). Docs 70-72 (segurança v14.4, MVP×wow=Flight Recorder v0, v14.0 buildável=37 tasks) + docs 74-79 (resiliência, DDL, produtividade, alertas+allowlist, testes, glossário + **registro mestre de 39 questões**).
5. **Mockup interativo** do Overview (black-gold OKLCH) renderizado na sessão.

**Commits limpos:** `90b3062` (ADR+plano), `2f0d288` (wave 74-79); resto via autosync. `work` 0/0. idea-doctor verde.

**Aberto 🔴 (doc 70/79):** Q1 — autenticação de origem cross-máquina (`sha256 ≠ assinatura`) — faz a v14.4 ser **gate, não milestone**; o `/spec` de segurança consome as 9 questões.

**Próximo passo:** `/gsd-plan-phase v14.0` (consome `specs/_archive/2026-06-20-v14-cockpit-foundation/tasks.md`) **quando o v13 tagar** — não entrelaçar milestones. Ver `docs/ideiaos-console/00-BLUEPRINT.md` + `.planning/milestones/v14-cockpit-PLAN.md`. Learning: [[learning-deterministic-replay-needs-structured-event-store]].

## Sessão 2026-06-20 — v13 Security Freshness Gate (núcleo + surfacing C + propagação)

Milestone **v13** ("Selo de Frescor de Segurança"): segurança verificada periodicamente e **por sistema**, não só sob demanda. Padrão SOAK aplicado a dívida de segurança — **gatilho determinístico** (risk-weighted por superfície × idade) → revisão `@security-reviewer` → re-selo determinístico. **Nunca gateia PR de feature.**

1. **Núcleo W1-W4 (IdeiaOS-side, `8779d88`):** `scripts/check-security-freshness.sh` (tier/bootstrap/record/gate) + ledger `.security/review-ledger.log` (commitado; exceção no `.gitignore`) + idea-doctor **§14** (ADVISORY, nunca FAIL) + rule `security-freshness` + sandbox 10/10 (prova o caminho de FALHA egrégio+gate→exit 1).
2. **Surfacing por produto — decisão do usuário = opção C (`a6ab59d`):** hook **`post-commit` ADVISORY** por produto (post-commit não bloqueia por construção → satisfaz "não enrijecer"). `SECFRESH_ROOT` override → **1 engine** (no IdeiaOS) audita qualquer repo → produto **não versiona script algum** (zero trigger de Lovable em `main`). Throttle 6h, silencioso quando fresco. `setup_security_freshness_layer()` no `setup.sh --project-only` (bootstrap de ledger local + install husky-aware via `core.hooksPath` + `.git/info/exclude` p/ ledger/marcador/hook-husky). Sandbox 14/14 (não-bloqueia, avisa em stale, throttle, husky-path, preserva pre-commit).
3. **Propagação aos 4 produtos (local-only, surgical — sem `setup --project-only` completo p/ evitar churn de rule-mirror em `main`):** nfideia (`.husky/post-commit`, excluído), ideiapartner/lapidai/cfoai (`.git/hooks/post-commit`). Todos: ledger-baseline local (tier=ok) + `.git/info/exclude`. **Verificação binária:** 4/4 com hook executável+marker+engine-path-substituído, **0 tracked changes introduzidos** (sem trigger de Lovable, sem race com autosync). Live-test em cfoai: warn→exit 0, fresco→silêncio.
4. **SOAK heartbeat v13** gravado (`.planning/soak/v13-security-freshness.log`, 1 máquina/0d). **Rule auto-propaga** via post-merge (lapidai já recebeu); não foi commitada manualmente nos produtos.

**Fechamento PARCIAL/no-tag** (igual v12): tag `v13.0` aguarda SOAK ≥2 máquinas + span ≥1d. Ligar `SECFRESH_GATE_ENABLED=1` é decisão pós-1º-ciclo. _(v11.0 foi tagueado hoje — fila do SOAK agora = v12.0 + v13.0.)_

## Sessão 2026-06-20 (noite, Mac mini) — SOAK 2ª máquina v12/v13 + LaunchAgent

Rodada operacional na **Mac mini** (2ª máquina) para destravar o SOAK dos dois milestones PARCIAL/no-tag:

1. **v12-qa-security** — heartbeat da 2ª máquina gravado (idea-doctor PASS + regressão PASS), commit+push (`462ce2b`, capturado pelo autosync — benigno, conteúdo correto, branch certa). Ledger = **2 máquinas distintas** (`192` 06-19 + `Mac-mini-de-Gustavo` 06-20); span 0d (fecha hoje 22:36:36). **Tag `v12.0` agendada** p/ hoje 22:45 via task local one-shot `close-soak-v12-tag-tonight` (re-grava → verifica → tag, aborta se gate falhar).
2. **v13-security-freshness** — heartbeat da 2ª máquina gravado (`703da4d`, pushed). Ledger = **2 máquinas** (MacBook-Air-2 + Mac mini); span 0d (fecha amanhã 2026-06-21 17:46:26). Tag `v13.0` aguarda re-gravação após esse horário.
3. **LaunchAgent mensal AI-security ATIVADO na Mac mini** — `com.ideiaos.refresh-ai-security` copiado p/ `~/Library/LaunchAgents/` + `bootstrap` (status 0; dispara dia 1 de cada mês 09:00). Pendência v12 do LaunchAgent FECHADA.
4. Autosync pausado durante a cirurgia (pause-file) e **religado** ao fim. `work = origin/work` limpo.

Anomalia notada (não-bloqueante): a 1ª entrada do ledger v12 tem host-label `192` (provável fragmento de IP do `hostname -s` na MacBook-Air); é máquina genuinamente distinta da Mac mini, gate ≥2 satisfeito. Ledger é append-only/commitado → não editado.

## Sessão 2026-06-20 (noite, Mac mini) — housekeeping: doctor 0-WARN + planning sync v13 + design pin

Limpeza pós-auditoria de pendências (pedido "deixe 100% correto"). **idea-doctor: 75 OK · 0 WARN · 0 FAIL.**

1. **idea-doctor 3 WARN → 0:** (a) **/spec drift** — global sem 3 libs do v11 (`spec-{analyze,converge,grammar}.sh`); `setup.sh --global-only` é **version-gated** e não as copiou → espelhei o dir com `cp -Rp` ([[learning-global-skill-deploy-version-gated-misses-lib-changes]]). (b) **AI-security snapshot** ausente → bootstrap via `refresh-ai-security.sh` (local/gitignored). (c) **Suíte de Design seed** — pin re-ancorado à **proveniência real `b7e3af80`** (verificado por content-match: 5/7 skills idênticas + overlays IdeiaOS em design-system/banner-design) em `versions.lock`+`.design-suite-version` (`f1c4e53`). ⚠️ `update-design-suite.sh` é DESTRUTIVO p/ ref-SHA — cai no HEAD e apaga o dataset ([[learning-design-suite-sha-pin-clone-destructive]]).
2. **Branch `planning` reconciliado + sincronizado → v13** (`4dd9c1f`): estava diverso 10/2 (churn do memory-sync cross-máquina). Merge de `origin/planning` (conflitos v11 em MEMORY.md + fato → resolvidos p/ a versão shipped; **memory store preservado, 47 facts, 0 perdido**), `.planning/STATE.md` v10→v13, `.planning/ROADMAP.md` + v9–v13. `planning` agora **0/0** com origin. (Descoberta: origin/planning já estava em v10 — a auditoria leu o planning *local* stale.)
3. **Os 2 defeitos de script CORRIGIDOS** (ambos testados): (a) **`setup.sh`** deploy version-gated → content-aware (`diff -rq` + re-espelha o dir inteiro), `4c878b5`. (b) **`update-design-suite.sh`** destrutivo → `cp -RL` + salvaguarda anti-deleção + clone direto p/ ref-sha (`a5d3590`+`4ab4e9a`). **Causa real do (b) NÃO era o clone-por-sha** (esse funciona) e sim o `cp -R` puro copiando os symlinks `data//scripts/` do upstream como DANGLING → dataset some. Reproduzir o passo exato pegou minha diagnose errada inicial (o pin `b7e3af80` está certo, mas o forensic que o "confirmou" tinha um checkout que não surtiu efeito). Net-del 9374→112 no teste. Ambos em memória ([[learning-global-skill-deploy-version-gated-misses-lib-changes]], [[learning-design-suite-sha-pin-clone-destructive]]).

Estado final: `work` + `planning` ambos **0/0** com origin; doctor **0-WARN**; tags SOAK v12.0/v13.0 no piloto automático (tasks agendadas).

## Sessão 2026-06-18 — remediação doctor + incidente autosync + housekeeping produtos

Sessão de **manutenção/remediação** (não altera o milestone v10). Disparada por `ideiaos-update.sh` → `idea-doctor` deu FAIL de secret.

1. **Scanner de secrets endurecido** (`scripts/idea-doctor.sh:225`) — o FAIL era **falso-positivo** num dummy de fixture (`sk-abcdEFGH1234…` do `test-memory-export.sh`). `plausible_sk()` agora rejeita corridas sequenciais/dicionário (`abcdefgh`, `0123456789`, `qwerty`). Fix **durável na heurística**, não caça-transcripts (o "observer effect" propaga o dummy ao auditá-lo). Doctor → **65 OK / 0 / 0**.
2. **Incidente autosync × cirurgia git** — o daemon correu em paralelo às operações multi-repo: entregou o IdeiaOS sozinho, **bloqueou** o push do nfideia (clone defasado) e **contaminou** uma branch do ideiapartner (varreu `package-lock.json` + `CONTINUATION_HANDOFF.md` com marcadores de conflito). Com autorização do usuário: autosync **pausado** (`bootout`) → repos reconciliados → autosync **religado** (`bootstrap`, status=0).
3. **Housekeeping produtos:** nfideia `.env` **untrackeado** + push (`94fffd05`, branch `work` — Lovable nunca na main); ideiapartner branch suja **removida** (local+remote), de volta na `main` `d0dc883c` (split público/secret do `.env` preservado por design).
4. **2 learnings extraídos** → memória + vault: `secret-scanner-observer-effect`, `autosync-races-ai-git-surgery`.

### Gap-closure audit (2026-06-18, wf_247740a6 — 6 auditores read-only)
5. **Regressão de segurança HIGH achada e REMEDIADA:** a contenção Lovable MCP (`deny=19`) estava só em **2/5** alvos (lapidai+IdeiaOS) — os blocos que a sessão de rollout deixou **uncommitted na main** de nfideia/cfoai se perderam (regressão silenciosa). Reaplicado e **PERSISTIDO**: nfideia `e43f35f5` + cfoai-grupori `cdfa8d6` (commit na branch `work` + push); ideiapartner via `settings.local.json` (local, `.claude` gitignored lá). Agora **5/5** (deny=19, revalidado binário). 3ª learning extraída: `uncommitted-security-config-ephemeral`.
6. **Housekeeping rules (PRG-03):** materializadas as 8 `.claude/rules/ideiaos-common-*.md` + `.cursor/rules` nos 3 produtos (paridade com lapidai). Gap de propagação em si = já fechado em código (66598c1); memória `propagate-rules-gap` corrigida de PENDENTE→RESOLVIDO.
7. **Doc/memória stale corrigidas:** item "Jarvis/iCloud secrets" encerrado no handoff; memória v10 reconciliada (4-produtos→2/5→5/5). **`.env` ideiapartner confirmado SEGURO** (3 vars públicas; NÃO untrack — untrack causou SEV-1 antes). Itens user-decision (rotação de secrets em histórico, stashes) deixados para você.
8. **Prevenção + fechamento:** novo **check 7e** no `idea-doctor` (valida `deny>=19` por produto Lovable, **FAIL** se regredir) + teste de regressão (`tests/idea-doctor/`, 9 asserts) + wiring no CI; verificação adversarial 4 lentes (`wf_a910bea1`/`wf_455c4880`) = PASS após corrigir claims stale. **ENV-06 DESCONSIDERADO** (Ideia Chat é teste, não vai a produção). **PR [#3](https://github.com/Ideia-Business/ideIAos/pull/3)** (`work`→`main`) aberto consolidando a sessão (11 commits).

**Estado git ao fim:** IdeiaOS `work` = `origin/work` limpo (**PR #3** aberto p/ `main`); nfideia/cfoai `work` com deny+rules pushados; ideiapartner `main` intacta (contenção local); autosync ativo. **Verificação binária:** doctor **69/0/0** (5 alvos contidos via 7e), deny=19 em 5/5, teste 9/9.

## Sessão 2026-06-16 (Cursor) — pesquisa+plano milestone v9

Sessão de **pesquisa + planejamento** do milestone **v9 — "Camada de Alinhamento"** (absorção seletiva de `mattpocock/skills`, MIT). **Nenhuma skill/código implementado** — só planejamento; tudo já commitado/pushado.

1. **Análise comparativa** — `docs/research/2026-06-16-mattpocock-skills-analise.md` (8 seções): mattpocock/skills × IdeiaOS, 3 GAPs reais (glossário de linguagem ubíqua durável; grilling colaborativo pré-plano desacoplado de fase GSD; ritual de "deepening" arquitetural), veredito por skill, encaixe do `/grill-with-docs`, orquestração da Deia, exemplo real no nfideia.
2. **Quarentena** — `security/quarantine/mattpocock-skills/` (18 arquivos, LICENSE MIT preservada, `scan-absorbed.sh` PASS/exit 0).
3. **ADR** — `docs/decisions/v9-mattpocock-skills-absorcao.md` (Aceito): absorver a TÉCNICA, não a ideologia anti-framework; `/grelha` roda SOB a Deia (gate opcional). Espelhado no Obsidian `Decisions/`.
4. **Plano GSD** — `.planning/milestones/v9-REQUIREMENTS.md` (R9-01..R9-07) + `v9-ROADMAP.md` (Fases A–F) + `v9-IMPLEMENTATION-PLAN.md` (grafo de dependências, esforço, gates, DoD, Fase G could-haves) + `v9-phases/{A..F}-*/*-01-PLAN.md` (PLAN por fase, formato GSD).
5. **Recomendação** — skill `/grelha` (alias `/grill`) = grilling pré-plano + glossário `CONTEXT.md` + ADR inline; rule nova `ubiquitous-language`; gate opcional Passo 1.5 na Deia (`source/skills/idea/SKILL.md`); SHOULD: `/improve-architecture` (deepening). Padrão de absorção = igual v8.

**Estado git ao fim:** `main` == `work`; `planning` pushado; working tree limpo (ver `git log`).

## Sessão 2026-06-16 (Cursor) — fechamento final

1. **Encerramento + docs** — handoff/STATE sincronizados; commits `a834544` → `d4d5887` (autosync Mac mini).
2. **Alinhamento de branches** — `main` = `work` fast-forward (23+ commits v6–v8); `planning` merge + memória v5 preservada.
3. **Commit/push (pedido do usuário)** — `fd56c8d`, `0ffd912`, `647c242` pushed em `work`/`main`/`planning`. Repo limpo @ `647c242`.
4. **Propagação** — `propagate-if-changed` rodou ao merge em `main`; setup propagado a 6 projetos `~/dev/*` (0 erros).
5. **Verificação** — README 112/112 ✅ · idea-doctor 2 FAIL (secrets Jarvis/iCloud Projects — remediação manual).

## Pendências não-bloqueantes

- ✅ **Propagação v12 aos produtos + ROADMAP — COMPLETA (2026-06-19 noite):** `.planning/ROADMAP.md` (parado no v8) atualizado com v9–v12 (`843f499`) + vault Changelog. **Propagação v12 ATIVA nos 4 produtos** (drift 7/8/9/8 → 10 `ideiaos-common` + `credential-isolation`): lapidai/cfoai-grupori (commit em `work`); ideiapartner (rules gitignored, local); **nfideia via PR [nfideia#41](https://github.com/Ideia-Business/nfideia/pull/41) MERGED** (squash `9728b153`) + pull ff-only. Autosync pausado/religado por `trap`. Gate @devops satisfeito via `AIOX_ACTIVE_AGENT=devops` (command-scoped, autorização explícita). **Restam só 2 operacionais:** tags SOAK v11/v12 + LaunchAgent Mac mini.
- 🟢 **Milestone v12 — QA & AI-Security — 4 ondas + refresh mensal DONE (fechamento PARCIAL/no-tag, 2026-06-19, commit `8d18650`):** análise multi-agente (`wf_50d8299b-f69`, 20 agentes) de 3 repos externos → absorção **conceito-only** (licenças via GitHub API: Hercules **AGPL-3.0**, TalEliyahu MIT, muellerberndt **SEM LICENÇA**). **W1** antifragile-gates (artefato-exit-code vs runtime-NL) + operating-discipline #6 + nova rule `credential-isolation` (SEC-05) · **W2** security-reviewer (OWASP LLM Top 10 condicional + prompt-injection-runtime) + mcp-hygiene (critérios MCP SlowMist/TTPs + Excessive Agency) · **W3** `docs/process/qa-coverage-index.md` + `docs/reference/ai-governance-crossmap.md` · **W4** EVAL-026/27/28 anti-injection (ADVISORY) · **refresh mensal** `scripts/refresh-ai-security.sh` (snapshot **LOCAL/gitignored** — muellerberndt sem licença, não redistribuível) + plist launchd + idea-doctor §13. ADR `v12-qa-security-absorption`; plano `.planning/milestones/v12-qa-security-PLAN.md`. Verificado: idea-doctor **73/1/0**, readme 120/120, evals 3 casos. **TAG `v12.0` pendente do SOAK** (`.planning/soak/v12-qa-security.log`: 1 máquina/0d). Dogfood pegou alucinação de licença (Hercules Apache→AGPL). ✅ **2ª máquina (Mac mini) gravada 2026-06-20** + ✅ **LaunchAgent mensal AI-security ATIVADO na Mac mini** — **✅ tag `v12.0` SHIPPED 2026-06-21** (`57daf9c`) — a task agendada de 06-20 22:45 DISPAROU mas ABORTOU antes de taggear (sem re-record no ledger); fechado manualmente na Mac mini 2026-06-21 18:31.

- 🟢 **Milestone v11 — Integridade & Auditoria de Spec — TODAS as 6 ondas DONE (fechamento PARCIAL/no-tag, 2026-06-19):** plano+notas em `.planning/milestones/v11-arsenal-absorption-PLAN.md`. **W1** autosync guard-aware (`44336c5`) · **W2** CI repo-self-consistency gates + `check-source-headers` + design-suite ref resolvido (`ccb3ff0`) · **W3** SOAK gate `check-soak.sh` + surface budget + `/idea` routing eval cases EVAL-023/24/25 (`70f0cd6`) · **W4** `/spec --analyze`+`--converge` (libs `spec-grammar`/`analyze`/`converge` + tests/spec-analyze.bats 23 asserts) (`e65d0e0`) **+ hardening pós-verificação adversarial** wf_99173505 (`4011186` — corrigiu bloqueador A2-template-FP + 9 achados) · **W5** deltas LOW R2/R4/R6/R8 (`4637b1d`) · **W6** 2 ADRs (`v11-spec-kit-analyze-converge`, `v11-license-provenance-quarantine`) + SOAK heartbeat gravado. **Design por painel (wf_449a5952) + verificação adversarial (wf_99173505).** **TAG `v11.0` — SOAK 2/2 máquinas PASS (2026-06-19):** ledger `.planning/soak/v11-arsenal.log` tem MacBook-Air-2 @17:51 (`4011186`) + Mac-mini-de-Gustavo @18:30 (`2ca25df`), ambos idea_doctor+regressão PASS → **durabilidade cross-máquina GREEN**. Falta só o **span ≥1d** (ambos heartbeats de 06-19, ~39min → `0d`): gravar 1 heartbeat **≥ 2026-06-20 17:51:44** (qualquer máquina) → `check-soak.sh v11-arsenal` exit 0 → `git tag v11.0`. Nada de código pendente.

- ✅ **Lovable MCP Fase A — rollout FECHADO (2026-06-19):** teste e2e `/lovable-mcp verify-deploy` **RODADO contra nfideia real** → verdict binário **`IN_SYNC`** (`CLOUD_SHA latest_commit_sha=3921f440a44eed620de6e60d3832f5c16f1022b8` == `origin/main`); resolver de escopo `in:todos` (projeto na pasta canônica "Grupo Ideia"); só tools read-only (`get_me`/`list_projects`/`get_project`); repo não-shallow. Toggles de painel já feitos; deny=19 **PERSISTIDO em 5/5** (regressão 2/5 remediada 2026-06-18, `wf_247740a6`). Fase A 100% operacional e validada end-to-end.
- ✅ **Fase B (sandbox) CONCLUÍDA — veredito 🔴 BLOQUEAR `publish`.** Read-only (A1-namespace ACOPLADO + A3 PASS) + escrita ao vivo (fork descartável, janela `deny→ask` aberta+fechada). Muro de viabilidade: o MCP não tem superfície p/ gitsync GitHub → A1-lag + A2 inmensuráveis no sandbox → bloqueio conservador. Fases C/D gateadas até medir A2 fora do MCP (gitsync manual na UI). Fork descartável **deletado pelo usuário** (confirmado get_project=404) — zero resíduo na conta Lovable.
- ✅ **Segurança (ENV-06) — DESCONSIDERADO (decisão do usuário, 2026-06-18):** o Ideia Chat é um **teste e NÃO vai a produção** → o `IDEIA_CHAT_SYSADMIN_PASSWORD` no histórico de `origin/main` do ideiapartner é inócuo; rotação dispensada. _Reabrir SÓ se o Ideia Chat for promovido a produção: aí rotacionar antes de ativar `FEATURE_IDEIA_CHAT_PROVISIONING_ENABLED`._
- ✅ **OpenRouter (ENV-04) — FECHADO (2026-06-19, decisão do usuário):** revogação da chave antiga confirmada/dispensada no painel. Sem ação pendente.
- ✅ **Stashes — RESOLVIDO (2026-06-19):** nfideia `stash@{0}` + ideiapartner autostash dropados (noise regenerável); ideiapartner `stash@{1}` (type-safety pass) revisado e **arquivado** em patch git-excluded (`~/dev/ideiapartner/.stash-archive/`, 24 KB) + dropado — não aplicado (repo Lovable, stale 3 sem, 4 mudanças schema-coupled não-verificáveis, conflito em 1 arq). Ambas as pilhas vazias.
- ✅ **Higiene de memória Claude — RESOLVIDO (2026-06-18):** o FAIL do `idea-doctor` (seção 7) era **falso-positivo** num dummy de fixture (`OPENAI_API_KEY=sk-abcdEFGH…`, do `test-memory-export.sh`); varredura exaustiva confirmou **zero secret real** comprometido (só anon keys Supabase públicas-por-design + tokens de sessão expirados em transcripts locais). Fix durável: `plausible_sk()` rejeita corridas sequenciais. Doctor verde 65/0/0.
- ✅ **nfideia spec PR — RESOLVIDO (2026-06-19):** specs do piloto portadas p/ `main` via **PR limpo [nfideia#40](https://github.com/Ideia-Business/nfideia/pull/40)** (`spec/pilot-port`, cherry-pick doc-only sobre a main atual, 6 arquivos 100% em `specs/`, zero código). Branch stale `spec/multi-tenancy-pilot` (71 atrás) **não** foi arrastada. Achado: o `fix(nfse-retry-stuck)` da branch original **já está na main** (não-órfão); a metodologia `/spec` já é viva na main (`onda1-honorarios`). Autosync pausado/religado durante a cirurgia. **PR #40 MERGED (2026-06-19).**
- **gsd-browser:** monitorar upstream (ainda não publicado).
- **DeepSeek V4 Pro:** decisão adiada — habilitar nos produtos (fora do escopo IdeiaOS); ver handoff sessão consultiva 2026-06-16.
- ✅ **Mac mini — FECHADO como baixo-risco (2026-06-19, decisão do usuário):** git-synced confirmado (autosync `Mac-mini-de-Gustavo` ativo 06-18 12h→18h + sessão 06-19 07:53); `main` em dia lá. Rodar `ideiaos-update.sh` no mini só quando for usá-lo (aplica overlay de patches no install local; `versions.lock` já protegido repo-wide).

## Fonte de verdade

- Operacional curto prazo: este arquivo + `docs/CONTINUATION_HANDOFF.md`
- Médio/longo prazo: `.planning/*` no branch `planning`
- Especificação canônica: `docs/IDEIAOS.md`
